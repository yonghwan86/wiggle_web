# Wiggle Web 현재 상태

> 마지막 갱신: 2026-09-23
> 목적: 긴 대화가 압축되거나 담당 AI가 바뀌어도 실제 구현·검증·배포 상태를 잃지 않기 위한 기준 문서

## 2026-09-26 몽그리 플로팅 카드 + 카드에서 바로 답하기 (`claude/sync-to-origin-20260926`)

코덱스 인계 `docs/design-assets/mongri-floating-handoff-20260926/` 구현. 제품 결정 25항을 개정했다
(2026-09-23 읽기 전용 → 답하기 되살림, 사용자 "되살리되 원인 셋을 먼저 해결").

**첫 만남에는 알아맞히지 않고 묻는다**(사용자 추가 지시). 서버가 `recentEvents.length === 0`이면
`firstTurn`을 맥락에 실어 "무엇을 그리고 있는지 묻고 그럴듯한 것들을 선택지로 줘"라고 시킨다.
아이가 알려 준 답은 `recentContext`로 돌아와 다음 질문이 그 위에서 이어진다. P-014(뜬금없는 짐작)의
뿌리를 UI 차원에서도 막는 수다 — 몽그리가 판정하지 않고 아이가 알려 준다.

**읽기 전용으로 갔던 세 이유를 설계로 막았다.**
| 그때 문제 | 지금 |
|---|---|
| 답을 골라야 「이제 그려 볼 일」이 나타나 늦게 알았다 | **답하기 전부터 보인다** |
| 답을 바꿔 두 번 보내면 409 `이미 처리한 도움 기록이에요` | 새 `action:"reply"`가 **같은 줄을 덮어쓴다** |
| 칩을 눌러도 반응이 없었다 | 고른 즉시 **초록 윤곽 + 체크 + `aria-pressed`** |

- 서버 `reply`는 그린 뒤 기록용 `answer`와 **분리**했다. `answer`는 document·image·newElements를 요구하고
  한 번만 기록되므로 즉시 답하기에 그대로 붙이면 409가 돌아온다(인계도 같은 점을 지적했다). 그대로 남겨 뒀다.
- 넓은 가로 화면(≥900×600)은 도화지 **왼쪽 위 플로팅 카드**이고 도화지가 폭을 다 쓴다. 좁은 화면은 기존 바텀시트 유지.
- 장식 WebP 3종(`crayon-frame`·`name-swash`·`green-underline`)만 쓰고 시안 자체를 배경으로 깔지 않았다.
  몽그리는 기존 `/brand/mongri/*.png` 원본 그대로. 자산 목록에 출처·SHA-256 등록.

**직접 만들었다가 잡은 것 셋**
1. `.grimi-scroll`의 기본값이 `display:contents`라 상자가 없어, 내가 준 `max-height`·`overflow`가
   아무 일도 못 했다 → 「그냥 내 마음대로 그릴래」가 카드 밖으로 밀렸다. 넓은 화면에서만 진짜 상자로 만들었다.
2. 연속 탭에 **요청이 2번** 나갔다(실측). `replyState`는 비동기라 같은 틱에 둘 다 통과한다 → `replyingRef` 잠금.
3. 시안 차례(질문 → 답하기)로 두었더니 320·844에서 「이제 그려 볼 일」이 접힌 아래로 밀렸다
   (browser-check `onScreen:false`). **보장이 시안 차례보다 앞선다**고 보고 답 줄 위로 되돌렸다.

**검증**: typecheck·lint(오류 0, 경고 14)·`npm test` **411/411**·build·`git diff --check`·
`browser-check` **전항목 통과**(칩 보임·고르기 전 잠김·고르면 표남 세 검사를 새로 넣었다).
브라우저 실측 1180×820: 칩 고르기 → 보내기 열림 → 직접 쓰면 선택 해제 → 연속 탭에도 요청 1건 →
확인 문구 표시. 320·390·844·768에서 칩 52px, 가로 넘침 0, 탈출 버튼 카드 안.

**2026-09-26 후속: 몽그리 잘림과 중복 단추 정리**(사용자 지적).

- **몽그리가 잘렸다** — 내가 준 `margin:-34px 0 0 -30px`가 과해 얼굴이 본문 위로 **5px 넘쳤고**,
  `.studio`가 `overflow:hidden`이라 거기서 잘렸다(실측). `-16px 0 0 -26px`로 줄여 **넘침 0px**.
  ⚠️ 이때 처음 띄워 둔 데모 창이 **옛 빌드 CSS**(`7a6be7f3…`)를 물고 있어 더 심하게 잘려 보였다.
  그 창으로 진단할 뻔했다 — 창을 새로 띄워 현재 빌드(`e397c89b…`)에서 다시 쟀다.
- **중복 단추를 없앴다** — 카드에 나가는 길이 셋이었다: `×`, 「그리러 가기」, 「그냥 내 마음대로 그릴래」.
  이 중 **`×`와 「그냥 내 마음대로 그릴래」는 똑같이 `dismissGrimi()`를 불렀다**(이름만 다른 같은 단추,
  읽기 전용 시절의 유물이고 시안에도 없다). 후자를 없앴다. 남은 둘은 뜻이 갈린다 —
  **× = 몽그리 보내기**, **「그리러 가기」 = 접고 그리기**.
- 없애고 나니 **낮은 시트에서 나갈 길이 접힌 아래로 밀렸다**(`.free-exit`은 스크롤 밖에 고정돼 있었다).
  320×568은 답 줄을 바닥에 붙여 해결했고(`position:sticky`), 844×390은 시트가 너무 낮아 sticky로도
  부족했다(sticky는 부모 상자 안에서만 붙는다). 그 화면에서 **늘 보이는 나갈 길은 머리에 고정된 ×**이므로
  browser-check의 기대를 그 사실에 맞췄다 — 「그리러 가기」는 스크롤 뒤에 닿는지로 지킨다.
- **「다른 것도 물어보기」도 없앴다**(2026-09-26 사용자 결정). 머리 줄의 「몽그리 부르기」와 **완전히 같은
  `askGrimi()`**였고 카드가 열린 동안 둘 다 보여 같은 단추가 둘이었다. 다시 묻는 길은 머리 줄 하나로 모았다.
  browser-check의 「한 번 스크롤로 맨 아래 행동에 닿음」은 그 자리인 주 행동 「이렇게 답할래」로 옮겼다.

**카드 정리 결과**: 단추가 일곱에서 다섯으로 줄었다 — `×`(몽그리 보내기) · 답 칩 3개 ·
「이렇게 답할래」 · 「그리러 가기」(접고 그리기). 같은 일을 하는 단추가 하나도 남지 않았다.

**2026-09-26 후속 2: 「내 그림」이 아직 옛 보관함으로 갔다**(사용자 지적).

그림 자리가 생긴 뒤로 아이의 집은 `/student`인데, 「내 그림」을 누르면 옛 보관함이 떴다. 네 곳을 옮겼다.

| 화면 | 전 | 후 |
|---|---|---|
| 그리기 화면 뒤로가기 | `/student/archive` 「내 그림으로 나가기」 | `/student` 「내 그림 자리로 나가기」 |
| 그림 자세히 머리 | `← 내 그림` → 보관함 | `← 내 그림 자리` → `/student` |
| 그림책 목록 머리 | `← 내 그림` → 보관함 | `← 내 그림 자리` → `/student` |
| **보관함** | **돌아가는 길이 없었다(막다른 곳)** | 머리 첫 자리에 `← 내 그림 자리` 추가 |

보관함 자체는 그대로 살아 있다 — 그림 자리의 「내 그림 모두 보기」와 그림 자세히의 「다른 그림 보기」로 간다
(인계 지시: 기존 보관함과 그림책 편집기를 잃지 않는다).

실측으로 네 화면 모두 돌아가는 길이 `/student`를 가리키는 것과, 보관함에서 실제로 눌렀을 때
그림 자리가 뜨는 것을 확인했다.

⚠️ 이 확인 과정에서 **옛 서버가 3310을 붙잡고 있어 새 빌드가 뜨지 않았다**(`EADDRINUSE`, 로그에만 남고
`curl`은 200을 돌려줬다). 네 화면 모두 "돌아가는 길 없음"으로 잘못 읽힐 뻔했다 — 포트를 확실히 비우고 다시 쟀다.
같은 날 데모 창이 옛 CSS를 물고 있던 것과 같은 종류의 함정이다.

⚠️ **남은 위험**: 로컬에 `OPENAI_API_KEY`가 없어 **실제 모델 응답으로는 확인하지 못했다**. 코칭 응답을
흉내 내 화면 상태 기계만 돌렸다. 「첫 만남에 무엇을 그리는지 묻는지」와 선택지 품질은 운영에서 확인해야 한다.

**2026-09-26 후속 3: 몽그리 얼굴 잘림의 진짜 범인과 카드 크기, 그리고 답한 뒤 나갈 길**(사용자 지적).

- **얼굴 잘림**은 카드가 아니라 `.grimi-head>div`의 `overflow:hidden`이었다. 이름을 말줄임하려고
  넣은 규칙이 **밖으로 내민 얼굴의 음수 margin을 잘라 냈다**. 말줄임은 안쪽 `<b>`가 이미 하므로
  그 상자에서는 `overflow:visible`로 되돌렸다. 카드 기준으로만 재던 종전 "넘침 0px"이 왜 통과했는지의 답이다.
- **카드가 그리기를 방해할 만큼 컸다** → 폭 `min(560px,46vw)` → `min(400px,32vw)`, 여백·모서리·글자를 함께 줄였다.
  높이 607 → 517px. **칩 52px은 그대로 뒀다** — 아이 손가락이 닿아야 하는 값이다.
- **답을 보낸 뒤 나갈 길이 ×뿐이었다.** 「그리러 가기」가 답하기 블록(`.grimi-answer`) 안에 있어
  보내는 순간 함께 사라졌고, 남는 ×는 접기가 아니라 **코칭 이벤트를 dismiss** 하는 동작이다.
  `.grimi-answer-actions`를 블록 **밖으로** 옮겨 「그리러 가기」가 답 전후 모두 남게 했다.
  낮은 시트에서 이 줄을 바닥에 붙이는 sticky 규칙도 같은 wrapper를 잡으므로 함께 따라간다.
  혼자 남으면 폭을 다 쓴다(`:not(:has(.grimi-send))`).

검증: 320×568·390×844·844×390에서 `browser-check`가 **답을 실제로 보낸 뒤** 확인 문구가 뜨고
「그리러 가기」가 닿는 것을 본다(새 검사 2개). 1180×820 플로팅 카드에서도 실측했다 —
보낸 뒤 카드 519→410px, 「그리러 가기」 350px 폭으로 화면 안에 남는다.

**2026-09-26 후속 4: 답을 들으면 제안 한 줄을 다시 쓴다 + 몽그리 자리**(사용자 결정).

- 화면의 「이제 그려 볼 일」은 아이가 답하기 **전에** 만들어진 말이었다. `action:"reply"`가 답을 저장한 뒤
  `next_action` 한 줄만 새로 받아 돌려주고, 화면이 그 자리에서 바꾼다. 확인 문구는 고정.
  새 질문은 하지 않는다. 자세한 근거는 `docs/product-decisions.md` 학습 과정 25항.
- **깊이·옆 구분 없앰**: `새 소재나 새 주제를 네가 가져오지 않는다`를 지침에서 뺐다(사용자 결정).
- **코칭 예산 10분 8회 → 20회.** 답 뒤 제안 호출은 도움 기록당 첫 답 한 번으로 묶여 있어 이 예산을 따로 쓰지 않는다.
- `requestStructuredOpenAI`의 `imageDataUrl`을 선택으로 바꿨다 — 답 한 줄은 글자만으로 충분하고,
  그림을 실으면 아이가 「이렇게 답할래」를 누른 뒤 더 기다린다.
- **몽그리가 또 잘려 보인 건 CSS가 아니었다.** 실측으로 자르는 조상이 없음을 확인하고(모든 cut 0px)
  그림 파일을 열어 보니 `public/brand/mongri/*.png` **6장 모두 아랫단이 반듯하게 잘린 상반신**이다
  (opaque bbox가 224px 중 210~221에서 끝난다). 내가 넣었던 `margin:-12px 0 0 -20px` 내밀기가
  그 단면을 카드 모서리 위에 그대로 드러냈다. 내밀기를 빼고 머리 줄 안에 56px로 앉혔다.
  ⚠️ 단면 자체는 그림 파일의 성질이라 CSS로 없앨 수 없다 — 진짜 전신이 필요하면 그림을 새로 받아야 한다.

검증: typecheck·lint·test(413, +1)·build 통과. `browser-check`가 세 화면에서
**답을 보내면 「이제 그려 볼 일」이 실제로 바뀌는 것**까지 본다. 1180×820 실측으로 얼굴이 카드·머리 줄
어디에도 걸리지 않음을 확인했다(cut 0px).

⚠️ 로컬에 `OPENAI_API_KEY`가 없다. 새 `reply_next_action` 호출의 **실제 모델 문장은 확인하지 못했다** —
스키마·검사·화면 교체만 흉내로 돌렸다.

**2026-09-26 후속 5: 선생님이 보고 있다는 알림이 내내 떠 있었다**(사용자 지적).

배너가 `teacherViewing`에 그대로 묶여 있어 **선생님이 보는 동안 내내** 도화지를 가렸다.
알림은 알림이지 상태가 아니다 — 보기 시작할 때 **5초만** 뜬다. 문구도 「선생님이 지금 내 그림을
보고 있어요」 → **「선생님이 내 도화지를 보고 있어요」**로 바꿨다(사용자 지시).

`teacherViewing` 자체는 건드리지 않았다. 그 값은 자동 저장 간격(보는 동안 500ms/2s)과
자동 호출 억제가 쓰므로, 배너 표시만 `viewingNoticeOpen`으로 따로 뒀다. 보기가 끝나면 되돌려
다음에 다시 볼 때 알림이 한 번 더 뜬다.

실측(1180×820, `/api/student` 응답에 `teacherViewing`을 끼워 넣어):

| 시점 | 배너 |
|---|---|
| 보기 전 | 없음 |
| 보기 시작 | 「선생님이 내 도화지를 보고 있어요.」 |
| 3초 | 그대로 |
| 6초 | **사라짐** |
| 껐다 다시 보기 | 다시 뜸 |

## 2026-09-26 몽그리가 뜬금없는 짐작을 하던 것 (`claude/sync-to-origin-20260926`)

운영 제보: "너무 뜬금없는 대답을 한다", "집을 그리면 다른 걸로 추측해버린다". 미결정 P-014로 진단한 뒤
사용자가 **(가) 확신도로 갈린다**를 골랐다("가로 가자"). 원인이 둘이고 서로 곱해지던 것이라 둘 다 고쳤다.

**① 몽그리가 그림을 제대로 못 봤다 (결함)**
- AI에 보내던 이미지는 `imageData(canvasRef.current, 1024)` — 그린 칸을 자르지 않은 **도화지 전체**였다.
  넓은 도화지(span 3)는 대부분이 흰 여백이다. 거기에 `detail:"low"`가 긴 변을 **512px**로 또 줄였다.
  실측: 340×290 집이 모델 눈에 **57×48px, 넓이의 5.6%**.
- `documentImage()`로 바꿨다 — span>1이면 `contentBounds`로 그린 칸만 잘라 같은 크기에 **9배** 크게 담는다.
  썸네일·완성본은 이미 이 방식을 쓰고 있었고 **AI 경로만 안 쓰고 있었다.** `detail`도 `high`로 올렸다.
- 쓰는 곳이 없어진 `imageData()`는 지웠다.

**② 일부러 빗나가게 시키고 있었다 (설계 — 개정)**
- 종전 지시문: "일부러 덜 구체적으로 짐작한다 … **두 번째로 그럴듯한 읽기를 고른다.**"
  학습 과정 4항 `틀리는 해석자`(Weng et al. 2025)의 구현이라 버그가 아니었다.
  다만 그 전제는 **그럴듯하게 살짝 빗나간 짐작**(우산↔자전거 바퀴)이고, ①로 첫 짐작이 이미 틀리면
  거기서 또 두 번째를 고르므로 **완전히 무작위**가 된다.
- 새 규칙: **짐작은 실제로 본 것에서 나온다. 일부러 빗나가게 고르지 않는다.**
  알아보면 본 대로 짐작하고 확인을 청하고(「내 눈에는 집처럼 보이는데, 맞아?」),
  알아보기 어려우면 갈래로 말한다(「동물처럼 보이는데?」). 아이가 자기 말로 고치게 한다는 **목적은 그대로다.**
  묻는 것은 판정이 아니며, 아이의 답을 맞다 틀리다 가르지 않는다는 한계도 그대로다.
- `docs/product-decisions.md` 학습 과정 4항을 개정하고 종전 문장을 이력으로 남겼다. P-014는 종결.

**검증**: typecheck·lint(오류 0, 경고 14 — `imageData` 제거로 다시 기존 수준)·`npm test` **410/410**·
build·`git diff --check`·`browser-check` 전항목 통과.
옛 동작을 고정하던 시험 네 곳을 새 계약으로 옮겼다(`auto-grimi`·`contracts`·`mvp2-ai`·`drawing-tools`).

⚠️ **남은 위험**: 로컬에 `OPENAI_API_KEY`가 없어 **실제 응답 품질은 아직 비교하지 못했다.**
그림이 9배 크게 들어간다는 것과 지시문이 바뀐 것까지가 확인된 범위다. 운영 반영 뒤 실제 호출로
"집을 집이라고 하는지"를 확인해야 한다. `detail:"high"`는 토큰·비용·지연을 늘린다 — 그린 칸만 잘라
이미지가 작아진 만큼 상쇄되는 부분이 있지만 실제 청구는 관찰이 필요하다.

## 2026-09-26 도구 막대가 아직도 끌린다는 제보 — JS 방어를 막대에도 걺 (`claude/student-art-desk-20260925`)

**「저번에 고치지 않았느냐」에 대한 답: 고쳤고, 그 수정은 지금도 살아 있다.** 다만 부족했다.

- `28d0d8f`(2026-09-23)가 선택·끌기 금지 CSS에 `.tool-dock`을 넣었고, 지금도 그대로 있다
  (`-webkit-user-drag:none` · `-webkit-touch-callout:none` · `user-select:none`, 실측 확인).
- **그 커밋 자신이 적어 두었다**: "iOS 실제 길게 누르기 동작은 크롬으로 재현되지 않아 실기기
  재확인이 필요하다." 재확인이 없었고 제보가 이어졌다.
- 확인한 진짜 빈틈: **도화지는 CSS와 `onDragStart` 방어를 둘 다 갖고 있는데**(`canvas-wrap`과
  `draw-canvas` 각각), 막대는 2026-09-23에 **CSS만** 받았다. 같은 JS 방어를 막대에 걸었다.
  끌기 이벤트는 위로 올라오므로 막대 하나면 안의 도구 그림·색 단추가 모두 덮인다.
  `onContextMenu` 방어도 함께 걸었다(길게 누르기 메뉴).
- 그 밖에 이미 막혀 있어 원인이 아니었던 것들: 막대를 접는 스와이프는 손잡이 단추에만 걸려 있고,
  손잡이와 굵기 슬라이더에는 `touch-action:none`이 있으며, `.studio`가 `overflow:hidden` +
  `overscroll-behavior-y:none`로 페이지 고무줄 스크롤을 막는다.

**검증**: 브라우저에서 실제 `dragstart`를 쏴 확인했다 — 도구 그림·막대 **둘 다 막힘(defaultPrevented)**,
**비교군으로 규칙 밖 머리 줄 단추는 막히지 않는다**(검사가 헛돌지 않음). 계산값 `user-select:none`,
`-webkit-user-drag:none`, `draggable="false"`.
`npm test` 410/410, typecheck·lint(경고 14)·build·`git diff --check`·`browser-check` 전항목 통과.

⚠️ **남은 위험(2026-09-23과 같은 종류)**: iOS의 길게 누르기 끌기는 **크롬으로 재현되지 않는다.**
여기서 막은 것은 표준 `dragstart` 경로이고, 도화지에서 같은 방어가 실제로 듣고 있다는 점이 근거다.
**실기기(아이패드) 재확인이 필요하다.** 이번에도 안 되면 원인이 끌기가 아니라 다른 제스처라는 뜻이므로,
무엇이 어떻게 움직이는지(도구 그림만? 막대 전체? 화면 전체?) 확인해야 한다.

## 2026-09-26 「종이가 가득 찼어」의 정체와 세 가지 수정 (`claude/student-art-desk-20260925`)

운영(`https://www.kkumtle.app`, 아이패드)에서 **여백이 많은데도** 「🌟 종이가 가득 찼어!」가 떴다는 제보.

**진단**: 넓이와 무관하다. `documentTooLarge()`(`DrawingStudio.tsx`)가 보는 것은 둘뿐이다 —
획 4,800개(= `MAX_DOCUMENT_OPS` − 200), 또는 직렬화 용량 1.15MB(= `MAX_DOCUMENT_BYTES` − 100KB).
제보 그림은 획이 수십 개이므로 **용량**에 걸렸다. 용량을 채우는 것은 **점 개수**다(점 하나 ≈ 44바이트,
1.15MB ÷ 44 ≈ 26,000점). 굵은 붓으로 넓은 면을 여러 번 칠하면 화면엔 덩어리 하나인데 점이 수만 개
쌓인다. 막아 둔 이유 자체는 옳다 — 넘기면 이후 모든 저장이 413으로 실패해 작품이 조용히 유실된다.

**고친 것 (2026-09-26 사용자 결정: 가·나·다 모두)**

1. **(가) 문구** — 「종이가 가득 찼어」는 화면과 어긋나는 거짓말이었다.
   → 「그림이 아주 커졌어! ‘완성’을 눌러 저장하자」(저장 상태 줄도 같은 뜻으로).
2. **(나) 점 간격을 굵기에 맞춤** — 종전에는 굵기와 상관없이 2.5 고정이었다. 렌더러는 점을 찍지 않고
   둥근 이음으로 **이어** 그리므로 굵은 붓에 촘촘한 점은 낭비다. `strokePointGap(tool, width)` =
   `max(2.5, 보이는굵기 × 0.25)`. 가는 연필은 바닥값 2.5가 그대로라 세밀한 그림의 밀도는 그대로다.
   도구별 굵기 배율(marker 1.6, watercolor 2)은 `TOOL_DRAWN_WIDTH_SCALE` 한 곳으로 모아
   렌더러와 간격 계산이 같은 값을 보게 했다.
3. **(다) 한도 1.25MB → 2.5MB.** 완성 PNG는 별도 바이너리로 올라가고 본문에는 키만 실리므로
   (`lib/save-transmit.ts` `splitCompletionBody`) 최악 본문은 문서 2.5MB + 썸네일 ≈ 3.2MB로
   Vercel 4.5MB 한도 아래다.
   ⚠️ **`app/api/ai/coaching/route.ts`에 1,250,000이 숫자로 박혀 있었다.** 그대로 뒀으면 2.5MB짜리
   그림은 저장은 되는데 **몽그리 부르기만 413**으로 막혔을 것이다. 같은 상수를 쓰게 고치고 시험으로 묶었다.

**검증**: typecheck·lint(오류 0, 경고 14)·`npm test` **409/409**(새 시험 4개)·build·
`git diff --check`·`browser-check` 전항목 통과.
(나)의 효과와 위험을 헤드리스 크롬에서 실측했다 — 같은 왕복 경로를 두 간격으로 렌더해 비교:
**점 1,483개 → 536개(2.8배 감소)**, 달라진 픽셀 **784 / 1,048,576 = 0.07%**(곡선 가장자리
안티앨리어싱뿐). 즉 같은 그림을 훨씬 적은 용량으로 담는다.
(나)와 (다)를 합치면 굵은 붓으로 칠할 수 있는 양이 **대략 5~6배**로 는다.

**썸네일만 WebP 무손실로 바꿨다**(2026-09-26 사용자 결정). 썸네일은 `thumbnailDataUrl`로
**자동 저장마다** 본문에 실린다(완성본은 완성할 때 한 번뿐). 반복 비용이 거기 있어 거기만 바꿨다.

클라이언트 기준 실측(물감 많이 쓴 그림, 픽셀 손상 0 / 최대 오차 0):

| | PNG | WebP 무손실 |
|---|---|---|
| 썸네일 256 | 40.3KB | **23.9KB (−41%)**, 본문 base64 53.7 → 31.8KB |
| 썸네일 인코딩 | 2.2ms | **1.8ms (더 빠름)** |
| 완성본 1024 | 248.5KB | 99.6KB이지만 인코딩 4.5ms → **14.8ms(3배)** → **PNG로 둠** |

업로드 환산: 평소(6초마다) 537KB/분 → 318KB/분, **선생님이 볼 때(2초마다) 1.6MB/분 → 954KB/분.**

- 클라이언트는 256일 때만 `encodeThumbnail()`로 굽는다. `toDataURL`은 못 굽는 형식을 주면
  **말없이 PNG를 돌려주므로** 결과 접두사를 보고 판단한다 — 옛 기기는 자동으로 PNG가 되고 서버가 둘 다 받는다.
- 서버는 썸네일만 `THUMBNAIL_TYPES`(webp+png), 완성본은 `FINAL_TYPES`(png)만 받는다.
  R2에 **받은 형식을 그대로** 적는다(하드코딩하면 WebP를 PNG라고 적어 보내 그림이 깨진다).
- **옛 작품은 그대로 열린다** — 서빙이 저장된 `contentType`을 쓰고(`image/route.ts`), 옛 썸네일은 PNG로 남아 있다.
- 함께 고친 곳: `ai/teacher-draft`의 `imageMime`이 PNG·JPEG만 알아 **WebP 썸네일이면 415로 막혔을 것**이다
  (썸네일을 먼저 읽는다). `family/session`은 `"image/png"`를 하드코딩하고 있었다 — 지금은 완성본(PNG)만
  읽어 문제가 없지만 같은 종류의 지뢰라 저장된 형식을 쓰게 바꿨다.

실제 왕복 확인: 저장 200 · 서빙 `content-type: image/webp` · 브라우저에서 256×144로 디코딩 ·
완성본에 WebP를 보내면 **413으로 거부**(PNG 전용 유지) · 같은 그림 2,210B(WebP) vs 3,789B(PNG).

남은 위험: 자동 저장은 문서 전체를 보내므로 한도를 올린 만큼 최악 저장 본문도 커진다(2.5MB).
다만 (나)로 같은 그림의 실제 용량이 줄어 평소 저장은 오히려 가벼워진다. 느린 교실 와이파이에서의
체감은 운영 반영 뒤 확인이 필요하다.

## 2026-09-25 참여 코드 뒤 「그림 자리」 (`claude/student-art-desk-20260925`)

코덱스 인계 `docs/design-assets/student-art-desk-handoff-20260923/`(시안 `reference/selected-art-desk.png`) 구현.

- **갈림길은 저장된 그림 총수 하나다.** 0장이면 중간 화면 없이 바로 `/student/draw/new?mode=free`,
  1장 이상이면 「그림 자리」(`StudentArtDesk`)를 보여 준다. 페이지 크기(artworks 40장)가 아니라
  `artworkTotal`로 본다 — 41장째부터 0장으로 읽히면 안 된다.
- **그리던 그림을 자동으로 열지 않는다.** 종전 `StudentEntry`는 `latestUnfinishedArtwork`가 있으면
  곧바로 그것을 열었고, 그래서 그리다 만 그림이 있는 아이는 **새 그림을 시작할 길이 아예 없었다.**
  이제 목록 카드에서 아이가 직접 「이어 그리기」를 고른다.
- 한 화면에 **내 그림(최근 3장) + 새 그림 그리기 + 내 그림책(2권)** 이 함께 있다. 나머지는 기존
  `/student/archive`·`/student/books`로 보낸다 — 두 화면과 그림책 편집기는 **그대로 두었다.**
- 시안을 배경 한 장으로 깔지 않았다. 바탕은 글자 없는 `desk-surface.webp` 하나뿐이고, 화분·문구·노트는
  분리된 장식으로 모서리에만 놓았다(`aria-hidden`, `pointer-events:none`, 899px 미만 숨김).
  그림·책 제목·쪽 수·상태·행동은 모두 실제 데이터와 DOM이다.
- **책 표지 썸네일은 목록 응답에 없다**(`/api/storybooks`가 주지 않는다). 인계 지시대로 임의의 아이
  그림을 만들어 넣지 않고 빈 책 틀 SVG + DOM 제목만 쓴다. 표지가 필요하면 별도 미리보기 경로가 필요하다.
- 좁은 화면은 「새 그림 그리기」가 내 그림보다 **먼저** 보인다(인계 지시). CSS `order`로만 옮기면
  보이는 차례와 탭 차례가 어긋나므로 DOM에서 먼저 두고, 1100px 이상에서만 `order`로 줄 끝에 보낸다.
- 장식 에셋 9종을 `public/student-desk/`에 넣고 `public/brand/asset-manifest.json`에 출처·SHA-256과
  함께 등록했다. 인계가 "필요 없으면 생략"이라 한 `crayons-edge.webp`(196KB)는 쓰지 않아 뺐다.

**검증**: typecheck·lint(오류 0, 경고 14 — 기존 수준)·`npm test` **405/405**(새 시험 7개 포함)·
production build·`git diff --check`·`browser-check` **전항목 통과**.
320×568 / 390×844 / 844×390 / 1440×1000 실측: 페이지 가로 스크롤 0, 44px 미만 터치 목표 0,
카드 안 링크 중첩 0, 장식은 1440에서만 3장 보이고 좁은 화면에서는 숨는다.
0장 분기도 실측했다 — `/student`를 거치지 않고 바로 도화지로 갔다.

두 군데의 **낡은 기대를 함께 고쳤다.** 둘 다 옛 자동 이동을 고정하고 있었다.
- `tests/feedback-20260809.test.mjs` — `latestUnfinishedArtwork`로 자동 이동하던 단언.
- `scripts/browser-check.mjs` 재입장 검사 — 도착지를 `/student/draw`로 못 박고 있었다. 이 검사가
  지키는 것은 「동물을 다시 묻지 않는다」이므로, 도착지는 그림 수에 따라 달라지는 둘 다 받게 했다.

**완성한 뒤에도 이 화면으로 돌아온다**(2026-09-25 사용자 지시). `complete()`가 성공하면 종전에는
`/student/archive`(보관함)로 갔는데 이제 `/student`로 간다 — 방금 그린 그림과 새 그림·그림책을
한자리에서 본다. 보관함은 「내 그림 모두 보기」로 한 번에 간다. 같은 뜻으로 ① 완성본 사본을 만든 뒤
② 이미 완성된 그림을 열려고 했을 때(두 군데 `disposition.action === "archive"`)도 `/student`로 보낸다.
스튜디오 머리의 뒤로 가기(「내 그림으로 나가기」)는 이름 그대로 보관함을 가리키므로 그대로 뒀다.
`tests/offline-save-queue.test.mjs`가 도착지를 `/student/archive`로 못 박고 있어 함께 옮겼다.
실측으로 확인했다 — 도화지에 획을 긋고 완성 → 소감 → 「작품 완성」까지 누르면 `/student`에 도착하고
그림 자리가 뜬다(390×844).

**저장하는 동안 화면 전체를 덮는다**(2026-09-25 사용자 요청). 종전에는 소감 모달의 단추 안에서만
표시가 돌아 아이가 눈치채지 못하고 그동안 다른 것을 누르려 했다. 이제 모달(z-index 20)보다 위에
막(`.saving-veil`, z-index 30)을 깔고 가운데에 몽그리·「그림을 저장하고 있어요」·움직이는 점 세 개를
보여 준다. 글을 못 읽어도 무엇을 기다리는지 알게 얼굴과 움직임을 함께 둔다. 움직임 줄이기에서는
튀지 않고 밝기만 바뀐다. 종전의 `.completion-pending` 한 줄은 막이 대신하므로 없앴다.
실측(390×844, 저장 요청을 일부러 늦춰 확인): 막이 390×844로 화면을 꽉 덮고, 모서리·닫기 자리를
눌러도 `.saving-veil`이 받는다(뒤로 새지 않음). 닫기 단추는 그대로 `disabled`다.

**2026-09-26 시안에 맞춰 생김새를 다시 손봤다**(사용자: "실제 이 이미지처럼 보이고 싶었는데 좀 다르네").
첫 구현이 구조는 맞췄지만 모양이 시안과 어긋나 있었다. 고친 여섯 가지:

| 어긋난 곳 | 시안 | 고친 뒤 |
|---|---|---|
| 그림 카드 | 책상에 놓인 흰 종이(테두리 없음) | 둥근 테두리 + 안쪽 흰 사각형(이중 액자)을 걷어내고 그림자만 남김 |
| 카드 아래 줄 | 제목·날짜 왼쪽 / 행동 오른쪽 **한 줄** | 세로로 쌓던 것을 한 줄로 — 카드가 짧아져 한 줄에 네 장이 들어간다 |
| 「그리는 중」 배지 | 진초록 알약 | 노랑 → 진초록(글자는 그대로라 색만으로 구분하지 않는다) |
| 새 그림 그리기 | 흰 종이 + 큰 연필 | 점선 상자를 없애고 다른 그림과 같은 종이로 |
| 제목 줄 | 노란 밑줄과 안내가 **한 줄** | 밑줄 아래로 줄바꿈하던 것을 나란히 |
| 그림책 | 책이 맨몸으로 놓이고 글이 옆에 | 테두리 카드를 걷고 책을 살짝 눕혀(rotateY) 평면처럼 보이지 않게 |

메모지 글자(「오늘도 멋진 그림 기다릴게!」)를 HTML로 올렸다 — 인계대로 그림 속 메모지는 비어 있다.
빼 두었던 `crayons-edge.webp`도 왼쪽 아래 장식으로 되살리고 자산 목록에 다시 등록했다.

실측(320 / 390 / 844 / 1100 / 1536): 페이지 가로 스크롤 0, 44px 미만 터치 0, 중첩 링크 0.
카드 아래 줄은 320px(카드 폭 200)에서도 한 줄 44px이고 제목이 잘리지 않는다.

남은 위험: 책 표지가 빈 틀이라 책이 여러 권이면 겉보기로 구분되지 않는다(제목·쪽 수는 글자로 있다).
표지 썸네일 API는 이번 범위 밖이다.

## 2026-09-23 후속 화면 DESIGN.md 적용 (검증 완료, PR #18 제출)

- 직전 PR #17은 main `e56896d`에 병합됨을 GitHub에서 확인했다. 해당 최신 main에서 `codex/design-system-audit-20260923` 분기. 최종 fetch에서도 main 변동 없음. 이번 디자인 수정은 아직 병합·배포하지 않았다.
- 첫 화면·학생 입장·그리기 경로를 제외하고 학생 그림책/보관함, 교사, 관리자, 가족 경로에만 `DesignSurface`를 둔다. 문서의 초록·노랑/어른 진초록, 폰트·버튼·모서리·평면 표면 규칙을 적용한다. 실제 작품 내용·좌표·배경·본문 폰트는 유지한다.
- 교사 편집기의 고정 안내가 도구를 덮는 문제는 작업 영역 안 접이식 안내로 옮겼다. 모바일 제목/저장 버튼 배치를 정리하고 도구 글자 잘림·미리보기 폭 넘침을 수정했다. 경로별 적용/보존 범위는 [디자인 점검 기록](design-audit-2026-09-23.md)에 정리했다.
- 최종 production build 포함 `npm test` **393/393**, `typecheck`, `lint` 오류 0(기존 경고 14), 작업 diff check 통과. 재사용 가능한 디자인 브라우저 검증은 **21화면 × 4크기(1440×1000, 320×568, 390×844, 844×390)**에서 넘침·44px 버튼·편집 헤더 겹침·도구 글자 잘림·피드백 PDF 바 대비를 통과했다.
- 첫 화면/입장/그리기는 수정 전 빌드에서 보관한 computed style과 4개 크기로 비교해 동일함을 확인했다. 일반 모바일, 직접 클릭/한글 입력/저장/완성 조건, 피드백 PDF·ZIP, 주문/PDF 가져오기/관리자 모의 발주, 실시간 보기·조언 브라우저 회귀 검증도 통과했다. 실시간 반영은 로컬 952ms 실측이며 운영 지연 보장은 아니다.
- 로컬 예시 데이터 미리보기: 학생 `http://localhost:51880/student/books/demo`, 교사 `http://localhost:51880/teacher/class/class_feedbackpreview/books/feedback`. 앱의 브라우저 패널에 두 화면 열기를 요청했다. 검증 로그·스크린샷은 Git 제외 `work/design-*`에 보관한다. 운영 DB/파일·main에는 쓰지 않았다.
- 기능 커밋 `0f978af`를 개인 포크에 push하고 운영 저장소 main 대상 [PR #18](https://github.com/yonghwan86/wiggle_web/pull/18)을 제출했다. OPEN·MERGEABLE·원격 HEAD 반영 확인. 최신 운영 main 대비 diff check 통과(오래된 로컬 main의 기존 notes.md EOF 경고는 별도). Vercel의 fork 미리보기는 관리자 배포 승인 대기이며, main 병합·운영 배포는 하지 않았다.

## 2026-09-23 이야기 쓰기 버튼 제거·직접 입력 (검증 완료, PR #17 제출)

- PR #16 병합 main `af95a2f`에서 `codex/storybook-direct-text-20260923` 분기. 글 영역 부모의 과거 `pointer-events:none` 규칙이 직접 클릭을 막고 있음을 확인했다.
- 편집용 textarea에 pointer-events를 명시적으로 복원하고 ‘이야기 쓰기’ 버튼과 버튼용 ref를 제거했다. 직접 초점을 받으면 그림 선택을 해제한다. 읽기 전용 미리보기는 유지한다.
- 이전 `.fill()` 검사는 마우스 hit testing을 거치지 않아 이 문제를 발견하지 못했다. 실제 브라우저에서 클릭→포커스→키보드 입력, 1440/320/390/844px 너비에서 그림 선택 후 터치→초점·선택 해제를 확인했다. 자동 줄바꿈·한글 조합·저장·제목 경합·완성 조건 회귀 검증도 통과했다.
- `typecheck`, `lint`(오류 0, 기존 경고 14), production build 포함 `npm test` 393/393, 변경분 `git diff --check` 통과. 일반 브라우저 검증은 320×568/390×844/844×390에서 통과(390px 핀치만 기존 CDP 환경 문제로 제외).
- 최신 운영 기준 `legacy-origin/main` 대비 공백 검사는 통과했다. 오래된 로컬 `main...HEAD` 검사는 기존 `docs/design-assets/studio-tool-dock/notes.md:107`의 EOF 빈 줄을 보고하며 이번 변경과 무관하다.
- 로컬 미리보기: http://localhost:59625/teacher/class/class_feedbackpreview/books/storybook_livepreview0/edit — 브라우저에서 글 영역을 실제 클릭하고 “책 위를 눌러 바로 이야기를 써요.” 입력까지 확인했다. 운영 데이터는 사용하지 않았다. 이 추가 수정은 아직 병합·배포하지 않았다.
- 기능 커밋 `90f359b`를 개인 포크의 전용 브랜치에 push하고 [PR #17](https://github.com/yonghwan86/wiggle_web/pull/17)을 운영 저장소 main 대상으로 제출했다.

## 2026-09-23 PR #16 병합·운영 배포 완료 확인

- 사용자 상태 확인 요청으로 GitHub API를 조회했다. [PR #16](https://github.com/yonghwan86/wiggle_web/pull/16)은 한국시간 2026-09-23 03:34:39에 병합됐으며 main은 `af95a2f`이다. 최종 기능 커밋 `1bc9491`(실시간 보기·조언·버튼 배치 포함)을 모두 포함한다.
- 병합 커밋의 Vercel 체크와 Production deployment `6597991823`은 한국시간 03:36:01에 success / Deployment has completed로 확인됐다. [운영 배포 기록](https://vercel.com/yonghwan86s-projects/wiggle-web/3KQX9SqiyS4ydQbTnStTEVcRGmDe). PR 브랜치의 과거 승인 필요 실패는 운영 배포 성공과 별개다.
- 이 확인에서는 코드 변경·병합·추가 배포를 실행하지 않았다. 실제 수업 코드로 운영 저장·이미지·실시간 조언을 재현하는 배포 후 실측은 아직 수행하지 않았다.

## 2026-09-23 그림책 실시간 보기·조언 및 수정 버튼 배치 (검증 완료, PR #16에 추가)

- 열린 PR #16의 기존 기능 브랜치에서 이어서 구현. 운영 main은 여전히 `e7da180`이다. 초안 목록을 flex 행·간격·44px 버튼으로 바꿔 긴 제목과 작은 화면에서 ‘이어서 수정’이 겹치지 않는다.
- 수업실 오늘 수업과 그림책 목록에서 `/teacher/class/[id]/books/live`로 진입한다. 학생 목록은 6초마다 메타데이터만, 선택한 학생의 책은 1초마다 저장 번호/현재 쪽을 확인한다. 그대로인 쪽은 document JSON 읽기·본문 전송을 생략하고 바뀐 한 쪽만 보낸다. 이미지 blob 캐시를 재사용한다.
- 학생은 현재 책·쪽·접속 상태와 해당 책의 최근 조언을 4초마다 가볍게 교환한다. 쪽 이동은 250ms 뒤 알린다. 기존 teacher_views 만료 방식으로 함께 보기 여부를 표시하며 교사가 보는 동안 입력 후 500ms·연속 입력 최대 2초로 저장한다. 일반 편집도 연속 입력 최대 10초로 저장 지연 누적을 막는다.
- 쪽별 교사 조언(300자)과 학생 읽음 확인. 조언 작성 시작 시 대상 쪽을 고정해 학생이 쪽을 넘겨도 다른 쪽으로 보내지 않는다. 책 원문·revision을 변경하지 않는다. 활성 학급·학생·소유권 검증, 요청 중복 방지, 숨김 탭/잠시 멈춤/언마운트 시 폴링 중단을 적용했다.
- 런타임·Drizzle 정본에 `storybook_presence`, `storybook_advice`와 조언 조회 인덱스를 추가한다. 기존 데이터 변경 없음. 전체 production build·테스트 **393/393**, typecheck·lint 오류 0(기준선 경고 14) 통과. HTTP에서 교사/학생/학급 분리, 비활성 학생 거절, CSRF, 현재 쪽·revision·변경 없는 응답·조언·읽음·만료를 검증했다.
- 실제 두 브라우저: 글 변경→교사 반영 948ms(로컬 실측, 운영 보장 수치 아님), 쪽 따라가기/수동 이동, 같은 이미지 재사용, 조언 대상 고정·읽음, 연속 입력 자동 저장, 중단/숨김, 1.5초 응답 지연 중 최대 동시 조회 1건. 320/390/844/1440폭 넘침·44px 버튼·초안 버튼 간격 통과. 기존 학생 편집 브라우저도 재통과. 로그와 화면은 Git 제외 `work/book-live-*`에 보관한다.
- 교사/관리자 PDF·주문·가져오기·5화면×4크기 및 일반 모바일 320×568/390×844/844×390 브라우저 검증도 통과했다. 작업 diff와 실제 운영 main 기준 diff check 통과(오래된 로컬 main의 기존 notes.md EOF 경고는 이번 변경이 아님).
- 예시 데이터 미리보기: `http://localhost:60978/teacher/class/class_feedbackpreview/books/live`. 사용자 요청으로 같은 기능 브랜치에 커밋·push하여 [PR #16](https://github.com/yonghwan86/wiggle_web/pull/16)에 함께 제출한다. main 병합·배포는 하지 않는다. 운영 저장·이미지·실시간 지연은 사용자 배포 후 실제 수업 코드로 확인해야 한다.

## 2026-09-23 그림책·피드백 변경 push 및 PR 제출

- 사용자 요청 “싹 다 push 하고 pr 날려”에 따라 기능 커밋 `46bd780`과 평가 기준 하단 이동 `6c4d2a0`을 개인 fork `qudcks1940/wiggle_web`의 `codex/storybook-editor-fixes-20260923`에 push하고 운영 저장소 main 대상 [PR #16](https://github.com/yonghwan86/wiggle_web/pull/16)을 생성했다.
- 최신 운영 main을 fetch하여 `e7da180`으로 변동이 없고 현재 브랜치의 조상임을 확인했다. 코드 변경 없이 기존 production build·392/392 테스트·typecheck·lint·브라우저/PDF 검증 결과를 유지하며 diff check를 다시 확인했다.
- GitHub에서 PR OPEN·충돌 없음(MERGEABLE), 변경 파일 31개와 push한 HEAD 반영을 확인했다. Vercel 체크는 `Authorization required to deploy.`로 실패 표시되며, 미리보기 배포에 프로젝트 관리자의 승인이 필요한 상태다.
- 이 작업에 속한 모든 기능 변경을 포함한다. 기존 학교 제안서 산출물·의존성 폴더와 다른 작업의 인수인계 파일은 그대로 보존했다. main push·PR 병합·운영 배포 없음. 배포 후 실제 수업 코드로 운영 저장/이미지 경로 실측이 필요하다.

## 2026-09-23 피드백 평가 기준 위치 조정 (로컬 검증 완료, 사용자 미리보기)

- 사용자 요청으로 평가 기준·엑셀 양식/교체·전체 배점과 AI 프롬프트를 학생별 PDF 구성/미리보기 아래, 페이지 마지막으로 이동했다. 상단에는 피드백 생성만 유지한다.
- 엑셀 교체 결과·오류도 하단 기준 영역에서 표시하며 생성/교체의 중복 실행 방지는 유지한다. 기존 기능 브랜치에서 수정하며 운영 반영 없음.
- production build + 전체 테스트 **392/392**, typecheck, lint 오류 0(기준선 경고 14), 피드백 브라우저 검증 및 일반 3개 화면 크기 검증 통과. 실제 DOM과 화면에서 평가 기준이 PDF 미리보기 다음이자 마지막 영역임을 확인했다. 작업 diff와 실제 운영 main 기준 diff check 통과(오래된 로컬 main 기준의 기존 EOF 빈 줄 경고는 이전과 동일).
- 미리보기: `http://localhost:64809/teacher/class/class_feedbackpreview/books/feedback`. 기능 브랜치에 커밋했으며 이후 사용자 요청으로 위 PR #16에 포함했다. main push·배포 없음.

## 2026-09-23 학생 그림책 편집·완성·인쇄 개선 (로컬 검증 완료, 사용자 미리보기)

- PR #15 병합 main e7da180에서 codex/storybook-editor-fixes-20260923 분기. 기존 작업과 추가 병합을 포함한다. 사용자에게 미리보기를 먼저 제공하며 main push·운영 배포 없음.
- 책의 글 영역에 직접 왼쪽 정렬로 입력, 한글 조합·자동 줄바꿈·가시 영역 입력 한도. 기존 긴 글은 원문을 보존하고 표시 크기를 줄여 모두 보이게 한다. 우측 본문 입력란 제거.
- 저장 응답은 최신 편집 상태의 revision/status만 갱신하여 입력 중 제목을 이전 값으로 되돌리지 않는다. 빈 제목 초안은 보존하고 기본 제목/빈 제목 완성은 클라이언트·서버 모두 차단. 학생은 표지 포함 최소 24쪽. 학생 상한은 표지 1+내지 130=131쪽.
- 이미지 요청·blob URL을 편집기 생명주기 안에서 공유하고 32MB 초과 시 미사용 항목을 정리한다. 그림 선택 창은 보이는 썸네일부터 로드한다. 기존 모서리 비율 조절과 ‘비율 유지’ 해제 후 한 축 조절을 함께 제공한다.
- 완성/미리보기 화면에 첫 화면으로 링크. 피드백 생성·엑셀 설정은 피드백 관리에 모으고 완성책 목록의 중복 생성/다운로드 부분 제거.
- 인쇄는 첫 쪽만 앞표지, 2쪽~끝만 내지, 뒤표지는 빈 배경. 제작사 최소 24·짝수 내지 규격의 빈 쪽은 끝에만 추가(총 24쪽 책은 내지 23+빈 1). 이전 구분 방식으로 준비한 PDF는 재생성해야 다운로드/새 주문 접수 가능. 이미 접수한 주문 원고는 보존한다.
- 첨부 영상에서 제목이 되돌아가는 현상을 확인. production build + 전체 테스트 **392/392**, typecheck, lint 오류 0(기준선 경고 14). 작업 diff 및 실제 운영 main 기준 diff check 통과. 오래된 로컬 main...HEAD 검사는 기존 notes.md:107 EOF 빈 줄을 보고하며 이번 변경이 아니다.
- 학생 브라우저: 지연 PUT 응답 중 제목 변경→저장→재접속, 한글 조합·줄바꿈 보존·글 넘침 차단, 한 축 크기 변경·재접속, 쪽 재방문/미리보기 이미지 GET 재사용, 23쪽/기본·빈 제목 차단·24쪽 완성·완성 상태 유지, 320/390/844/1440 폭 실제 DOM 통과.
- 교사 브라우저: 피드백 생성·중단·재개·서버 진행 재확인·선택 항목만 PDF·다권 ZIP·오류 복구·엑셀 양식, 5개 폭 통과. 인쇄 준비·교사 요청·관리자 모의 발주·PDF 가져오기 편집(3쪽 교사책)·5화면×4크기 통과. 일반 check:browser도 320×568/390×844/844×390 전 항목 통과(DevTools 연결을 위해 승인된 샌드박스 밖 실행).
- 생성 표지와 내지를 실제로 렌더링하여 한글 배치·첫 내지·마지막 원본·빈 쪽 확인. 픽셀 검사로 첫 쪽의 내지 중복/마지막 쪽의 뒤표지 재사용이 없음을 검증했다. 이전 형식 준비 파일 다운로드 409 및 재생성 경로도 실제 HTTP 통과.
- 로그·스크린샷·PDF는 Git 제외 work/storybook-0923/. 최종 미리보기 서버는 http://localhost:59088: 학생 /student/books/storybook_c210dfc6b7c0416e, 교사 /teacher/class/class_feedbackpreview/books. 예시 데이터만 사용했다. 브라우저에 학생 편집 화면을 열었다.
- 기능 브랜치에 작업 커밋했으며 이후 사용자 요청으로 위 PR #16에 push·제출했다. main 병합·운영 배포 없음. 실제 운영 저장/이미지 경로 실측은 사용자 배포 후 수업 코드로 진행해야 한다.

## 2026-09-20 피드백 구성 선택·엑셀 안내 (로컬 검증, 사용자 미리보기)

- 2026-09-22 추가 요청: PR #12 병합 뒤 운영 main `2cdc64f`를 기존 PR #15 브랜치에 병합했다. 직전 main `da246fb`와의 실제 차이는 `docs/current-state.md`뿐이며 코드·테스트·설정은 동일하다. 날짜 충돌을 정리하고 문서 기록을 보존했다. 앱 트리가 앞서 389/389 검증한 `4b101b6`과 동일함을 Git diff로 확인했으므로 문서 병합 뒤 전체 테스트는 반복하지 않았다. PR만 갱신하며 main push·운영 배포는 하지 않는다.
- 2026-09-22 사용자 요청으로 변경을 커밋하고 최신 운영 main `da246fb` 위로 rebase 완료(`157074a`). 코드 충돌 없이 상태 문서의 갱신 날짜만 해결했다. 최신 main의 입장 잠금·그림 보관함·넓은 도화지 변경을 보존했다.
- 재검증: production build + 전체 테스트 **389/389**, typecheck, lint 오류 0(기준선 경고 14), 실제 PR 기준 `git diff --check legacy-origin/main...HEAD` 통과. 보관함 테스트에도 LF 전용 정규식이 있어 CRLF를 허용하도록 수정했다(제품 코드 변경 없음). 일반 브라우저 320×568·390×844·844×390 및 피드백 관리 320/390/768/844/1440 폭·PDF·ZIP·엑셀 시나리오 재검증 통과. 운영 안내 문서도 새 UI와 파일명으로 갱신했다. 로그: `work/feedback-pr-*.log`.
- 기능 브랜치를 개인 fork `qudcks1940/wiggle_web`에 push하고 운영 저장소 main 대상 [PR #15](https://github.com/yonghwan86/wiggle_web/pull/15)를 생성했다. 구현 `157074a`, 최신 main 검증·안내 갱신 `cae6b5b`. main push·병합·배포 없음.
- 실제 운영 원본 `legacy-origin/main` (`yonghwan86/wiggle_web`, `7f9c7e2`)에서 `codex/feedback-workspace-20260920` 분기. `origin/main`에는 이 기능이 없어 운영 main 기준으로 작업했다.
- 그림책 목록의 학년·반 저장 UI와 PDF 필수 입력 조건을 제거했다. 파일명은 `번호_이름_책제목_피드백.pdf`, 번호/이름이 없으면 제공된 정보와 ‘학생’으로 생성한다. 인쇄 주문의 학급 정보는 변경하지 않았다.
- 현재 루브릭의 XLSX 수정 양식 다운로드(담당 학급 권한 확인), 실제 A·B·C열 미리보기, B/C열 수정·영역 추가/삭제·점수 합계 안내를 추가했다. 생성 파일의 ‘수정 안내’ 시트에도 사용법을 포함한다. 다운로드→수정→재업로드를 실제 파서로 확인했다.
- `/teacher/class/[id]/books/feedback`에서 학생 ID별 책 목록·이름/번호/제목 검색·완료 상태 필터·현재 책 버전의 평가 결과 선택, 평가 영역·점수·종합 의견·근거 쪽 번호 선택과 내용 미리보기를 제공한다. 개별 PDF, 선택한 여러 책 ZIP, 같은 기준의 책에 구성 일괄 적용, 다운로드 진행/부분 실패 안내를 제공한다. 구성 선택은 화면에 머무는 동안 유지하며 원본 평가를 변경하지 않는다.
- production build + 전체 테스트 **353/353**, typecheck, lint 오류 0(기존 경고 13). 기존 테스트의 LF 전용 줄바꿈 정규식 한 곳을 CRLF도 허용하도록 고쳤다. Next/webpack 증분 캐시가 WasmHash 오류를 내어 생성 캐시를 별도 보관한 뒤 재빌드했고 전체 테스트가 통과했다.
- `scripts/check-feedback-library-browser.mjs` 통과: 생성/중단/재개/재접속 진행, 학생 ID 분리, 같은 제목 파일 보존, XLSX 다운로드, 실제 PDF 텍스트에서 미선택 내용·점수 제외, 빈 선택 방지, 동일 기준 일괄 적용, 3권 ZIP, 검색·키보드, 320/390/768/844/1440 폭 검증. 다운로드한 한국어 PDF를 이미지로 렌더링하여 배치·줄바꿈을 육안 확인했다.
- 일반 `npm.cmd run check:browser -- http://localhost:57247`도 320×568·390×844·844×390 전 항목 통과했다. 샌드박스 안 Chrome DevTools 연결 실패 후 승인된 실행으로 재검증했다.
- 변경분 `git diff --check` 및 실제 기준 `git diff --check legacy-origin/main` 통과. 요구된 `git diff --check main...HEAD`는 로컬의 오래된 main과 운영 main 간 기존 이력인 `docs/design-assets/studio-tool-dock/notes.md:107` EOF 빈 줄을 보고한다(이번 변경 아님).
- 예시 학생만 있는 격리 test DB의 미리보기는 2026-09-22에 `http://localhost:51388/teacher/class/class_feedbackpreview/books/feedback`로 다시 띄웠다(종전 57247). 실행/캡처 스크립트와 PDF·화면 이미지는 Git 제외 `work/feedback-preview.mjs`, `work/feedback-qa/`에 있다. 실제 운영 데이터/AI 제공자 호출 없음. 사용자가 미리보기 후 PR 제출을 요청했으며 main 병합·배포는 하지 않는다.

## 2026-09-18 관리자 진입 버튼 (로컬 검증 완료)

- PR #7이 병합된 최신 main `debb800`에서 `codex/admin-entry` 생성.
- 교사 API가 서버의 isAdmin 판단을 전달하고 /teacher 머리 줄에 관리자에게만 /admin 링크 표시. 기존 /admin·관리자 API의 서버 접근 제한 유지.
- production build·전체 테스트 350/350·typecheck·lint(기존 경고만)·diff check 통과. 실제 Chrome에서 관리자만 버튼 표시/클릭 진입, 일반 교사 직접 URL 제한·관리자 API 403, 320/390/844/1440 폭 넘침 없음 확인.
- 사용자 요청으로 개인 fork `qudcks1940/wiggle_web`의 `codex/admin-entry`에 커밋·push. main push/운영 배포 없음.

## 2026-09-17 BookPrint 하드커버 고정 (로컬 검증 완료)

- 2026-09-18 PR 준비: 구현 `3374d52`, 최신 main `32d94fd` 병합 `8fc9ff1`. CSS 끝부분 양쪽 추가 규칙 보존. 병합 후 production build·전체 테스트 350/350·typecheck·lint·diff check 통과. 개인 fork `qudcks1940/wiggle_web`의 `codex/bookprint-squarebook`에 push하고 운영 저장소 main 대상으로 [PR #7](https://github.com/yonghwan86/wiggle_web/pull/7) 생성 완료. 교사/관리자 브라우저 재검증도 통과. API 키/환경 파일 제외, main 병합·배포 없음.

- 사용자 지정 main `cdfe23e`를 `C:/Users/user/Desktop/Project/wiggle_web_bookprint_20260917`에 새 clone. 브랜치 `codex/bookprint-squarebook`, 원래 작업 폴더 보존. main push·배포 없음.
- 새 책 생성/작품에서 생성/PDF 가져오기는 `squarebook-hc`(243:248) 고정. 모양 선택/변경 UI 제거. 기존 저장 책 비율 보존, DB 일괄 변환 없음.
- 인쇄 상품 SQUAREBOOK_HC 고정, 공식 규격과 제작사 계산 응답 교차 확인. 내지는 재단 크기에 맞춘 원고+3mm 가장자리 도련으로 만들고, 생성 후 표지·내지 전 페이지를 재검사한다. 기존 관리자 전송/발주 권한과 주문 흐름 유지.
- typecheck, lint 오류 0(기존 경고 13), production build, 전체 테스트 349/349, 기본 check:browser 전 항목 및 check-book-browser(교사/관리자 5화면×4크기) 통과. 새 책 실제 생성/단일 버튼/미리보기/243:248 비율을 320·390·844·1440 폭에서 실측, PDF 렌더 육안 확인. 최초 1건 실패는 Windows CRLF를 허용하지 않는 기존 테스트 정규식이 원인으로, clone 작업 파일을 Git 정본 LF로 맞춰 통과(관계없는 소스 변경 없음).
- 실제 제공자 API 접수와 사용자 배포 후 운영 검증은 아직 하지 않음. 모의 제작사 전송에서 상품 UID와 표지/내지 전 페이지 크기·쪽 수를 검증했다. 2026-09-18 사용자 요청으로 기능 브랜치를 커밋하고 개인 fork에 push한 뒤 운영 저장소 main 대상 PR을 제출한다. main 병합·운영 배포는 하지 않는다.

## 2026-09-16 관리자 운영·원고 주문 접수 (로컬 검증 완료)

- 사용자 최신 지시대로 관리자 `qudcks1940@gmail.com` 허용 목록, `/admin` 화면과 API를 추가했다. 교사 직접 견적·결제 API는 403이며 원고 준비는 로컬 저장까지만 수행한다.
- 교사는 완성책 또는 표지/내지 원본 PDF(각 30MB, 3MB 분할 업로드), 학교/학년/반/배송/수량을 제출한다. 모든 페이지의 크기·쪽 수·회전·잘림을 서버에서 검사하고 제출 원고 사본/해시/실측값을 보관한다.
- 관리자만 원고 전송·최신 견적·발주·배송 조회를 실행한다. 책별 처리, 요청 lease, 생성/결제 멱등성, 20시간 이상 미확인 자동 재전송 차단을 적용했다.
- 가입 교사/학급/명단/최초 입장/접속 인원, 30초 heartbeat와 90초 만료를 추가했다. CPU/RAM은 로컬 전체 또는 현재 함수 범위를 명시하고 운영 최대 인원은 부하 테스트 근거와 함께 설정한다. 자동 추정이나 접속 제한은 아니다.
- 자기 브랜치 `codex/rubric-feedback-book-orders`, 작업 폴더 `new_wiggle`. 개인 원격 `personal`과 대상 `upstream`을 추가했다. 대상 main은 현재 HEAD의 조상이며 PR에 new_wiggle의 기존 학급 명단/화면 등 선행 변경도 함께 포함된다.
- production build + 전체 테스트 **327/327**, typecheck, lint 0 errors(기존 경고 10), diff check 3종 통과. 모의 제공자 HTTP 검증에서 관리자/교사 경계, 마지막 PDF 페이지 규격 오류, 회전/잘림 거절, 수정 불가 접수 사본, 접속 중복/만료, 발주 응답 유실 복구를 확인했다.
- `check-book-browser` 실제 Chrome에서 일괄 평가/재시도/ZIP, 외부 PDF 가져오기/편집, 인쇄 PDF 직접 업로드, 교사 요청 제출, 관리자 견적/발주, 교사 2화면+관리자 3탭의 320/390/844/1440 폭 및 초점 이동 통과. 기본 `check:browser` 3뷰포트 전 항목 통과.
- 로컬 production 서버 `http://localhost:3012` 실행 중. 사용자 브라우저에서 `/admin`의 가입 교사/학급/CPU/RAM 화면까지 확인했다. 로컬 가상 교사를 무시되는 .env.local 허용 목록에만 추가했다. 운영 기본 관리자 계정은 승인된 Google 이메일뿐이다.
- 실제 OpenAI/Sweetbook 키는 로컬에 없으므로 제공자 실접수/유료 발주는 실행하지 않았다. 운영 키·Sandbox 실검증, 배포 후 실제 수업 코드 운영 점검은 남아 있다. 구현 커밋 `e409599`를 개인 저장소 기능 브랜치에 push했고, 대상 저장소에 [PR #4](https://github.com/yonghwan86/wiggle_web/pull/4)를 생성했다. main 병합·배포 없음.
- 대상 main 전체 diff 검사에서 선행 커밋의 디자인 문서 줄 끝 공백 1곳을 발견해 문구 변경 없이 제거했다. 폰트 라이선스의 줄 끝 공백도 제거했으며 라이선스 문구는 유지했다.

## 2026-09-09 그림책 루브릭 평가·인쇄 주문 구현

- `wwwiggle/new_wiggle` main에서 새 clone, `codex/rubric-feedback-book-orders`에서 작업. 커밋·push·배포 없음. 아래 옛 작업 기록은 당시 이력이다.
- 제공 XLSX 원본을 `config/rubrics/storybook.xlsx`로 보관. 학급별 엑셀 업로드 교체, 영역·점수 검증, 실제 프롬프트 공개, 파일/프롬프트/책 버전별 평가 저장. OpenAI에 전체 페이지 글+그림 전달, 단권·50권 순차 큐·재시도·교사용 검토·한글 PDF/ZIP.
- 학급 생성 학년·반 입력, 기존 명단 번호·실명 연동. 파일명 `학년_반_번호_이름_그림책이름.pdf`. 교사용 실명 필드를 AI 메타데이터에 보내지 않는다.
- 별도 주문 화면, Sweetbook Sandbox 기본 연결, 표지/내지 생성·업로드·확정, 최신 판형/치수·견적, 단권/여러 종/수량, 배송지, 주문 이력·응답 유실 복구. Live는 production + 명시 환경 변수로만 실행.
- DB 자동 provision 및 Drizzle 0008 신규 4개 테이블. 기존 피드백 POST도 새 큐로 연결. 운영 DB/R2와 은퇴한 `.openai/hosting.json`은 접근·수정하지 않음.
- production build + 전체 테스트 **324/324**, typecheck 통과, lint 0 errors(기존 경고 10개), 두 git diff --check 통과. 실제 HTTP 테스트에서 제공자는 모의 응답이며, 실제 유료 호출·실물 발주는 하지 않음.
- 신규 화면 브라우저: 320×568, 390×844, 844×390, 1440×1000에서 두 화면 가로 넘침·키보드 초점 확인. 일괄 평가·실패 재시도·ZIP 다운로드 및 일괄 인쇄 준비→견적→모의 Sandbox 주문 실제 브라우저 통과. 재현 스크립트: `scripts/check-book-browser.mjs`. 한글 PDF 렌더 육안 확인 및 인쇄 내지 쪽 수 테스트.
- 기존 전체 `check:browser`는 844×390 QR 입장 번호 화면/빈 번호 입장 검사 2건이 `no-seat-input`으로 실패. 신규 그림책 두 화면 검사는 별도로 통과했으며, 학생 입장 검사 실패 원인은 이번 작업에서 확정하지 못함.
- 설정과 사용자 절차는 `docs/book-feedback-and-printing.md`. 실제 키가 없어 실제 AI 품질/Sweetbook Sandbox 및 실물 교정은 남음. 사용자가 배포한 뒤 실제 수업 코드로 기존 deployed 검사와 신규 경로 운영 실측 필요.

## 상태 기준

- 현재 작업 저장소: `C:\Users\user\Desktop\Project\new_wiggle` (신규 main clone; 기존 wiggle_web 보존)
- 신규 개발 GitHub: `https://github.com/wwwiggle/new_wiggle`
- 기존 운영 연결 GitHub: `https://github.com/yonghwan86/wiggle_web` — Vercel 프로젝트의 Git 연결을 바꾸기 전까지 현재 운영 배포 원본으로 유지한다.
- 현재 clone의 Git 원격: `origin`은 신규 개발 GitHub다. 기존 폴더의 `legacy-origin`은 이번 clone에 추가하지 않았다. GitHub 접근은 사용자 승인에 따라 `qudcks1940` 계정 권한을 사용할 수 있다(자격증명은 저장소에 기록하지 않는다).
- **공개 운영: `https://www.kkumtle.app`** — Vercel 프로젝트 `wiggle-web`이 **`yonghwan86/wiggle_web`(원격 `origin`)의 `main`**을 자동 빌드·배포한다(서울 리전). `wwwiggle/new_wiggle`에만 올린 것은 운영에 나가지 않는다(2026-09-23 실측). 두 저장소는 함께 최신으로 유지한다 — `origin`은 `fork`+PR, `wwwiggle`은 직접 push다.
- 옛 공개 Sites `https://wiggle-classroom-web.chan1940.chatgpt.site`는 2026-08-19 재플랫폼으로 은퇴 — 사이트·데이터는 보존(폐기는 사용자 승인 필요), 신규 서버는 데이터 이전 없이 새로 시작했다.
- 운영 저장소: DB는 Turso(libSQL), 그림 파일은 Cloudflare R2 버킷 `wiggle-artworks`(S3 API). 운영 자격증명은 Vercel 대시보드에서만 관리한다.

## 매우 중요한 작업 폴더 상태

- 입장 재설계·정리 브랜치(`claude/entry-redesign-cleanup-20260818`)와 별명 확대 브랜치(`claude/nickname-spacing-20260818`)는 Codex GO 판정으로 `main` 병합·공개 배포까지 끝났다 (Sites v31·v32, 위 상태 기준 참조).
- 2026-08-18 배포 체계 전환 결정: 개발은 이 폴더, 코드는 GitHub push, 공개 배포는 Vercel. Sites+Codex 게이트는 전환 완료 후 은퇴. 학생 홈 트레이·펜 굵기 수정은 GitHub `main`에 직접 push됨.
- 진행 중 재플랫폼 브랜치: `claude/vercel-replatform-20260818` — 계획은 `docs/vercel-migration-plan.md`(v2).
  1. 1·2단계 완료: Turso(D1 호환 어댑터)·R2 S3 어댑터(+로컬 파일 폴백)·`next dev/build/start` 전환·보안 헤더 재현. `next build` 성공, 전환기 테스트 201+어댑터 6 통과, browser-check 3뷰포트 통과(390×844 핀치 1항목은 환경 사유 SKIP — 계획 문서 남은 위험 참조).
  2. 5단계(교사 인증) 코드 완료: 구글 OAuth 자체 구현(PKCE·state·미검증 이메일 거부, 새 패키지 0개), SIWC 헤더 신뢰 경로 제거(위조 위험), 교사 페이지·UI·로그아웃 교체. 실왕복은 사용자 OAuth 클라이언트 대기.
  3. 3단계(4.5MB 저장 분리) 완료: 완성 PNG를 바이너리 별도 업로드로 분리(전송 계층만 수정, 큐 스키마·스튜디오 무수정), `scripts/check-large-save.mjs`로 3.4MB 실저장·회수·경계 검증 통과.
  4. 4단계(하네스 포팅) 완료(2026-08-19): Miniflare·wrangler 테스트 11파일을 `tests/harness/`(파일 libsql DB + `next start` 서버) 기반으로 포팅, Workers 잔재(worker/·vite.config·wrangler 설정·devDeps 7종) 제거. `npm test` 268/268, browser-check 통과. 이때 `cf-connecting-ip` 신뢰를 제거하고 `lib/client-ip.ts`의 `clientIp`(x-vercel-forwarded-for 우선)로 교체 — Vercel에서 위조 가능한 IP 헤더로 rate limit이 뚫리는 문제의 선제 수정.
  5. 사용자 인프라 준비 완료(2026-08-19): 원격 Turso 생성·어댑터 계약 실측 통과·스키마 프로비저닝 완료, R2 버킷 `wiggle-artworks`+S3 토큰 발급·3.4MB 실왕복·비공개 확인. 자격증명은 `.env.local`에 `# vercel-only:` 주석으로 보관(활성화 금지 — 로컬이 운영을 물었던 사고 있음, 계획서 남은 위험 참조).
  6. 5단계 실왕복 완료(2026-08-19): 사용자 OAuth 클라이언트(`wiggle` 프로젝트) 생성, 로컬 3001에서 구글 동의→콜백→교사 세션 실측. 원격 테스트 행은 사용자 승인 후 삭제(전 테이블 0행 복원).
  7. **6단계(Vercel 연결) 완료(2026-08-19)**: 사용자 repo import(프로젝트 `wiggle-web`)·env 9종·보호 해제·서울 리전(icn1), 공개 주소 `https://wiggleweb.vercel.app`(무료 서브도메인 — wiggle-web.vercel.app은 타인 선점). 운영 버그 3건을 실측으로 발견·수정: ① `"type": "module"`이 Vercel 함수 런처의 require를 깨서 전 요청 500 → 제거(50a7965) ② 교사 세션 쿠키 strict가 구글발 리디렉션 연쇄에서 탈락해 로그인 무한 루프 → 콜백만 lax(e2df6de) ③ Next 런타임 fetch가 aws4fetch 스트림 본문을 chunked로 보내 R2가 411 → 서명·전송 분리로 버퍼 본문 유지(ef80e31). **최종 운영 E2E 통과**: 실제 학급(코드 9315) 학생 입장→작품 생성→3.4MB 바이너리 업로드(1.6초)→완성 저장(435B)→R2 회수 340만 바이트 전수 일치→남의 키·유령 키 413. 구글 리디렉션 URI는 localhost 3000·3001 + wiggleweb.vercel.app 3종 등록.
  8. **7단계(문서·파이프라인 재정의) 완료(2026-08-19) — 재플랫폼 전 단계 종료.** CLAUDE.md·AGENTS.md·README·agent-pipeline(은퇴 공지)·product-decisions를 GitHub+Vercel 흐름으로 교체, `pipeline:ready` 제거, 운영 실측 게이트 `npm run check:deployed` 신설.
  9. 사용자 선택으로 남은 것: `wiggleweb.app` 도메인 구매(미등록·구매 가능 확인됨), 구글 OAuth 클라이언트를 프로덕션으로 전환(타 교사 로그인 허용 — 전환 전에는 테스트 사용자만. 콘솔 등록의 게시 상태이며 모바일 앱이 아니다), 교사 화면에서 검증용 학생(검증화가·업로드탐색·헤더탐색) 정리, 인쇄물 QR 새 주소로 재출력.

- 커밋되지 않은 파일은 사용자 소유 참고 자료(`examples/*.jpg`, 시안 원본)와 로컬 도구 설정(`.claude/`)뿐이다. 임의로 삭제하지 않는다.
- `git reset --hard`, `git checkout --`, 광범위한 삭제를 사용하지 않는다.
- 새 작업을 시작하기 전에 `git status --short`와 최근 커밋을 확인한다.
- 로컬·브랜치 커밋은 게이트를 통과했어도 `main` 반영(사용자 push) 전까지 배포 상태로 간주하지 않는다.

## 2026-09-23 아이패드에서 도구 막대가 선택돼 끌려다녔다 (`claude/eraser-footprint-size-20260923`)

사용자가 아이패드 에어 실기기 사진을 보내 주셨다 — 도구 줄(연필~지우개)이 **파란 선택 영역**으로
잡히고 iOS 선택 손잡이가 붙은 채였다.

- **원인**: 2026-08-09에 넣은 `-webkit-touch-callout / user-select / -webkit-user-drag: none`
  규칙이 **도화지 네 요소에만** 걸려 있었다. 막대는 그 밖이라 iOS가 도구 그림을 선택·드래그
  항목으로 잡았다. `<img draggable={false}>`는 HTML5 끌기만 막고 이 동작은 못 막는다.
- **고친 곳**: `globals.css`의 같은 규칙에 `.tool-dock, .tool-dock *`와 `.zoom-controls,
  .zoom-controls *`를 더했다(둘 다 도화지 위에 떠 있는 조작부다).
- **검증**: `browser-check`에 캐스케이드 뒤 computed 값 검사를 넣었다. 같은 화면에서
  막대 도구 그림 `none` / 규칙 밖인 `.studio-header button` `auto`로 갈리는 것을 확인했다
  (= 이 규칙이 값을 바꾼 게 맞다). `-webkit-touch-callout`은 사파리 전용이라 크롬 computed에
  안 나와 선언 자체는 `drawing-tools.test.mjs`가 지킨다.
- **남은 확인**: iOS의 실제 길게 누르기 동작은 크롬으로 재현되지 않는다. **아이패드 실기기 재확인 필요.**

### 같이 발견했지만 아직 안 고친 것 — 접기 손잡이가 도화지를 덮는다
`.dock-toggle`은 76×44px에 배경이 투명한데, 그중 **43px이 막대 위(도화지 쪽)에 떠 있다**.
눈에 보이는 탭은 아래 30px뿐이라 14px은 보이지 않는다. `elementFromPoint`로 그 구간을 찍으면
`draw-canvas`가 아니라 `dock-toggle`이 잡히고, 펜·마우스로 아래로 16px 넘게 끌면 막대가 접힌다
(실측: 획은 하나도 안 그려지고 `tool-dock` → `tool-dock is-collapsed`). 손가락은 click이 취소돼
막대는 그대로지만 획이 안 그려지는 것은 같다. 고치는 방법 세 가지를 사용자에게 제시하고 대기 중.

## 2026-09-23 지우개 네모가 실제 지움 칸의 3배였다 (`claude/eraser-footprint-size-20260923`)

사용자: "지움 범위가 지움 범위를 표시하는 네모칸에 비해 작아."

- **원인**: 네모(`.eraser-footprint`)의 `width`는 도화지(`.canvas-stack`, 화면 span장 너비) 기준 %인데
  화면에서 고른 굵기를 그대로 넣고 있었다. 실제로 파이는 칸은 저장 단위(`굵기 ÷ span`)로 그려진다
  (`eraseWithSquareFootprint`: `op.width * size / 1024`). 2026-09-20부터 새 작품은 `span = 3`이라
  네모만 정확히 3배로 떴다.
- **고친 곳**: `DrawingStudio.tsx`의 `updateEraserFootprint` 한 줄 —
  `documentWidthUnits(eraserWidth) / 10.24`로 저장 단위를 쓴다.
- **검증**: `scripts/browser-check.mjs`에 실측 검사를 새로 넣었다. 지우개로 한 번 눌러 도화지에서
  알파가 0이 된 가로 구간을 재고, 같은 순간의 네모 `getBoundingClientRect()`와 비교한다(허용 25%).
  고치기 전 실측: 네모 18.28px / 실제 지움 5.71px(격차 69%, = 1/3). 고친 뒤 세 뷰포트 모두 통과.
- 소스 문자열만 보던 `tests/drawing-tools.test.mjs`의 지우개 검사도 새 식으로 갱신했다 —
  그 검사는 두 크기가 어긋나 있는 동안에도 계속 초록불이었다.

## 2026-09-23 병합 (`claude/merge-20260923` → `wwwiggle/main` 57f6818)

`wwwiggle/main`(3cdf64f) 위에 이날 작업 두 갈래를 합치고, **사용자 지시로 Claude가 직접
`wwwiggle` `main`에 push했다**(fast-forward `3cdf64f..57f6818`, 2026-09-23).
CLAUDE.md 개발 절차 8항은 Claude가 `main`에 직접 push하지 않는다고 적고 있으나, 같은 문서의
지속 컨텍스트 규칙("문서와 사용자의 최근 명시적 지시가 충돌하면 최근 지시를 우선한다")에 따랐다.
이 한 번에 대한 지시였고, 8항을 상시로 바꿀지는 아직 정하지 않았다(사용자 확인 필요).

⚠️ 이 push는 **운영 반영이 아니다.** 미결정 P-009대로 Vercel 프로젝트가 아직 옛 저장소
`yonghwan86/wiggle_web`에 연결돼 있어 `wwwiggle/new_wiggle`의 `main`은 배포되지 않는다.
운영까지 가려면 `origin`(yonghwan86) 쪽에 따로 PR이 필요하다.
받는 쪽은 새 의존성(`pdfjs-dist`, `pdf-lib` 등) 때문에 **`npm ci`를 먼저 돌려야 한다.**

- `claude/create-class-roster-rows-20260923` — 충돌 없음.
- `claude/zoom-past-canvas-20260923` — `app/globals.css`에서 충돌 2건.
  둘 다 **몽그리 접기 단추 잘림을 양쪽이 각자 고친** 것이라, 규칙은 하나로 합치고 주석 둘을 모두 남겼다.
  (그 사이 main에도 같은 `:not(.grimi-collapse)` 수정이 들어와 있었다. 이쪽 브랜치가 더한 값은
  이제 그 수정을 묶어 두는 회귀 시험이다 — main에는 시험이 없었다.)
- main이 새 의존성(`pdfjs-dist`, `pdf-lib` 등)을 들여왔으므로 병합 뒤 **`npm ci`가 필요하다.**

**검증**: typecheck·lint(경고 14, 기존 수준)·`npm test` **397/397**·build·`git diff --check` 통과.

**`scripts/browser-check.mjs`를 읽기 전용 몽그리 카드에 맞춰 고쳤다 — 이제 전항목 통과한다.**

병합 직후 9건이 실패했는데, 깨끗한 worktree에 `wwwiggle/main`만 받아 대조해 보니 **병합 이전부터
main이 혼자서도 똑같이 실패**했다(양쪽 OK 239 / FAIL 9, 실패 목록 완전 동일). 병합이 만든 실패는 0건.
원인은 `3cdf64f 몽그리 카드를 읽기 전용으로 바꾸기`가 답 칩과 「그린 뒤 했어요」 왕복을 **의도적으로
없앤 것**이고, 게이트의 기대가 옛 UX에 묶여 낡았다. 2026-09-23 사용자 지시로 **그 커밋을 정본으로 삼아**
기대를 옮겼다. 각 검사가 지키던 보장을 버리지 않고 새 화면으로 옮긴 것이다:

| 옛 검사 | 새 검사 | 지키는 것 |
|---|---|---|
| 첫 선택지를 바로 누를 수 있음 | **「이제 그려 볼 일」이 고르지 않아도 바로 보임** | 아이가 무엇을 할지 **늦게** 알지 않는다 — 그 커밋이 고치려던 문제를 그대로 시험한다 |
| (없음) | **답을 되돌려 보내는 칩이 없음** | 왕복이 되살아나 409 `이미 처리한 도움 기록이에요`가 다시 나지 않는다 |
| 한 번 스크롤로 확인 버튼에 닿음 | **한 번 스크롤로 「다른 것도 물어보기」에 닿음** | 맨 아래 행동에 스크롤 한 번으로 닿는다 |
| 접은 상태에서 '그렸어요'를 누를 수 있음 | **접힌 줄에 답을 요구하는 단추가 없음** | 접힌 줄도 읽기 전용이다 |

곁들여 **`닫기가 고정되어 보임` 검사가 엉뚱한 단추를 보고 있던 것도 고쳤다.** 선택자가
`.grimi-head > button`이라 DOM 순서상 먼저인 `.grimi-collapse`(그리러 가기)를 집고 있었다.
`[aria-label="몽그리 닫기"]`로 바꿨다 — 줄곧 통과하던 검사가 실은 × 단추를 본 적이 없다.

## 2026-09-23 보관함이 390px에서 잘리던 것 수정 (`claude/zoom-past-canvas-20260923`)

- **증상**: `/student/archive`가 좁은 화면에서 오른쪽으로 잘려 나갔다. 뷰포트 390px인데
  `.app-header`와 `.archive-stage`가 **882px**로 잡혔다. `html`이 잘라 내므로 가로 스크롤은
  생기지 않고 내용만 사라진다 — 「수업 마치기」·「다시 보기」가 화면 밖이라 누를 수 없었다.
- **원인**: `.archive-book-page` / `.archive-stage` / `.archive-book` 세 겹이 모두
  `grid-template-columns` 없이 `display:grid`다. 암묵 `auto` 칼럼이 안쪽 썸네일 줄의
  max-content(886px)까지 부푸는 전형적인 grid blowout이다. 세 곳에 `minmax(0,1fr)`을 줬다.
- **왜 여태 안 걸렸나**: `scripts/browser-check.mjs`가 `/student/archive`를 **방문하지 않는다.**
  모바일 게이트의 사각지대다. 그래서 `tests/responsive-drawing-ui.test.mjs`에 CSS 계약 시험을 넣었다.
- **검증**: 320×568 / 390×844 / 844×390 / 1440×900 실측 — 머리·무대 폭이 뷰포트와 같고,
  가로 스크롤 없음, 머리 단추와 「다시 보기」·넘기기가 모두 화면 안, 터치 목표 44px 이상.
  남는 넘침은 썸네일 줄뿐인데 이건 진짜 가로 스크롤러다(390에서 내용 886 / 밀 수 있는 거리 528 실측).
  `npm test` 377/377, typecheck·lint(경고 14)·build·`git diff --check`·`browser-check` 통과.
- 남은 위험: 보관함은 여전히 browser-check 경로 밖이다. 다음에 이 페이지를 크게 손볼 때
  (그림책 만들기 작업) 실제 방문 단계를 추가하는 것이 맞다.

## 2026-09-23 몽그리 접기 단추 글자 잘림 재수정 (`claude/zoom-past-canvas-20260923`)

- `.grimi-head>button`의 **원형 규칙은 닫기(×) 전용**인데 `:not(.grimi-collapse)`가 없어
  글자 단추인 접기까지 잡아 폭 48px에 가뒀다. 「✏️ 그리러 가기」는 102px이 필요해 글자가 잘렸다
  (844×390 실측: 폭 48 / 필요 135 → 잘림). 세 규칙(34px·44px·48px) 모두에서 접기를 뺐다.
- **같은 수정이 한 번 있었다가 원격 main이 앞서 나가며 사라졌다.** 시험이 없어서 조용히 사라졌으므로
  이번에는 `tests/responsive-drawing-ui.test.mjs`에 회귀 시험을 함께 넣었다 — 세 규칙에 `:not()`이
  있는지 보고, 제외하지 않은 옛 규칙(`.grimi-head>button {`)이 남으면 실패한다.
- **검증**: `npm test` 376/376(새 시험 1개 포함), typecheck·lint(경고 14, 기존 수준)·build·
  `git diff --check` 통과, `browser-check` 전항목 통과. 320×568 / 390×844 / 844×390 모두
  실측 폭 106 / 필요 102 / 잘림 없음, 닫기 단추는 48px 그대로.

## 2026-09-23 도화지 바깥까지 보이게 축소 (`claude/zoom-past-canvas-20260923`)

- **왜**: 축소가 33%에서 멈추는 게 이상하다는 물음에서 시작했다. 33%는 `1/span`(span 3)이라
  "도화지 전체가 틀에 꼭 맞는" 배율이었다 — 버그가 아니라 바닥이었지만, 종이 끝이 보이지 않아
  아이가 "왜 더 안 줄지"를 알 수 없었다. 사용자 결정: **종이 바깥까지 보이게 한 칸 더 줄인다.**
- `lib/canvas-view.ts`에 `minScaleFor(span) = 1 / span / 1.5`를 두었다. 단추 한 번(1.5배)만큼
  전체 보기보다 더 줄어든다 — span 3이면 33% → 22%, 옛 작품(span 1)이면 100% → 67%.
  배율 한계를 넘기지 않는 호출(가족 보기·교사 실시간 보기)은 예전 그대로 1~4배다.
- `clampView`가 종이를 **가운데에 놓는다**. 종전에는 종이가 틀보다 작아지면 `x`·`y`를 0으로 붙여
  왼쪽 위에 치우쳤다 — 그때는 그런 상태가 만들어지지 않아 드러나지 않던 계산이다.
- 틀 바탕색을 `#e4e9e3`(`--paper-backdrop`)으로 바꿨다. 종이도 틀도 흰색이면 어디까지가 종이인지
  보이지 않는다. 더 줄일 수 없는 상태에서는 종이가 틀을 덮으므로 이 색은 나타나지 않는다.
- **`scripts/browser-check.mjs`의 핀치 검사를 상대 비교로 고쳤다.** 그 검사는 축소를 끝까지 누른
  상태에서 시작하는데 `zoomScale > 1.05`라는 절대 숫자를 쓰고 있었다. 바닥이 0.333일 때만 맞는
  값이라 이번 변경에서 두 뷰포트가 깨졌다(0.844). 핀치 전 배율을 재서 `> before * 1.5`로 바꿨다 —
  핀치와 무관한 변경에 다시 깨지지 않는다.
- **검증**: typecheck·lint(경고 14, 기존 수준)·`npm test` 375/375, `git diff --check`, build 성공,
  `node scripts/browser-check.mjs http://localhost:3310` 전항목 통과. 축소 단추를 바닥까지 눌러
  320×568 / 390×844 / 844×390에서 실측 — 배율 100%→67%→44%→30%→22%에서 멈추고,
  30%·22%에서 좌우 여백이 같다(가운데 정렬), 바탕색 `rgb(228,233,227)` 확인.
- 남은 위험: 22%에서는 그림이 꽤 작아진다. 아이가 그 상태로 그리려 하면 선이 굵게 들어가는데,
  이는 축소 상태 전반의 성질이고 이번 변경으로 새로 생긴 것은 아니다.

## 2026-09-23 학급 만들기도 번호·이름 칸으로 (`claude/create-class-roster-rows-20260923`)

- **왜**: 학급을 만든 뒤 학생을 더할 때는 번호 칸·이름 칸이었는데, 학급을 만들 때만
  「한 줄에 번호 이름」 텍스트 상자였다. 입력이 두 가지면 선생님이 같은 일을 두 번 배운다
  (2026-09-23 사용자 요청).
- **한 컴포넌트로 합쳤다**: `app/components/RosterRowsEditor.tsx`가 두 화면의 공통 편집기다.
  `TeacherRosterSettings.tsx`에 있던 줄 편집·붙여넣기·번호 자동 채움을 그대로 옮겼고,
  `TeacherApp.tsx`의 `RosterField`(textarea)와 `parseRosterText` 사용은 없앴다.
  편집기가 `./TeacherRosterSettings.css`를 직접 import하므로 설정 화면을 함께 묶지 않아도 모양이 선다.
- **대시보드는 `.teacher-workspace` 밖이다**. 그 CSS의 색 토큰(`--tw-line` 등)과 44px 칸 규칙이
  모두 `.teacher-workspace` 안에서만 걸려 있어, 그대로 쓰면 테두리가 사라지고 칸이 44px 아래로 내려간다.
  `app/globals.css`의 `.create-class-roster`에서 같은 값을 다시 준다.
- 320px에서 번호 칸 88px이 이름 칸을 84px로 밀어서, 편집기 CSS에 `max-width:420px` 한 줄을 더해
  번호 칸을 56px로 줄였다. 두 화면이 같은 파일을 쓰므로 설정 화면도 함께 고쳐진다.
- 쓰지 않게 된 `.roster-field/.roster-hint/.roster-errors` CSS는 지웠다.
- **검증**: typecheck·lint(경고 14, 기존 수준)·`npm test` 375/375, `git diff --check`,
  `npm run build` 성공. `node scripts/browser-check.mjs http://localhost:3310`(이 트리를 빌드해 띄운 서버) 전항목 통과.
  학급 만들기 폼은 320×568 / 390×844 / 844×390에서 실측: 가로 스크롤 없음, 지우기·「학생 한 명 더」 44px,
  이름 칸 잘림 없음, 테두리 `#dce1de` 적용, 이름 두 개를 넣으면 줄이 자동으로 늘고 「2명 확인」이 뜬다.
- 남은 위험: 없음. 서버의 `createClassroom` 검증(`parseRoster`)은 건드리지 않았다 — 화면만 바뀌었다.

## 2026-09-22 참여 코드를 거듭 틀리면 그 **기기만** 잠깐 쉬게 함 (`claude/archive-book-20260920`)

- **왜 기기 단위인가**: 학교는 반 전체가 공인 IP 하나를 쓴다. IP로 잠그면 한 아이가 다섯 번 틀렸을 때
  같은 교실 스물아홉 명이 함께 못 들어간다. 그래서 잠금 단위는 브라우저가 만들어 보내는 기기 키다.
  지우면 초기화되므로 단독 방벽이 아니다 — 기존 IP·학급 상한(180회/10분, 학급+IP 60회/10분)이 뒤를 받친다.
- **수업 코드와 참여 코드가 같은 계수를 쓴다.** 따로 세면 둘을 번갈아 찍어 두 배로 시도할 수 있다.
- **단계**(2026-09-22 사용자 결정): 5번 → 20초, 5번 → 1분, 5번 → 3분, 그 뒤로는 **3번** → 10분.
  거기까지 갔으면 실수가 아니라 찍고 있을 가능성이 크다는 판단이다.
- **5분 조용하면 단계가 처음으로 돌아간다.** 없으면 아침에 한 번 잠긴 아이가 오후에 첫 실수로 3분을
  기다린다(실제 사용자 보고로 발견). 맞게 들어오면 계수와 단계를 함께 지운다.
- 잠긴 기기는 **맞는 코드도 통과시키지 않는다** — 맞는지 확인해 주는 것 자체가 찍기를 돕는다.
- **화면**(GPT 인계 `participation-cooldown-option1/option3`, 2026-09-22):
  - **틀림이 쌓이는 동안**은 시안 1의 연초록 안내다. 빨간 경고 상자를 쓰지 않고, 작은 몽그리 얼굴과
    「참여 코드가 맞는지 다시 확인해 봐」, 「앞으로 N번 더 틀리면 잠깐 쉬어요.」를 보여 준다. 숫자판은 그대로 쓸 수 있다.
  - **잠기면** 넓은 화면은 시안 3의 옆 카드(책상 몽그리·말풍선·남은 시간·막대), 좁은 화면은 같은 줄 안내에
    시간 칩만 더한다. 같은 말을 두 번 하지 않으려고 둘을 화면 폭으로 나눴다.
  - 숫자판은 치우지 않고 잠근다. 치우면 아이가 무엇을 기다리는지 잃는다. 입장 단추는 「잠시 기다리는 중」이 된다.
  - **숫자판 색을 시안의 연초록으로 바꿨다**(시안에서 뽑은 rgb(233,237,219)). 코드 표시칸과 한 덩어리로 읽힌다.
  - 시계는 **만료 시각에서 매번 다시 잰다**. 매초 1씩 빼면 탭이 뒤로 갔다 오거나 타이머가 밀릴 때 어긋난다.
    탭 복귀 시 즉시 다시 잰다. 끝나면 「이제 다시 입력할 수 있어요.」를 띄우고 **자동 제출하지 않는다**.
  - 대기 진입과 완료만 `aria-live`로 한 번씩 알린다. 매초 읽지 않는다.
  - 도움 단추는 실제로 아무것도 보내지 않으므로(손을 들라고 알려 줄 뿐) 기존 문구 「선생님 불러요」를 쓴다.
    시안의 헤드셋 아이콘은 고객센터처럼 보여 쓰지 않았다. QR은 치우지 않고 잠그기만 한다.
- **선생님 화면**: 손들기 줄 오른쪽에 「입장이 잠긴 기기 N대」와 「잠금 풀기」가 선다. 세 탭 어디서나 보인다.
  잠금은 기기 단위라 누구인지는 알 수 없어 인원이 아니라 기기 수로 알리고, 푸는 것도 학급 단위다.
- 들인 자산(출처·해시는 `public/brand/asset-manifest.json`): `/entry-green/mongri-waiting-desk.webp`,
  `/entry-green/mongri-waiting-face.webp`. 시계 아이콘은 새로 만들지 않고 기존 lucide에서 가져왔다.
- 실측(자체 빌드 서버): 네 단계를 끝까지 돌려 20초→1분→3분→10분과 4단계의 3번 문턱을 확인했다.
  같은 IP의 다른 기기가 멀쩡한 것, 잠긴 기기가 정답도 막히는 것, 성공 시 초기화, 선생님 잠금 풀기까지 확인했다.
- 검증: typecheck · lint(오류 0, 경고 14 = 기준선과 동일) · `npm test` 374/374 · `git diff --check`.
- **남은 것**: 20초짜리 짧은 잠금도 선생님 화면에 뜬다. 너무 잦으면 일정 시간 이상만 표시하도록 거를 수 있다.

## 2026-09-20 내 그림 보관함 = 펼친 스케치북 (GPT 인계 에셋 적용) (`claude/archive-book-20260920`)

사용자가 전달한 **GPT(Codex) 인계 묶음** `archive-sketchbook-handoff v2`를 그대로 따라 구현했다.
원본 위치: `~/.codex/visualizations/2026/09/07/01a07a79-…/archive-sketchbook-handoff/`
(CLAUDE-HANDOFF.md · MOTION-SPEC.md · BOOK-ASSETS.md · design-tokens.json · approved-reference.png).

- **들인 에셋**(`public/archive-book/`, 출처·해시는 `public/brand/asset-manifest.json`):
  `book-open-blank.webp`(펼친 책 장식), `book-page-right.webp`(도는 낱장), `paper-grain.webp`.
  원본 PNG는 1.2~3.2MB라 webp로 줄여 들였다(95KB·60KB·86KB). 넘김 중에도 펼친 책이 바탕에 남으므로
  `book-cover-green`은 쓰지 않아 들이지 않았다.
- **책은 장식, 내용은 DOM**이다. 인계 지시대로 시안 전체를 배경으로 깔고 투명 버튼을 얹지 않았다.
  책 그림은 `alt=""`·`aria-hidden`이고, 글자·목록·단추·아이 그림은 모두 실제 DOM이다.
- **두 쪽 좌표는 에셋을 실제로 재서 넣었다**(`book-open-blank.webp` 1810×869):
  크림 왼쪽 쪽 x 4.86~34.03%, 흰 오른쪽 쪽 x 34.48~95.30%, 두 쪽 모두 y 6.10~92.87%.
  그래서 책은 그 비율을 지키고 쪽을 따로 늘리지 않는다.
- **「새 그림」을 뺐다**(인계 지시). 새 작품 생성 API나 다른 화면의 생성 기능은 그대로다.
- **페이지 넘김**(MOTION-SPEC.md): 400ms, `cubic-bezier(.22,.61,.36,1)`, `perspective:1600px`.
  0~200ms에 가는 면이 책등 쪽으로 닫히고 200~400ms에 오는 면이 펼쳐진다. 복제 레이어만 돌고
  저장된 그림 데이터는 건드리지 않는다. 상태는 `selectedId`·`phase`·`direction`으로 나눴고
  목록 클릭·이전/다음·스와이프·키보드가 **하나의 `goTo`**로 들어온다. 규칙은 `ArchiveFlip.ts`에 따로 뒀다.
  - 대상 그림을 받은 **뒤에** 돈다. 준비 전에 빈 종이로 돌지 않는다.
  - `animationend`만 믿지 않고 타임아웃으로도 정착한다. 넘기는 중 추가 입력은 버린다(큐를 만들지 않는다).
  - 복제 레이어는 `aria-hidden`·`inert`·`pointer-events:none`이라 초점이 두 벌 생기지 않는다.
  - 끝나면 `aria-live`로 「3장 중 2번째 그림, …」을 **한 번만** 알린다.
- **넘기기 UI**: 48px 이전/다음 단추와 `1 / 3` 쪽수를 그림 아래에 둔다. 한 장이면 둘 다 숨긴다.
  순환하지 않고 첫/끝에서 해당 방향만 잠근다. 스와이프는 가로 48px 이상이고 세로보다 1.5배 이상일 때만
  센다(`touch-action: pan-y pinch-zoom` — 세로 스크롤과 두 손가락 확대를 뺏지 않는다).
- **반응형**: 760px 미만은 양면을 억지로 줄이지 않고 **한 장 레이아웃**으로 간다(제목 · 가로로 미는 그림 고르기 ·
  그림 한 장 · 이어 그리기). 눕힌 기기(가로 모드·세로 560px 이하)는 폭이 좁아도 펼친다.
- **실측**(자체 빌드 서버):

  | 확인한 것 | 결과 |
  |---|---|
  | 1440×900 넘김 | 복제 레이어 2면 생성·회전, 끝난 뒤 제거·제목/쪽수 갱신·aria-live 문구 |
  | 첫/끝 | 1/3에서 이전 잠김, 3/3에서 다음 잠김 |
  | 키보드 ← | 2/3으로 이동, 초점 유지 |
  | 움직임 줄이기 | 복제 레이어 없음, 회전 없이 즉시 전환 |
  | 그림 1장 | 넘기기 단추·쪽수 없음 |
  | 그림 0장 | 「아직 저장된 그림이 없어요.」, 새 그림 CTA 없음 |
  | 390×844 · 320×568 | 한 장 레이아웃, 가로 넘침 없음 |
  | 844×390 · 820×1180 | 펼친 책, 가로 넘침 없음 |

- 검증: typecheck · lint(오류 0, 경고 14 = 기준선과 동일) · `npm test` 361/361 · `git diff --check` ·
  브라우저 실측 기본 모드 통과. `--ipad`의 「대칭이 반대쪽에도 그려짐」은 이 작업과 무관한 기존 건이다
  (원인은 분기 기준보다 앞선 `603cd24` 도화지 3배 확대 — 같은 날 손들기 항목에 가려낸 근거가 있다).
- 이 작업으로 기존 시험 둘을 함께 고쳤다. `pre-reader-ux`는 「새 그림」 그림 표지를, `feedback-20260809`는
  목록 미리보기 호출 모양을 고정하고 있었다. 지키려는 **뜻**(글 못 읽어도 고를 그림이 있다 / 목록은 썸네일)은
  그대로 두고 문장만 새 구조에 맞췄다.
- **남은 것**: 인계의 `paper-grain.webp`는 아직 화면에 얹지 않았다(권장 opacity 0.06~0.10). 책 에셋만으로
  종이 느낌이 충분한지 사용자 확인 뒤 정한다. 실기기 터치 스와이프는 시뮬레이터 좌표로만 확인했다.

## 2026-09-20 손든 아이 호출 줄 — 세 탭 어디서나 보이게 (`claude/hand-raise-strip-20260920`)

- **고친 문제**: 손든 표시가 「오늘 수업」 탭 안에만 있었다(`TeacherWorkspace.tsx`의 `tab === "today"` 블록).
  교사가 「작품·그림책」이나 「명단·설정」에 있으면 아이가 불러도 알 수 없었다. 사용자 보고로 시작해 실제 재현했다.
- **호출 줄**(`HandRaiseStrip`)을 **탭 내용 바깥**, 탭 막대 바로 아래에 두었다. 세 탭에서 같은 자리에 보인다.
  탭 안에 두면 문제가 그대로 남으므로, 이 위치가 기능의 핵심이다 — `tests/hand-raise-strip.test.mjs`가 지킨다.
- **넓은 화면은 칩 나열(가안), 좁은 화면은 한 명 + 「외 N명」(나안).** 두 시안을 합쳐 하나의 반응형 요소로 만들었다.
  접는 기준은 CSS 미디어 쿼리(760px)다. 화면 폭을 JS로 재면 서버 렌더와 어긋나고 resize 처리가 붙는다.
- **오래 기다린 아이가 먼저 온다**(`handRaisedAt` 오름차순). 기본 학생 목록은 번호순이라 번호가 뒤인 아이가 밀렸다.
- **경과 시간은 서버 시계 기준**이다. 교사 응답에 `serverNow`를 실어(`app/api/teacher/route.ts`) 기기 시계가
  틀어져도 「3분째」가 어긋나지 않게 했다. 렌더 중 `Date.now()`는 린트(`react-hooks/purity`)가 막으므로 effect에서만 읽는다.
- 「오늘 수업」 탭 이름 옆에 인원 배지를 붙였다. 손 내리기는 낙관적으로 즉시 사라지고, 6초 폴링이 확인한다.
- **자동 몽그리가 손 든 아이에게 끼어들지 않게 막았다**(`DrawingStudio.tsx`의 `autoGrimiReady`).
  손든 상태는 "손이 멈춘 상태"라 `idleMs` 조건에 그대로 걸려, 막지 않으면 선생님을 부른 직후 몽그리가 떴다.
- 좁은 화면(760px 이하)에서는 「오늘 수업」 안내 문단을 접어 호출 줄이 쓴 자리를 돌려받는다(320px에서 162px).
- **실측**(자체 빌드 서버, 학생 5명이 손든 상태): 세 탭 × 세 화면 크기 9가지 모두에서 줄이 보였다.
  넓은 화면 72px에 칩 4개 + 「+1명 더」, 좁은 화면 97px에 칩 1개 + 「외 4명」, 손 내리기 44px, 가로 넘침 없음.
  손 내리기를 누르면 칩이 즉시 사라지고 배지가 줄며 서버에서도 해당 행이 사라지는 것까지 확인했다.
- 검증: typecheck · lint(오류 0, 경고 14 = 기준선과 동일) · `npm test` 348/348 · `git diff --check` ·
  브라우저 실측 기본 모드 통과. 원격 `main`의 「몽그리 대기 화면」 커밋(`74671e9`)을 병합한 뒤 다시 돌린 결과다
  (같은 파일 `DrawingStudio.tsx`를 건드리지만 충돌 없이 합쳐졌고, 자동 몽그리 가드와 새 대기 화면이 모두 살아 있다).
- **남은 것**: 학급 목록 화면은 아직 덮지 못했다. 목록 조회에 손든 수가 없고 그 화면에는 폴링도 없다
  (`TeacherApp.tsx`의 타이머는 `classroomId`가 있을 때만 돈다). 별건으로 남긴다.
- **이 브랜치 밖의 기존 실패**: `--desktop`·`--ipad` 모드의 「대칭이 반대쪽에도 그려짐」 검사가 실패한다.
  **원인은 이 작업이 아니라 분기 기준 커밋 `603cd24`(도화지를 화면 3장 크기로 키움)다.** 가려낸 방법:
  1) 내 변경을 모두 뺀 `603cd24` 그대로도 같은 실패가 났고, 2) 한 커밋 앞인 `6aa7a1f`는 세 번 연속 통과했으며,
  3) 비어 있는 새 DB로도 똑같이 실패했다(DB 누적 상태 아님). 교사 CSS는 학생 화면 번들에 들어가지 않는다(별도 파일 확인).
  검사가 뷰포트 비율로 픽셀 위치를 잡는데 도화지가 3배로 커지면서 앞 뷰포트에서 그린 획과 겹치는 것으로 보인다.
  **검사 쪽을 고쳐야 하는 별건이다.**

## 2026-09-09 번호 입력 화면 2차 — 수첩이 그려진 배경으로 교체 (사용자 피드백 "너무 다르지 않아?")

- 사용자가 준 수첩 포함 교실 원화(1690×931)를 `public/entry-green/classroom-notepad.webp`(77KB)로
  인코딩해 배경으로 쓰고, CSS로 그리던 수첩(스프링·종이·간판 칩)을 넓은 화면에서 제거 —
  이제 그림 위에 글자·키패드만 얹는다. 종이 영역은 픽셀 실측(x 45.4~84%, y 8.7~93.4%),
  반 이름은 그려진 나무 간판(y 10.6%) 위에 겹친다. 헤드리스 스크린샷으로 시안 대비 확인.
- 좁은 화면은 크림 배경 + CSS 수첩 카드 유지(원화가 없으므로), 나무 칩 간판으로 전환.
- 원본 PNG 출처: 사용자 채팅 전달(2026-09-09) — sharp webp q82 인코딩만 수행.

## 2026-09-09 번호 입력 화면 — 교실 수첩 시안 적용 (브랜치 claude/entry-seat-keypad-20260909)

- `/join` 번호 입력 화면을 사용자 시안(교실 배경 + 스프링 수첩 키패드)으로 재구성.
  넓은 화면은 기존 `entry-green` 교실 무대(`classroom-with-mongri.webp`)를 재사용해 왼쪽에
  「내 번호를 눌러요」 제목, 오른쪽에 수첩 패널(반 이름 나무 간판, 내 번호 표시, 1~9·0·지우기
  키패드, 노랑 들어가기, 수업 코드 다시 입력하기). 좁은 화면은 크림 배경 문서 흐름 + 작은 오리.
- 검증 스크립트 계약 유지: `.seat-input`은 실제 입력칸(키보드 입력도 그대로 동작),
  `.seat-card .child-primary-action`이 들어가기. browser-check 수정 없음.
- 부수 수정: 넓은 화면 cqw 축소가 44px 터치 목표를 깨던 기존 결함 3곳에 44px 바닥
  (`password-actions`·`entry-mode-back`·`nickname-row` 버튼) — desktop 뷰포트 검사에서 발견.
- 게이트: typecheck · lint · 테스트 319/319 · build · browser-check(:3299) 기본/‑‑ipad/‑‑desktop 전부 통과.

## 2026-09-09 3차 병합 — 명단 입장 모델 합류, 반 번호 기능 은퇴

- main의 PR #8~#10(도화지 풀블리드, 교사 명단 번호+실명, 자기 등록 입장 제거)을 아크
  브랜치에 병합. 충돌 8개 해소 원칙: **명단 입장 모델(저쪽)이 이긴다** — 2026-09-07 사용자
  결정으로 실명 금지 원칙 자체가 바뀌었고, 자기 등록과 명단이 공존하면 아이가 두 프로필로
  갈라지기 때문. 이틀 전의 "선택 입력 반 번호(class_number)" 기능은 **구현째 제거**
  (컬럼·API·입력 UI·테스트, pending-decisions에 대체 사유 기록).
- 유지한 것: 아크 전체(포인터·조종석·오늘 회차·서랍), DESIGN.md Sunshine 팔레트
  (저쪽의 독자 초록 hex 팔레트 대신 토큰 정본 유지, 은퇴한 teacher-activity-book 블록만 저쪽),
  전시관 대문의 교사 로그인 안내·정책 링크.
- 개인정보처리방침 1절을 명단 모델에 맞게 다시 씀(이름은 담임 화면에만, 학급 삭제 시 명단 삭제).
- 아크 실서버 테스트는 명단 자리(seat 7)를 시드해 번호 입장으로 갱신.
- 게이트: typecheck ✓ · lint 0 오류(경고 13 기존) · 테스트 **319/319** · build ·
  browser-check(:3299 명시) 전 항목 통과.

## 2026-09-07 정본 저장소 이동·병합 (브랜치 wpdbs1229/bmad-product-planning)

- 반 번호(출석번호) 선택 수집 구현(2026-09-07 사용자 결정, 이름은 계속 수집 안 함):
  `student_profiles.class_number`(DDL+조건부 ALTER), join API 선택 파싱(1~99 외는 저장 생략,
  입장 안 막음), 신규 프로필 화면 `내 번호` 입력(선택, 생성 모드만), 교사 학생 타일·미리보기에
  `· n번` 표시, 개인정보처리방침 갱신. 인증·중복 판정에는 쓰지 않는다(테스트가 소스 계약으로 잠금).
  `tests/class-number.test.mjs` 3건 추가. 게이트: typecheck·lint·311/311·browser-check(:3299) 통과.
- 2차 병합(같은 날, PR 충돌 해소 요청): main에 새로 합류한 PR 4건(몽그리 개명 #7, 4:3 도화지 #5,
  오리 전시관 대문 #3, 플로팅 도구 #4)을 아크 브랜치에 병합. 충돌 4개 해소 —
  레슨 카탈로그 은퇴 유지(`lib/lesson-content.ts` 빈 카탈로그), 학생 홈은 아크 카드 유지 +
  몽그리 개명 반영, 대문은 main의 전시관을 채택하고 우리 쪽 `teacherAuth=failed` 안내와
  정책 페이지 링크를 이식, product-decisions는 양쪽 신규 결정 모두 보존(4:3 항은
  「캔버스와 입력」 9항으로 이동). 게이트: typecheck·lint(경고 11 기존)·테스트 308/308·
  browser-check(:3299 명시) 전 항목 통과.
- 타 팀 교사 화면 시안(파랑 「내 학급」) 피드백을 `docs/design-feedback-teacher-mockup-2026-09-07.md`로
  정리 — 필수 4건(팔레트, 학교명 수집, 삭제 문구, 아크 조종석 부재) + 정본 문서 목록.

- 정본 원격을 `wwwiggle/new_wiggle`로 확정하고 그 main(교사 그림책 열람·피드백, 가이드 변형,
  next 16.3.3 독자 패치)을 아크 브랜치에 병합. 충돌 17개 해소 — 교사 그림책은 전부 보존,
  레슨 은퇴는 유지(가이드 변형은 무력화), guide_variant 컬럼은 스키마에 유지(AD-2).
- dev 스크립트도 `--webpack`으로(저쪽 보안 회귀 테스트 채택), eslint-config-next 16.3.3 정합.
- ⚠️ 검증 사고 기록: browser-check가 localhost:3000의 타 워크트리 서버를 검사한 사례 발견.
  이제 대상 주소 명시가 규칙(CLAUDE.md 개발 절차 5항).
- 타 브랜치 발견: `claude/drawing-studio-floating-20260907`(플로팅 도구 재배치, 다른 세션 작업)에
  '다른 도구 더 보기' 버튼이 헤더에 가리는 실결함 있음 — 그 브랜치 합류 시 수정 필요.
- **미해결**: Vercel 프로젝트가 어느 저장소의 main을 빌드하는지 확인 필요 — 새 저장소로
  재연결 전에는 wwwiggle main push가 운영 배포로 이어지지 않을 수 있다.

## 2026-08-30~31 이야기 아크 재기획·구현 (브랜치 wpdbs1229/bmad-product-planning, main 미반영)

- **기획 정본 신설**: BMAD 브라운필드 재기획으로 PRD(`docs/bmad/planning-artifacts/prds/prd-wiggle-web-2026-08-30/`),
  아키텍처 스파인(AD 19개, `.../architecture/architecture-wiggle-web-2026-08-30/`),
  에픽·스토리 12개(`.../epics.md`), UX 스파인(DESIGN.md·EXPERIENCE.md, `.../ux-designs/ux-wiggle-web-2026-08-31/`)이 확정됐다.
  `product-decisions.md` 「학습 과정」이 회차 누적 이야기 아크(정본: See-Think-Wonder + Studio Thinking + Storyline Method)로 개정됐다.
- **보안**: next 16.2.6→16.3.3(CVSS 10.0 RCE 2건 수정). 16.3.3 Turbopack이 한글 소스에서 패닉해
  `build`는 `--webpack`(dev는 Turbopack 유지). `agentRules: false`. ws 8.21.1 override로 운영 의존성 취약점 0.
  `engines`를 실측에 맞춰 `>=22.18.0`으로 정정(22.13에서는 테스트 하네스가 돌지 않는다 — registerHooks·타입 스트리핑).
- **에픽 1 일부**: `/privacy`·`/terms` 정책 페이지(초안 — 게시 전 사용자 검토 필요), 랜딩 링크,
  구글 콜백 실패 시 `/?teacherAuth=failed` 안내(학교 Workspace 차단 대응 — Story 1.3).
  **OAuth 클라이언트 프로덕션 전환(Story 1.2)과 운영 잔여 정리(1.4)는 사용자 작업으로 남음.**
- **에픽 2·3 구현 완료** (모두 이 브랜치, main 미반영):
  - `lib/arc-content.ts`(자전거 아크 2회차, 안정 episodeId, 체크섬 버전 감시) + `lib/arc-session.ts`(AD-14 판별).
  - `classrooms.current_arc_id/current_episode_id`, `artworks.arc_id/episode_id/arc_version` —
    schemaStatements·schema.ts 양쪽 + `provisionSchema()` 조건부 ALTER(기존 운영 테이블 대응),
    `(student, arc, episode)` 완성작 부분 UNIQUE.
  - 교사 수업 조종석(학급 화면 맨 위, setEpisode/listArcs), 학생 홈 오늘 회차 카드(A안),
    재사용 우선 작품 생성(두 태블릿 경합 대응), 오프라인 flush 귀속 불변,
    지난 이야기 서랍(보기 전용, 귀속 컬럼 조회).
  - 레슨 카탈로그 은퇴: 활동 30개·화면 5종·라우트 4종 삭제, `normalizeActivityKey` 조용한 폴백 제거
    (알 수 없는 키는 free로, 한 곳에서만 처리). 레거시 lesson_slug 작품은 자유 모드로 열린다.
  - 책 팀 계약: 저장 경로의 옛 완성 키 삭제 제거(AD-6 — storybook_assets 스냅샷 보호),
    UNIQUE 위반 409 매핑, `docs/arc-book-contract.md` 신설.
- **검증**: Node 22.23.2에서 typecheck·lint·`npm test`(294개)·`check:browser` 전 항목 통과 —
  이 워크트리에서 전 게이트가 완주된 최초 상태. 신규 회귀 테스트: 아크 체크섬, classrooms ALTER,
  두 태블릿 경합, flush 귀속, AD-15 응답 형상, 완성 키 비삭제, 서랍 계약.
- **남은 사용자 작업**: ① 구글 OAuth 클라이언트 In production 전환(콘솔 — 정책 URL은 준비됨, main push 후)
  ② 정책 페이지 문안·연락처 검토 ③ 검증용 학생 3명 정리 승인 ④ QR 재출력 ⑤ main push.
- **알려진 잔여**: 장면 삽화(sceneImage)가 null — 현장 투입 전 제작 필요(21항).
  reopen(재완성) 미구현이라 관련 회귀 테스트 이연. 수업 닫힘 홈 최종 구성은 P-004 잔여.

## 현재 구현된 핵심 흐름

### 학생

- 대문에서 4자리 수업 코드 입력 또는 학급 QR로 입장
- QR로 이미 학급이 정해진 경우 프로필 화면에서 수업 코드를 다시 입력하지 않음
- 수업 코드와 QR 모두 서버의 같은 학급 상태 확인을 거쳐 동일한 입장 흐름 사용
- 입장은 **반(QR 또는 수업 코드 4자리) → 아이 참여 코드 4자리** 두 단계다(2026-09-12 개정). 반을 확인하면 참여 코드 수첩(`.entry-code-input`, 숫자판)이 열리고, 코드가 맞는데 처음이면 동물 하나만 고르는 화면(`.animal-card`)이 뜬다. 그다음은 **곧바로 빈 도화지**다 — 학생 홈은 없다. 명단이 없는 학급은 "아직 준비 중이에요" 화면에서 멈춘다(`NO_ROSTER`)
- 옛 자기 등록 흐름(`새로 시작하기 / 내 그림 이어가기` 갈림길, 별명·동물 재입장, `allowDuplicate` 중복 프로필)은 예비 경로도 남기지 않고 제거했다 (`PROFILE_EXISTS`·`PROFILE_CREDENTIALS_EXIST`는 `SEAT_CLAIMED`로 대체)
- 참여 코드 하나로 어느 태블릿에서나 기존 익명 학생 ID 재입장(코드가 곧 자리). 교사는 명단·설정 탭에서 코드를 보고 복사·새로 뽑기·코드표 인쇄를 한다(`rotateEntryCode`)
- 별명 표시는 띄어쓰기를 유지하되, 신규 중복 검사·동일 자격정보 검사·재입장·관련 rate-limit 대상 키는 모든 공백 차이를 무시한다 (`lib/nickname.ts`)
- 그림 별명 후보는 동물별 10개(전체 100개, 공백 무시 기준으로도 중복 없음)이며 `다른 별명` 주사위는 현재 별명을 반복하지 않는다 (`lib/nickname-ideas.ts`)
- 공유 태블릿 학생 전환 시 기존 활성 세션을 해지하고, 최근 학생 목록은 입장 화면에 표시하지 않음
- 기초 → 따라 그리기 → 관찰 그리기 → 자유 창작의 4단계 활동
- 터치·스타일러스 캔버스, 자동 저장, 오프라인 임시 보존, 작품 보관함
- 점선·시범 가이드는 작품 원본과 분리
- 소감과 짧은 글을 작품에 연결

### 교사

- 교사 로그인, 학급 생성, QR·수업 코드 관리
- 로컬 개발 서버의 `/teacher`는 최근 사용한 로컬 교사로 자동 로그인하며, production과 Sites는 기존 ChatGPT 인증을 그대로 요구
- 오늘 활동 선택, 전체·개별 텍스트 메시지
- 학생 진행 상태·썸네일·과거 작품 확인
- 학생 카드에서 동물·별명·프로필 생성 시각·마지막 접속 시각·전체/진행/완료 작품 수·중복 별명 여부를 읽기 전용으로 확인
- 개인 QR 재발급은 기본 교사 UI에서 제거했으며, 가족 제한 공유 기반 기능은 기존 범위를 유지

### 저장·권한

- 관계형 기록은 D1, 이미지·썸네일은 R2에 저장
- AI 호출은 서버에서만 수행
- 학급 입장 정보만으로 다른 학생의 작품을 열 수 없음
- 실제 작품 소유권은 서버가 발급한 익명 학생 ID와 학생 세션으로 확인

## 현재 UI 방향

- 초등학교 3학년 이상이 핵심 사용자이며, 읽기 수준 차이를 위해 그림·아이콘·음성·큰 터치 영역을 함께 제공
- 큰 그림·아이콘·음성 안내·큰 터치 영역을 우선
- 대문, 프로필 생성, 학생 홈은 그림책 형태의 일러스트 중심 UI
- 학생 홈 핵심 메뉴 순서는 `이어 그리기 → 내 그림 → 활동 고르기`
- 수업 마치기는 일반 활동 메뉴와 분리
- 도구는 아이콘 중심이며 펜 입력을 기본값으로 사용

## 확정 시안 반영 상태

- `examples/KakaoTalk_20260817_182137151.jpg`: 메인 화면의 확정된 넓은 화면 시안
- `examples/KakaoTalk_20260817_185428983.jpg`: 학생 프로필 생성 화면의 확정된 넓은 화면 시안
- `examples/KakaoTalk_20260817_194906484.jpg`: 학생 홈의 확정된 넓은 화면 시안
- 세 시안의 디자인 자체는 추가 수정 대상이 아니다.
- 2026-08-18 로컬 변경에서 세 시안의 구성·그림·역할을 유지하면서 폭·간격·정렬·잘림과 반응형 배치를 보정했다.
- 메인은 기존 제목·문구·학생 카드·교실 그림·4자리 코드 카드를 유지한다. 세로가 짧은 가로 화면에서는 비어 있던 양옆 공간에 학생 카드와 코드 카드를 배치해 첫 화면에서 행동을 찾을 수 있게 했다.
- 휴대전화 크기와 높이가 짧은 화면의 신규 생성·기존 로그인은 초점이 다음 입력으로 이동하는 3단계 안내형, 태블릿 크기는 시안처럼 모든 입력이 보이는 한 화면형으로 구현했다.
- 입장 페이지의 음성 버튼은 상단 안내 하나만 유지하고 필드별·오류별 음성 버튼은 제거했다.
- 학생 프로필 생성·재입장의 넓은 화면은 확정 시안의 `1488×1057` 좌표계를 단일 무대로 사용한다. 로고, 배경 그림, 제목, 학생증 미리보기, 세 입력 영역, 하단 행동 버튼이 서로 분리되지 않고 같은 비율로 축소된다.
- 그림 비밀번호 버튼은 시안처럼 그림과 한글 이름을 함께 표시한다.
- 학생증과 동물 선택 버튼은 정사각 셀로 재구성한 `public/brand/animal-portraits-v2.png`의 동물 초상 10개를 사용한다. 기본 이모지를 억지로 키우거나 동물을 가로로 늘리지 않고, 선택한 동물의 큰 초상을 참고 화면과 같은 중심과 비율로 표시한다.
- 학생증의 동물 초상은 실제 불투명 영역을 파란 원의 중심에 맞추고, 별명은 배경 그림의 점선 상자 전체에서 가로·세로 중앙 정렬한다. 배경에 이미 그려진 비밀번호 상자를 다시 HTML 테두리로 덮지 않고 `그림 비밀번호` 리본 문구와 선택한 그림·물음표만 올린다.
- 리본 문구는 파란 리본의 가운데 띠 중심에 맞추고, 비밀번호 그림과 물음표에는 별도의 상하 이동 보정을 적용하지 않아 각 점선 칸의 기하학적 중앙에 놓는다.
- 넓은 화면의 `다시 골라요`와 `한 칸 지우기`는 절대 좌표를 사용하지 않고 3번 비밀번호 단계의 설명 행에 포함한다. 2번·3번 시작선에는 구분선을 표시해 각 단계와 보조 버튼의 소속을 명확하게 구분한다.
- 2026-08-18 실제 브라우저 좌표 검증에서 `1488×1057`, `1213×815`, `1024×768`, `768×1024`, `844×390`, `390×844`, `320×568` 모두 보조 버튼과 그림 선택 칸·입장 버튼의 교차가 0건이었고 가로 오버플로도 없었다. 세 비밀번호 아이콘과 각 점선 칸의 중심 좌표 차이도 모든 화면에서 0px로 확인했다.
- 음성 안내는 AI 음성 생성 모델이 아니라 브라우저의 Web Speech를 사용한다. 확정된 시나리오 문장을 변경하지 않고 읽으며, 설치된 음성 중 자연스러운 한국어 음성을 우선하고 `0.96` 속도·기본 음높이로 재생한다.
- 학생 홈 하단 나무 선반의 세 카드(이어 그리기·내 그림·활동 고르기)는 시안처럼 아이콘·제목·상태 칩을 한 덩어리로 칸 중앙에 모은다(721px 이상). 시안에 없는 `활동 고르기`의 `열기` 칩은 제거했다. 선반 좌우 패딩은 배경 PNG의 색연필 장식 비율(`clamp(30px,4.8vw,72px)`)을 따라가 좁은 화면에서도 카드 밖으로 내용이 넘치지 않는다. 휴대전화(≤720px)의 기존 스택 배치는 그대로다.
- 학생 홈 상단 선생님 말씀 버튼은 💌 아이콘을 쓰고 넓은 화면(721px 이상)에서는 `선생님 말씀` 글자 라벨을 함께 보여준다(다른 헤더 버튼과 같은 아이콘+글자 규약). 휴대전화 폭에서는 44px 정사각으로 접고 안 읽음 배지는 모서리 배지로 옮긴다. 그리기 화면의 44px 컴팩트 버튼은 그대로다.

## 2026-08-18 구현·검증·릴리스 상태

- 검증 원본 브랜치: `claude/entry-redesign-cleanup-20260818` (base `1cafeed`, 2차 후보 `4a9829b`)
- Codex 2차 독립 재검증에서 1차 차단 사유인 전체 범위 공백 오류와 동물 초상 자산 provenance 누락이 모두 해소된 것을 확인했다.
- 후보와 최신 `main`의 fast-forward 병합 결과에서 typecheck·lint·전체 자동화 테스트 `243/243`·일반/전체 범위 diff check가 모두 통과해 GO로 판정했다.
- GO 릴리스는 이 문서를 포함한 최종 `main` 커밋을 GitHub와 기존 Sites 프로젝트에 동일 SHA로 반영하며, 최종 SHA와 Sites 버전은 릴리스 결과에 기록한다.
- 관련 API 통합 테스트에서 명단 학급 상태, 번호 입장, 이미 찬 번호 차단(`SEAT_CLAIMED`), 같은 별명 다른 번호, 학급 코드/QR 토큰 재입장, 동일 학생 ID 복귀를 확인한다.
- 2026-09-07 번호 전용 입장 검증: typecheck·lint·build·`git diff --check` 통과, 전체 테스트 `315/316`(유일한 실패 `pbkdf2-runtime`은 Node 22.13에 `registerHooks`가 없어서 나는 환경 실패로, 이 변경 이전 기준선과 같다), `npm run check:browser` `320×568`·`390×844`·`844×390` 전건 통과. 브라우저 검증 시드도 명단 두 자리를 만들어 1번은 재입장, 2번은 첫 입장 흐름으로 구동하도록 바꿨다.
- 실제 로컬 브라우저에서 신규 입장, 잘못된 비밀번호, 올바른 재입장, 학생 A 종료 뒤 학생 B 전환, 교사 읽기 전용 프로필 정보를 확인했다.
- 2026-08-18 추가 UI 검증에서 `1488×1057` 확정 시안과 로컬 렌더링을 직접 비교해 로고·외곽 카드·제목·좌우 칼럼·입력 그리드·하단 버튼의 축을 맞췄다. `768×1024`에서 충돌하던 이전 세로 태블릿 미리보기 규칙도 제거했다.
- 학생증 동물 원본의 4:3 스프라이트를 5×2 정사각 셀처럼 표시해 얼굴이 가로로 늘어나던 오류를 수정했다. `1213×815`에서 참고 화면과 동일한 원·초상·별명·리본·비밀번호 칸의 축을 비교하고, 고양이·코알라·사자 선택 시에도 인접 동물 조각이나 잘림이 없음을 확인했다.
- 로컬 브라우저에서 새 학생을 실제 생성한 뒤 학생 바꾸기로 활성 세션이 제거되어 메인으로 돌아가는 것을 확인했다. 새 탭에서 같은 학급의 동물·별명·그림 비밀번호를 입력해 재입장했을 때 해당 자격정보의 프로필 수와 익명 학생 ID가 각각 하나로 유지되고 같은 학생 홈이 열린 것을 확인했다.
- 확인 화면 크기: `320×568`, `390×844`, `768×1024`, `844×390`, `880×834`, `896×634`, `1024×768`.
- 모든 확인 크기에서 가로 넘침이 없고, 메인 핵심 버튼·입장 단계·태블릿 한 화면 입력·학생 홈의 기준 그림 구성이 유지되는 것을 확인했다.
- `880×834`, 기준 캡처와 같은 `896×634`를 포함한 태블릿·일반 화면에서는 상단 내비게이션부터 하단 안전 문구까지를 `1488×1057` 한 무대로 함께 축소한다. 교실 그림·학생 카드·수업 코드 카드가 세로 목록으로 분리되지 않고 확정 시안처럼 한 장면 안에 겹쳐진다. 휴대전화만 터치 크기와 가독성을 위해 세로 흐름을 사용한다.
  - 2026-08-20 보정: 무대는 가로 화면(601px 경계 유지)과 1025px 이상 전용으로 한정했다. 세로 태블릿(601~1024px)은 cqw 축소가 터치 목표 44px을 깨서 태블릿 전용 그림책 대문·2열 입장 카드가 맡는다 — 아래 "iPad 입장 화면 터치 목표" 항목 참조. `896×634` 등 가로 시안 검증 크기의 무대 렌더링은 그대로다.
- 로컬 전체 자동화 테스트 `243/243` 통과(중복 자격정보 대입 차단 실동작 테스트 포함), typecheck·lint·diff check 통과.
- 2026-08-18 Codex 1차 독립 검증은 FAIL: ① base(`1cafeed`)→후보(`c22371a`) 범위 `git diff --check`에서 문서 3종 3행 끝 공백 ② `animal-portraits-v1/v2.png`의 `asset-manifest.json` 미등재. 나머지 게이트(테스트 243/243, browser-check 3뷰포트 실패 0, 실브라우저 재현, 보안 헤더·rate limit 실동작, 비밀값·hosting.json 무변경)는 통과로 보고됐다.
- 두 결함을 수정했다: 끝 공백 제거, 두 PNG를 프로젝트 생성 자산으로 sha256과 함께 등재(v1은 1536×1024 RGBA 생성 원본 시트로 앱 미참조, v2는 `repack_animal_portraits.py` 파생본으로 입장 UI 사용), 재발 방지로 CLAUDE.md·AGENTS.md 최소 검증에 `git diff --check main...HEAD`를 추가.

## 2026-08-18 별명 후보 확대·공백 무시 매칭 (claude/nickname-spacing-20260818)

- 그림 별명 후보를 동물별 2개에서 10개로 확대했다 (`lib/nickname-ideas.ts`, 전체 100개, 한글 낱말, 공백 무시 기준 중복 없음). `다른 별명` 주사위는 현재 별명을 제외한 후보 전체에서 고른다.
- 별명 비교 규칙을 도입했다 (`lib/nickname.ts`): 표시는 띄어쓰기 유지, 신규 중복 검사·동일 자격정보 검사·재입장·관련 rate-limit 대상 키는 모든 공백 차이 무시. D1 migration 없이 조회 시점 SQL 식으로 기존 행을 매칭하며, 다른 한글 문자는 합치지 않는다. 교사 화면의 중복 별명 표시도 같은 규칙을 쓴다.
- Codex 독립 검증: typecheck·lint·전체 자동화 테스트 249/249·일반/전체 범위 diff check 통과. 신규 Miniflare 실동작 6건에서 공백 변형 재입장 동일 student ID, 공백 변형 신규 생성 409, 직접 삽입한 기존 행 검색, 공백 변형의 동일 brute-force 버킷 사용을 확인했다.
- `320×568`, `390×844`, `844×390` 브라우저 자동 점검은 실패 0건이었다. 실제 브라우저에서 토끼 별명 주사위를 10회 눌러 7개 후보와 연속 반복 0건을 확인했고, `토끼 화가` 생성 → 학생 전환 → `토끼화가`와 틀린 비밀번호 → `다시 골라요` → 올바른 비밀번호 순서로 같은 학생 홈이 열리고 교사 화면 인원이 1명으로 유지되는 것을 확인했다.
- 새 Sites 프로젝트를 만들거나 `.openai/hosting.json`, D1/R2 바인딩을 바꾸지 않았다.

## 2026-08-18 학생 홈 트레이·헤더 보정 검증 (진행 중 브랜치)

- 실제 브라우저 실측: `1350×850`, `1024×768`, `844×390`, `760×500`, `721×500`(경계), `390×844`, `320×568`에서 카드 클러스터의 좌우 여백이 0px 오차로 대칭이고, 카드 밖 넘침 0, 가로 스크롤 0, 헤더 버튼 3종 모두 높이 44px 이상을 확인했다.
- 수정 전 결함 재현: 1350px에서 제목 왼쪽 끝·칩 오른쪽 끝으로 벌어짐, 760px에서 첫 카드 내용이 카드 밖으로 좌우 13px씩 넘침, 390px에서 라벨 버튼이 옆 버튼을 62px/45px로 짓누름 — 모두 해소.
- 폰 폭의 버튼 접힘 규칙은 뒤에 있는 회귀 보호 규칙 `.student-message-button:not(.compact){width:100%}`(그리기 화면 배너용, 테스트로 원문 고정)보다 특이도를 높여 이긴다.
- 자동화: typecheck·lint·테스트 `249/249` 통과. pre-reader-ux의 버튼 아이콘 어서션을 💌 결정에 맞게 갱신했다.
- 그리기 화면 컴팩트 버튼은 브라우저 패널 숨김 상태(rAF 정지)로 실측하지 못했고 구조·CSS 무변경 + 테스트로 확인했다.

## 2026-08-18 펜 굵기 UX 개선 (claude/pen-width-ux-20260818)

- 문제: 굵기 줄이 채우기·도형·글씨·지우개를 지나친 패널 중간에 있어 "펜 → 세부 → 굵기" 3단계처럼 느껴졌다.
- 수정: 굵기 점 5개를 펜 4종 바로 아래로 이동(모든 레이아웃), `굵기` 라벨 제거, 점을 현재 선택 색으로 표시(지우개는 잉크색 유지), 데스크톱 레일 3열 랩 → 5열 한 줄, 밝은 색 대비용 안쪽 테두리.
- 검증: typecheck·build·관련 테스트 39/39·browser-check 3뷰포트 실패 0. 갤럭시 노트식 "다시 탭 → 팝업" 안은 숨은 단계가 생겨 비채택.

## 2026-08-20 iPad 실기기 스튜디오 높이 수정 (ba8547a, main 반영·배포 완료)

- iPad mini 실기기 보고 2건의 실체: "펜 굵기 개선 미반영" 체감 = 반영은 됐으나 사파리 실가용 높이(~880px)에서 높이 예산 초과로 도구 패널 하단(굵기·색)이 접힘, "어떻게 시작할까 카드 잘림" = 카드 423px > 캔버스 360px + overflow:hidden.
- 태블릿 세로 블록에서 안내 영역을 3행 병합(241→135px)해 굵기·모드·색·팔레트를 스크롤 없이 노출하고 도화지는 360→459px로 키웠다. `≤960px` 높이 보조 분기 신설(팔레트 44px은 양보하지 않음). 도형·글씨 옵션이 열리면 캔버스가 잠시 양보(`tool-options-open` 클래스 — 빌드가 신규 `:has()` 조합을 산출물에서 떨어뜨려 클래스 방식만 신뢰).
- browser-check `--ipad` 모드에 사파리 크롬 높이 뷰포트 `768×880`을 상시 추가했다.
- 검증: typecheck·lint·268/268(EXIT=0)·diff-check, 기본 3뷰포트 + `--ipad` 그리기 화면 전부 통과. main 반영·Vercel 배포 완료.

## 2026-08-20 iPad 입장 화면 터치 목표 44px (claude/ipad-entry-touch-targets)

- 문제: iPad 세로 3뷰포트(768×1024·768×880·820×1180)에서 대문·입장 폼 터치 요소 12건이 44px 미달 — 교사 수업 열기 31px, 수업 코드 칸 23×25, 동물 칩 37px, 그림 비밀번호 칩 33px, 다른 별명 27px, 다시 골라요 18px, 입장 방법 다시 고르기 19px 등.
- 원인: 데스크톱용 1488×1057 무대(전 요소 cqw 비례 축소)가 601px 폭부터 무조건 적용되어, 태블릿 전용 2열 입장 카드·그림책 대문 블록을 사장시키고 있었다. 768px에서는 모든 컨트롤이 시안의 52% 크기가 된다.
- 수정(app/globals.css): 무대 미디어 조건을 `가로 601px 이상 | 세로 1025px 이상`으로 한정하고, 태블릿 블록을 `601~900px | 세로 901~1024px(iPad Pro)`로 확장해 되살렸다. 되살린 레이아웃 보강: 수업 코드 칸 46×50, 비활성이라 실측에 안 걸리는 다시 골라요·한 칸 지우기 44px 바닥값, 그림 비밀번호 칩을 동물 칩과 같은 그림 위·이름 아래 구조로 정리, 미리보기 원본 img 숨김(배경 장면 사용), 높이 ≤960px 압축 분기(한 화면 폼이 첫 화면 안에 수납 — 976→약 840px).
- 함께 발견·수정한 기존 버그 2건: ① 대문 신뢰 문구 svg가 크기 속성이 없어 무대 밖 모든 화면에서 100% 폭(폰 370px, 운영 노출 중)으로 폭발 — 기본 16px 지정 ② 대문 topbar 마크업이 그림 래퍼 안 정적 배치라 absolute 그림에 덮여 로고·교사 버튼이 안 보임 — 태블릿 구도에서 절대배치 복구.
- 빌드 버그 추가 확인: 자손 선택자 `.landing-trust-note svg`로 쓰면 뒤 무대 블록의 동일 문자열 선택자와 병합되며 앞 규칙이 산출물에서 소실된다(`:has()` 소실과 같은 부류) — `>` 자식 결합자로 회피, 소스 주석으로 고정.
- 검증: typecheck·lint·268/268(EXIT=0)·`git diff --check`, browser-check 기본 3뷰포트 실패 0, `--ipad` 3뷰포트 실패 0(768×880 핀치 1건은 기록된 CDP 환경 문제 SKIP), 896×634(시안 기준 크기) 무대 렌더 불변 실측, 1024×1366(iPad Pro 세로) 44px 미달 0·가로 넘침 0. `--desktop` 8건 실패는 main과 완전 동일한 기존 문제(무대 자체의 소형 컨트롤, 데스크톱 굵기 버튼 폭 25px)로 이번 변경과 무관 — 별도 판단 대상.

## 2026-08-20 README·대표 이미지 현행화 (claude/readme-refresh)

- `public/og.png`(README 히어로 겸 OG 카드)가 7월 22일 MVP 1의 구브랜드 배너(꿈틀 로고)로 남아 있던 것을 현재 그림책 대문의 1488×1057 실캡처로 교체했다(2.27MB→1.39MB). `app/layout.tsx`의 OG 크기 메타데이터도 1488×1057로 갱신.
- README에 실캡처 미리보기 3장(태블릿 입장 2열 카드·그리기 화면·교사 진행실, `docs/images/`) 섹션을 추가했다.
- 본문 정정: 교사 세션 쿠키 서술을 실제 코드와 일치시킴(구글 콜백 발급만 Lax, 그 외 Strict — e2df6de 반영), `/student/activities` 경로 추가, `check:browser`의 `--ipad`(768×880 포함)·`--desktop` 모드 안내 추가.
- 검증: typecheck·lint·268/268(EXIT=0)·diff-check, 로컬 서버에서 `/og.png` 신규 파일 서빙 확인. `docs/security-data-model.md`에 은퇴한 worker/D1 서술이 남은 것은 별도 작업으로 분리.

## 2026-08-20 최신 디자인 기준 그림책 편집기 재통합 (`codex/updated-design-storybook`, 로컬 검증)

- 최신 `origin/main`(`b38dd2a`)을 기준으로 새 브랜치를 만들고, 이전 Moveable 편집기 작업은 stash로 별도 보존한 뒤 최신 Next.js + Turso + R2 구조에 다시 이식했다.
- 학생 홈의 확정된 `이어 그리기 → 내 그림 → 활동 고르기` 메뉴 순서는 유지하고 그 아래에 그림책 작업실 카드를 추가했다. 완성 작품 상세에서도 해당 그림을 첫 페이지에 넣은 그림책을 바로 만들 수 있다.
- `react-moveable@0.56.0`(MIT)로 이동·8방향 크기 변경·회전·가운데/요소 스냅·44px 조작 여백을 제공한다. 페이지/글/이미지 편집, 앞뒤 순서·잠금, 실행 취소·다시 실행, 배경 오리기, 미리보기와 자동 저장을 복구했다.
- 그림책 테이블 3종을 Turso 자가 프로비저닝 정본과 Drizzle 스키마에 추가했다. 이미지 업로드는 Vercel 4.5MB 본문 한도에 맞춰 base64 JSON 대신 3.5MB 이하 raw PNG로 R2 호환 저장소에 전송한다.
- 자동 검증: typecheck·lint·production build·전체 테스트 `281/281`·`git diff --check` 통과. 기존 `320×568`, `390×844`, `844×390` 브라우저 점검도 실패 0건이다.
- 실제 로컬 체험에서 샘플 완성 그림→그림책 생성, Moveable 그림 이동, 자동 저장, 글 추가, 실행 취소·다시 실행, 미리보기, 학생 홈 진입 카드와 가로 넘침 0을 확인했다. 중간 폭에서 도구줄 첫 버튼이 잘리던 문제도 가로 스크롤 도구줄로 보정했다.
- 로컬 서버는 `http://localhost:3000`, 체험 시작은 `/student/books/demo`다. 그림책 재통합은 `25f7046`으로 개인 원격의 `codex/updated-design-storybook` 브랜치에 push했으며 `origin/main`·운영 배포는 건드리지 않았다.

## 2026-08-20 Local·Preview·Production 환경 분리 (`codex/updated-design-storybook`)

- 사용자의 확정 지시에 따라 세 환경은 같은 UI·도메인·API 코드를 공유하고, 인증·데이터·파일 저장·시드만 환경에 맞게 선택하도록 정리했다. `NODE_ENV`를 배포 대상 판별에서 제거하고 Vercel의 `VERCEL_ENV`를 우선하는 `lib/runtime/environment.ts`를 정본으로 추가했다.
- 공통 교사 세션은 `lib/auth/`, localhost 전용 교사·그림책 시드는 `lib/dev-only/`, 로컬 데모 UI는 `app/components/dev-only/`로 분리했다. Vercel Preview와 Production에서는 localhost 기능을 강제로 켤 수 없고 모두 구글 OAuth를 사용한다.
- Local/Test는 원격 Turso·R2 값이 들어오면 실행을 거부한다. Preview/Production은 `WIGGLE_DATA_ENV`가 현재 환경과 일치하고 원격 Turso·R2·Google OAuth 설정이 모두 있어야 한다. Preview R2에는 adapter가 `preview/` 접두사를 적용하며 Production의 기존 object key는 유지한다.
- `/api/health`는 현재 환경과 DB·저장소·OAuth 설정을 검증하되 비밀값은 노출하지 않는다. 환경별 설정·Vercel 변수 범위·승격 절차의 정본은 `docs/environments.md`다. `.openai/hosting.json`은 이력 그대로 수정하지 않았다.
- 독립 검증: typecheck·lint·production build·전체 테스트 `288/288`·adapter 테스트 `19/19`·`git diff --check` 통과. 로컬 `/api/health`가 `environment=local`로 응답했고, `320×568`, `390×844`, `844×390` 실제 DOM 브라우저 점검도 실패 0건(390×844 핀치 1건은 기존 CDP 환경 사유 SKIP)이다.
- 이 환경 분리 변경은 이 문서를 포함해 개인 원격의 `codex/updated-design-storybook` 기능 브랜치에 push했다. `origin/main`과 Vercel 환경 변수·Production 배포는 건드리지 않았으며, 실제 Vercel Preview 연결은 전용 Turso·R2·OAuth 값을 준비한 뒤 별도로 확인한다.

## 2026-08-21 로컬 `.env.local` 재실행 확인

- 사용자가 준비한 `.env.local`을 Next.js 개발 서버가 인식한 상태로 `codex/updated-design-storybook` 브랜치를 `http://localhost:3000`에서 실행했다.
- 대문이 HTTP 200으로 렌더링되고 `/api/health`가 `{"ok":true,"environment":"local"}`을 반환해 로컬 환경 경계가 유지됨을 확인했다. 비밀값은 출력하지 않았다.
- `/student/books/demo`에서 로컬 전용 학생·작품·그림책 시드가 생성되고 실제 편집기로 이동했다. 그림책 조회와 이미지 자산 회수가 모두 HTTP 200이었고 브라우저 오류 로그는 없었다.

## 2026-08-21 어린이용 그림책 고정 레이아웃 개편 (`codex/updated-design-storybook`, 로컬 작업)

- 사용자 캡처와 실제 DOM을 대조해 선택 요소는 약 `604×360px`인데 내부 그림은 약 `604×453px`로 넘치던 원인을 확인했다. 그림의 자연 비율을 요소 모델에 저장하고, 요소와 내부 이미지가 같은 너비·높이를 쓰도록 바꿔 선택 박스와 그림 비율을 일치시켰다.
- 페이지마다 이야기 글은 정확히 하나만 두고 상단 가운데 고정 영역에 배치한다. 글 추가·복제·이동·회전·삭제는 제거하고, 오른쪽의 `이야기 글` 입력칸에서 내용·크기·색만 바꾼다.
- 그림은 글 아래 점선 영역 안에서만 추가·이동·크기 변경되며 자연 비율을 유지한다. 새 그림, 기존 그림, 배경 오리기 결과, 책 비율 변경 때 모두 같은 배치 계산을 사용하고 서버 저장 검증도 동일한 경계를 강제한다.
- 페이지 배경 이미지 자산을 별도로 지정할 수 있게 해 페이지 전체를 `cover`로 채우고 그 위에 이야기와 그림이 올라가도록 했다. 저장 API의 자산 소유권 검사에도 배경 자산을 포함했다.
- 편집기와 페이지 설정 양쪽에서 가로형·세로형·정사각형을 선택할 수 있다. 책 전체 비율을 바꾸면 모든 페이지의 고정 글 영역과 그림 배치를 새 비율에 맞게 다시 계산한다.
- 자동 검증: typecheck·lint·production build·전체 테스트 `289/289`, 그림책 집중 테스트 `9/9` 통과. 로컬 브라우저에서 수정 전 DOM 수치까지 재현한 뒤 브라우저 자동화의 localhost URL 정책에 막혀 최종 화면 자동 실측은 중단했으며, 생성된 CSS와 빌드 산출물에 높이·배경·배치 규칙이 포함된 것을 확인했다.

## 2026-08-23 그림 실제 경계 맞춤·생성 전 책 모양 선택 (`codex/updated-design-storybook`)

- 그림 선택 박스가 원본 PNG 전체 캔버스를 잡아 흰색·투명 여백까지 포함하던 문제를 수정했다. 가장자리와 이어진 배경색 및 투명 픽셀을 분석해 실제 내용 경계를 요소의 `crop`으로 저장하고, 원본 이미지 자산은 변경하지 않는다.
- 기존 그림과 새 그림은 로드 시 자동으로 경계를 계산한다. 오른쪽 그림 설정의 `빈 여백 없이 맞추기`는 더 넓은 색 허용 범위로 다시 계산하며, 자른 원본을 요소 안에서 확대·이동해 보이는 그림과 Moveable 선택 박스가 같은 경계를 사용한다.
- 완성 작품의 `이 그림으로 그림책 만들기`는 곧바로 가로형을 만드는 대신 가로·세로·정사각형 선택 창을 먼저 연다. 로컬 실제 흐름에서 세로형 선택 후 `format-portrait` 편집기가 열리는 것을 확인했다. 그림책 목록의 기존 세 모양 선택도 유지한다.
- 브라우저 실측: 데모 원본의 내용 경계가 `x=34.95%`, `y=4.08%`, `width=58.69%`, `height=76.69%`로 계산됐고, 내부 원본은 선택 요소 대비 `170.4%×130.4%`로 배치되어 빈 캔버스가 잘리고 실제 그림이 박스를 채웠다. typecheck·lint·production build·전체 테스트 `291/291`, 집중 테스트 `16/16`을 통과했다.
- 위 변경 전체는 개인 원격의 `codex/updated-design-storybook` 기능 브랜치에 push한다. `origin/main`과 Vercel Production은 건드리지 않는다.

## 2026-08-27 귀여운 점선 변형·교사용 완성 그림책 (`codex/teacher-storybooks-cute-guides`, 로컬 검증 완료)

- 따라 그린 아이 작품을 AI 이미지로 바꾸지 않는다. 대신 따라 그리기 10개 활동에 작품별 고정 가이드 번호를 저장하고, 같은 학생이 같은 활동을 새로 만들 때 `방긋 정면 → 반대쪽 인사 → 왼쪽 갸웃 → 오른쪽 갸웃` 네 가지 미리 만든 변형을 순환한다. 머리·몸 단계의 중심과 비율을 따로 조정해 단순 좌우 반전만 반복하지 않으며, 다시 열기·다른 기기 재입장에서도 같은 가이드가 유지된다.
- 교사 수업실에 `완성 그림책` 메뉴를 추가했다. 자기 학급의 `complete` 그림책만 동물·별명, 완성 시각, 쪽 수와 함께 볼 수 있고, 모든 쪽을 넘겨 보거나 브라우저 인쇄의 `PDF로 저장`을 사용할 수 있다. 그림책 이미지도 객체 키를 공개하지 않고 교사 세션·활성 학급 소유권을 다시 확인하는 전용 경로로 전달한다.
- 교사는 한 권씩 또는 체크박스로 최대 50권을 골라 피드백을 요청할 수 있다. 다른 반 책이나 작성 중인 책이 하나라도 섞이면 전체 요청을 거절한다. 루브릭 미확정 상태에서는 피드백을 생성하지 않고 `storybook_feedback_requests.status = waiting_rubric`으로만 저장한다.
- DB 정본과 런타임 자가 프로비저닝에 `artworks.guide_variant`, `storybook_feedback_requests` 및 인덱스를 추가했고 Drizzle `0006` 스키마 기록을 생성했다. 기존 작품의 가이드 번호는 `0`으로 안전하게 유지된다.
- 자동 검증: typecheck·lint·production build·전체 테스트 `294/294`·`git diff --check` 통과. 실제 HTTP 통합 테스트에서 가이드 `0→1→2→3` 순환, 교사 목록의 완성본 필터, 다른 교사 학급 IDOR 차단, 혼합 일괄 요청 원자 거절, `waiting_rubric` 저장을 확인했다.
- 브라우저 검증: 기본 `check:browser`의 `320×568`, `390×844`, `844×390` 전 항목 실패 0. 교사용 목록·열람 화면은 `320×568`, `390×844`, `768×1024`, `844×390`, `1024×768`에서 가로 넘침 0, 44px 미만 행동 0으로 확인했다. `1280×720`에서 책 아래가 잘리던 문제는 화면 높이에 맞춰 책 전체 비율을 축소하도록 고쳤다. 로컬에서 첫 강아지는 `방긋 정면`, 두 번째 새 강아지는 `반대쪽 인사`로 실제 생성되는 것을 확인했다.
- 아직 `main` 반영·Vercel 배포는 하지 않았다. 사용자가 브랜치의 로컬 화면을 확인한 뒤 기능 브랜치 커밋·push 여부를 결정한다. 피드백 루브릭과 승인·노출 정책은 `pending-decisions.md` P-004에 남아 있다.

### 강아지 점선 가이드 시각 결함 후속 수정

- 첫 구현은 좌표 범위·변형 순환 테스트를 통과했지만 실제 3단계 누적 화면에서 기존 원본의 중복 왼쪽 눈, 얼굴 중앙의 큰 주둥이 타원, 머리 안으로 파고드는 귀 선이 그대로 확대되어 기괴하게 보였다. 자동 테스트 통과를 귀여움 검증으로 잘못 간주한 결함이었다.
- `friendly-dog`는 일반 수치 변형에서 분리하고 `lib/friendly-dog-guides.ts`의 손설계 벡터 4종으로 교체했다. 눈 두 개는 작고 서로 떨어지며, 코·웃는 입만 얼굴 안에 두고 큰 주둥이 원을 없앴다. 귀는 얼굴 중앙을 침범하지 않고, 몸·앞다리·발·꼬리도 각 단계에서 서로 알아볼 수 있게 분리했다. AI 이미지 생성이나 아이 그림 교체는 사용하지 않는다.
- `scripts/render-guide-audit.mjs friendly-dog`로 4종의 모든 단계를 한 장에 누적 렌더링해 형태를 직접 비교할 수 있게 했다. 실제 브라우저에서는 새 작품의 1단계 머리→2단계 귀→3단계 얼굴→4단계 몸·앞다리→5단계 발·꼬리를 각각 열어 점선 크기·위치·잘림을 확인했다.
- 회귀 테스트는 중복 눈, 큰 얼굴 타원, 귀의 얼굴 중앙 침범을 구조적으로 차단한다. typecheck·lint·production build·전체 테스트 `295/295`·두 diff check가 통과했고, `check:browser`의 `320×568`, `390×844`, `844×390`도 실패 0건이었다(390×844 핀치 1건은 기존 CDP 환경 사유 SKIP).
- 신규 정본 저장소 `wwwiggle/new_wiggle`의 `codex/teacher-storybooks-cute-guides` 브랜치에 검증된 변경을 push했고, `qudcks1940` 계정으로 PR [#2](https://github.com/wwwiggle/new_wiggle/pull/2)를 열었다. `main` 병합과 Vercel 운영 배포는 수행하지 않았다.

### 대문 수업 코드 복원 후 입장 버튼 비활성 결함 수정

- 브라우저가 네 개의 수업 코드 입력값을 화면에는 복원했지만 React의 `digits` 상태는 빈 배열로 남겨 `그리러 가기`가 비활성화되는 현상을 로컬 코드 `4341`로 재현했다. 학급 조회 결과 `4341`은 정상적인 `로컬 연습반` 코드였다.
- 복원 시점의 입력값을 React 상태와 다시 맞추고, 제출 시에는 상태 배열 대신 실제 네 입력 요소의 값을 읽는다. 각 칸은 HTML `required`와 한 자리 숫자 패턴으로 검증하므로 화면에 완성된 코드가 있으면 상태 복원 시점과 관계없이 입장할 수 있고, 빈 칸이 있으면 해당 칸으로 안내된다.
- 검증: typecheck·lint·production build·전체 테스트 `296/296`·`git diff --check` 통과. `browser-check`에 입력 이벤트 없이 네 칸을 복원한 뒤 실제 입장 URL까지 확인하는 회귀 항목을 추가했고 `320×568`, `390×844`, `844×390`에서 모두 통과했다. 서버 재시작 후 실제 브라우저에서도 `4341` 입력 → 활성 버튼 → `/join?code=4341` → `어떻게 들어갈까요?`까지 확인했다. 수정은 기존 PR #2 브랜치에 추가하고 `main`과 운영 배포는 건드리지 않는다.

### Next.js 2026년 8월 긴급 보안 패치

- 공식 2026년 8월 보안 릴리스에서 Next.js `16.2.6`은 AVIF 이미지 최적화 경로의 비인증 원격 코드 실행과 Windows 호스팅 서버의 비인증 원격 코드 실행 취약 범위에 포함됐다. 7월 릴리스의 rewrite·Server Action·이미지 최적화 관련 HIGH/MEDIUM 취약점 범위에도 포함돼 있었다.
- Active LTS의 공식 수정판인 `next@16.3.3`과 `eslint-config-next@16.3.3`으로 올렸다. 잠금 파일에서 함께 갱신된 `sharp@0.35.4`, `postcss@8.5.23`도 기존 취약 버전에서 벗어났고, 운영 의존성의 `ws`는 호환 범위 내 `8.21.3`으로 갱신했다.
- 앱은 `next/image`·AVIF 설정, rewrite, Server Action, Cache Components를 사용하지 않아 일부 공격 경로의 실제 노출은 낮다. 그러나 App Router를 사용하고 로컬 개발이 Windows에서 이뤄지므로 버전 기반 패치는 필수다.
- `16.3.3` 기본 Turbopack 빌드는 한글 소스 오류 위치를 표시할 때 UTF-8 바이트 경계를 잘못 잘라 Rust panic을 일으키는 공개 이슈(`vercel/next.js#92641`)를 재현했다. 소스 오류는 Webpack 빌드에서 없음을 확인했고, 공식 지원 플래그인 `--webpack`을 dev/build 스크립트에 고정해 Vercel 빌드와 로컬 개발을 안정화했다. 보안 회귀 테스트는 Next가 `16.3.3` 아래로 내려가지 않고 eslint 설정 버전과 일치하며 두 스크립트가 Webpack을 유지하는지 검사한다.
- Next 16.3 개발 서버가 루트 `AGENTS.md`에 자체 규칙을 자동 추가하는 동작은 저장소의 제품·보안 규칙을 실행 때마다 오염시키므로 `next.config.ts`의 `agentRules: false`로 끄고 자동 생성분은 포함하지 않았다.
- `npm audit`의 Next.js·sharp·postcss·운영 의존성 경고는 0건이다. 전체 감사에 남은 4개 moderate는 실행 중 앱이 아니라 `drizzle-kit@0.31.10`이 내부적으로 쓰는 로컬 개발용 `@esbuild-kit/esm-loader → esbuild@0.18.20` 체인이다. 이를 자동 제거하는 유일한 제안은 `drizzle-kit@0.18.1`로 강제 다운그레이드하는 breaking change이므로 적용하지 않았다.
- 검증: typecheck 통과, lint `0 errors`(새 규칙이 기존 의도적 전체 페이지 이동 8곳을 warning으로 보고), Webpack production build와 전체 테스트 `297/297`, `git diff --check` 2종 통과. 실제 브라우저 점검은 `320×568`, `390×844`, `844×390`에서 실패 0건이었고, `/student`·`/teacher`는 `DENY`와 `frame-ancestors 'none'`, 공개 랜딩은 프레임 차단 없음, 외부 AVIF 이미지 최적화 요청은 HTTP 400임을 확인했다.
- 이 변경은 기능 브랜치와 기존 PR #2에만 반영하며 `main` 병합과 Vercel 운영 배포는 수행하지 않는다.

## 2026-08-23 신규 팀 GitHub 저장소 전환

- 사용자의 지시에 따라 이후 개발 대상 저장소를 `https://github.com/wwwiggle/new_wiggle`로 정했다.
- 신규 저장소의 기본 브랜치는 현재 `main`이며, 검증된 그림책 편집기 변경까지 병합된 `bbbc51e`가 정본이다. 전환에 사용한 임시 기능 브랜치는 원격에서 정리해 `main`만 유지한다.
- 로컬 `main`과 기능 브랜치는 신규 저장소의 `origin/main`을 기준으로 추적한다. 옛 저장소는 `legacy-origin`으로 보존한다. Vercel Git 연결은 아직 기존 `yonghwan86/wiggle_web` 저장소를 가리키므로, 연결 변경 전까지 신규 저장소의 `main` push는 공개 운영 배포를 시작하지 않는다.

## 2026-08-23 README AI 그림책 편집기 안내 (`codex/readme-storybook-editor`)

- README의 현재 기능에 AI 그림책 편집기의 진입 흐름, 3가지 책 모양, 고정 이야기 영역, 그림 배치 제한, 실제 내용 경계 맞춤, 페이지·레이어 편집, 저장·완성 기능을 추가했다.
- 외부 Canva 대신 Wiggle 내부 편집기와 MIT 라이선스 `react-moveable`을 사용한다는 점, Turso 그림책 문서와 R2 이미지 자산의 역할, 학생 화면 경로를 문서화했다.
- 신규 정본 저장소 주소를 `wwwiggle/new_wiggle`로 바꾸고 전체 테스트 표기를 최근 검증 결과인 `291/291`로 현행화했다. Vercel이 아직 기존 저장소에 연결된 전환 상태도 README 배포 안내에 명시했다.
- 문서 변경 뒤 typecheck·lint·production build·전체 테스트 `291/291`과 `git diff --check`를 다시 통과했다.

## 2026-09-07 오리 전시관 대문 구현 (`claude/landing-gallery-20260907`, 로컬 검증)

- 사용자가 고른 전시관 시안(`docs/design-assets/gallery/selected-reference.png`)대로 `app/page.tsx`를
  다시 만들었다. 화가 오리를 앞에 세우고 우주·바다·집 그림을 액자에 담아 뒤에 걸었으며, 하단에
  `모두 다른 답`·`AI는 대신 그리지 않아요`·`비교보다 발견` 소개 영역을 추가했다.
- 그림은 `public/landing-gallery/`의 개별 WebP와 직접 작성한 SVG 아이콘으로 배치했고, 문구·교사 버튼·
  4자리 코드 입력·`그리러 가기`는 모두 실제 HTML이다. 로고는 기존 `/brand/logo.png`를 그대로 쓴다.
  전시 그림과 도형 표식은 제품 소개용 장식이며 실제 학생 작품이나 레슨 씨앗 데이터가 아니다.
- 입장 동작은 손대지 않았다. `LandingCodeForm`(4자리 → `/join?code=`)과 `/teacher` 링크,
  인증·API·저장 경로는 기존 구현을 그대로 재사용한다.
- 은퇴 처리: 단일 `landing-scene-v2.png` 그림 하나에 HTML을 얹던 `1488×1057` cqw 무대를 제거했다.
  `app/globals.css`에서 셀렉터가 전부 `.landing` 계열인 규칙 182개(513줄)를 지우고 전시관 블록 하나로
  대체했다. `.entry-*`·`.student-*`와 섞인 규칙과 이번 저장소의 `.teacher-*` 추가분은 그대로 두었다.
  이 무대는 세로 태블릿에서 조작 요소를 44px 아래로 줄이던 원인이므로 새 대문은 cqw를 쓰지 않는다.
  `public/brand/landing-scene-v2.png`는 이제 어디서도 참조하지 않지만 파일은 남겨 두었다.
- `f0615ec`의 코드 입력 수정과 합쳤다. 제출 버튼은 더 이상 `disabled`가 아니라
  `:has(.landing-code-box:invalid)`로 흐려지며, 브라우저가 복원한 값도 그대로 제출된다.
  대문 CSS도 이 방식을 따르고 `:disabled` 규칙은 두지 않는다.
- 에셋: `public/landing-gallery/`(WebP 13·PNG 7·SVG 아이콘 10·조합용 CSS·매니페스트)와
  `docs/design-assets/gallery/`의 선택 시안·README·프롬프트·검수 화면·재생성 스크립트를 커밋했다.
  15MB 생성 원본과 검수 연락판, 배포용 zip은 커밋하지 않았다 — 재생성 경로는 `prompts.json`에 있다.
- `tests/landing-page.test.mjs`는 은퇴한 무대 계약(1488 좌표계, 옛 서브타이틀 폭, 옛 데스크톱 타이포)
  세 항목을 전시관 계약으로 교체했다. 코드 카드·교사 링크·제목 마크업·`/join`·복원 코드 계약은 유지했다.
  액자 안쪽 좌표는 `public/landing-gallery/gallery-assets.css`와 값이 어긋나면 실패하도록 교차 검사한다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build`, 두 범위의 `git diff --check` 통과.
- 실제 브라우저 검증(로컬, headless Chrome/CDP): `npm run check:browser`(320×568·390×844·844×390)와
  `--ipad`(768×1024·768×880·820×1180) 전 항목 통과, `--desktop`(1440×900·1920×1080)의 대문 4항목 통과.
  스크립트 인자는 `node scripts/browser-check.mjs <baseUrl> --desktop` 순서여야 한다
  (`npm run check:browser -- --desktop`은 `--desktop`을 주소로 읽어 실패한다).
- 6개 폭(320·390·844×390·768·1024·1440)에서 computed box를 직접 측정해 가로 스크롤 0,
  44px 미만 터치 목표 0, 수업 코드 네 칸 46~52×48~56px을 확인했다. 실제 브라우저에서 코드 입력·
  초점 이동·`/join?code=` 이동·`/teacher` 이동도 확인했다.
- 휴대전화는 `오리 → 학생 태그 → 수업 코드 → 안전 문구 → 전시 그림 → 소개` 순서로 세로 정렬하고,
  760px 이상에서 시안 구도(액자 뒤·오리 앞·오른쪽 아래 입장 카드)로 바뀐다.
- 남은 위험: 대문 전체 높이는 1440×900에서 약 1005px이라 소개 영역을 보려면 한 번 스크롤한다.
  휴대전화에서는 액자 3개가 세로로 쌓여 문서가 길다(390px 기준 약 1875px).

## 2026-09-07 그리기 화면 플로팅 도구 (`claude/drawing-studio-floating-20260907`, 로컬 검증)

- 기준 시안: `docs/design-assets/drawing-toolbar/drawing-studio-selected-reference.png`.
  넓은 화면(가로 900px·세로 600px 이상)을 3열 고정 사이드바에서 시안의 떠 있는 카드 배치로 바꿨다.
  도화지가 화면을 차지하고 그 위에 안내 카드(왼쪽 위), 도구 레일과 색 팔레트(오른쪽)가 뜬다.
- 도구 레일은 `public/drawing-tools/`의 실제 도구 사진을 56×22 슬롯에 놓는다. 위에서부터
  손잡이 · 되돌리기/다시하기 · 연필/크레용/마커/수채붓 · 지우개/대칭 · 더보기 순이며,
  고른 도구는 테두리가 생기고 오른쪽으로 6px 나온다.
- 선 굵기: 고른 펜을 다시 누르면 그 펜 왼쪽 줄에 드래그 슬라이더가 열린다. 다른 도구·바깥 누름·Esc로 닫는다.
  값은 기존 `STROKE_WIDTHS` 5단을 그대로 쓰는 `input[type=range]`이므로 저장 문서 형식은 바뀌지 않는다.
  좁은 화면은 실측으로 다듬어 둔 5단 점 버튼을 유지한다(`product-decisions.md` 캔버스와 입력 8번).
- 자주 쓰지 않는 도구(채우기·도형·글씨·펜/손가락 모드·전체 지우기)는 `더보기` 시트로 접었고 레일 왼쪽에 열린다.
- 안내 카드는 오른쪽 위 꺾쇠로 접고 펼 수 있다. 접으면 그림 썸네일만 남는다.
- 아이콘은 시안 문서가 제안한 `lucide-react` 대신 같은 모양·2px 둥근 선의 인라인 SVG(`app/components/StudioIcons.tsx`)로
  넣었다. 이 저장소는 아이콘을 인라인 SVG로 그려 왔고 필요한 모양이 10개뿐이라 패키지를 늘리지 않았다.
- 좁은 화면 안전장치: 새 래퍼(`.tool-rail`/`.tool-sheet`/`.tool-colors`)는 좁은 화면에서 `display:contents`로
  사라지고, 바뀐 DOM 차례는 `order`로 원래 보이던 순서로 되돌린다. 떠 있는 카드는 작은 도화지를 가리고
  44px 터치 목표를 깨므로 넓은 화면에만 적용한다.
- `scripts/browser-check.mjs` 3곳을 고쳤다. ① 숨겨진 도구 버튼은 접근성 검사 대상에서 뺀다(이미 `smallTargets`가
  쓰던 `visible()`과 기준을 맞춤) ② `label()`이 SVG의 `className`(문자열이 아님)에서 죽지 않게 `String()`으로 감쌈
  ③ 도구 아이콘 검사가 이모지 대신 실제로 보이는 아이콘(사진 또는 이모지)을 재고, 크기·아이콘 검사는 지금 보이는
  버튼만 대상으로 한다. 이름 검사는 도구 9개 전부에 그대로 적용된다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과.
  `drawing-tools`·`mobile-css`·`guide-demonstration`·`responsive-drawing-ui`·`pre-reader-ux`·`landing-page` 통과(66개).
  전체 `npm test`의 실패 33건은 대문 브랜치와 목록이 완전히 같다(Node v22.13.0의 `.ts` 로더 문제).
- 실제 브라우저 검증: `npm run check:browser`(320×568·390×844·844×390)와 `--ipad`(768×1024·768×880·820×1180)
  전 항목 통과. `--desktop`(1440×900·1920×1080)의 그리기 화면 항목은 전부 통과했고, 이전에 실패하던
  `그리기 화면 터치 목표 44px 이상`(5단 점 버튼 폭 25.4px)은 슬라이더로 바뀌며 해결됐다. 남은 실패 5건은
  입장 화면(`입장 단계`, `다시 골라요`)과 핀치 환경 문제로 이번 변경과 무관한 기존 항목이다.
- 1440×1000·1024×768에서 굵기 슬라이더 열림·값 변경·다른 도구 선택 시 닫힘, 더보기 시트 열림·Esc 닫힘,
  안내 접기·펼치기를 실제 브라우저로 확인했다.
- 남은 위험: 도화지는 여전히 정사각형 문서다. 시안의 가로로 긴 도화지는 저장 형식을 바꿔야 해서 적용하지 않았다.
  헤더의 `선생님 말씀`은 기존 컴포넌트라 💌 이모지를 그대로 쓴다(시안은 말풍선 아이콘).

## 2026-09-07 가로 도화지 (`claude/wide-canvas-20260907`, 로컬 검증)

- 새 작품의 도화지를 정사각(1024×1024)에서 가로 4:3(1024×768)으로 바꿨다. 그리기 화면 시안의
  넓은 도화지를 실제로 구현한 것이다.
- **저장 형식 변경**: `DrawDocument`에 `height?: 1024 | 768`을 더했다. 좌표는 x·y 모두 0~1로
  정규화돼 있어 세로가 달라지면 같은 문서가 다르게 그려진다. 그래서 세로를 문서에 함께 저장하고,
  `height`가 없는 기존 작품은 예전 그대로 정사각으로 읽는다. 이미 저장된 그림은 하나도 건드리지 않는다.
  서버 `validateDrawDocument`는 세로가 없거나 허용 목록 안일 때만 통과시키고, 문서를 다시 만들어
  돌려줄 때 `height`를 그대로 보존한다(빠뜨리면 저장된 가로 도화지가 조용히 정사각이 된다).
- 렌더러(`lib/draw-renderer.ts`)의 크기 인자가 숫자 또는 `{width,height}`를 받는다. x는 가로,
  y는 세로에 곱하고, 굵기·글자처럼 "문서 단위"로 저장된 값은 가로(1024) 기준으로 환산한다.
  숫자를 그대로 주면 예전과 완전히 같은 정사각 렌더다.
- `computeFloodFillMask`는 `options.height`로 세로를 받는다. 없으면 예전처럼 정사각을 훑는다.
- 점선·시범 안내는 정사각 기준 좌표라, 도화지 가운데의 정사각 영역에 `translate`+`scale`로 넣어
  동그라미가 타원이 되지 않게 했다.
- 지우개 자국은 가로 %로만 크기를 잡고 `aspect-ratio:1`로 정사각을 지킨다 — 가로 도화지에서는
  가로 %와 세로 %가 서로 다른 픽셀이라 예전 방식이면 실제로 지워지는 영역과 어긋난다.
- 교사 화면의 작품 썸네일(`.student-thumb`, `.teacher-artwork-history-grid`)을 `cover`에서
  `contain`으로 바꿨다. 가로 그림이 정사각 칸에서 양옆이 잘리면 아이 그림 일부가 사라진다.
- 타임랩스도 문서 비율로 재생한다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과.
  `tests/wide-canvas.test.mjs`(신규, 런타임 계약 5개)와 `tests/drawing-tools.test.mjs`(소스 계약 추가) 통과.
- 실제 브라우저·실제 저장 API로 확인한 호환 계약(`compat` 스크립트):
  ① `height` 없는 기존 문서를 서버가 그대로 받고 ② 다시 읽어도 `height`가 붙지 않으며
  ③ 화면·래스터 모두 1.000 비율로 렌더된다. 새 문서는 ④ `height:768`이 저장·회수되고
  ⑤ 화면·래스터 모두 1.333이다. ⑥ 허용 목록 밖 세로(500)는 서버가 거절한다.
- `npm run check:browser`(320×568·390×844·844×390) 전 항목 통과 — 가로 도화지에서 대칭·되돌리기·
  채우기·도형 2탭·핀치까지 실제 입력으로 확인했다.
- 이 맥의 Node v22.13.0은 `.ts`를 벗기지 못해 전체 `npm test`가 33건 실패한다.
  `node --experimental-strip-types`를 붙이면 305개 중 294개가 통과하고, 남은 11건은
  `node:module`의 `registerHooks`가 없어 실패하는 하네스 문제로 이번 변경과 무관하다.
- 남은 위험: 이미 저장된 작품은 정사각 그대로다. 기존 작품을 가로로 바꾸는 마이그레이션은 하지 않았다
  (좌표를 늘리면 아이 그림이 찌그러진다). 4:3은 제품의 전시 그림·액자 에셋(960×720)에 맞춘 값이며
  다른 비율을 원하면 `DOCUMENT_HEIGHTS`에 값을 더하고 `DEFAULT_DOCUMENT_HEIGHT`만 바꾸면 된다.

## 2026-09-07 AI 도우미 이름 몽그리로 변경 (`claude/mongri-rename-20260907`, 로컬 검증)

- 아이에게 보이는 이름과 AI 시스템 프롬프트의 이름을 `그리미`에서 `몽그리`로 바꿨다.
  앱 화면·API 오류 문구·README·검증 스크립트 라벨·데모 데이터까지 73건이다.
- **안전 필터는 넓히기만 했다.** `lib/openai-coaching.ts`의 "대신 그려 줄게" 차단 패턴과
  평가자 화자 집합(`EVALUATOR`)에 `몽그리`를 더하면서 `그리미`를 그대로 남겼다. 이미 저장된
  과거 코칭 기록과 모델이 옛 이름을 쓰는 경우를 계속 막아야 하기 때문이다.
  `tests/coaching-safety.test.mjs`에 두 이름 모두를 막는지 보는 사례를 더했다.
- 코드 식별자(`grimi-panel`, `grimiOpen`, `askGrimi` 등)와 자산 경로(`/brand/grimi-mascot.png`)는
  그대로 두었다. 아이에게 보이지 않고, 바꾸면 변경 범위와 위험만 커진다.
- 과거 기록 문서(`docs/claude-handoff-2026-07-27.md`, `ux-market-audit-2026-07.md`,
  `agent-handoff/latest.md`, `flutter-adoption-audit.md`)의 옛 이름은 그날의 기록이므로 남겼다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과.
  전체 테스트는 `node --experimental-strip-types`로 305개 중 304개 통과이며, 남은 1건
  (`tests/pbkdf2-runtime.test.mjs`)은 이 맥의 Node v22.13.0에 `node:module`의 `registerHooks`가
  없어서 나는 환경 문제다.
- 참고: 앞선 기록들이 "전체 테스트 실패 33건/11건"이라고 적은 것은 상당수가 로컬 dev 서버가
  포트를 잡고 있어 하네스 서버가 뜨지 못한 탓이었다. dev 서버를 내리고 돌리면 304/305다.

## 2026-09-07 도화지가 화면을 채우도록 (`claude/full-bleed-canvas-20260907`, 로컬 검증)

- 앞선 4:3 고정 도화지는 화면보다 좁아 좌우에 회색 여백이 남았다(1440×1000에서 좌우 221px).
  시안의 도화지는 화면을 가득 채우므로, 새 작품은 **첫 획을 긋기 전까지** 그릴 수 있는 넓이의
  비율을 따라가게 했다. 한 획이라도 그으면 그 비율로 굳는다.
- 문서 세로가 고정 목록(1024/768)에서 **범위**로 바뀌었다: 512~1024, 16의 배수.
  아무 값이나 받으면 저장된 그림의 비율을 마음대로 바꿀 수 있고 극단적 비율은 썸네일을 깨뜨린다.
  `clampDocumentHeight()`가 화면에서 잰 값을 항상 저장 가능한 값으로 맞추고 서버 검증도 같은 규칙을 쓴다.
- **찌그러짐 버그를 고쳤다.** 이전 CSS(`height:100%` + `max-width:100%`)는 도화지가 남는 자리보다
  넓을 때 `aspect-ratio`를 이겨 상자가 화면 비율이 됐다 — 세로로 긴 화면에서 가로 작품이 눌렸다.
  이제 남는 자리(`container-type:size` + `cq`)에서 폭을 직접 계산하고 높이는 비율이 정한다.
  이 버그는 앞선 PR(#5)에도 있었고, 그때 검증이 가로 작품을 가로 화면에서만 봐서 놓쳤다.
- 실제 브라우저로 확인한 것:
  ① 빈 도화지가 그릴 수 있는 넓이를 채운다(도화지 1.524 ≈ 넓이 1.542, 차이는 16단위 반올림)
  ② 아직 안 그린 도화지는 창 크기를 따라 바뀐다(1.524 → 1.208)
  ③ 실제 획을 그은 뒤에는 창을 줄여도 **비율이 굳는다**(1.524 → 1.524)
  ④ 기존 정사각 작품은 가로 화면(1600×800)에서도 1.000 유지
  ⑤ 가로 작품은 세로 화면(800×1200)에서도 1.333 유지 — 위 버그의 회귀 검사
- 검증: typecheck, lint, build, `git diff --check` 통과. `check:browser` 기본 3뷰포트·`--ipad`
  3뷰포트 전 항목 통과, `--desktop`의 그리기 화면 항목 전부 통과.
  전체 테스트는 `node --experimental-strip-types`로 **305개 중 304개 통과**(남은 1건은 이 맥의
  Node v22.13.0에 `node:module`의 `registerHooks`가 없어서 나는 환경 문제).
- 검증 중 `잘못된 수업 코드 흐름 재현`이 한 번 실패했다가 같은 코드로 다시 돌리니 통과했다.
  반복 검증으로 `student-entry` rate limit이 31까지 올라가 있던 구간의 흔들림으로 보인다.
  기준 커밋에서도 통과하고, 이번 변경은 `/join` 경로를 건드리지 않는다.
- 남은 위험: 작품 비율이 만든 기기에 따라 달라진다(태블릿 가로에서 만들면 넓고, 휴대전화
  세로에서 만들면 좁다). 다른 기기에서 열면 자기 비율을 지키며 가운데 놓인다.
  이미 저장된 작품은 정사각 그대로이며 마이그레이션하지 않는다.

## 2026-09-07 교사 학급 명단과 번호 입장 (`claude/teacher-roster-20260907`, 로컬 검증)

- 사용자 결정으로 **실명 금지 원칙을 바꿨다.** 교사가 학급을 만들 때 명단(번호 + 실명)을 직접
  입력하고, 만든 뒤에도 학생을 추가·제거·수정한다. 학생은 수업 코드나 QR로 들어와 **자기 번호**를
  입력하고 그림 비밀번호로 본인을 확인한다. `pending-decisions.md`의 P-001을 확정으로 옮겼다.
- **실명 경계**: 실명은 담임 교사 화면에만 나타난다. 학생 응답(`entryStatus`·`seatStatus`·`join`·
  `recover`)·가족 공유·그림책·AI 요청 어디에도 싣지 않는다. 명단 목록 자체를 서버가 주지 않으므로
  학생 화면에 반 전체 이름을 그릴 방법이 없다 — 수업 코드는 인쇄물·칠판에 적히는 값이라
  목록을 내보내면 그 코드를 아는 사람에게 반 전체 실명이 노출된다.
- 바뀐 약속 문구: 대문의 `학생 이메일 · 실명 없이 안전하게` → `학생 이메일 없이 안전하게`,
  교사 로그인 화면의 `학생 실명 없이 익명 프로필` → `우리 반 명단은 담임만 볼 수 있어요`,
  README 3곳. 실명을 받으면서 이 문구들을 그대로 두면 거짓말이 된다.
- 데이터: `student_profiles`에 `seat_number`·`real_name`·`claimed_at`을 더했다.
  `claimed_at`이 비어 있으면 아직 아무도 안 쓴 자리다. 기존 학생 행은 번호·실명이 없고
  `claimed_at`을 `created_at`으로 채워, 예전처럼 동물·별명·그림 비밀번호로 동작한다.
  같은 학급 안 번호는 유일하며(부분 유니크 인덱스), 뺀 학생의 번호는 다시 쓸 수 있다.
- **자리 차지는 원자적이다.** 자리 차지(UPDATE)·그림 비밀번호(INSERT)·세션(INSERT)을 한 배치로
  묶었다. 나눠 두면 중간 실패에서 자리는 차지됐는데 비밀번호가 없어 아이가 영영 못 들어온다.
  `tests/student-join-atomic.test.mjs`가 이 규칙을 이미 지키고 있었고, 처음 구현에서 이를
  어겨 실제로 잡혔다. 뒤 두 문장은 방금 쓴 `claimed_at` 값에 묶여 동시 입장도 막는다.
- 번호 재입장은 `recover:<학급>:seat:<번호>` 버킷으로 제한하고, 없는 번호에서도 해시를 계산해
  응답 시간으로 명단을 캐낼 수 없게 했다.
- 명단 입력은 한 줄에 한 명씩 `번호 이름`으로 받는다(공백·점·쉼표·탭 모두 허용).
  칸 15개로 나누면 입력이 느리고 기존 명부를 붙여 넣기도 어렵다. 파싱은 `lib/roster.ts`에 두고
  서버가 같은 규칙을 다시 검증한다 — 화면 검사만 믿지 않는다.
- 검증: typecheck·lint·build·`git diff --check` 통과. 전체 테스트 **316개 중 315개 통과**
  (남은 1건은 이 맥의 Node v22.13.0에 `node:module`의 `registerHooks`가 없어서 나는 환경 문제).
  신규 `tests/teacher-roster.test.mjs` 10개가 실명 경계와 원자성 계약을 지킨다.
- 실제 API·브라우저 검증: 명단 흐름 20항목(자리 차지·중복 거절·번호 재사용·잘못된 값 거절),
  번호 입장 화면 10항목(번호 화면 진입·처음/재입장 분기·실명 미노출·없는 번호 안내).
  `check:browser` 기본 3뷰포트와 `--ipad` 3뷰포트 전 항목 통과.
- 남은 위험: 학부모 동의·보관 기간·삭제 요청 처리 절차는 아직 제품에 없다. 지금은
  "담임만 열람 / 학급 삭제 시 함께 삭제 / AI 미전송"을 기본값으로 잡았을 뿐이다.
  명단이 없는 기존 학급은 예전 흐름(동물·별명·그림 비밀번호로 스스로 만들기)을 그대로 쓴다.

## 2026-09-12 ⚠️ 운영 사이트가 이 저장소를 빌드하고 있지 않음 (실측으로 확인)

표정 작업을 `main`에 올린 뒤 운영 스모크를 돌리다 발견했다. **2026-09-07 이후 `wwwiggle/new_wiggle`의
`main`에 올린 것은 한 번도 운영에 반영된 적이 없다.**

- 실측 1: 새로 올린 `/brand/mongri/*.png`가 10분을 기다려도 404다. 같은 자리의 옛 파일
  (`/brand/app_icon.png`, `/brand/grimi-mascot.png`)은 200이다 — 서버는 살아 있고 **빌드가 옛것**이다.
- 실측 2: 운영 랜딩의 아이콘 링크가 `<link rel="icon" href="/brand/app_icon.png">`다.
  `wwwiggle/main`의 `app/layout.tsx`는 `crayon-icon-32/180/512`를 가리킨다.
- 실측 3: GitHub에 `wwwiggle/new_wiggle` 배포 기록이 **0건**이고, 최근 커밋 다섯 개 모두 상태 검사가 없다.
  반면 옛 저장소 `yonghwan86/wiggle_web`에는 Production 배포 기록이 남아 있고 **마지막이 2026-08-23(`5d7e8b0`)**이다.
- 결론: Vercel 프로젝트 `wiggle-web`이 아직 **옛 저장소**에 연결돼 있다. 그래서 커리큘럼 은퇴,
  참여 코드 네 자리, 명단 인쇄·엑셀 가져오기, 크레용 아이콘, 자동 몽그리, 표정까지 **아홉 커밋이 운영에 없다**.
- 이 정정 전까지 `CLAUDE.md`와 이 문서는 "`main` push가 곧 운영 배포"라고 적고 있었다. 사실이 아니었다.
  Claude는 Vercel 대시보드에 접근할 수 없으므로 **연결 변경은 사용자만 할 수 있다**. `docs/pending-decisions.md` P-009.
- 코드 쪽에 할 일은 없다. `main`은 올바르고, 검증도 통과했다. 막힌 곳은 배포 연결 하나다.

## 2026-09-12 몽그리 표정 여섯 가지를 화면 상태에 연결 (`claude/blank-canvas-20260912`)

- 사용자가 준 표정 시트(1536×1024)를 여섯 장으로 잘라 `public/brand/mongri/`에 두었다
  (`curious` `thinking` `suggesting` `delighted` `listening` `reassuring`, 각 224px 정사각 RGBA).
  크림색 배경과 몸통 색을 flood fill로 걷어내 투명 배경으로 만들었고, 여섯 장 모두
  출처·SHA-256·용도와 함께 `public/brand/asset-manifest.json`에 등록했다.
- **얼굴은 화면 상태로만 고른다**(`DrawingStudio`의 `grimiFace`). AI가 쓴 문장에서 감정을
  읽어 고르지 않는다 — 그 통로를 열면 모델이 "대단"처럼 금지된 평가어를 쓰도록 유도된다.
  - 오류 → `reassuring`, 기다리는 중 → `thinking`, 접힌 채 제안이 남음 → `suggesting`,
    아이가 답을 고른 뒤 → `listening`, 질문이 떠 있음 → `curious`, 그 밖 → `listening`.
  - 소감 화면의 몽그리 짐작 머리글은 짐작이 나오기 전 `thinking`, 나온 뒤 `delighted`.
- **실측에서 잡은 회귀**: 96px 얼굴이 접힌 패널을 키워 가로로 눕힌 폰(844×390)에서 그릴 칸이
  140px 밑으로 떨어졌다(실측 103px). `@media (max-height:480px)`로 44px까지 줄여 되돌렸다.
  `tests/mongri-face.test.mjs`가 이 규칙을 지킨다.
- 검증: typecheck · lint(오류 0) · `npm test` 313/313 · `git diff --check` ·
  브라우저 실측 3종(`localhost:3299`, 기본 · `--desktop` · `--ipad`) 모두 통과.

## 2026-09-12 몽그리가 먼저 말을 건다 · 프롬프트 정정 · 대상 3~6학년 (`claude/blank-canvas-20260912`)

- **자동 개입**(사용자 결정). 종전 원칙 「아이가 호출했을 때만 개입한다」를 바꿨다. 아이가
  「몽그리 부르기」를 스스로 찾는 일이 드물어, 부를 때만 여는 구조에서는 이 역할이 없는 것과 같았다.
  조건은 `DrawingStudio`의 `AUTO_GRIMI`가 정본이고 `tests/auto-grimi.test.mjs`가 지킨다.
  - 뜨는 때: 여덟 획 이상 그렸고, 마지막 획에서 75초가 지났고, 펜이 닿아 있지 않을 때.
  - 뜨지 않는 때: 연 지 2분 안, 도화지가 거의 빔, 직접 부른 뒤 30초 안, 완성·소감 창, 저장 충돌, 선생님 보기 중.
  - 빈도: 그림 한 장에 자동 두 번까지, 자동끼리 5분 간격. 직접 부르는 것은 제한 없음.
  - 형태: 화면을 덮는 창이 아니라 옆 패널. 다시 그리기 시작하면 스스로 접힌다. 카드에 「내가 먼저 말 걸었어」 표시.
  - 자동과 수동은 같은 `askGrimi` → 같은 API를 지난다. 안전 필터를 우회하는 두 번째 길을 만들지 않았다.
- **대상 연령 정정**: 초등 **3~6학년**(사용자 정정, 종전 1~5학년·3학년 이상 표기를 모두 바꿈).
  두 학생용 프롬프트와 교사 초안 프롬프트, `CLAUDE.md`, 제품 결정 8항을 함께 맞췄다.
- **프롬프트 검토에서 나온 정정 넷** (역할 정의·스키마·검증기를 나란히 놓고 본 결과):
  1. 확장 협업자의 "앞서 끌고 가지 않는다"를 뺐다. 스키마가 `next_action`을 **필수**로 두고
     검증기가 `그려|더해|추가…` 패턴까지 강제하므로, 매번 지시문 한 줄을 내놓게 되어 있다.
     말과 코드가 충돌하면 코드가 이긴다 — 문장을 사실에 맞췄다.
  2. `uncertain=true`가 장식이었다. 확신이 없어도 `next_action`은 단정형으로 나갔다. 한 줄 추가.
  3. 자동 개입을 넣고도 모델은 누가 열었는지 몰랐다. 서버가 `openedBy`를 맥락에 실어 보낸다.
  4. 틀리는 해석자가 "틀려도 된다"고 **허락만** 했다. 시각 모델은 대개 맞히므로 역할이 성립하지
     않는다. "일부러 덜 구체적으로 짐작한다"는 **규칙**으로 바꾸고, 선택지 하나만 동의가 되도록 적었다.
     `answer`가 최상위가 아니라 각 choice의 필드임도 명시했다(스키마는 `guess`·`choices`만 받는다).
- 남은 것: 선택지가 전부 동의로 채워지는 것을 **검증기에서** 막는 규칙. 지금은 프롬프트로만 막았다.
- 검증: typecheck · lint(오류 0) · `npm test` 308/308 · `git diff --check`.

## 2026-09-12 탭 아이콘과 브랜드 마크를 크레용 그림으로 (`claude/blank-canvas-20260912`)

- 사용자가 준 크레용 그림(초록 바탕·노랑 크레용·흰 낙서, 1254×1254)을 파비콘으로 삼았다.
  `public/brand/crayon-icon-{32,180,512}.png` 세 크기이고, `app/layout.tsx`의 `icons`가 가리킨다.
- 32px은 **따로 더 바싹 잘라** 뽑았다. 원본 여백 그대로 줄이면 탭에서 크레용이 뭉개진다.
  180px은 iOS 홈 화면용(apple-touch-icon), 512px은 설치 아이콘·큰 화면용이다.
- 그림 자체는 바꾸지 않았다 — 자르기와 크기 줄이기, 팔레트 압축만 했다(512px 152KB).
- `public/brand/app_icon.png`(제품 앱 아이콘, wiggle_draw 참조 자산)은 **건드리지 않았다.**
  다만 그 자산의 `approvedFor`에서 favicon 역할을 빼고 무엇이 대신하는지 적었다.
- `asset-manifest.json`에 세 파일을 출처·해시와 함께 등록했다(정책상 등록된 자산만 배포된다).
- 화면 안 **브랜드 마크도 같은 크레용**으로 바꿨다(`Logo` 컴포넌트, `crayon-mark-128.png`).
  대문·교사·학생·그리기 화면의 머리에 모두 이 컴포넌트가 쓰이므로 한 자리만 고치면 전부 바뀐다.
  마크는 42px 안팎으로 그려져 128px이면 고해상도에서도 충분하다(9KB).
  옛 `/brand/logo.png`는 지우지 않고 이력으로 남기되 더는 참조하지 않는다.
- 검증: 빌드한 서버의 `<head>`에 네 개의 아이콘 링크가 나오고 세 파일 모두 200 image/png로 응답.
  대문·교사 화면을 실제로 찍어 마크가 크레용으로 그려지는 것을 확인했다(45px, 이미지 로드 성공).

## 2026-09-12 명단을 엑셀·CSV 파일로 추가 (`claude/blank-canvas-20260912`)

- 사용자 요청: 명단을 엑셀이나 CSV로도 넣을 수 있게. 「학생 추가」 대화상자에 **엑셀·CSV 파일
  불러오기**를 넣었다. 파일을 고르면 입력칸이 채워지고, 교사가 확인한 뒤 기존 `addStudents`로 저장한다.
- **새 의존성을 두지 않았다.** `.xlsx`는 zip + XML이고 브라우저·Node 모두 `DecompressionStream
  ("deflate-raw")`를 갖고 있어 `lib/roster-file.ts`가 직접 푼다(중앙 디렉터리 → sharedStrings +
  첫 시트). CSV·TSV는 BOM·따옴표·구분자 추정을 포함한 최소 파서다.
- 읽기 규칙: 제목 줄(번호/이름/No/Name…)은 건너뛰고, 첫 칸이 모두 숫자면 그 번호를 쓰고,
  아니면 1번부터 차례로 붙인다. 무엇을 했는지 화면에 문장으로 알려 준 뒤 교사가 고칠 수 있다.
- **파일은 서버로 보내지 않는다.** 실명이 든 파일이라 교사 브라우저 안에서만 읽는다.
  검증 경로도 하나를 유지한다 — 파일은 입력칸 글자로 바뀌고, 번호·이름 규칙은 `parseRosterText`가 본다.
- 검증: `tests/roster-file.test.mjs` 9건. 시험 자료는 **실제 엑셀 작성기 두 가지**(openpyxl,
  xlsxwriter)로 만든 진짜 `.xlsx`와 엑셀식 CSV(BOM·따옴표·쉼표 든 이름)다(`tests/fixtures/`).
  실브라우저로도 확인했다 — CDP `DOM.setFileInputFiles`로 `.xlsx`를 골라 입력칸이 채워지고
  저장까지 가서 4명이 네 자리 코드와 함께 명단에 들어갔다.
- 옛 `.xls`(이진 형식)는 읽지 않고 "xlsx나 CSV로 저장해 주세요"라고 안내한다.
- **엑셀 양식 내려받기**(2026-09-12 사용자 요청): 같은 대화상자에서 `위글-명단-양식.xlsx`를 받는다.
  `lib/xlsx-write.ts`가 압축하지 않는(stored) zip으로 xlsx를 만든다 — CRC32만 있으면 되고
  엑셀·넘버스·구글 시트가 그대로 연다. 서버에 정적 파일을 두지 않고 **읽는 코드와 같은 자리에서**
  만들어, 양식과 읽기 규칙이 따로 노는 일을 막는다. 시험이 양식→읽기→명단 검증을 한 바퀴 돌린다.
  실제 엑셀 라이브러리(openpyxl)로 열어 시트 이름과 내용을 확인했고, 브라우저가 내려받은 파일도
  같은 방법으로 열어 봤다.

## 2026-09-12 참여 코드표를 실제로 인쇄되게 함 (`claude/blank-canvas-20260912`)

- 결함: 「코드표 인쇄」가 한 번도 동작하지 않았다. `window.open(..., "noopener")`는 규격상 **null을
  돌려주므로**, 팝업을 허용해 둔 브라우저에서도 매번 "팝업이 막혀…" 안내만 나왔다.
- 고침: 팝업을 버리고 시트를 화면 안에 그린 뒤 브라우저 인쇄를 그대로 쓴다.
  `TeacherRosterPrint.tsx`가 시트를 그리고, 명단·설정 탭의 버튼이 대화상자로 미리보기를 연다.
- 인쇄물은 두 장이다. **1장 선생님 보관용 명단**(번호·이름·참여 코드 표, 자르라는 안내 포함),
  **2장 잘라 나눠 줄 쪽지**(아이마다 반 이름·번호·이름·학급 QR·수업 코드·내 참여 코드).
  쪽지 한 장이면 입장이 끝나도록 QR과 두 코드를 모두 넣었다.
- 인쇄 CSS: 대화상자가 `main > section.trs > backdrop > dialog` 깊이에 있어 body 직계만 숨기는
  방식으로는 페이지 전체가 함께 인쇄됐다(실측). 그래서 **시트의 조상도, 시트도, 시트 안도 아닌 것**을
  모두 숨긴다(`body:has(.roster-print) *:not(:has(.roster-print))…`). 대화상자의 `overflow:auto`와
  `max-height`도 풀어야 둘째 장이 잘리지 않는다. 두 시트 사이는 `break-before:page`.
- 검증: CDP로 `Emulation.setEmulatedMedia({media:"print"})` 전환 뒤 실측 — 시트만 보이고(top 0,
  폭 100%) 페이지 헤더·탭·명단표·대화상자 머리·인쇄 버튼은 전부 숨겨짐. `Page.printToPDF`로 A4를
  뽑아 **2쪽**(1장 명단, 2장 쪽지 7개)과 QR 7개 렌더링을 확인했다.

## 2026-09-12 커리큘럼 은퇴 · 빈 도화지 수업 · 참여 코드 네 자리 (`claude/blank-canvas-20260912`)

- 사용자 결정: **기획이 바뀌어 커리큘럼이 없다.** 수업은 빈 도화지에서 선생님이 진행한다.
  아이는 참여 코드를 누르면 곧바로 도화지를 연다(그리다 만 그림이 있으면 그것을 이어 연다).
  살린 것은 그림책 만들기·내 그림 보관함·교사의 오늘 수업 그림 보기·몽그리 부르기 넷이다.
- 같은 결정으로 **참여 코드를 여섯 자리에서 네 자리로** 줄이고, 반은 **QR을 기본**으로 안내한다.
  QR을 못 쓰면 대문에서 수업 코드 4자리를 넣는 길은 그대로다.
- 삭제한 것: `lib/arc-content.ts`, `lib/arc-session.ts`, `app/components/StudentHome.tsx`,
  아크 테스트 3종, 작품 생성의 씨앗 심기, 교사 `setEpisode`/`listArcs`와 조종석·수업 안내,
  작품 보관함의 회차 필터, 몽그리의 회차 문맥, 그리기 화면의 지난 회차 서랍.
- 새로 만든 것: `app/components/StudentEntry.tsx`(도화지로 보내는 중간 다리). 학생 홈이 없어져
  **내 그림 화면이 도화지 밖의 유일한 자리**다 — 헤더에 새 그림·그림책·수업 마치기를 두었다.
  그리기 화면의 나가기(←)는 내 그림으로 간다.
- 교사 오늘 수업은 회차 범위가 사라져 **언제나 학생별 최근 그림**이다(`monitorScope` 제거).
- 스키마: `randomEntryCode`가 네 자리를 뽑고, `provisionSchema`가 **네 자리가 아닌 옛 코드를
  다시 발급**한다. 기존 반의 여섯 자리 코드는 첫 요청에서 네 자리로 바뀌므로, 교사에게 코드표를
  다시 뽑으라고 알려야 한다. `artworks.arc_id`·`episode_id` 컬럼은 그때 그린 그림의 이력이라 남긴다.
- 죽은 CSS(학생 홈·회차 카드·선반·조종석·회차 서랍) 정리는 **되돌렸다.** 지웠더니 새로 띄운 서버의
  첫 회차에서 그리기 도구 막대가 12열 독 레이아웃으로 잡혀 44px 목표를 깨뜨렸다(320×568에서 33px).
  기준 커밋과 대조했더니 도구 규칙 자체는 그대로였고, 원인은 삭제로 `@media` 블록 배치가 달라지면서
  빌드 단계에서 규칙이 다른 미디어 블록 아래로 들어가는 것이었다(빌드 산출 CSS에서 확인). 죽은 규칙은
  눈에 보이는 해가 없고 도구 막대가 깨지는 것은 실제 결함이라, 규칙을 남기고 필요한 것만 더했다.
  정리는 원인을 제대로 잡은 뒤 따로 한다.
- 검증: typecheck 0 · lint 0 errors(13 기존 경고) · `npm test` 288/288 · `git diff --check` ·
  browser-check 기본 245 / `--desktop` 92 / `--ipad` 245 전부 통과(자체 빌드 :3299).
  ⚠️ 찬 서버에서 처음 돌린 회차에는 그리기 도구 막대가 좁게 잡혀 11건이 실패했다가, 같은 빌드에서
  다시 돌리니 전부 통과했다. 기준 커밋(fe9d00a)과 computed style을 직접 비교해 도구 규칙이 그대로임을
  확인했다(`tool-*` 156개 규칙 동일, 버튼 71.25px 동일). 회귀가 아니라 첫 요청 지연에서 오는 실측
  흔들림으로 본다 — browser-check 간헐 멈춤과 같은 계열이다. 실패가 보이면 서버를 데운 뒤 다시 돌린다.
- 남은 위험: 네 자리는 만 개라 여섯 자리보다 추측이 쉽다. 학급+IP 60회/10분 한도가 유일한 방벽이다(`pending-decisions.md` P-008).
- 남은 일: `docs/wiggle/`와 사용자 문서 폴더의 화면 캡처 21장은 이 변경 **이전** 화면(학생 홈·여섯 자리 코드)이라 더 이상 맞지 않는다. 다시 찍어야 한다.

## 2026-09-11 화면 캡처 모음 (`docs/wiggle/`)

- 사용자 요청으로 현재 main(`4794197`)의 화면을 실제 빌드에서 찍어 `docs/wiggle/`에 정리했다.
  학생 8종(대문·참여 코드·코드 오류·동물 고르기·홈·그리기·내 그림·그림책), 교사 6종(학급 목록·오늘
  수업·작품·명단과 코드표·회차 변경·입장 QR), 개인정보 1종. 데스크톱 1440×900과 휴대전화 390×844,
  2배로 찍어 1배 webp로 저장(21장 785KB).
- `docs/wiggle/capture.mjs`가 그 캡처 스크립트다. 교사 로그인 → 명단 학급 → 회차 열기 → 학생 입장 →
  캔버스에 몇 획(씨앗 동그라미 위 얼굴)까지 시드하므로, 빈 화면이 아니라 수업 중 상태가 찍힌다.
- 캡처 자료는 전부 지어낸 것이다(학급 `햇살 2반`, 이름 6개, 수업 코드 3270). 실제 학생 정보가 아니다.

## 2026-09-09 아이 참여 코드 입장 (`claude/entry-code-20260909`, 로컬 검증)

- 사용자 결정: 번호 + 그림 비밀번호 입장이 1~2학년에게 너무 어려워, **명단을 만들 때 자리마다 무작위 참여 코드 6자리**를 붙이고 아이는 수업 코드(또는 QR) 다음에 그 코드 하나만 누르면 들어온다. 수업 코드·QR은 유지. 처음 들어올 때 동물 하나만 고르고 별명은 자동(`NICKNAME_IDEAS[animal][0]`).
- 스키마(`db/runtime.ts`): `student_profiles.entry_code TEXT` + 부분 유니크 인덱스 `(classroom_id, entry_code)`. `provisionSchema`가 코드 없는 활성 학생(코드 도입 전 명단, 옛 자기 등록 프로필)에 코드를 채운다(`backfillEntryCodes`).
- 학생 API(`app/api/student/route.ts`): `join { entry, entryCode, animal? }` 하나만 남았다. 학급 + 코드로 자리를 찾고, 미차지면 `animal` 없이는 `{ firstTime: true }`(세션 없음), `animal`이 오면 자리 차지 + 세션을 한 배치로(같은 코드 동시 입장은 먼저 성공한 쪽이 차지하고 늦은 쪽은 재입장). 차지된 자리는 세션만 새로 발급. 틀린 코드는 404 `ENTRY_CODE`. `seatStatus`·`switchProfile`·`recover`(개인 QR 복구 포함)와 `lib/picture-password.ts`, `/join/recover`는 제거. 무차별 대입은 기존 학급 + IP 버킷(60회/10분)이 막는다(대상 버킷 없음).
- 교사 API(`app/api/teacher/route.ts`): `createClassroom`·`addStudents`가 학급 안에서 겹치지 않는 코드를 붙여 넣고, `createClassroom` 응답에 `entryCodes[{seatNumber, entryCode}]`. 학급 GET 학생 행에 `entryCode`. `rotateEntryCode { studentId }` 추가, `resetStudentRecovery`와 `resetActiveStudentRecovery` 제거.
- 화면: `JoinClient.tsx`는 `checking | code | animal | noRoster` 네 모드. 코드·동물 화면은 번호 수첩(`EntryCheck.module.css` `.pad`)을 그대로 쓴다(`.code-card`, `.animal-card`). 교사 명단·설정 탭(`TeacherRosterSettings.tsx`)에 참여 코드 열(복사·새로 뽑기)과 `코드표 인쇄`(새 창에 번호·이름·코드 표, 실명이 있으니 잘라서 나눠 주라는 안내). `globals.css`에서 그림 비밀번호·별명 카드·1488 무대 등 죽은 규칙 약 770줄 제거.
- 검증 스크립트: `scripts/browser-check.mjs`는 코드 화면(숫자판 10키·44px·틀린 코드 → 선생님 불러요·동물 화면)을 실측한다. `check-deployed.mjs`는 인자가 `<주소> <수업코드> <참여코드>`로 바뀌었고, `check-large-save.mjs`는 명단과 함께 학급을 만들어 첫 학생 코드로 들어간다.
- 테스트: `tests/entry-code.test.mjs` 신설(첫 입장·재입장·틀린 코드·삭제 학생·교사 코드 발급/새로 뽑기·스키마 백필·소스 계약). `picture-password`·`join-nickname-default` 테스트 삭제, 12개 테스트 파일을 코드 흐름으로 고침.
- 수첩 레이아웃 결함 2건 수정(번호 수첩 때부터 있던 것, 이번 실측에서 발견): 좁은 화면에서 `.pad`가 `position: relative`로 바뀌어도 넓은 화면의 `left/top/bottom`이 상대 오프셋으로 살아 카드가 47% 오른쪽으로 밀려 잘리던 것(`inset: auto`), 넓은 화면에서 고정 높이 키가 종이 아래로 넘쳐 「수업 코드 다시 입력하기」가 들어가기 위에 겹치던 것(키 행이 남는 높이를 나눠 갖게 `flex: 1` + `grid-auto-rows: minmax(44px, 1fr)`). 좁은 화면 무대는 `width: 100%`로 못 박았다(동물 화면에서 내용 폭 221px로 줄던 문제).
- 검증(2026-09-09, 자체 빌드 :3299): typecheck 0 · lint 0 errors(12 기존 경고) · `git diff --check` · `npm test` 317/317 · browser-check 기본(320×568·390×844·844×390)·`--desktop`·`--ipad` 전부 통과 · CDP 스크린샷으로 코드 수첩·틀린 코드·동물 고르기·학생 홈·교사 명단 탭(코드 열·복사·새로 뽑기·코드표 인쇄)을 390×844/1280×800/844×390/320×568에서 눈으로 확인, 터치 목표 44px 미달 0건·가로 스크롤 0.
- 남은 위험: 아직 미병합 브랜치 `claude/prep-sketchbook-20260909`(준비 중 화면)는 `JoinClient.tsx`의 `noRoster` 분기를 고치므로 이 브랜치 뒤에 다시 리베이스해야 한다. 코드는 평문 저장이라 DB가 새면 코드도 샌다(수업 코드와 같은 등급, 교사가 새로 뽑으면 무효). 옛 학생 행의 백필 코드는 교사가 명단 탭에서 확인하기 전까지 아무도 모른다.

## 2026-09-09 초록 팔레트 전환·수업 확인 화면 교실 시안 (`claude/green-entry-20260909`, 로컬 검증)

- 사용자 결정(2026-09-07 "파란 화면을 초록으로")을 최신 main(명단 입장 이후) 위에 다시 적용했다. 이전 시도(`wpdbs1229/storybook-design-sync-green`, 8월 23일 main 기준)는 `새로 시작하기 / 내 그림 이어가기` 갈림길에 붙어 있었는데 그 화면 자체가 명단 입장으로 사라졌으므로 리베이스하지 않고 폐기 대상으로 둔다(브랜치는 남아 있음).
- 팔레트: `app/globals.css`의 `:root`를 시안 색(주 `#4f9a58`, 진한 `#2f6b3d`, 배경 크림 `#f4f3ea`, 잉크 `#2b4a33`)으로 바꾸고, 하드코딩된 파란 계열 hex·rgba 346곳과 `public/landing-gallery/gallery-assets.css`의 토큰 9곳을 색상(hue) 회전 스크립트로 일괄 초록화했다(채도 62%, 명도 유지). 그리기 팔레트의 물감 색(`DrawingStudio.tsx`)과 브랜드 로고·앱 아이콘·오리 마스코트(파란 모자·스카프)는 건드리지 않았다.
- 그림 안에 파란색이 있던 래스터 7종은 파란 화소만 같은 방식으로 초록으로 돌려 제자리 교체했다: 대문 `paper-blue`(png+1024 webp)·`frame-blue`(png+480/960 webp), 입장 `student-entry-scene-v3`·`student-entry-arch`, 학생 홈 `student-home-book-v2/-left/-right`. `public/landing-gallery/manifest.json`의 해당 5개 파일 sha256·bytes를 갱신했다(원본 `source` 해시는 그대로).
- 수업 확인 화면(`/join` 대기·실패)을 `docs/design-assets/entry-green/`의 교실 시안(1672×941)으로 구현했다(`app/components/EntryCheck.module.css`). 넓은 화면은 `public/entry-green/classroom-with-mongri.webp`(오리가 팔레트를 잡은 완성 장면, 219KB)를 비율 그대로 한 무대로 두고 로고·듣기·`우리 반` 간판·제목·상태 쪽지·버튼을 HTML로 올린다. 760px 이하·높이 520px 이하·세로 비율(5:4 이하)은 크림 배경 + 독립 오리(`/landing-gallery/duck-painter-640.webp` 재사용) + 문서 흐름으로 바뀐다. 코드 오류(404)는 `수업 코드 다시 입력하기`(노란 주 버튼)·`선생님 불러요`(손 들기 안내 토글)·`다시 확인하기`(밑줄) 순서, 연결 오류는 `다시 확인하기`가 주 버튼이고 코드 재입력은 보조다. 인증·API·저장 로직은 무수정.
- 시안 문구와 다른 곳 하나: 도움 버튼은 시안의 `선생님, 도와주세요` 대신 기존 확정 문구 `🙋 선생님 불러요`를 유지했다(`tests/entry-error-recovery`·browser-check가 이 문구를 계약으로 잡고 있고, 음성 안내 문장을 바꾸지 않는 결정과도 맞춘다).
- 검증: typecheck·lint(경고 10건은 기존 것)·`git diff --check` 통과, 전체 테스트 **318/318**(Node 22.23; 이 맥의 기본 22.13은 `.ts` 로더 오류로 실행 불가). 팔레트가 바뀌어 hex를 고정하던 테스트 2건(`pre-reader-ux` 단계 구분선 `#d0e2d3`, `drawing-tools` 지우개 발자국 `#264c2e`)만 갱신. `npm run check:browser` 기본 3뷰포트 전 항목 통과(browser-check의 듣기 버튼 셀렉터에 `.entry-check .speak-button`을 추가). 실제 Chrome(CDP) 실측으로 수업 확인 실패 화면을 `1440×900`, `1280×800`, `1024×768`, `768×1024`, `1024×1366`, `844×390`, `390×844`, `320×568`에서 확인: 가로 넘침 0, 44px 미만 터치 목표 0, 본문 16px 이상(12px은 기존 듣기 버튼 캡션). 대문·교사·번호 입장 화면도 초록으로 렌더링됨을 캡처로 확인.
- 남은 것: `public/og.png`(README 히어로)는 옛 파란 대문 캡처 그대로다. `docs/design-assets/entry-green/assets/`의 원본 PNG와 zip은 사용자 소유 자료로 커밋하지 않았다(README·PROVENANCE·manifest·tokens·reference만 커밋).

## 2026-09-09 씨앗 선 커리큘럼 계획 (`claude/curriculum-seed-plan-20260909`, 문서만)

- 사용자와 대화로 커리큘럼 뼈대를 확정하고 `docs/curriculum-seed-plan.md`를 새로 썼다. 12회 표에 씨앗 좌표(정규화 폴리라인), 될 수 있는 것 3가지, 안내 문장 4개, 토론 질문 3개, 40분 배분, 앱에 필요한 것 5가지를 채웠다. 기존 30개 레슨은 기준에서 제외했다.
- `product-decisions.md`에 학습 과정 결정 2건과 "같은 학급·교사가 연 동안·이름 없이" 반 작품 보기 예외를 기록했고, 남은 확인 항목은 `pending-decisions.md` P-007에 두었다.
- 코드는 바꾸지 않았다. 구현 순서 제안: ① `claude/seed-line-lesson` 브랜치의 `Lesson.seed`·작품 생성 시 획 심기를 최신 main으로 이식 ② 12회 데이터 ③ 반 작품 크게 보기 화면.

## 2026-09-09 씨앗 이야기 아크 4개 구현 (`claude/curriculum-seed-plan-20260909`, 로컬 검증)

- 대상 학년을 초등 1~5학년으로 확정했다(사용자, P-006 종결).
- 사용자 설명으로 커리큘럼 구조를 바로잡았다: 씨앗 선은 **첫 회차 한 번**이고 2회차부터는 아이 자신의 지난 그림이 씨앗이며, 5회차가 모여 동화책이 된다(기존 아크 모델 그대로). 같은 날 먼저 넣었던 12회 독립 씨앗 아크(`seed-lines`)는 main에 가기 전에 교체했다.
- `lib/arc-content.ts`: `seedStoryArcs()`가 씨앗 이야기 아크 4개(`circle-story`·`curve-story`·`zigzag-story`·`cross-story`, 각 5회차, version 1)를 만든다. 회차 형상에 선택 항목 `seed`(1회차만)·`prompts`(힌트 안내 3개)·`discussion`(교실 토론 질문 3개)을 더했고, 체크섬 재료에는 있을 때만 넣어 자전거 아크의 체크섬은 그대로다. 원은 기본 도화지(1024×640)에서 둥글게 보이도록 세로 반지름을 1.6배로 잡는다.
- `app/api/artworks/route.ts`: 회차 작품을 처음 만들 때 씨앗이 있으면 `pencil` 8px 획으로 문서에 심는다(`seededDocument`). 점선 힌트가 아니라 보통의 획이라 아이가 지울 수 있고 저장 이미지에 남는다. 2회차부터는 씨앗이 없어 빈 도화지로 열리고 지난 회차 그림은 기존 그리기 화면의 지난 이야기 참조로 본다.
- 학생 홈: 오늘 회차 카드가 힌트 안내를 번호 목록으로 보여 주고 음성이 장면 문장과 함께 읽는다. 교사 조종석: 현재 회차의 `교실 토론 질문 · 40분 배분` 접이식. 학생 응답에는 토론 질문을 싣지 않는다(테스트 고정).
- 검증: `tests/arc-content.test.mjs`에 씨앗 이야기 불변식(아크 3~4개, 각 5회차, 1회차만 씨앗, 씨앗이 문서 검증 통과, 힌트 2개 이상·토론 3개, 문장 40자 이내) 추가. typecheck·lint(경고는 기존 것)·`git diff --check` 통과, 전체 테스트 결과는 아래 기록. `npm run build && npx next start -p 3299`에 `check:browser` 전 항목 통과(12회 버전에서 실행, 이후 변경은 콘텐츠 상수·테스트·문서뿐). 실제 Chrome(CDP) 실측: 로컬 교사로 학급을 만들고 `circle-story` 1회차를 연 뒤 1번 학생으로 입장해 ① 홈 카드에 제목·장면 문장·힌트 3개 ② `그리기 시작` → 작품 문서에 25점 원 획 1개, 도화지 가운데 둥근 원 ③ 교사 학급 화면에 토론 질문 3개를 확인했고, 2회차(`circle-story-who`)는 씨앗 없이 빈 도화지로 열리는 것을 확인했다.
- 남은 것: 반 작품 크게 보기 화면(토론용), 장면 삽화(`sceneImage` 전부 null). `claude/seed-line-lesson` 브랜치는 이 구현으로 대체되어 폐기 대상이다.

## 2026-09-09 교사 학급 목록(/teacher) 디자인 개편 (`claude/teacher-dashboard-20260909`, 로컬 검증)

- 사용자가 제시한 시안대로 `/teacher` 대시보드를 재구성했다: 흰 배경 상단바(교사 이름 | 로그아웃), `내 학급` 제목 + `＋ 새 학급` 버튼(누르면 인라인 생성 폼), `최근 사용한 학급` 카드 3개(updatedAt 내림차순), `모든 학급` 표(학급명·수업 코드 복사·QR 보기, 오늘 활동, 등록 학생, 입장 상태, 최근 수업, 학급 열기, `⋯` 메뉴 안 학급 삭제) + 이름 검색 + 전체/입장 열림/입장 닫힘 필터. 초록은 DESIGN.md 공통 팔레트(`--primary`/`--primary-dark`)를 `.teacher-dashboard` 범위 변수로 받아 쓴다. 새 학급 폼은 main의 명단 필수 규칙(`RosterField`)을 그대로 품는다.
- `학급 열기`는 입장 열림 학급만 초록 채움 버튼, 입장 닫힘 학급은 텍스트 링크다(사용자 결정 2026-09-09: 교사가 지금 열 학급이 먼저 보이게). 카드·표 모두 같은 기준.
- 시안의 `보관됨` 탭·정렬 드롭다운·페이지네이션은 현재 데이터 모델(보관 개념 없음, 학급 수 적음)에 맞지 않아 넣지 않았다. 필요해지면 `docs/pending-decisions.md`에 올린다.
- QR 보기는 수업실의 큰 QR 대화상자(`qr-modal-backdrop`)를 재사용한다. 이 대화상자는 Tailwind preflight의 `margin:0` 때문에 왼쪽에 붙어 있었으므로 `margin:auto`를 추가했다(수업실 화면도 함께 중앙 정렬됨).
- 변경 파일: `app/components/TeacherApp.tsx`(대시보드 분기·`ClassroomCard`·`ClassroomRow`·`lessonDate`), `app/globals.css`(구 `teacher-welcome`/`class-card` 규칙 삭제, 대시보드 블록 추가), `tests/classroom-delete.test.mjs`(행 구조에 맞게 소스 검사 갱신).
- 처음 `5d7e8b0` 기준으로 만든 뒤 최신 main(명단 입장·초록 팔레트) 위에 다시 적용했다.
- 검증(최신 main 위 재적용 후): typecheck·lint(경고 13은 main 기존)·`npm test` 320/320·`git diff --check`·`git diff --check main...HEAD` 통과. `next build && next start -p 3399`로 현재 트리를 띄워 `scripts/browser-check.mjs http://localhost:3399` 3뷰포트 통과(FAIL 0, 핀치 SKIP 1은 기존). 별도 CDP 스크립트로 320×568·390×844·844×390·1440×900에서 가로 넘침 0, 44px 미만 터치 목표 0, 텍스트 잘림 0을 실측했고 QR 대화상자 열기·Escape 닫기·초점 복귀, 새 학급 폼(명단 포함) 열림을 확인했다.
- 사고 기록: 3299 포트에 다른 워크트리(menhaden) 서버가 떠 있는 줄 모르고 한 번 검사해 그 로컬 DB에 검증용 학급이 생겼다(로컬 `.data` 전용, 운영 무관). CLAUDE.md 경고 그대로 — 검사 전 포트 소유 프로세스의 cwd를 확인한다.
- 주의: 이 저장소의 테스트 하네스는 Node 타입 스트리핑을 쓰므로 Node 22.13에서는 `ERR_UNKNOWN_FILE_EXTENSION`이 난다. Node 22.18+ 또는 23.6+로 실행한다. `next dev`는 `.next/dev` 캐시가 CSS 편집을 놓칠 수 있어 스타일 검증 전 캐시 삭제 후 재시작이 필요할 수 있다.

## 2026-09-09 교사 학급 상세 작업공간 개편 (`main`, 로컬 병합 완료)

- 사용자가 선택한 1번 시안을 기준으로 학급 상세를 `오늘 수업 / 작품·그림책 / 명단·설정` 세 탭의 흰색 작업공간으로 바꿨다. 기존의 큰 조종석, 입장 카드, 메시지 카드, 학생별 여러 행동 버튼을 한 화면에 이어 붙인 구성을 제거하고 각 작업의 맥락에 맞게 나눴다.
- 오늘 수업은 학급의 현재 `arcId + episodeId`와 일치하는 학생별 작품만 4열 격자로 보여 준다. 현재 회차가 없는 자유 그리기 학급은 학생별 최근 작품임을 표시한다. 학생 그림을 누르면 기존 교사 관찰 ping, 개별 텍스트 메시지, 교사 검토 AI 초안, 보호자 동의 가족 공유, 지난 그림 보기를 유지한다.
- 작품·그림책은 소유 교사의 활성 학급·활성 명단으로 제한한 작품 보관함 API를 추가했다. 학생·아크·회차 필터, 실제 썸네일, 최신순 커서 페이지네이션을 제공하며 다른 교사·삭제 학급·삭제 학생을 닫는다. 완성 그림책은 기존 소유권 보호 API와 책 보기·PDF 화면으로 연결한다.
- 명단·설정은 번호·이름·별명·첫 입장 표, 검색, 붙여 넣기 학생 추가, 번호·이름 수정, 학생 삭제/복원, 입장 열기/닫기, 코드·주소 복사, QR, 코드 재발급, 가족 링크 취소, 학급 삭제를 한 화면에 모았다. 명단 저장 실패 시 입력을 유지하며, 코드 변경은 기존 코드와 QR이 무효가 됨을 확인한다.
- 시각은 선택 시안의 흰 배경, `#315d46` 초록 강조, 1px 구분선, 4~8px 모서리, 그림 중심 격자를 따른다. 브랜드 로고와 실제 저장된 학생 작품·그림책 표지만 사용하고 목업의 가상 학생 그림은 제품에 넣지 않았다. 아이콘은 `lucide-react`를 설치해 사용한다.
- 검증: typecheck, lint(기존 경고만), production build, 전체 테스트 **325/325**, `git diff --check`를 통과했다. `npm run check:browser`의 320×568·390×844·844×390 전 항목도 통과했다(390px 핀치 1건은 하네스 환경 SKIP이며 기존 단위·형제 뷰포트가 검사). 실제 로컬 브라우저에서 세 탭, 학생·회차 작품 필터, 저장 작품 미리보기, 전체 메시지, 큰 QR, 명단 표, 학생 추가 모달의 Escape 닫기와 초점 복귀를 확인했다. 세 시안과 최종 1536×1024 캡처를 비교한 `design-qa.md`는 `Final result: passed`다.
- 사용자 요청으로 검증 커밋을 로컬 `main`에 fast-forward 병합했다. GitHub `main` push와 Vercel 운영 배포는 저장소 경계에 따라 사용자가 실행한다.

## 2026-09-09 들어 보기(읽어 주기) 기능 전부 제거 (`claude/remove-speak-20260909`, 로컬 검증)

- 사용자 지시("들어보기 기능은 전부 제거")로 브라우저 `speechSynthesis` 기반 읽어 주기 버튼을 코드베이스에서 완전히 뺐다. 삭제: `app/components/SpeakButton.tsx`, `lib/speech.ts`, `tests/speak-button.test.mjs`. 사용처 24곳 정리: `DrawingStudio`(코칭·AI 가이드·레슨 단계·시작 선택·종이 가득·글씨 넣기·소개), `StudentHome`(오늘 회차·자유 그리기 인트로), `StudentMessageCenter`(배너·이력), `JoinClient`(수업 확인 오류·번호 화면·명단 없음·재입장·프로필 화면). 교사 목소리 귓속말(`VoiceWhisper`, 오디오 재생)은 별개 기능이라 그대로 둔다.
- CSS: `.speak-button*`·`speaker-pulse` 규칙 전부 삭제, 버튼 자리를 비워 두던 열(`spoken-prompt`·`lesson-spoken-prompt`·`text-composer-title-row`·`canvas-message`·`guide-choice-heading`)을 제거해 빈 칸이 남지 않게 했다. `EntryCheck.module.css`의 `.speak` 자리 규칙 삭제.
- 테스트·스크립트: `pre-reader-ux`(SpeakButton 부재를 단언), `entry-error-recovery`, `mobile-css`(canvas-message 2열), `browser-check.mjs`의 "소리로 들을 수 있음" 2항목 삭제.
- 문서: `CLAUDE.md` 제품 원칙의 "그림, 음성, 큰 터치 목표"에서 음성을 뺐고, `docs/product-decisions.md` 교사와 메시지 8항에 결정을 적었다.
- 검증: typecheck·lint(경고 14는 기존)·`npm test` 309/309·`git diff --check`·`git diff --check main...HEAD` 통과. `next build && next start -p 3399` 후 `browser-check.mjs http://localhost:3399` EXIT 0(알려진 SKIP 1).
- 남은 위험: 비문해 아이가 안내 문장을 소리로 들을 수단이 없어졌다. 필요해지면 서버 TTS 등 다른 방식으로 다시 논의한다.

## 2026-09-09 수업 확인 대기 화면 시안 반영 (`claude/remove-speak-20260909`, 로컬 검증)

- 사용자 시안(1693×929): 크림 배경(#f8f8f3) 가운데에 초록 선 너머로 고개 내민 몽그리 + `잠깐만 기다려 줘!` + `수업실을 준비하고 있어요`. 로고·버튼 없음. `JoinClient`의 `checking` 모드에서 오류가 없는 대기 상태만 이 화면으로 바꿨고, 수업 코드 오류·연결 오류 상태는 종전 교실 장면 화면(`몽그리랑 다시 찾아보자!`)을 그대로 쓴다.
- 에셋: 시안에서 몽그리와 선만 잘라 `public/entry-green/wait-mongri.png`(476×340, PNG 214KB — 이 맥에 cwebp가 없어 webp로 못 바꿨다. 있으면 webp로 바꿔 무게를 줄인다). 스타일은 `EntryCheck.module.css`의 `.waitShell`/`.waitMongri`. 구 `.entry-loading` 규칙과 `entry-paint-bob` 키프레임은 사용처가 없어져 삭제.
- 검증: typecheck·lint(경고 14 기존)·`npm test` 309/309·`git diff --check` 통과. `next build && next start -p 3399` 후 CDP로 `/api/student` 요청을 멈춰 대기 화면을 고정해 320×568·390×844·844×390·1440×900·1693×929 실측: 가로·세로 넘침 0, 1693×929에서 몽그리 위치·폭(x609 w474)과 제목 52px·부제 28px이 시안과 일치. browser-check(3399) 통과.

## 2026-09-09 그리기 팔레트 무스크롤 + 무지개 버튼 색 고르기 대화상자 (`claude/remove-speak-20260909`, 로컬 검증)

- 사용자 요청: 색 카드 안의 색이 스크롤 없이 다 보이고, 무지개 버튼을 누르면 색을 상세하게 고르는 화면이 뜬다.
- 팔레트(넓은 화면 떠 있는 카드, `min-width:900px and min-height:600px`): `max-height:250px; overflow:auto`를 걷어 12색이 두 칸 6줄로 다 보인다. 화면 높이 880px 이하에서는 색 카드를 156px로 넓혀 세 칸 4줄로 바꿔 1024×768·1280×800에서도 카드가 화면 안에 들어간다(종전에는 12색 중 2색이 스크롤 뒤에 있었고 카드 자체가 768·800 화면 아래로 62px 넘쳤다). 900×600(블록 최소 높이)에서는 도구 카드 383px + 색 카드 278px이 본문 534px보다 커서 여전히 넘친다 — 종전에도 같았고 이번 범위 밖.
- 색 고르기 대화상자 `app/components/ColorPickerDialog.tsx`(+ `lib/color.ts` HSV↔HEX): 밝기·진하기 면(포인터 드래그, 화살표 키), 무지개(색상) 슬라이더, 현재 색 이름, 고운 색 모음(종전 `MORE_PALETTE` 22색이 여기로 이동), 「닫기 / 이 색으로 그리기」. 기존 `useModalDialog`로 초점 가둠·Escape·배경 inert. 팔레트 밖 색을 쓰는 동안 무지개 버튼이 눌린 상태(초록 테두리)가 되고 안쪽 테두리로 그 색을 보여 준다. 종전 「🎨 색 더보기」 펼침(`colorsExpanded`)은 제거.
- 주의: 색 적용 네 줄(setColor·글씨 색·지우개 복귀)을 헬퍼 함수로 빼면 React Compiler가 `DrawingStudio` 전체를 컴파일 대상으로 삼아 기존 `performance.now()` 호출 3곳을 lint 오류로 잡는다(현재는 컴파일 제외 상태). 그래서 팔레트 원과 대화상자 `onPick`에 같은 네 줄을 인라인으로 두었다. 컴포넌트를 컴파일 대상으로 바꿀 때는 그 3곳부터 고친다.
- 대화상자 크기: 세로 flex라 화면이 낮으면 색 모음만 안에서 스크롤하고 면·무지개·버튼은 항상 보인다. 가로 낮은 화면(≤520px)은 면 왼쪽 / 나머지 오른쪽 2열, 세로 낮은 화면(≤640px)은 면을 20vh로 줄인다.
- 검증: typecheck·lint(경고 14 기존)·`npm test` 309/309(`drawing-tools`에 HSV 왕복·CSS 단언, `guide-demonstration` 갱신)·`git diff --check` 통과. `next build && next start -p 3399` 후 CDP 실측(교사 로그인→학급→학생 입장→작품 생성 시드): 팔레트 scrollHeight 초과 0, 12색 44px, 색 카드 아래끝 1024×768=755·1280×800=786·1440×900=886(모두 화면 안); 대화상자 320×568·390×844·844×390·1024×768·1440×900에서 화면 안, 내부 스크롤 0, 44px 미만 목표 0, 배경 inert, 초점 안쪽. browser-check(3399) 통과.
- 알려진 결함: `scripts/browser-check.mjs`가 간헐적으로 멈춘다(2026-09-09 두 번). 정상 실행은 45초에 끝나고 결과는 마지막에 한 번에 출력하므로, 로그가 비어 있는 상태로 몇 분이 지나면 멈춘 것이다. 죽이고 다시 돌리면 통과했다. 원인은 아직 안 봤다.
- 실측 스크립트: 세션 스크래치 `studio-panel.mjs`(포트·뷰포트 env, 세 번째 인자로 열기 동작). 검증용 교사 `studio-measure@local.test`·학급 「팔레트 실측반」이 로컬 `.data`에 남는다.

## 다음 작업 시작 전 확인

1. 이 문서와 `product-decisions.md`, `pending-decisions.md`를 읽는다.
2. `git status --short`로 사용자 소유 변경을 확인한다.
3. 요청이 확정 결정인지, 검토 의견인지 구분한다.
4. 미결정 항목을 임의로 제품 정책으로 확정하지 않는다.
5. 구현·검증·배포 상태가 바뀌면 이 문서를 함께 갱신한다.

## 2026-09-09 몽그리 이름 잔존 정리 (`claude/mongri-role-20260909`)

- `app/privacy/page.tsx`의 `AI 도우미(그리미)`가 2026-09-07 이름 변경에서 빠져 있었다 → `몽그리`로 교체. 개인정보 안내는 학부모·교사가 읽는 화면이라 옛 이름이 남아 있으면 안 된다.
- `docs/product-decisions.md`의 살아 있는 결정 문장(확장 협업자·틀리는 해석자·VTS 진행자 규칙·음성 금지) 11곳도 `몽그리`로 통일. 6항의 "옛 이름 `그리미`" 표기는 안전 필터가 두 이름을 함께 검사하는 이유를 설명하므로 그대로 둔다.
- `lib/openai-coaching.ts`의 `EVALUATOR`·완성 대행 정규식에 남은 `그리미`는 의도된 보존이다(과거 기록·모델이 옛 이름을 쓰는 경우 차단). 건드리지 않았다.

### 이 작업에서 폐기한 계획

옛 저장소(`yonghwan86/wiggle_web`) 기준으로 세웠던 몽그리 역할 정리 두 건은 현재 main에서 성립하지 않아 반영하지 않았다.
- `단계 가이드 만들기`를 4단계 자유 창작 전용으로 제한 → `LESSONS`가 빈 배열이라 `lesson`은 항상 undefined이고, 게이트를 넣어도 동작이 같은 무의미한 코드가 된다.
- 빠른 주제 칩을 2단계 따라 그리기 주제와 연결 → 30개 레슨 카탈로그가 은퇴해(2026-08-30 결정 16항) 참조할 주제가 없다.

`QUICK_DRAW_TOPICS`(우주·강아지·마법 숲·자전거)는 옛 자유 그리기 흐름의 잔재로 남아 있다. 씨앗 이야기 아크와 어떻게 맞출지는 미결정이다.

## 2026-09-09 몽그리 두 역할 구현 (`claude/mongri-role-20260909`)

`docs/product-decisions.md` 학습 과정 4항(확장 협업자·틀리는 해석자)이 문서에만 있고 코드에는 없던 것을 반영했다.

**이야기 문맥 연결 (두 역할의 전제).** `artworks` 행에 `arc_id`·`episode_id`가 저장돼 있었는데 코칭 API가 모델에 보내지 않고 있었다. 그래서 몽그리는 아이가 어느 이야기의 몇 회차에 있는지 모른 채 답했다. `storyContext()`가 아크 제목·회차 제목·장면 문장·회차 번호를 맥락에 싣는다. 아크가 없는 자유 그림이면 null이고 종전처럼 그림만 보고 답한다.

**확장 협업자 (그리는 중).** `STUDENT_COACHING_INSTRUCTIONS`에 세 줄을 더했다 — 아이가 이미 그린 것과 고른 답이 먼저이고 몽그리가 뒤를 잇는다, 새 소재·주제를 몽그리가 가져오지 않는다, 아이 그림에 없는 것을 있다고 말하지 않는다.

**틀리는 해석자 (마무리).** 새 종류 `story_interpretation`. 몽그리가 짐작 하나를 물음표로 내놓고(`guess`) 아이가 답 칩으로 고친다. 완성 버튼을 누르면 소감 화면이 열리면서 `action: "interpret"`을 부르고, 아이가 고른 말이 소감의 `storyText`로 저장된다. `storyText`는 이미 큐·API·DB·작품 상세까지 연결돼 있어 스키마 변경이 없었다.
- 검증기는 `guess`에 물음표 하나를 요구한다. 단정하면 아이가 고칠 여지가 사라진다.
- 답 칩 검사는 `parseCoachingChoices()`로 학생 코칭과 공유한다. 규칙이 갈라지면 한쪽에만 안전 검사가 남는다.
- 짐작 요청은 완성을 막지 않는다. 실패하면 조용히 접히고 소감 화면은 종전대로 동작한다. OpenAI 키가 없는 로컬에서도 완성이 된다.
- 새 작품 버전을 만들지 않으므로 `coaching_events`에 기록하지 않는다. 아이 답은 `storyText`에 남는다.
- AI 문장이라 음성 버튼을 붙이지 않았다(20항). 비문해 아이는 답 칩 이모지로 참여한다.

### 검증

typecheck·lint(오류 0) 통과, `npm test` 324/325(실패 1은 Node 22.13에 `registerHooks`가 없어 생기는 `pbkdf2-runtime` 환경 실패로 이 변경과 무관). `browser-check`는 세 뷰포트 전부 통과 — 짐작 노출·가림·잘림, 답 칩 누르기, 답 선택이 내 말 칸에 반영, 44px, 음성 버튼 없음을 실제 DOM으로 측정했다. 코칭 API 스텁에 `action`별 분기를 더해 실제 React 상태로 렌더된 화면을 쟀다.

### 검증 중 사고 (기록)

첫 `browser-check` 실행은 **다른 체크아웃의 서버를 검사했다.** 포트 3299가 이미 `/Users/persimmontree/orca/wiggle_web`(주 체크아웃)의 `next start`에 잡혀 있어서 내 `next start`가 붙지 못했고, 스크립트는 그 서버를 검사해 전부 통과로 보고했다. 다음 실행에서 그 서버의 수업 코드 자릿수가 달라 시드가 실패하면서 드러났다. CLAUDE.md 개발 절차 5번이 경고한 그 사고다. 이후 빈 포트(3411)에 띄우고 `lsof`로 그 포트를 잡은 프로세스의 cwd가 이 워크트리인지 확인한 뒤 다시 측정했다. **앞으로 browser-check 전에는 포트 소유자의 cwd를 반드시 확인한다.**

### 남은 것

- 몽그리 패널의 빠른 주제 칩(우주·강아지·마법 숲·자전거)과 `단계 가이드 만들기`는 옛 자유 그리기 시절 잔재로 그대로 있다. 아크가 이미 회차별 힌트 세 개를 주므로 역할이 겹친다. 정리 여부는 미결정.
- 틀리는 해석자 문안은 글로만 나온다. 글을 못 읽는 1학년에게 짐작을 어떻게 전달할지는 미결정(답 칩 이모지가 1차 채널).
- 운영 실측은 하지 않았다. 저장 경로 중 소감의 `storyText` 값만 바뀌므로 배포 후 완성 저장을 한 번 확인하는 편이 좋다.

## 2026-09-09 단계 가이드·빠른 주제 칩 은퇴 (`claude/mongri-role-20260909`)

사용자 지시로 몽그리의 `단계 가이드 만들기`와 빠른 주제 칩(우주·강아지·마법 숲·자전거)을 걷어냈다. 결정은 `docs/product-decisions.md` 학습 과정 4-1항.

**제거한 것.** `drawing_guide` 종류 전체 — 지시문·JSON 스키마·`validateDrawingGuide`·`isKoreanFreeCreationStep`·`DrawingGuide`/`GuideStep` 타입. API의 `guide`·`finishGuide` 액션과 `requestedTopic`. 스튜디오의 `aiGuide`·`aiGuideStep`·`guideTopic` 상태, `requestAiGuide`·`chooseGuideStep`·`finishGuide`, 가이드 진행 화면과 주제 입력 화면. 죽은 CSS(`.ai-guide`·`.guide-request`·`.quick-topic-row`).

**남긴 것과 이유.**
- 점선 시범(`guideTraces`·`renderGuideFrame`·`guidePhase`)은 수업 카탈로그가 몰던 별개 기능이고 제품 결정 5~7항이 보조 수단으로 유지하기로 한 것이라 남겼다. 다만 `LESSONS`가 비어 있어 지금은 도달할 수 없는 상태다.
- `coaching_event_details.guide_steps_json` 열과 `response_kind`의 `guide` 값은 이미 저장된 옛 기록이 있어 지우지 않았다. 새 행은 항상 빈 배열과 `question`이다.
- `childChoice`·`CHOICE_DRAWING_SETUP`은 수업 단계 선택지도 쓰므로 남겼다.

**옛 기록 안전장치.** `recordCoachingAfter`의 종류가 `question_answer` 하나로 줄었지만 `response_kind`·`status` 조건은 그대로 좁혀서 건다. 남아 있는 `guide` 행을 질문-답 경로가 집어 상태를 바꾸지 않는다는 것을 `tests/mvp2-storage.test.mjs`가 실제 DB로 검증한다. 옛 guide 이벤트를 닫으려던 `GUIDE_AFTER_REQUIRED` 분기는 제거했다 — 이제 만들 수 없는 상태를 요구하는 안내였고, 삭제 UPDATE는 원래부터 `question`으로 좁혀져 있다.

**검증.** typecheck·lint(오류 0), `npm test` 308/309(실패 1은 Node 22.13 `registerHooks` 환경 문제로 무관), `browser-check` 세 뷰포트 전부 통과. 검증 서버는 빈 포트를 찾아 띄우고 LISTEN 프로세스의 cwd가 이 워크트리인지 확인한 뒤 돌렸다.

## 2026-09-13 복사 버튼이 조용히 실패하던 문제 수정 (`claude/copy-text-20260913`)

사용자 신고: 수업 코드 복사를 눌러도 반응이 없다.

**원인.** `app/components/TeacherApp.tsx`의 복사 버튼들이 `navigator.clipboard?.writeText(...)` 한 줄이었다. 세 가지가 겹쳤다.
- 성공해도 아무 표시가 없어 눌리지 않은 것으로 보였다.
- 옵셔널 체이닝이 `navigator.clipboard` 부재를 삼켜 조용히 끝났다. 이 API는 보안 맥락(HTTPS·localhost)에서만 존재하므로, 교실 태블릿에서 내부 주소(`http://10.0.0.5:3000`)로 열면 아예 없다.
- 반환된 Promise를 받지 않아 거부돼도 화면에 아무것도 안 떴다.

**수정.** `lib/copy-text.ts`에 `copyText()`·`copyNoticeText()`를 두고 모든 복사를 여기로 모았다. 클립보드가 없거나 거부하면 `execCommand("copy")` 폴백을 한 번 더 시도해 비보안 맥락에서도 실제로 복사된다. 결과를 boolean으로 돌려 부르는 쪽이 반드시 알리게 했다.

바꾼 곳: 학급 목록의 수업 코드 복사, 입장 주소 복사 두 곳(학급 목록 QR 대화상자·수업실 QR 대화상자), 가족 링크 복사 두 곳, 명단 설정의 수업 코드·참여 코드·입장 주소 복사, `FamilyView`의 1회용 링크 복사. `TeacherApp`에 `copyNotice` 상태와 `.copy-notice` 배너를 두 화면에 추가했다. `FamilyView`는 링크 발급 실패와 복사 실패를 한 catch로 묶어 비보안 맥락의 복사 실패를 "링크를 만들지 못했다"로 잘못 알리던 것도 함께 갈랐다.

**회귀 테스트가 잘못된 형태를 고정하고 있었다.** `tests/qr-entry.test.mjs`가 `navigator\.clipboard\?\.writeText\(joinUrl\)`를 단언하고 있어 이 결함이 테스트로 보호되던 상태였다. 한 경로 호출을 단언하고 `navigator.clipboard` 직접 사용이 없는지 함께 본다.

**검증.** `tests/copy-text.test.mjs`가 navigator·document를 갈아 끼워 네 갈래(보안 맥락 성공, 클립보드 없음+폴백 성공, 클립보드 거부+폴백 성공, 둘 다 실패)와 빈 문자열, 문구 생성을 실제로 태운다. typecheck·lint(오류 0), `npm test` 316/317(실패 1은 Node 22.13 `registerHooks` 환경 문제로 무관), `browser-check` 세 뷰포트 통과.

실제 브라우저로 교사 화면을 열어 끝까지 확인했다 — 진짜 클릭으로 `수업 코드를 복사했어요.` 배너가 뜨고, 검색 칸에 붙여넣으니 그 학급의 수업 코드 `6541`이 그대로 들어왔다. 사용자 제스처 없이 JS로 `click()`만 부른 경우에는 브라우저가 클립보드를 거부하는데, 이때 새 코드가 조용히 끝나지 않고 `자동 복사가 되지 않아요…` 안내를 띄우는 것도 같은 화면에서 확인했다. 실패 쪽 분기까지 실제로 본 셈이다.

**남은 것.** 로컬 개발 DB에 점검용 학급 `복사 점검반`(교사 `copycheck@local.test`)이 남아 있다. 로컬 파일 DB라 운영과 무관하며 필요 없으면 지우면 된다.

## 2026-09-13 입장 화면 고정 비율 무대 제거 (`claude/entry-responsive-20260913`)

사용자 신고: 참여 코드를 잘못 입력했을 때 화면이 반응형이 아니라 이미지가 박혀 있는 것 같다.

**원인(실측).** 입장 확인·실패 화면과 참여 코드 입력 화면이 교실 그림을 `aspect-ratio: 1672/941` 고정 무대로 깔고, 제목·안내·버튼까지 그 안에 퍼센트 좌표로 붙여 놨다. 화면이 정확히 그 비율일 때만 꽉 차고 나머지는 크림색 빈 띠가 남았다.

| 화면 | 빈 여백 |
|---|---|
| 1920×1080 | 없음 |
| 1440×900 | 아래 90px |
| 1180×820 | 아래 156px |
| 900×700 | 아래 193px |
| 2560×1080 | 좌우 641px |

**수정.** `.shell`이 교실 그림을 `cover`로 덮고, 내용은 `.stage`의 가운데 세로 흐름으로 쌓는다. 문·나무 간판에 글자를 맞추던 절대 좌표(`.sign`, 옛 `.padBadge`·`.seatTitle`·`.pad` 오버레이)는 배경이 잘리면 어긋나므로 걷어내고, 이미 휴대폰용으로 만들어 두었던 카드 레이아웃을 모든 크기로 올렸다. 참여 코드 입력 화면도 같은 CSS를 쓰므로 함께 바뀐다.

**세로 화면은 예외.** cover는 화면 비율이 그림과 멀어질수록 많이 자른다. 390×844에서는 오리 몸통만, 820×1180에서는 머리가 화면 밖으로 나갔다. `@media (max-aspect-ratio: 5/4)`에서 배경 그림을 끄고 크림 배경 + 독립 오리 그림으로 되돌린다.

**검증.** 일곱 뷰포트(1440×900, 1920×1080, 2560×1080, 1180×820, 820×1180, 900×700, 390×844)를 헤드리스로 실측했다 — 전부 빈 띠 없음, 가로 넘침 0, 화면 밖 버튼 0, 최소 터치 목표 44px, 세로 스크롤 불필요. 두 화면 모두 스크린샷으로 눈으로 확인했다. typecheck·lint(오류 0), `npm test` 316/317(실패 1은 Node 22.13 `registerHooks` 환경 문제로 무관), `browser-check` 세 뷰포트 통과.

`tests/classroom-entry-flow.test.mjs`가 옛 고정 무대 규칙을 단언하고 있어, 새 보장(가운데 세로 흐름·고정 비율 없음·cover 배경·세로 화면 예외)을 단언하도록 고쳤다.

**남은 것.** 참여 코드 입력 화면 배경 그림에 그려진 빈 나무 간판이 이제 쓰이지 않는다(학급 이름은 별도 나무 칩으로 표시). 그림 자산을 고칠지는 미결정.

## 2026-09-13 입장 화면을 대문과 같은 방식으로 재구성 (`claude/entry-responsive-20260913`)

앞선 수정(교실 장면을 `cover`로 꽉 채우기)에 사용자가 "이미지 확대가 아니라 홈화면처럼 진행되는 것"을 원한다고 알려 와 방향을 바꿨다.

**왜 한 장짜리 장면으로는 안 되는가.** 대문(`.gallery-landing`)이 자연스럽게 늘어나는 이유는 배경에 종이 질감만 있고 오리·액자·잎사귀가 전부 독립 요소이기 때문이다. 입장 화면의 교실은 문·오리·액자·화분·이젤·바닥이 한 장에 다 그려진 그림이라, 통째로 늘리거나(빈 띠) 자르는(오리 잘림) 선택지밖에 없었다.

**조각으로 나눴다.** `docs/design-assets/entry-green/assets/classroom-empty.png`(오리·UI 없는 빈 교실, 1672×941)에서 `sharp`로 잘라 `public/entry-green/scene/`에 세 조각을 만들었다.
- `floor.webp` — 물체가 없는 깨끗한 바닥 구간(원본 x 700~1250, y 762~941). 가로로만 늘린다.
- `left-group.webp` — 액자·문·화분(원본 x 0~700, y 60~941).
- `right-group.webp` — 압정 그림·이젤·크레용통(원본 x 1280~1672, y 370~941).
- 벽은 이미지가 아니라 평평한 색(`--wall: #fceece`)이다. 벽 조각을 타일로 반복했더니 이음매가 보였다.
- 잘라낸 사각형의 벽 질감이 평평한 벽과 만나 경계선이 보여서, 바깥쪽·위쪽으로 갈수록 투명해지는 알파를 자산에 구워 넣었다. 바닥과 화면 바깥 끝은 불투명하게 남긴다.

**배율을 하나로 묶었다.** 장식과 바닥 띠가 따로 커지면 걸레받이 선이 좌우에서 어긋난다. `--sceneH`(원본 881px에 대응) 하나로 묶어 바닥은 `× 179/881`, 오른쪽 장식은 `× 571/881`로 파생시켜 원본 장면의 상대 위치를 유지한다. 좁은 화면(`max-width: 1100px` 또는 `max-height: 560px`)에서는 장식을 내려놓는다.

**검증.** 일곱 뷰포트 실측 — 전부 가로 넘침 0, 화면 밖 버튼 0, 최소 터치 44px, 세로 스크롤 불필요. 입장 실패 화면과 참여 코드 입력 화면을 1920·1440·390에서 스크린샷으로 확인했다. typecheck·lint(오류 0), `npm test` 316/317(실패 1은 Node 22.13 `registerHooks` 환경 문제), `browser-check` 세 뷰포트 통과.

**남은 것.** 장식 조각은 빈 교실 그림에서 잘라낸 것이라 배경 벽이 함께 들어 있다(알파로 녹임). 완전히 투명한 개별 오브젝트 자산이 생기면 더 자유롭게 배치할 수 있다.

### 2026-09-13 몽그리를 원래 장면 자리로 (같은 브랜치)

사용자 요청으로 몽그리를 문 앞, 원래 교실 장면에서 서 있던 자리로 되돌렸다.

- 두 원본(`classroom-with-mongri.png`, `classroom-empty.png`)의 차이를 왼쪽 절반에서 열·행 히스토그램으로 재 몽그리 상자를 구했다: 원본 1672×941 기준 x 258~754, y 369~887.
- 자산(`duck-painter-640.webp`)은 640×640 캔버스에 불투명 영역이 x 48~603, y 8~622다. 투명 여백을 걷어낸 값으로 환산해 `--sceneH` 배율에 맞춰 `left 231/881`, `bottom 39/881`, `height 540/881`로 배치했다. `duck-painter-640.webp`와 `mongri-original-transparent-640.webp`는 불투명 영역이 완전히 같아 같은 그림임을 확인했다.
- 여백을 `.shell`에서 `.stage`로 옮겨 두 상자를 일치시켰다. 그러지 않으면 바닥 띠(`.shell` 기준)와 몽그리(`.stage` 기준)의 아래 기준선이 어긋난다. 대신 `.head`는 절대 배치라 패딩을 따로 줘야 로고가 화면 끝에 붙지 않는다.
- 좁은 화면에서는 종전처럼 흐름 안의 그림이다. 키패드 화면의 작은 몽그리 규칙(`.seatShell .duck`)은 선택자 우선순위가 높아 넓은 화면 배치를 덮어썼으므로 좁은 화면 전용으로 범위를 좁혔다.

검증: typecheck·lint(오류 0), `npm test` 316/317(실패 1은 Node 22.13 환경 문제), `browser-check` 세 뷰포트 통과, 일곱 뷰포트 실측 이상 없음. 두 화면을 1920·1440·390에서 스크린샷으로 확인했다.

### 2026-09-13 카드는 항상 가운데, 장면은 태블릿 폭에 맞춰 축소 (같은 브랜치)

사용자 신고: 몽그리를 원래 자리로 옮기자 가운데 카드와 겹쳤다(1440×900에서 77px, 1180×820에서 157px 실측). 처음에는 카드를 몽그리 오른쪽으로 비켜 세웠지만 카드가 화면 가운데에서 벗어나, 사용자가 "태블릿에 맞춰 진행"하기로 정했다.

**방식.** 카드는 항상 가운데에 두고, 장면 배율 `--sceneH`에 세 번째 상한을 더했다: `calc((50vw - 230px - 28px) * 881 / 754)`. 몽그리 오른쪽 끝(원본 x=754)이 가운데 카드(최대 460px)의 왼쪽 끝보다 28px 앞에 오는 최대 배율이다. 태블릿에서는 이 항이 배율을 정해 장면 전체(문·몽그리·이젤·바닥)가 같은 비율로 줄어든다. 이젤 쪽은 오른쪽에서 272px(원본) 거리라 제약이 더 느슨해 따로 막을 필요가 없다.

카드를 비켜 세우던 좌우 패딩과 1300px 미만에서 오른쪽 장식을 숨기던 규칙은 걷어냈다. 장식·몽그리 배치 구간은 1024×768 태블릿 가로까지 포함하도록 1000px 이상·세로 600px 이상으로 넓혔다.

**실측(9개 뷰포트).** 카드 중앙 오차 전부 0px, 몽그리 겹침 0, 이젤 겹침 0, 최소 터치 44px, 가로 넘침 0.

| 화면 | 몽그리 키 |
|---|---|
| 아이패드 가로 1024×768 | 182px |
| 아이패드 가로 1180×820 | 238px |
| 갤럭시탭 1280×800 | 274px |
| 아이패드 프로 1366×1024 | 304px |
| 노트북 1440×900 | 331px |
| 데스크톱 1920×1080 | 429px |

세로 태블릿(768×1024, 820×1180)과 휴대폰은 장식 없이 몽그리가 카드 위 흐름에 있다. typecheck·lint(오류 0), `npm test` 316/317(실패 1은 Node 22.13 환경 문제), `browser-check` 세 뷰포트 통과. 1024·1180·1366 가로와 768 세로를 스크린샷으로 확인했다.

**남은 것.** 1024×768에서 몽그리 키가 182px로 작다. 더 키우려면 카드 폭을 줄이거나 몽그리를 문 앞에서 조금 왼쪽으로 옮겨야 한다.

## 2026-09-13 입장 화면 QR 찍기 (`claude/qr-scan-entry-20260913`)

사용자 요청: 참여 코드 입력만 있는 입장에 QR 촬영을 더한다. 어디에 넣을지·QR에 무엇을 담을지는 보안이 달라져 먼저 물었고 "둘 다"로 정했다(`product-decisions.md` 학생 입장 4-2항).

**구현.**
- `lib/qr-entry.ts` — 입장 QR 규칙 한곳. `entryQrUrl`(쪽지 주소 생성), `parseEntryQr`(찍은 글자에서 네 자리 숫자 둘만 꺼냄), `readEntryHash`, `entryPathFor`.
- `app/components/QrScanner.tsx` — 카메라 창. `getUserMedia` + `jsqr`로 해석한다. 브라우저 내장 `BarcodeDetector`는 아이패드 사파리에 없어 쓰지 않았다. 긴 변 640px로 줄여 풀어 태블릿 부담을 줄였다. 보안 맥락이 아니거나(내부 주소) 권한 거부·카메라 없음이면 코드로 들어오라고 안내한다. 찍은 주소를 따라가지 않는다.
- 첫 화면 `LandingCodeForm`에 `QR로 찍기`, 참여 코드 화면 `JoinClient`에 `내 쪽지 QR로 찍기`.
- `JoinClient`는 마운트 때 `#entry=`를 읽자마자 네트워크 호출 전에 지우고, 반 확인이 끝나면 그 코드로 바로 제출한다. `submit`이 코드를 인자로 받게 바꿨다 — QR로 채운 직후에는 `setCodeInput`이 아직 반영되지 않는다.
- 인쇄 쪽지(`TeacherRosterPrint`) QR을 아이별 주소로. 선생님 화면의 큰 QR은 반 QR 그대로다.
- 새 의존성 `jsqr@1.4.0`(QR 해석). 기존 `qrcode`는 생성 전용이라 해석을 못 한다.

**검증 중 찾은 버그.** 이미 같은 반 화면(`/join/4341`)에 있는 탭에서 태블릿 카메라로 쪽지 QR(`/join/4341#entry=…`)을 찍으면, 경로가 같고 조각만 바뀌어 페이지가 다시 읽히지 않는다(같은 문서 이동). 마운트 처리가 돌지 않아 조각이 무시됐다. `hashchange`를 따로 받아 같은 규칙(읽자마자 지우고 제출)으로 처리하게 고쳤다.

**검증.**
- `tests/qr-scan-entry.test.mjs` — 규칙 단위 테스트, 그리고 **인쇄 → 촬영 → 해석 전체**: 쪽지 QR을 `qrcode`로 그리고 `jsqr`로 풀어 `parseEntryQr`가 원래 반·참여 코드를 되찾는지 실제로 태운다.
- 실제 브라우저 끝까지(21항목 통과): Chrome 가짜 카메라(`--use-file-for-fake-video-capture`)에 아이별 쪽지 QR 영상을 넣어, 첫 화면에서 `QR로 찍기` → 카메라가 QR을 읽음 → 그 반으로 이동 → 조각이 주소창·최종 주소에서 지워짐 → 키패드 없이 동물 고르기까지 확인했다. 없는 코드 조각은 지우고 입장하지 않으며 오류를 보인다. 같은 반 화면에서 조각만 바뀌어도 바로 입장한다. 카메라 창은 1180×820·768×1024·390×844에서 화면 안에 들어오고 가로 넘침 없음, 닫기·코드로 들어가기 44px 이상, 초점이 창 안으로 들어가고 Escape로 닫힌다.
- typecheck·lint(오류 0), `npm test` 322/323(실패 1은 Node 22.13 `registerHooks` 환경 문제), `browser-check` 세 뷰포트 통과.
- 옛 제출 변수명(`entryCode: codeInput`)과 반 QR 쪽지를 글자 그대로 단언하던 테스트 네 파일을 새 형태로 고쳤다. 보안 회귀 테스트는 "서버로 가는 것은 참여 코드뿐, 기기 저장값에서 오지 않는다"는 의도를 그대로 지키도록 단언을 보강했다.

**남은 위험.**
- 실제 아이패드 카메라로는 확인하지 못했다(가짜 카메라로 스트림·해석·입장까지 확인). 운영(HTTPS)에서 실기기로 한 번 찍어 보는 편이 좋다.
- 로컬에서 태블릿이 내부 주소(`http://10.x`)로 열면 카메라를 쓸 수 없다(보안 맥락 아님). 안내 문구가 뜨고, 운영 HTTPS에서는 해당 없다.
- 쪽지를 새로 인쇄해야 아이별 QR이 생긴다. 이미 나눠 준 쪽지의 QR은 반 QR이라 찍으면 참여 코드 화면까지만 간다.

### 2026-09-13 쪽지 안내 문구 정정 (`claude/slip-foot-20260913`)

아이별 QR로 바꾸면서 쪽지 맨 아래 안내("QR을 찍고 내 참여 코드 네 자리를 누르면 도화지가 열려요.")를 놓쳐, 찍기만 하면 들어가는 실제 동작과 QR 아래 "찍으면 바로 들어가요"와 서로 다른 말을 하고 있었다. 사용자 확정 문구로 바꿨다: "QR을 찍으면 바로 도화지가 열려요. 찍기 어려우면 참여 코드 네 자리를 눌러요." 참여 코드가 아직 없는 자리는 반 QR이라 종전 문구를 유지한다.

검증: 가상 학급 코드표를 print 미디어로 PDF 출력해 네 쪽지 모두 새 문구가 한 줄에 들어가는 것을 확인했고, 인쇄된 QR을 풀어 각 아이 코드가 맞게 담긴 것도 다시 확인했다. `tests/teacher-roster.test.mjs`에 문구 단언 추가. `npm test` 322/323(실패 1은 Node 22.13 환경 문제), lint 오류 0.

### 2026-09-13 참여 코드 카드 다듬기 (`claude/remove-pad-rings-20260913`)

- **수첩 스프링 고리 제거.** 카드 위 CSS 동그라미 줄(`.pad::before`)이 넓은 화면에서 어색하다는 사용자 지적. 원래 좁은 화면 전용 장식이었는데 입장 화면을 모든 크기에 같은 레이아웃으로 올리면서 넓은 화면에도 보이게 됐다. 고리 자리로 비워 둔 위쪽 여백(26px)도 18px로 맞췄다. 오류 화면 쪽지의 압정(`.note::before`)은 다른 요소라 그대로 둔다.
- **지우기 키 정렬.** 아이콘(⌫)과 "지우기" 묶음이 56px 키를 거의 다 채워(1440×900에서 위 여백 3px) 아이콘이 위 테두리에 붙어 보였다. 아이콘을 20px로 줄이고 줄 간격을 1.15로 좁혀, 1440·1180·390 모두 위아래 여백이 9px로 같아졌다(실측).

검증: `npm test` 322/323(실패 1은 Node 22.13 환경 문제), lint 오류 0, `browser-check` 세 뷰포트 통과.

## 2026-09-13 "아직 준비 중이에요" 화면 시안 반영 (`claude/not-ready-screen-20260913`)

사용자가 준 시안(1689×931)대로 명단 없는 반 화면을 다시 만들었다. 종전에는 전역 `.entry-card` 카드형이었다.

**구성.** 카드 없음. 위 왼쪽 로고, 오른쪽 반 이름. 가운데 고개 내민 몽그리, "아직 준비 중이에요", 두 줄 안내("선생님이 우리 반 명단을 넣으면 / 내 참여 코드로 들어올 수 있어요."), 노란 "🔄 다시 확인하기", "수업 코드 다시 입력하기". 스타일은 `EntryCheck.module.css`의 `.ready*`.

**시안 실측으로 맞춘 값.** 배경 `#fefaea`, 버튼 면 `#fbce4d`·턱 `#dcab33`·571×84(구현 574×84), 제목 잉크 높이 52px, 부제 23px 두 줄. 태블릿 기준으로 `vw` 비례에 휴대폰 최소값을 뒀다.

**몽그리 그림.** 처음에는 기존 `wait-mongri.png`에서 흰 배경과 아래 받침 선을 지운 투명 그림을 만들었으나, 원본이 받침 선에 발끝이 가려진 그림이라 시안처럼 둥근 발끝·발밑 그림자가 나오지 않았다. 해상도가 둘 다 약 330px로 같아, 시안 속 몽그리를 잘라(`public/entry-green/peek-mongri.webp`, 384×368) 그대로 쓴다. 불투명 그림이라 페이지 배경을 `#fefaea`로 고정했고, 잘라낸 상자 위·왼쪽·오른쪽 경계는 배경과 차이 1 이하, 그림자 끝이 걸린 아래 경계는 마지막 14줄을 배경색으로 녹여 차이 0으로 만들었다. 시안 원본은 `docs/design-assets/not-ready/mockup-2026-09-13.png`에 보관한다.

**비교.** 시안과 구현을 반쪽씩 이어 붙여 로고·반 이름·몽그리·제목이 이음새 없이 맞는 것을 확인했고, 제목 자간(-0.045em→-0.015em)·부제 위 여백·버튼 아이콘 간격을 시안에 맞춰 줄였다.

**검증 중 주의.** 여러 번 반복 촬영하다 로컬 입장 확인 제한(학급+IP 60회/10분)에 걸려 오류 화면이 찍혔다. 제한 기록은 로컬 DB에 있어 서버를 바꿔도 같다. 우회하지 않고 창이 풀리길 기다려 다시 찍었다.

마지막으로 세로 가운데 정렬이 시안보다 약 7px 아래라 아래 여백을 늘려 올렸다(1689×931에서 버튼 y=700, 시안과 같음). 1689·1180·768·390·320에서 가로 넘침 0, 세로 스크롤 없음, 버튼·링크 44px 이상.

typecheck·lint(오류 0), `npm test` 323/324(실패 1은 Node 22.13 환경 문제), `browser-check` 세 뷰포트 통과. `tests/classroom-entry-flow.test.mjs`에 이 화면 구성 단언을 추가했다.

## 2026-09-13 친구 고르기 화면 시안 반영 (`claude/animal-picker-20260913`)

첫 입장 동물 고르기를 시안(1689×931)대로 바꿨다. 결정은 `product-decisions.md` 학생 입장 4-3항.

**이름이 곧 별명.** `lib/nickname-ideas.ts` 각 동물의 첫 후보를 친구 이름(솔이·보리·루루·호두·모모·두부·나비·코코·해솔·초롱)으로 넣었다. 서버는 원래 첫 후보를 기본 별명으로 붙이므로 서버 코드 변경은 없다. 새 `lib/animal-characters.ts`가 동물·종류·그림 경로를 한곳에 두고 이름은 첫 후보를 읽는다. `withGwaWa`가 받침에 따라 와/과를 붙인다(해솔과·초롱과). 기존 후보와 겹치는 이름이 없고 후보 규칙(한글 2~16자, 전체 중복 없음, 동물당 10개 이상)을 그대로 만족한다.

**화면.** 머리 줄(로고·반 이름) 위에 제목이 겹쳐 올라가는 시안 배치, 5×2 카드 격자(휴대폰 2열), 선택 카드 초록 테두리·체크, 연두 이름 띠, 노란 시작 버튼, "참여 코드 다시 누르기". 스타일 `EntryCheck.module.css`의 `.pick*`. 시안 실측: 카드 229×279(시안 약 228×270), 시작 버튼 473×72(시안 473×70). 옛 수첩 안 동물 칩·스프라이트 스타일(`animal-choice-*`, `emoji-chip` 수첩 규칙, `[data-animal-index]`)은 쓰는 곳이 없어 지웠다.

**그림.** 폭신한 3D 동물·붓 이름 띠·장식은 코덱스 요청 대기(`docs/design-assets/animal-picker/REQUEST-CODEX.md`). 이 세션에는 코덱스가 연결돼 있지 않아 요청서만 썼다. 임시로 옛 2D 스프라이트를 동물별 파일(`public/entry-green/animals/<slug>.webp`)로 잘라 두었다 — 같은 이름으로 덮어쓰면 바뀐다. 자르면서 sharp가 `resize`를 `composite`보다 먼저 적용해 동물이 원래 크기로 왼쪽 위에 붙는 문제가 있어, 합성과 크기 조절을 나눠 다시 만들었다(내용 가운데 256, 아래끝까지 확인).

**검증 중 찾은 버그 둘.**
- 쪽지 QR로 들어와 친구를 고르고 시작하면 "참여 코드 네 자리를 눌러 주세요"로 막혔다. 첫 입장 확인 뒤 반 확인이 다시 돌면 `codeInput`이 비워졌기 때문이다. 첫 입장이 확인된 코드를 `claimCode`에 붙잡아 친구 고르기 제출에 쓴다.
- 개발 모드(StrictMode)는 마운트 효과를 두 번 돌리는데, 두 번째에 이미 지운 주소 조각을 읽어 받아 둔 참여 코드를 null로 덮어써 쪽지 QR 입장이 키패드에서 멈췄다. 코드가 있을 때만 저장한다.

**검증.** 운영 빌드에서 쪽지 코드로 들어가 솔이를 고르고 시작 → 도화지로 이동, 서버 별명 "솔이"·동물 🐰 저장 확인. 1689·1366·1180·1024·820·390에서 가로 넘침 0, 이름 잘림 0, 태블릿·데스크톱 세로 스크롤 없음(휴대폰은 카드 10장이라 스크롤하되 시작 버튼이 아래에 붙음). `browser-check`의 첫 입장 흐름을 새 화면으로 옮기고 "시작 버튼이 친구 이름을 부름", "고른 뒤 시작 버튼이 화면 안에 보임"을 더했다 — 후자가 가로 휴대폰(844×390)에서 버튼이 밀린 것을 잡아 낮은 화면에서도 버튼을 붙였다. typecheck·lint(오류 0), `npm test` 327/328(실패 1은 Node 22.13 환경 문제), `browser-check` 세 뷰포트 통과.

**작업 중 사고(기록).** 죽은 전역 CSS를 줄 단위 스크립트로 지우다 한 줄짜리 규칙을 여러 줄 블록으로 착각해, 닫는 괄호를 찾아 내려가며 학생·스튜디오·교사 스타일 197줄을 잘못 지웠다. 캔버스·교사·QR 등 테스트 20개가 깨져 드러났고, 파일을 git에서 되돌린 뒤 지울 규칙을 글자 그대로 하나씩 지워 23줄만 제거했다. 괄호 균형 검사는 통과했으므로 그것만으로는 못 잡는다 — 삭제 뒤 `git diff`로 지운 줄을 직접 확인한다.

## 2026-09-14 친구 고르기 동물 그림을 코덱스로 교체 (`claude/codex-animal-art-20260914`)

사용자 지시("너가 직접 코덱스가 구독으로 되어있는데, 그걸 연결해봐라")로 이 기기의 Codex CLI(ChatGPT 구독 로그인, `image_generation` 기능)를 직접 불러 동물 10종을 만들었다. 인증 파일은 열지 않았다.

**만드는 법.** `codex exec`에 스타일 기준 토끼 한 장·몽그리·시안을 붙이고 한 마리씩 생성시켰다. 코덱스 이미지 생성은 투명 배경을 못 만들고 체크무늬를 그려 넣으므로, 평평한 자홍색(#FF00FF) 배경으로 받아 `scripts/key-magenta.mjs`로 따냈다. 자홍 정도 `min(R,B)-G`로 투명도를 정하고, 섞인 털 가장자리는 배경 몫을 빼내(색 복원) 크림 카드 위에 자홍 테가 남지 않게 했다(보이는 픽셀의 자홍 기운 최대 22, 개구리 볼). 코덱스가 잡은 구도를 자르지 않고 그대로 512px로 줄였다 — 내용 기준으로 잘라 맞추면 귀 긴 토끼만 머리가 작아진다. 원본(1254px)은 PNG 21MB라 품질 92 webp(2.9MB)로 `docs/design-assets/animal-picker/codex-assets/`에 둔다. 판다 한 장은 코덱스가 흰 배경으로 뽑았다가 스스로 다시 뽑았다.

**검증.** 개발 서버(3301, 이 폴더 소유 확인)에서 쪽지 코드로 들어가 1689·1366·1180·1024·820·390 렌더: 가로 넘침 0, 이름 잘림 0, 태블릿 세로 스크롤 없음, 솔이로 시작 → `/student` 이동·서버 별명 "솔이"·🐰 저장. 시안과 나란히 보면 동물 질감이 맞는다.

**장식(같은 브랜치, 둘째 커밋).** 코덱스로 붓 이름 띠·제목 반짝 선·손글씨("그림과 / 친구가 되는 / 특별한 시간!", 글자 확인)·크레용 해·무지개·꽃을 같은 자홍 배경 방식으로 만들어 `public/entry-green/picker/*.webp`에 넣었다. 붓 든 몽그리는 새로 만들지 않고 투명 배경이 있는 `public/landing-gallery/duck-painter-640.webp`를 쓴다. 나무 간판 세 조각은 만들지 않았다 — 지금 나무색 칩으로 충분하고 반 이름 길이 대응이 필요해질 때 한다.
- 이름 띠·반짝 선은 모든 화면에 보인다. 옆 장식(손글씨·몽그리·해·무지개·꽃)은 카드 격자(최대 1220px) 옆 여백에만 둘 수 있어 `min-width: 1600px`·`min-height: 760px`에서만 보인다. 태블릿(1180·1366)은 여백이 없어 카드를 가리므로 숨긴다. 크기는 옆 여백 폭을 따르되 250px에서 멈춘다 — 1920에서 몽그리가 카드 절반 크기로 커졌던 것을 실측으로 잡았다.
- 장식 층이 앞에 깔리면 위치 지정 요소가 일반 흐름 위에 칠해져 버튼을 가리므로, 입력 영역(`.pickBody`)을 `position: relative; z-index: 1`로 올렸다.
- 이름 띠 위아래 여백을 1px 늘렸더니 1536×864에서 3px 세로 스크롤이 생겨 되돌렸다.
- 렌더를 여러 번 돌리다 로컬 입장 한도(학급+IP 60회/10분)에 걸려 10분 기다렸다(우회하지 않음).
- 검증: typecheck·lint(오류 0, 기존 경고 13)·`npm test` 327/328(실패 1은 Node 22.13 환경), 렌더 1920·1600·1536·1689·1366·1180·1024·820·390에서 가로 넘침 0·세로 넘침 0(휴대폰 제외)·이름 잘림 0·솔이로 입장 후 별명 저장. `browser-check`(3301) 실패 1은 844×390 캔버스 두 손가락 핀치 — 기존에 SKIP으로 처리하던 CDP 터치 전달 환경 항목이고 이번 변경은 캔버스를 건드리지 않았다.

## 2026-09-14 친구 20종 + 쪽 넘기기 (`claude/animal-picker-20-20260914`)

사용자: "캐릭터 20종류까지 늘려줘 옆으로 이동할 수 있도록 표시도해주고". 결정은 `product-decisions.md` 학생 입장 4-3항.

**친구 10종 추가.** 펭귄 도리·햄스터 콩이·돼지 봄이·원숭이 미소·송아지 누리·양 구름·기린 키키·코끼리 다솜·부엉이 별이·수달 하루. 동물은 이모지로 저장되고 목록은 `lib/nickname-ideas.ts`(서버가 첫 후보를 기본 별명으로 붙임)와 `lib/animal-characters.ts` 두 곳에만 묶여 있어 서버 코드는 바꾸지 않았다. 동물마다 별명 후보 11개, 전체 중복 없음(기존 테스트 규칙). 돼지 후보에서 놀림처럼 들릴 수 있는 "통통 돼지"는 뺐다. 그림은 코덱스에 기존 곰·여우·토끼를 스타일 기준으로 붙여 같은 세트로 만들고 자홍 배경을 따냈다(`scripts/key-magenta.mjs`, 보이는 픽셀 자홍 기운 최대 30). 코덱스 배경은 순수 #FF00FF가 아니라 (245~253, 2~5, 246~252)였지만 기준값 안이라 그대로 따진다.

**쪽 넘기기.** 한 쪽에 10종(5×2, 휴대폰 2열)씩 두 쪽. 쪽들은 가로 스냅 스크롤 상자라 태블릿에서 손가락으로 밀어 넘기고, 양옆 둥근 화살표(44~60px)와 아래 점(현재 쪽은 긴 초록 점)이 넘길 수 있음을 보여 준다. 끝 쪽 화살표는 흐리게 막힌다. 쪽 번호는 스크롤 위치에서 읽어 밀어 넘겨도 점이 맞는다. 휴대폰은 화살표·점을 격자 위로 올렸다 — 아래에 두면 카드 10장을 내려야 보였다(390×844 캡처로 확인). 격자 최대 폭이 화살표 칸만큼 넓어져 옆 장식 여백 계산을 1348px 기준으로 바꿨다.

**검증 중 찾은 것.** 왼쪽 화살표 CSS 규칙이 없어 클래스가 문자열 "undefined"로 들어가 브라우저 검사에서 앞 화살표를 못 찾았다 — `.pickPrev`를 만들었다. 1689×931에서 점 줄 때문에 2px 세로 넘침이 생겨 점 위 여백을 줄였다.

**검증.** 개발 서버(3301, 이 폴더 소유 확인)에서 1689·1180·1024·820·390·320·844×390: 처음 1쪽(앞 화살표 막힘, 솔이부터 10장), 다음 → 2쪽(도리부터 10장, 다음 화살표 막힘, 점 이동), 앞 → 1쪽 복귀, 화살표 44px 이상, 가로 넘침 0, 태블릿·데스크톱 세로 넘침 0. 2쪽 도리를 고르면 "도리와 시작하기" → `/student` 이동, 서버 별명 "도리"·🐧 저장. 반복 렌더로 로컬 입장 한도(IP 180회/10분)에 두 번 걸려 기다렸다.
- `browser-check`(3301): 친구 고르기 항목 전부 통과. 동물 수 기대값을 10에서 20으로 고쳤다(처음 실행에서 "동물 선택이 10개 :: 20"으로 실패). 남은 실패 1은 844×390 캔버스 두 손가락 핀치 — 기존 CDP 터치 전달 환경 항목이고 캔버스는 건드리지 않았다. typecheck·lint(오류 0), 관련 테스트 27/27.

## 2026-09-14 선생님 음성 귓속말 코드 제거 (`claude/remove-voice-whisper-20260914`)

사용자: "음성 귓속말 기능 코드는 아예 제거해줘". 지운 것: `app/api/voice/route.ts`, `lib/voice-whisper.ts`, `lib/voice-whisper-validation.ts`, `app/components/VoiceWhisper.tsx`(선생님 녹음 버튼은 이미 어디서도 안 쓰였고, 학생 도화지는 음성이 켜졌을 때 1.5초마다 받아 가는 표시만 남아 있었다), `db/runtime.ts`의 `WHISPER_RELAY` 릴레이 연결, 전역 CSS의 `.voice-whisper`·`.button.whisper`·`.voice-speaking` 규칙, `.env.example`·README의 `WIGGLE_VOICE_WHISPER_ENABLED`·`WHISPER_RELAY_URL`. DB 테이블은 원래 없었다. `tests/mvp3.test.mjs`의 음성 계약 테스트는 "네 파일이 없고 런타임에 릴레이가 없다"는 되살아남 방지 검사로 바꿨다. 옛 계획·인수인계 문서의 언급은 이력이라 두었다.

검증: typecheck 0(개발 서버가 만든 `.next/dev/types/app/api/voice` 생성 파일이 남아 오류가 나 그 폴더만 지웠다), lint 오류 0, `npm test` 326/327(실패 1은 Node 22.13 환경), `/api/voice` 404, `browser-check`(3301) 실패 1은 기존 844×390 캔버스 핀치 환경 항목.

같은 작업에서 선생님 관여 방식 결정(따로 된 층)을 `product-decisions.md` 교사와 메시지 5항에 기록했다.

## 2026-09-14 선생님 관여: 실시간 보기·따로 된 층 표시·버튼 답·손들기 (`claude/teacher-marks-20260914`)

사용자와 정한 방향(`product-decisions.md` 교사와 메시지 5항): 자유 채팅 대신 ① 원본 크기 거의 실시간 보기 ② 아이 원본과 따로 된 층에 선생님 표시 ③ 아이는 큰 버튼으로 답 ④ 아이가 먼저 손들기. 네 가지를 모두 만들었다.

**선생님 화면.** 학생 그림 미리보기가 256px 썸네일 대신 `TeacherLiveView`를 연다. `GET /api/teacher?classroomId&liveStudentId`가 최근 작품 문서(ops)와 그 작품의 최신 표시(답 포함)를 주고, 화면은 아이 도화지와 같은 렌더러(`renderDrawDocument`)로 1024px 원본을 다시 그린다. 미리보기가 열린 동안만 3초마다 부르고, 저장 번호가 바뀔 때만 다시 그린다. 그 위 따로 된 캔버스에 손가락·마우스로 표시를 그리고 짧은 말(40자)을 붙여 "표시 보내기". 보낸 표시는 옅게 겹쳐 보이고, 아이 답(👍 알겠어요 / 🤔 잘 모르겠어요)이 오면 바뀐다. "표시 거두기"로 거둘 수 있다. 완성한 그림에는 보낼 수 없다(서버 409). 지난 그림을 골라 연 경우에는 예전처럼 썸네일을 보여 준다. 학생 카드에 🙋, 제목 옆에 "🙋 N명이 불렀어요", 미리보기에 "선생님을 불렀어요 · 손 내리기". 미리보기 문구의 "스피커로도 들을 수 있어요"(읽어 주기 제거 뒤 남은 틀린 안내)도 지웠다.

**아이 도화지.** 캔버스 묶음에 `mark-canvas`(z-index 4, pointer-events none)를 더해 표시를 그린다 — 점선 안내 층과 같은 방식이라 확대·이동을 따라가고 그리기를 막지 않는다. 상단에 "선생님이 표시를 보냈어요" 카드와 짧은 말, 큰 답 버튼 두 개(52px). 답하면 카드와 층이 바로 사라진다. 머리 줄에 "🙋 선생님" 버튼(좁은 화면은 손 모양만). 받아 오기는 평소 8초, 선생님이 보고 있거나 손을 들었거나 표시가 떠 있으면 3초.

**서버·저장.** 새 표 `teacher_marks`(표시 획 JSON·짧은 말·답·답한 시각)와 `hand_raises`(아이당 한 행). `db/runtime.ts`가 정본이고 `db/schema.ts`·`drizzle/0008_thin_rogue.sql`도 맞췄다 — drizzle-kit이 예전에 기록이 빠진 컬럼 ALTER까지 섞어 만들어 새 표 두 개만 남기고 이유를 주석으로 적었다. 표시 획은 `lib/teacher-marks.ts`가 검사한다(0~1 좌표, 30획·획당 400점·전체 3000점, 어긋나면 고치지 않고 거절). 아이 화면에는 답하지 않은 최신 표시 하나만 가고, 새 표시를 보내면 옛 것은 "바뀜"으로 닫힌다. 손은 표시를 보내거나 그 아이에게 메시지를 보내거나 선생님이 내리면 내려가고, 10분 지나면 보이지 않는다. 아이 응답에는 실명·선생님 정보가 없다. 학급 삭제·학생 보관 때 손 행을 함께 지운다.

**검증.**
- 새 `tests/teacher-marks.test.mjs` 4개: 획 검사, 담임만 보내기(다른 교사·다른 아이 그림·다른 반·완성 그림 거절), 아이 화면 최신 표시 하나, 바뀜·거두기, 한 번만 답하기, 남의 표시에 답 못함, 작품 ops 불변, 손들기 내려가는 조건·10분 만료·다른 교사 거절.
- 실제 브라우저 두 창(선생님 1180×820, 아이 1180×820) 끝까지 29/29: 아이가 그은 선이 선생님 실시간 그림(1024px)에 보임 → 선생님이 동그라미+짧은 말 보냄 → 0.8초 뒤 아이 카드·표시 층 표시, 아이 그림 캔버스에는 주황 픽셀 0, 표시가 떠 있어도 계속 그림, "잘 모르겠어요" → 카드 사라짐·선생님 화면에 답 → 저장 문서에 표시 없음 → 손들기 🙋가 선생님 카드·미리보기에 뜨고 내리면 아이 화면도 내려감 → 390·320·600·844×390 머리 줄 넘침 없음.
- 검증 중 고친 것: 헤드리스에서 아이 탭이 앞에 오면 선생님 탭이 숨김 상태가 되어 받아 오기가 멈췄다(숨은 탭은 안 받아 오는 규칙, 실제 사용에는 영향 없음) — 검사를 두 창으로 바꿨다. 좁은 화면 머리 줄 아이콘 규칙(`.button.ghost.compact:before`)이 손 버튼에도 "⏱"을 붙여 우선순위를 올려 🙋로 바꿨다. 600px에서 손 버튼이 20px로 눌려 줄지 않게 했다.
- typecheck 0, lint 오류 0, 전체 테스트 330/331(실패 1은 Node 22.13 환경), `browser-check`(3301) 실패 1은 기존 844×390 캔버스 핀치 환경 항목.
- 남은 점: 320px에서는 버튼이 하나 늘어 그림 제목 자리가 거의 없어진다. 선생님 실시간 보기는 아이 자동 저장(멈춘 뒤 1.5초, 길어도 6초)만큼 늦다. 미리보기가 열린 동안 선생님 한 명당 3초마다 요청 하나가 더 생긴다.

## 2026-09-14 선생님 실시간 보기 지연 줄이기 (`claude/faster-live-view-20260914`)

사용자가 고른 방법(선생님이 볼 때만 간격 줄이기)으로 바꿨다. 결정은 `product-decisions.md` 교사와 메시지 5항.

- **아이 쪽 자동 저장.** 선생님이 보고 있다는 응답을 받은 동안만 멈춘 뒤 0.5초·길어도 2초마다 저장한다(평소 1.5초·6초 그대로). 보기 여부는 ref로 읽는다 — 저장 효과 의존성에 넣으면 보기 시작·끝마다 바뀐 것 없는 저장이 한 번 더 나가고 "그리는 중…"이 잠깐 뜬다.
- **선생님 쪽.** 미리보기가 열린 동안 3초 → 1초마다 확인한다. 앞 요청이 끝나기 전에는 다음을 보내지 않는다. 가진 작품 id·저장 번호를 함께 보내 같으면 서버가 그림 문서(`ops_json`, 최대 1.25MB)를 DB에서 읽지도 보내지도 않고 표시 상태만 준다.
- **실측(개발 서버 3301, 두 창).** 선을 멈춘 뒤 선생님 화면에 보이기까지 765·1279·1269ms. 0.3초 간격으로 쉬지 않고 이어 그릴 때 첫 선이 보이기까지 2693ms. 바꾸기 전 설정은 멈춘 뒤 1.5초+확인 최대 3초, 계속 그리면 최대 6초+3초였다.
- **남은 점.** 선생님이 미리보기를 연 직후에는 아이 화면이 평소 받아 오기(8초)로 "보고 있음"을 알아채야 짧은 저장이 시작된다. 보는 동안 아이 한 명당 저장 요청(썸네일 포함)과 선생님 확인 요청이 늘어난다.
- **검증.** `tests/teacher-marks.test.mjs`에 같은 저장 번호면 문서를 빼는 응답·번호가 바뀌거나 다른 작품이면 문서를 보내는 응답·짧은 저장 상수와 ref 사용 검사를 더했다. typecheck 0, lint 오류 0, 전체 테스트 330/331(실패 1은 Node 22.13 환경), 표시 흐름 두 창 검사 29/29, `browser-check` 실패 1은 기존 844×390 핀치 환경 항목.

## 2026-09-14 선생님 화면 첫 불러오기 뼈대 (`claude/teacher-loading-skeleton-20260914`)

사용자가 코덱스 시안 1을 골랐다(`product-decisions.md` 교사와 메시지 7항, P-010 종결).

- **재현한 문제.** 선생님 화면은 첫 응답 전까지 빈 페이지 위 점선 상자 "수업실을 준비하는 중…"만 보였다. 첫 응답이 401이 아닌 실패(다른 선생님 계정의 학급 403 등)면 `authorized`가 null로 남아 상자가 안내 없이 끝나지 않았다 — 사용자 크롬이 다른 로컬 선생님으로 자동 로그인돼 시연 반 주소가 6초마다 403인 상태를 서버 로그로 확인.
- **바꾼 것.** `TeacherApp.tsx`의 `TeacherLoading`: 실제 로고·"학급 목록" 링크, 학급 이름·버튼 2개·탭 3개·학생 그림 카드 8장(실제 카드와 같은 1.44 비율)의 반짝이는 회색 뼈대, "학급을 불러오고 있어요". 학급 목록 주소(`/teacher`)는 뒤로 링크 없이 "학급 목록을 불러오고 있어요". 실패하면 안내 줄이 서버 오류 문구 + [학급 목록으로][다시 시도](44px)로 바뀌고 `aria-busy`가 꺼지며 뼈대는 멈춰 흐려진다. 움직임 줄이기 설정이면 반짝임·회전이 멈춘다. 학급을 받은 뒤 다시 불러오는 경로(`!classroomData`)도 같은 화면을 쓴다.
- **검증.** 실제 브라우저에서 선생님 API 응답을 붙잡아 불러오는 중 상태를, 반 주인이 아닌 로컬 선생님으로 로그인해 실패 상태를 1440·1180·390에서 렌더: 가로 넘침 0, 뼈대 30개, 실패 시 버튼 115×44·89×44, "다시 시도"는 여전히 권한 없음이면 안내 유지, 주인 계정으로 로그인 뒤 "다시 시도"하면 학급 화면이 뜬다. 시안과 1440×900 캡처가 같은 배치다. `tests/teacher-workspace-data.test.mjs`에 옛 문구 제거·두 분기·실패 안내·움직임 줄이기 규칙 검사를 더했다. typecheck 0, lint 오류 0, `npm test` 331/332(실패 1은 Node 22.13 환경).
- **로컬 시연 안내 정정.** 앞서 알려 준 시연 반은 `demo@local.test` 선생님의 반이라, 다른 선생님으로 자동 로그인된 브라우저에서는 열리지 않는다. 이제는 "이 학급을 볼 권한이 없어요"와 [학급 목록으로]가 보인다.

## 2026-09-14 그리기 화면 머리 줄 버튼 묶음 시안 1 반영 (`claude/studio-header-quiet-20260914`)

사용자가 코덱스 시안 1을 골랐다(`product-decisions.md` 교사와 메시지 8항, P-011 종결). 처음에 Claude가 "이부분"을 그리기 화면 전체로 받아들여 전체 시안 4종을 먼저 받았고, 사용자가 버튼 묶음이라고 바로잡았다.

- **바꾼 것.** `DrawingStudio.tsx` 머리 줄의 `.button ghost/grimi/primary compact` 네 버튼과 💌 버튼을 `.studio-actions` 묶음(과정 보기·몽그리 부르기·선생님·선생님 말씀) + 따로 된 `.studio-finish` 완성 버튼으로 바꿨다. 아이콘은 인라인 SVG(`StudioIcons.tsx`에 PlayCircle·Hand·Mail 추가, 패키지 추가 없음), 몽그리는 기존 `/brand/mongri/listening.png` 얼굴. 몽그리를 기다리는 동안은 점선 원이 돈다(움직임 줄이기면 멈춤). `StudentMessageCenter`에 `header` 모드를 더해 "선생님 말씀" 글자와 빨간 수를 붙였다(다른 모드는 그대로).
- **지운 것.** 좁은 화면에서 머리 줄 버튼을 ⏱·✨·🙋·✓ 이모지로 접던 규칙, 머리 줄 `.button.compact` 크기 규칙, 옛 손들기 버튼 규칙(`globals.css`).
- **휴대폰.** 세로(≤720)는 머리 줄을 두 줄로 — 둘째 줄 4칸(아이콘 위·글자 아래, 56px), 완성은 첫 줄. 머리 줄이 121px이 되어 새 말씀 알림·선생님 표시 카드·보고 있음 알림이 둘째 줄을 가리던 것을 실측으로 찾아 그 아래로 내렸다. 가로로 눕힌 좁은 화면은 한 줄에 아이콘만(글자는 화면 읽기용으로 남김).
- **검증.** 안 읽은 말씀 2개·손 든 상태로 1440·1180·1024·820×1180·768×1024·844×390·390·320 렌더: 머리 줄·페이지 가로 넘침 0, 버튼 모두 44px 이상이고 화면 안, 새 말씀 알림이 버튼을 가리지 않음, 빨간 수 2·손 내리기 표시. 선생님 표시 두 창 검사 29/29(손들기 버튼 좁은 화면 포함). `tests/mobile-css.test.mjs`의 옛 이모지 규칙 검사를 새 묶음 구조·이모지 없음·완성 진초록·휴대폰 두 줄 검사로 바꿨다. typecheck 0, lint 오류 0, `npm test` 331/332(실패 1은 Node 22.13 환경), `browser-check` 실패 1은 기존 844×390 캔버스 핀치 환경 항목.
- **남은 점.** 휴대폰 세로에서는 머리 줄이 약 60px 높아져 종이가 그만큼 작아진다. 도구 막대(P-012)는 아직 고르지 않았다.

## 2026-09-14 그리기 도구를 아래 도구 막대(B안)로 교체 (`claude/tool-dock-crayon-box-20260914`)

사용자가 코덱스 시안 B를 골랐다(`product-decisions.md` 교사와 메시지 9항, P-012 종결).

- **도구 그림.** 코덱스로 세워진 연필·크레용·마커·수채붓·지우개·나비 6장을 받았다(자홍 배경, 지금 색으로 칠할 끝·띠는 초록). 이미지 생성이 색을 정확히 지키지 않아 `scripts/build-dock-tools.mjs`가 "정도"로 갈라 바탕 그림(`public/drawing-tools/dock/<id>.webp`, 초록 자리는 옅은 회색)과 칠하기 틀(`<id>-tint.webp`)을 만든다. 화면은 틀을 CSS mask로 써서 `--dock-color`를 칠하고 빛 방향 그라데이션을 얹는다. 원본은 무손실 webp로 `docs/design-assets/studio-tool-dock/tools/`에 두었다(다시 만들어도 결과 동일 확인). 옛 가로 도구 사진(`public/drawing-tools/*.png`·manifest)은 쓰는 곳이 없어 지웠다.
- **화면.** `DrawingStudio.tsx`의 `aside.tool-panel`(오른쪽 카드·좁은 화면 격자)을 `aside.tool-dock`으로 바꿨다. 붓 4·지우개는 `DOCK_TOOLS` 한 목록, 대칭은 따로. 굵기는 고른 도구를 한 번 더 누르면 뜨는 5단 점 버튼(`.dock-width`, 예전 슬라이더 제거). 색은 8색+무지개 → 12색 창(`.dock-palette`) → 색 더보기 대화상자. ⋯(`.dock-sheet`)에 채우기·도형·글씨·도형/글씨 옵션·입력 방법·전체 지우기를 그대로 옮겼다. 색 고르기 로직은 `pickColor` 하나로 합쳤다. 도형·글씨를 고를 때는 옵션을 이어 고르도록 더보기 창을 열어 둔다(브라우저 검사 중 도형 모양 선택이 안 보이던 빈틈을 찾아 고침). 도구로 스크롤하던 `mobile-tool-peek`와 `toolPanelInView` 관찰은 없앴다.
- **배치·CSS.** 막대는 `position:fixed`이고 `.studio-body`가 `--dock-space`(태블릿 126px·휴대폰 세로 160px·낮은 가로 80px)만큼 아래를 비운다. 격자의 오른쪽 도구 칸(180·200·130px)과 옛 도구 판 전용 규칙 약 150줄, 320px 고정 도화지 우회를 지웠다. 폭 900px 아래는 색을 지금 색 버튼 하나로 줄인다. 작은 화면의 몽그리 판은 처음에 막대 위로 올렸다가 320×568·844×390에서 접은 판과 막대가 겹쳐 도화지가 사라져(브라우저 검사 실패) 예전처럼 막대를 덮고 판이 열린 동안 막대 자리를 비우지 않게 되돌렸다.
- **검증.** 1440·1180·1024·820×1180·768×1024·844×390·390·320에서 도화지 아래끝이 막대 위끝보다 위(겹침 0), 가로 넘침 0, 막대 버튼 모두 44px 이상·화면 안, 크레용 두 번 → 굵기 5단, 무지개 → 12색 창, ⋯ → 채우기·입력 방법·전체 지우기가 화면 안에 열림. `browser-check`(3301)를 막대 기준으로 고쳐(도구 6개·그림 불러옴·더보기 안 채우기/도형·색 창) 대칭·되돌리기·채우기·도형 2탭·몽그리 판 접기까지 통과, 실패 1은 기존 844×390 핀치 환경 항목. 선생님 표시·손들기 두 창 검사 29/29. 옛 도구 판 구조를 확인하던 테스트(`drawing-tools`·`mobile-css`·`responsive-drawing-ui`·`guide-demonstration`·`lesson-step-progress`·`feedback-20260809`)를 막대 계약(항상 보임·44px·도화지 안 가림·몽그리 판 규칙)으로 바꿨다. typecheck 0, lint 오류 0, `npm test` 331/332(실패 1은 Node 22.13 환경).
- **남은 점.** 태블릿 가로에서 막대만큼 도화지가 낮아진다(1180×820에서 627px). 굵기는 고른 도구를 한 번 더 눌러야 보여 처음에는 설명이 필요할 수 있다. 흰색을 고르면 붓 끝이 크림색 몸통과 비슷해 덜 보인다.

## 2026-09-15 굵기를 1픽셀 단위 끌기 슬라이더로 (`claude/width-slider`)

사용자가 굵기 5단 점 대신 픽셀 단위로 끌어 고르기를 원했다(`product-decisions.md` 교사와 메시지 10항).

- **저장 형식.** `lib/drawing-model.ts`의 `STROKE_WIDTHS` 목록을 없애고 `STROKE_WIDTH_MIN 1`·`STROKE_WIDTH_MAX 60`·`isStrokeWidth`(정수·범위)로 획과 도형 굵기를 검사한다. 렌더러는 원래 굵기를 숫자로 곱해 쓰므로 바꿀 곳이 없었다.
- **화면.** `.dock-width`를 `input type="range"`(1~60, 1씩) + −·+ 버튼 + 지금 굵기 점·숫자로 바꿨다. 트랙은 고른 만큼 진초록으로 칠한다. 전역 `input` 규칙(테두리·50px 높이·초점 그림자)이 슬라이더에 씌워져 있어 막았다.
- **검증.** 실제 브라우저(1180×820 마우스, 820×1180·844×390·390×844·320×568 터치)에서 슬라이더를 끌어 값이 16→19~24로 바뀌고, + 한 번 +1, − 두 번 −2, 끄는 동안 창이 닫히지 않고 화면 안이며 44px 미달 0. 굵기 22로 크레용 한 획을 그어 서버 저장본이 `crayon:22`로 남는 것까지 확인. `drawing-upgrade` 테스트를 범위 계약(1·60 통과, 0·61·12.5·"12" 거절, 도형 동일)으로 바꿨다. typecheck 0, lint 오류 0, `npm test` 334/334(Node 22.23 — 22.13은 `.ts` 불러오기를 못 해 40건이 환경 실패).
- **남은 점.** 휴대폰 세로에서 슬라이더 길이가 약 128px이라 끌기 한 칸이 약 2픽셀이다 — 정확히 맞출 때는 −·+를 쓴다.

## 2026-09-15 과정 보기 제거·도화지 화면 가득·확대 축소·도구 막대 접기 (`claude/studio-fullscreen-zoom`)

사용자 지시 네 가지(`product-decisions.md` 교사와 메시지 11~14항).

- **과정 보기.** `DrawingStudio.tsx`에서 버튼·`timelapseOpen`·`TimelapsePlayer` 연결을 뺐다. `TimelapsePlayer`는 가족 보기(`FamilyView`)가 계속 쓴다. 휴대폰 세로 둘째 줄은 3칸.
- **화면 가득.** 원인: 새 도화지 세로가 512~1024(가로 2:1~정사각)·16 단위로만 저장돼 휴대폰 세로·가로에서 초록 여백이 남았고, `.studio-body`가 막대 높이만큼 아래를 비웠다. `lib/drawing-model.ts` 범위를 320~2240·1 단위로 넓히고(옛 값은 범위 안), `.studio-body` 아래 비움과 `.canvas-zone` 여백을 없앴다. 막대는 도화지 위에 뜨고, 안내 문구는 `--dock-space`만큼 올린다(접으면 64px).
- **확대·축소.** `.zoom-controls`(+ / 배율·화면 맞춤 / −)가 옛 `.zoom-reset`을 대신한다. `lib/canvas-view.ts`에 `zoomView`(한 점 붙잡고 배율 변경)를 더하고, `clampView`·`pinchView`가 세로 길이를 따로 받게 고쳤다 — 전에는 세로 이동도 가로 길이로 가둬 세로로 긴 도화지 아래쪽까지 옮길 수 없었다. 트랙패드는 `wheel`을 passive:false로 직접 붙여 ctrl+휠은 확대, 확대 중 휠은 옮기기.
- **접기.** `aside.tool-dock`에 `.dock-toggle`(막대 위 탭, 누르는 자리 44px)을 두고 `dockOpen`으로 `is-collapsed`를 붙인다. 접으면 막대 틀은 투명·클릭 통과, "도구" 단추만 보인다.
- **검증.** 새 작품으로 1440·1180·1024·820×1180·768×1024·844×390·390×844·320×568 모두 도화지와 화면 사이 여백 0, 가로 넘침 0, 과정 보기 없음, + 두 번 150%→225%, − 한 번, 배율 단추로 1배, 확대 단추 44px·화면 안, 접으면 도구 숨김·"도구" 단추 44px 이상·그 자리 아래가 도화지, 다시 펴짐. 1180에서 ctrl+휠 확대·휠 옮기기(브라우저 페이지 배율은 1 그대로). 390×844에서 225% 확대 뒤 가운데에서 45px 그은 획이 x 0.5→0.551(계산값과 같음), 막대를 접고 맨 아래에 그은 획이 y 0.972로 저장. 그 세로 도화지(1898)를 1180×820에서 열면 가운데 407px 폭으로 놓이고 1.5배에서 아래 끝까지 옮겨짐(−377px, 계산값과 같음). `browser-check`는 도화지가 막대·확대 단추 밑까지 차면서 가려진 자리를 누르던 3곳(대칭 줄 높이, 채우기 자리, 도형 전 더보기 창 닫기)을 고쳐 모두 통과(핀치 SKIP은 기존 환경 항목). typecheck 0, lint 오류 0, `npm test` 335/335(Node 22.23).
- **남은 점.** 막대를 펼친 동안 도화지 아래쪽 막대 밑은 가려진다(접거나 확대해 옮겨서 그림). 이미 그린 작품을 다른 모양 화면에서 열면 양옆이나 위아래에 여백이 남는다. 오른쪽 위 확대 단추 밑 작은 자리는 확대해 옮겨야 그릴 수 있다.

## 2026-09-15 이미 그린 그림도 화면 가득·막대가 미끄러지는 접기 (`claude/studio-fill-drawn`)

사용자가 크롬에서 이콩이 그림을 열어 보고 도화지가 화면을 채우지 않는다고 했다(`product-decisions.md` 교사와 메시지 15항).

- **재현한 원인.** 그 작품(`artwork_d45da…`)은 선 14개·세로 720이었다. 앞 작업은 새 작품만 화면 비율에 맞추고, 그린 작품은 비율대로 가운데 두었다(검증도 빈 도화지로만 했다).
- **세로로 긴 화면.** `lib/drawing-model.ts` `growDrawOps`가 모든 점의 y를 새 높이로 다시 나누고 위아래로 같은 만큼 덧댄다. 가로 1024 기준 값(굵기·글씨·스티커)은 그대로라 화면 모양이 같고, 가운데 정렬이라 수업 점선과도 맞는다. `fitPaperToScreen`이 되돌리기·다시하기 묶음과 전체 지우기 되돌림까지 함께 늘리고 자동 저장한다. 긋는 중·완성·저장 충돌 중에는 하지 않는다.
- **옆으로 넓은 화면.** `.canvas-wrap`은 비율과 상관없이 자리를 꽉 채우는 틀이 되고, `.canvas-stack` 크기는 `coverPaper`(틀을 덮는 1배 종이)로 JS가 정한다. `clampView`·`zoomView`·`pinchView`가 틀과 종이 크기를 따로 받아 1배에서도 넘친 만큼 옮기고, 휠·두 손가락이 그만큼 움직인다. 1배로 돌아가면 종이 가운데를 보여 준다.
- **접기.** 접으면 `translate`로 막대가 화면 아래로 0.32초 동안 내려가고 `visibility:hidden`이 뒤따른다. 손잡이 탭만 남고, 탭을 끄는 방향으로 접기·펴기가 된다.
- **검증.** 세로 720 그림(가운데 선 y 0.5·위 끝 선 y 0.02)을 넣고 실제 브라우저로 열었다. 1180×820에서는 여백 0에 폭 맞춤, 아래로 76px 넘쳐 휠로 끝까지 옮겨지고 저장본은 그대로(720). 390×844에서는 여백 0, 1898로 늘어나 y가 0.5·0.3179로 저장(가운데 선 그대로, 위 끝 선은 덧댄 만큼 내려감), 빨간 선 픽셀 두께 49줄 그대로. 늘린 1898 그림을 1180×820에서 다시 열면 폭 맞춤·가운데·1433px 옮김. 새 작품 8개 화면 여백 0. 390×844에서 225% 확대 획 좌표·막대 접고 맨 아래 획 좌표가 계산값과 같음. 접기: 누르면 0.12초에 막대 위끝 684→830, 끝나면 850(화면 밖)·도구 숨김, 탭을 위로 48px 끌면 684로 복귀, 펼친 채 위로 끌면 그대로. typecheck 0, lint 오류 0, `npm test` 337/337(Node 22.23). `browser-check`는 같은 작품을 320×568→390×844→844×390 순서로 여는데, 마지막 가로 화면에서 도화지가 위아래로 넘쳐 "도화지가 잘리지 않음" 등 4건이 실패했다. 이것은 이번 결정 그대로의 동작이라, 검사를 "도화지가 자리를 빈틈 없이 채움"으로 바꾸고 누르는 좌표를 화면에 보이는 띠 안으로 옮겼다(도형 끝점은 확대 단추를 피함). 모두 통과(핀치 SKIP은 기존 항목). 검사 중 넘친 종이를 담은 `overflow:hidden` 틀이 `scrollIntoView`로 몰래 스크롤될 수 있어 `overflow:clip`을 더했다.
- **남은 점.** 세로 휴대폰에서 늘린 긴 그림을 가로 화면에서 열면 한 번에 약 3분의 1만 보여 위아래로 옮겨야 한다. 창을 키웠다 줄이면 늘어난 도화지는 줄지 않고 넘친 만큼 옮겨 본다. 늘리기 전에 선생님이 보낸 표시는 새 높이에서 조금 어긋나 보일 수 있다.

## 2026-09-09 사용자 로컬 체험 시작

- 사용자 요청에 따라 `new_wiggle`에서 `npm.cmd run dev -- --port 3012` 실행. `/api/health`가 local/ok임을 확인했다.
- `scripts/check-book-browser.mjs` 재실행 통과: 모의 제공자로 일괄 평가·재시도·ZIP·인쇄 준비·견적·주문 및 4개 화면 크기를 검증했다.
- 직접 체험 주소: `http://localhost:3012/teacher/class/class_4cc7c5e4c05e4b4e/books`, 주문은 뒤에 `/orders`. 테스트 학급 수업 코드 7704, 학년 4·반 7, 가상 학생 2명과 글·배경색만 있는 3쪽 완성 그림책 2권을 로컬 파일 DB에 준비했다.
- 사용자 체험 서버는 실제 앱이며 모의 AI/제작사 응답을 끼워 넣지 않았다. API 키가 없으면 평가 연결 대기·주문 연결 준비 안내가 정상이다. 운영 배포·원격 DB 변경 없음.

## 2026-09-09 외부 PDF 가져오기·교사 편집

- 교사 그림책 목록에 PDF 다중 선택·파일별 학생/제목 지정·진행/실패 표시를 추가했다. 30MB·1~24쪽·50권까지이며 각 학생을 명시적으로 선택한다. PDF.js로 쪽 전체를 비율 유지해 렌더링하고 3.5MB 이하 PNG로 나눠 전송한다. 원본 파일 자체는 서버에 저장하지 않는다.
- 기존 그림책 모델/이미지 저장/피드백/주문을 재사용한다. 모든 쪽 저장 후에만 완성 처리하며 중간 저장은 초안으로 보존한다. ‘편집 중인 책’에서 초안을 열 수 있다.
- 교사 `/api/teacher/book-editor/[id]` 경로는 학생 토큰 없이 교사 쿠키·담당 학급 소유권을 확인하고 기존 저장 충돌·이미지 소유권 검증을 공유한다. 기존 학생 경로는 학생 인증을 유지한다.
- 기존 StorybookEditor를 교사 경로에서도 사용한다. 원본 PDF 글자 개별 편집은 지원하지 않으며, 쪽 배경으로 보존하고 글/그림 추가·배경 교체·쪽 구성 편집을 제공한다. 수정 후 완성하면 새 버전 평가·인쇄 준비에 반영된다.
- 실측 중 ‘완성하기’ 뒤 예약 자동 저장이 초안으로 되돌리던 결함을 수정했다. 미리보기 위로 편집 화면 텍스트가 올라오던 z-index 결함은 stage isolation으로 수정했다.
- production build + 전체 테스트 325/325, typecheck 통과, lint 0 errors(기존 경고 10), 두 diff check 통과. 브라우저에서 3쪽 PDF 렌더/가져오기→교사 편집→완성 상태 유지→모의 AI 평가→모의 인쇄 준비 통과 및 미리보기 육안 확인. 기존 일괄 평가/ZIP/주문과 4개 크기 화면 검증도 통과했다.
- PDF.js worker/CMap/font/WASM 및 LICENSE는 predev/prebuild에서 설치 패키지로부터 public/pdfjs에 복사한다. 생성 파일은 Git/ESLint 대상에서 제외했다.
- 로컬 3012 재시작. main 병합·커밋·push·배포 없음. 실제 제공자 호출은 하지 않았다.
- 최종 기존 전체 `npm run check:browser -- http://localhost:3012`도 320×568·390×844·844×390 전 항목 통과. 이전 실행의 QR 번호 화면 2건 실패는 이번 재실행에서 재현되지 않았으므로 당시 기록은 이력으로 남긴다. 로컬 실제 사용자 URL에서도 업로드 패널·루브릭 로딩과 3개 폭 가로 넘침 없음을 확인했다.

## 2026-09-16 운영 저장소 합치기 (`claude/deploy-sync-20260916`)

운영 배포는 옛 저장소 `yonghwan86/wiggle_web`(Vercel 연결)이 빌드한다. 2026-09-13에 올린 PR #3(정본 동기화)은 2026-09-16에 병합 없이 닫혔고, 그 사이 같은 저장소에 PR #4(관리자 대시보드·그림책 루브릭 피드백·PDF 가져오기·인쇄 주문, 84파일 약 1.1만 줄)가 병합됐다. 그래서 정본(`wwwiggle/new_wiggle`) 83개 커밋과 PR #4를 한 나무로 합쳤다.

- **충돌 12개 처리.** `.gitignore`·`app/globals.css`는 양쪽을 모두 남겼다(옛 저장소의 번호 재입장 CSS는 정본에서 이미 폐기된 흐름이라 가져오지 않았다). `package.json`은 양쪽 의존성을 합치고 `package-lock.json`을 다시 만들었다.
- **교사 API·교사 화면·입장 흐름은 정본 그대로다**(2026-09-16 사용자 지시: "방 참여하는 거는 QR + 번호입장 즉 우리가 작업한게 맞고, 교사 API쪽도 우리쪽게 맞아, 교사화면도 내쪽이 맞고"). 한 번 합쳐 넣었던 옛 저장소의 학교 이름·학년·반 입력과 `isAdmin` 링크를 되돌렸다.
- **되돌려도 인쇄 주문은 동작한다.** 학교 이름·학년·반은 `lib/print-requests.ts`가 주문할 때 `classroom_profiles`·`classroom_book_settings`에 upsert하고, 조회는 값이 없으면 빈 값으로 내려준다. 관리자 화면은 `/api/admin`이 서버에서 `isAdmin`을 직접 확인하므로 교사 화면 링크 없이도 주소로 열린다.
- **마이그레이션.** 양쪽이 `0008`을 동시에 썼다. 옛 저장소의 `0008_huge_secret_warriors`·`0009_unknown_supernaut`를 살리고, 정본의 `0008_thin_rogue`(손들기·선생님 표시)는 지운 뒤 합친 스키마에서 `0010_red_speed_demon`으로 다시 만들었다. DB에는 실행하지 않았다 — 파일만 만든다.
- **문서.** 제품 결정 6·7·8항은 정본 문장을 기준으로 두고, 옛 저장소가 더한 "완성 그림책 교사용 루브릭"을 예외로 덧붙였다.
- **검증.** typecheck 0, lint 오류 0, `npm test` 346/346(양쪽 테스트 모두 — 관리자·인쇄·PDF·루브릭 통합 테스트 포함), `browser-check` 통과(핀치 SKIP은 기존 환경 항목).
- **남은 점.** 옛 저장소의 번호 재입장 테스트 2개는 정본이 참여 코드 입장으로 바꾼 흐름이라 지웠다. 운영 반영은 이 PR을 저장소 주인이 병합해야 한다.

## 2026-09-17 README 서비스 소개서·복사 표시 체크 (`claude/readme-service-intro`)

- **README.** 아키텍처 중심이던 문서를 서비스 소개서로 다시 썼다(사용자 요청). 왜 쓰는지(AI 시대에는 잘 그리는 손보다 보는 안목), 몽그리의 두 역할(확장 협업자·틀리는 해석자)과 예시 대화, 아이가 얻는 것, 교실에서 쓰는 3단계, 근거(See-Think-Wonder·Studio Thinking·Storyline Method, StoryDrawer·Weng et al.), 개인정보 약속을 담았다. 개발자 안내는 실행 명령과 문서 링크만 남겼다. 옛 화면 캡처 3장은 지우고 지금 화면으로 다시 찍었다(`docs/images/landing.png`·`studio.png`·`entry.png`). 주소는 `https://www.kkumtle.app`로 갱신했다.
- **복사 표시.** `app/components/useCopyFeedback.ts`를 두고, 교사 학급 목록·QR 대화상자·가족 링크·명단 설정의 복사 단추가 성공하면 그 자리에서 초록 체크로 1.8초 바뀐다(`product-decisions.md` 교사와 메시지 16항). 실패만 기존 안내 줄을 쓴다. `.sr-only` 규칙이 전역에 없어 13곳의 읽어 주기 전용 글이 그냥 보이던 것도 함께 고쳤다.
- **검증.** 실제 브라우저에서 클립보드 권한을 준 뒤 교사 학급 목록의 복사를 눌러 단추 글자가 체크(svg 1개)로 바뀌고, 들리는 안내가 "수업 코드를 복사했어요."로 바뀌며, 1.8초 뒤 "복사"로 돌아오는 것을 확인했다. 권한이 없을 때는 체크 없이 실패 안내 줄이 뜬다. 단추 크기는 44×44 그대로다. typecheck 0, lint 오류 0, `npm test` 347/347, `browser-check` 통과.

## 2026-09-18 관리자 진입 PR #8 운영 배포 확인

- 사용자가 PR #8을 운영 저장소 main에 병합했다. 병합 커밋: `f0b9e880e7d96a4b51551db6330d791f5f2b8877`, 병합 시각: 2026-09-18 21:05:35 KST.
- 해당 커밋의 GitHub Vercel 상태가 success / Deployment has completed임을 확인했다.
- https://www.kkumtle.app 읽기 전용 확인: /api/health 200 (production), 비로그인 /teacher는 Google 인증으로 307, /admin은 관리자 로그인 안내, /api/admin은 403을 반환했다.
- 운영 관리자 로그인 세션으로 버튼 표시·이동을 직접 확인하지는 않았다. 관리자/일반 교사별 버튼 및 접근 제어는 앞선 로컬 브라우저·HTTP 테스트에서 검증했다.


## 2026-09-18 PDF 가져오기 24쪽 제한 제거 (`codex/pdf-import-all-pages`)

- 운영 main `f0b9e88`에서 분기. PDF 읽기·접수·문서 검증에서 페이지 수 상한을 제거했다. 교사 이미지 저장의 60개 제한도 제거해 긴 PDF의 각 쪽을 모두 보관한다. 학생의 기존 직접 제작 한도는 유지한다.
- 교사 편집기는 24쪽 이상 책에서도 쪽 추가·복제를 허용한다. 문서 메타데이터 한도는 전송 제한 아래 3.5MB로 늘렸고, 파일당 30MB와 PNG당 3.5MB는 유지한다. 쪽 수와 주문 가능 범위는 별개다.
- PDF 가져오기 중 명시적인 429 응답은 같은 요청으로 60초 후 재시도(최대 3회)한다. 성공한 쪽은 초안에 순서대로 저장하고, 마지막 쪽까지 저장했을 때만 완성으로 표시한다.
- 인쇄 규격 24~130쪽·짝수, 최소 쪽 수 보충, 판형·도련 검사는 변경하지 않았다.
- 검증: production build 성공, 전체 테스트 351/351 통과, typecheck 통과, lint 오류 0(기존 경고 13). 모델 검증은 25·131·1,000쪽 보존, HTTP 검증은 131쪽 저장·재열람·권한 및 충돌 검사를 포함한다. 일반 browser-check는 320×568·390×844·844×390에서 통과했다.
- 실제 브라우저에서 131쪽 PDF 전 쪽 가져오기·서로 다른 이미지 131개·완성 저장·재열람·마지막 쪽 수정 및 429 후 자동 재개를 검증했다. 가져오기 화면 3개 크기에서도 가로 넘침 없음. 재현 스크립트: `scripts/check-pdf-import-browser.mjs`.
- 검증 완료한 변경을 기능 브랜치 커밋·push와 운영 main 대상 PR로 제출한다. 운영 데이터는 수정하지 않았으며 main 병합과 배포 후 운영 실측은 별도다.


## 2026-09-19 피드백 진행 표시·학생별 모음 (`codex/student-feedback-progress`)

- PR #9 병합 후 운영 main `f96d54c`에서 분기했다. 피드백 요청 접수·작성·PDF 다운로드에 진행 패널, 회전 표시, 실제 처리 수, 경과 시간, 현재 학생/책 및 버튼 상태 문구를 추가했다. 임의 예상 퍼센트는 표시하지 않는다.
- 처리 중 작업을 주기적으로 읽어 현재 책과 완료 결과를 갱신한다. 새로고침으로 들어와도 서버의 처리 중 상태를 표시하고 완료를 갱신한다. 중단 예약·이어가기, 오류 후 재시도, 완료 안내를 행동 근처에 표시한다.
- 그림책을 학생 ID별/번호순으로 구분하고 하단에 학생별 완성 피드백 PDF 목록을 추가했다. 한 학생의 여러 책은 개별 PDF 및 ZIP으로 받으며 파일명이 같아도 각각 보존한다. 이전 루브릭으로 만든 현재 책 버전의 결과는 ‘이전 평가 기준’으로 표시한다. 수정 전 책의 오래된 피드백은 기존 API 정책대로 내려받기 대상에서 제외한다.
- 학년·반·학생 번호·이름이 없으면 PDF에 필요한 정보를 먼저 등록하도록 안내한다. 저장·권한·평가·PDF 생성 서버 경로는 변경하지 않았다.
- 검증: production build, 전체 테스트 351/351, typecheck 통과. lint 오류 0(기존 경고 13). 일반 browser-check 320×568·390×844·844×390 통과.
- `scripts/check-feedback-library-browser.mjs`: 같은 이름의 학생 2명/책 3권으로 학생 ID 구분, 실시간 생성 상태, 중단/이어가기, 새로고침 중 처리 상태 자동 갱신, 학생별 PDF 유지, 동명 책 2권 ZIP의 서로 다른 유효 PDF 2개, 개별 PDF, 다운로드 진행 및 오류 복구를 실제 Chrome에서 검증했다. 320/390/844/1440 폭 넘침 없고 모바일 캡처 확인.
- 검증한 기능 브랜치를 커밋·push 후 운영 main 대상 PR로 제출한다. 운영 데이터 변경과 main push는 하지 않았다.
## 2026-09-20 큰 도화지와 선명한 선 (`claude/big-canvas`)

사용자 요청 두 가지: 축소할 때 선이 깨져 보이는 것과, 삼성 노트처럼 축소하면 종이가 넓어지는 큰 도화지(`product-decisions.md` 교사와 메시지 17항, 두 안 중 2번).

- **선명도.** `renderDocument`가 늘 가로 1024로만 그려 1512px 창·레티나에서 3배 가까이 늘여 보였다. `rasterWidthFor`(보이는 크기 × 기기 픽셀, 상한 3072px·900만 픽셀)로 바꾸고 배율이 바뀌면 다시 그린다. 실측: 1180×820·DPR2에서 100% 2361px, 225% 3072px(전에는 둘 다 1024px).
- **큰 도화지.** `DrawDocument.span`(1~3, 없으면 1)을 더했다. 새 작품은 3 — 100%에서 화면 한 장, 끝까지 축소(33%)하면 화면 3장 너비의 도화지 전체가 보인다. 좌표·가로 단위(1024)·렌더러는 그대로라 저장 형식이 바뀌지 않는다. 화면에서 고른 굵기(1~60픽셀)·글자 크기는 `toDocumentUnits`로 span배 저장하고, 선생님 표시 굵기도 같은 배로 그린다. `clampView`·`zoomView`·`pinchView`는 배율 한계를 인자로 받아 1/span~4배를 쓴다.
- **내보내기.** 넓은 도화지는 흰 여백이 대부분이라 도화지 전체를 1024로 줄이면 그림이 약 341px로 들어간다(그림책은 저장된 PNG를 그대로 쓰고, 자동 크롭은 보기만 고친다 — 2026-09-20 조사). 그래서 `contentBounds`로 그린 칸만 잘라 긴 변 1536(썸네일 256)으로 내보낸다.
- **검증.** 실제 브라우저(1180×820)에서 새 작품이 100%·종이 3542×2262(자리 1180×754)로 열리고, 축소 끝(33%)에서 종이가 자리에 딱 맞으며(여백 0), 16픽셀로 그은 획이 48단위로 저장되고 `span: 3`이 남는 것을 확인했다. 완성까지 진행해 완성 PNG가 그린 칸만 1516×1376으로 나오는 것(도화지 전체였다면 1024×654)도 확인했다. `npm test` 342/342, typecheck 0, lint 오류 0, `browser-check` 통과. `browser-check`는 도화지가 화면보다 커진 만큼 두 곳을 고쳤다 — 검사 전에 아이처럼 축소 단추로 도화지 전체를 보이게 하고, "도화지 한가운데" 대신 "그리기 자리 한가운데"가 도화지인지 본다.
- **남은 점.** 넓은 도화지에서 채우기(페인트통)는 상한 래스터(최대 900만 픽셀) 기준으로 도는 만큼 아주 확대한 상태에서는 가장자리가 1~2픽셀 뭉툭할 수 있다. 옛 작품(span 1)은 도화지가 넓어지지 않는다 — 이미 그린 그림이 도화지를 꽉 채우고 있어, 넓히면 그 그림이 1/3로 작아진다.

## 2026-09-20 기다리는 화면 하나로 (`claude/big-canvas-20260920`)

그리기 화면만 글자 한 줄짜리 대기 화면을 쓰고 있었다(`product-decisions.md` 교사와 메시지 18항).

- `app/components/WaitMongri.tsx`를 만들어 입장 확인(`JoinClient`)과 그리기 화면(`DrawingStudio`의 불러오는 중, `app/student/draw/[id]/page.tsx`의 Suspense)이 같은 화면을 쓴다. 모습은 2026-09-09 입장 화면 시안 그대로이고 문구만 화면에 맞춘다.
- 실패 문구가 영어로 나올 수 있어(`Failed to fetch`) 우리말이 아니면 "연결이 잠깐 어려워요. 다시 들어와 줄래?"로 바꿔 보여 준다.
- 글자만 있던 `.drawing-loading` 규칙과 문구는 지웠다.
- (같은 날 보고) `/student`에서 도화지로 넘어가는 중간 화면(`StudentEntry`)에 옛 "🎨 도화지를 펴는 중…" 카드가 남아 먼저 스쳐 지나갔다. 그 화면도 같은 대기 화면을 쓰게 바꿨다(실패했을 때만 다시 해 보기 안내를 연다). 실측: `/student`부터 도화지까지 0.4초 간격으로 훑어 옛 화면이 한 번도 나타나지 않는 것을 확인했다.
- 검증: 실제 브라우저에서 작품 API를 막아 대기 화면을 띄워 1180×820·390×844 모두 몽그리 그림(330×236·220×157)과 두 줄이 입장 화면과 같게 나오는 것을 확인했다. typecheck 0, lint 오류 0, `npm test` 356/356, `browser-check` 통과.

## 2026-09-20 명단 추가를 번호 칸·이름 칸으로 (`claude/roster-number-name-fields`)

사용자 요청: "각자 칸의 번호칸 이름칸 나눠서 적도록" — 한 칸에 `1 김민준`처럼 몰아 적던 텍스트 상자를 없앴다.

- **화면.** `명단에 학생 추가`는 줄마다 번호 칸과 이름 칸을 따로 받는다. 번호는 기존 명단의 마지막 번호 다음부터 미리 채우고(2명이 있으면 3번부터), 마지막 줄에 이름을 적으면 다음 줄이 저절로 열린다. 줄 지우기(×)와 `학생 한 명 더`를 둔다. 25명이 들어가도 다이얼로그가 늘어나지 않게 목록만 세로로 스크롤한다.
- **검증 규칙.** `parseRosterRows`가 저장 직전 경로다. **이름이 빈 줄은 아직 안 쓴 줄로 건너뛴다** — 화면이 번호를 미리 채우므로 이 규칙이 없으면 다이얼로그를 열자마자 "3번 이름이 비어 있어요"가 네 줄 뜬다(실제 브라우저에서 확인하고 고쳤다). 이름을 적고 번호를 비우면 그때는 오류다.
- **파일·붙여넣기.** 엑셀·CSV 불러오기와 표 붙여넣기는 그대로다. `rosterTextToRows`가 읽은 글자를 칸으로 펼치고, 번호가 없는 줄은 `withSeats`가 앞 줄 다음 번호로 채운다. 읽지 못한 줄도 버리지 않고 이름 칸에 남겨 교사가 고치게 한다. `parseRosterText`는 이 입구 전용으로 남는다.
- **서버는 그대로.** `addStudents` 요청 모양(`roster: [{seatNumber, realName}]`)과 서버 검증은 바뀌지 않았다.
- **검증.** 실제 브라우저(로컬 3100, 학생 2명 있는 학급)에서 확인: 다이얼로그가 3~7번으로 열리고 오류 없음 → 3번에 `김민준`, 7번에 `최하윤`을 적자 8번 줄이 저절로 생기고 `2명 확인` → 저장 후 명단 표에 3·7번이 참여 코드와 함께 들어갔다. 이름만 3줄을 이름 칸에 붙여 넣자 8·9·10번이 자동으로 붙고 `3명 확인`이 떴다. 다이얼로그 폭을 320px로 좁혀 측정했을 때 번호(88px)·이름(104px)·지우기(44×44) 한 줄 유지, 가로 넘침 0, 최소 터치 44px. `npm test` 342/342(이 맥의 Node 22.13에서는 `pbkdf2-runtime` 1건이 `registerHooks` 미지원으로 떨어진다 — 코드와 무관), typecheck 0, lint 오류 0.
- **남은 위험.** 전체 `browser-check`(3뷰포트)는 이 환경에서 35분 넘게 끝나지 않아 결과를 얻지 못했다. 명단 다이얼로그는 위 실측으로 확인했고, 학생 화면 3뷰포트 회귀는 확인하지 못했다. 교사 화면을 390px 실뷰포트로 연 검사도 하지 않았다(다이얼로그 폭 축소 측정으로 대신했다).


## 2026-09-20 완성 화면에서 소감 고르기 제거

`product-decisions.md` 교사와 메시지 19항. 사용자가 완성 화면의 "마음에 드는 곳은?"·"왜 마음에 들어?"·"직접 글로 쓰고 싶어요"를 가리키며 없애 달라고 했다.

- `DrawingStudio`에서 두 칩 묶음과 직접 쓰기 접이 영역, 그에 딸린 `favoritePartChoices`·`FAVORITE_REASON_CHOICES`·상태 두 개를 지웠다. 완성 단추는 저장 중이 아니면 언제나 누를 수 있다.
- 완성 저장이 소감을 필수로 보던 검사 두 곳(`app/api/artworks/[id]/route.ts`, `lib/client-session.ts`)을 풀었다. 완성에 필요한 것은 완성 그림뿐이다. 소감 칸은 빈 값으로 저장된다.
- 아이 말로 남는 것은 몽그리 짐작을 고친 문장(`storyText`) 하나다. 성장 기록(`lib/growth-reports.ts`)은 원래 `spokenDescription → storyText → …` 순으로 고르므로 그대로 동작한다. 내 그림 자세히 보기는 값이 있을 때만 보여 주므로 옛 작품 기록은 그대로 보인다.
- 검증: 실제 브라우저에서 완성까지 진행해 소감 창에 "🎨 더 그릴래 / ⭐ 작품 완성"만 남고 완성이 저장되는 것(상태 complete, 완성 PNG 1516×1376)을 확인했다. typecheck 0, lint 오류 0, `npm test` 346/346, `browser-check` 통과.

## 2026-09-20 넓은 도화지의 굵기 환산을 거꾸로 넣은 것 수정

사용자 보고: "도화지 커진만큼 굵기 100%일때가 많이 커진것같은데".

- **원인.** 도화지가 100%에서 화면 3장 너비로 펼쳐지는데 저장 값도 3배로 곱했다. 화면 굵기가 3×3 = 아홉 배가 됐다(1180 화면에서 18.4px → 165.9px).
- **수정.** `toDocumentUnits`를 곱셈에서 나눗셈으로 바꿨다(16 → 5.33단위). 굵기 검증은 정수 1~180에서 0.01 단위 소수 0.2~60으로, 글자 크기는 `TEXT_SIZES ÷ span`까지 받도록 바꿨다. 선생님 표시 굵기도 `span`배에서 `1/span`으로 뒤집었다.
- **검증.** 실제 브라우저에서 기본 굵기로 그은 선이 화면에서 **18.4px**로 나온다(옛 도화지와 같은 값). 저장은 `width: 5.33`, `span: 3`. typecheck 0, lint 오류 0, `npm test` 346/346, `browser-check` 통과.

## 2026-09-20 전체 지우기를 도구 막대로

사용자: "그리고 전체 지우기도 있어야할 것 같아". 실제로는 ⋯ 더보기 안에 있었지만 찾기 어려웠고, 아무것도 그리지 않았을 때는 꺼진 상태라 없는 것처럼 보였다.

- `.dock-clear`(휴지통) 단추를 되돌리기·다시하기와 같은 묶음에 두고, ⋯ 안의 `.clear-all-button`과 그 CSS는 지웠다(같은 단추를 두 곳에 두지 않는다).
- 확인 창("정말 다 지울까요?")과 되돌리기 한 번으로 되살아나는 동작은 그대로다.
- 검증: 1180×820·844×390·390×844·320×568에서 단추 44px 이상(48·44·48·48), 막대 넘침 0, 가로 스크롤 0, 누르면 확인 창이 뜨는 것까지 확인. `npm test` 351/351, lint 오류 0, `browser-check` 통과.

## 2026-09-20 색 단추 둘로 가르기·무지개 아이콘 정리 (`claude/ipad-color-picker`)

사용자 요청(아이패드 미니 세로 기준): 단색 아이콘은 단색만, 무지개 아이콘은 색 섞는 화면을 열게. 그리고 무지개 아이콘이 깨져 보인다.

- **두 단추의 일을 나눴다.** `.dock-current-color`(단색 점)는 단색 12색 창만 열고, `.dock-more-colors`(무지개)는 `ColorPickerDialog`를 바로 연다. 전에는 둘 다 같은 창을 열었고 그 안의 `🎨 색 더보기`로 한 번 더 들어가야 했다. 그 단추와 `.dock-palette-wheel` 규칙은 지웠다.
- **단색 점을 모든 폭에서 보이게 했다.** 무지개가 더 이상 12색 창을 열지 않으므로, 넓은 화면(막대에 8색만 보임)에서 9~12번째 색으로 가는 길이 이 단추뿐이다. `.dock-colors` 그리드 앞에 44px 열을 하나 더 두고 세로 가운데에 붙였다.
- **팔레트 창 넘침을 고쳤다.** 좁은 화면(≤900px) 규칙이 `left:50%; transform:translateX(-50%)`로 색 막대 기준 가운데 정렬이라, 막대가 오른쪽 끝에 있는 768px에서 창이 **오른쪽으로 27px 잘려 나갔다**(이번 변경 전부터 있던 문제). `right:0`으로 바꾸고 `max-width:calc(100vw - 16px)`를 뒀다.
- **무지개 아이콘.** 6색 conic이라 44px에서 면이 각지고 가운데 수렴점이 얼룩처럼 보였다. 색을 12개로 늘리고 가운데를 종이색 원으로 덮었다. 마스크는 `closest-side`를 명시한다 — 기본값(farthest-corner)이면 네모 귀퉁이까지 덮여 색 띠가 원 밖으로 삐져나온다.
- **검증.** 헤드리스 Chrome을 CDP로 몰아 네 뷰포트에서 실측했다(768×1024, 1024×768, 320×568, 1440×900). 모두: 두 단추 보임·터치 44px, 단색 점 → 단색 12개만(다른 단추 0개)·창이 화면 안, 무지개 → 색 섞는 대화상자(색상 슬라이더 있음)가 화면 안, 가로 넘침 0, 막대 단추 중 화면 밖으로 나간 것 0. 아이콘은 실제 크기(2배 DPR) 화면 갈무리로 원이 매끈하고 띠가 삐져나오지 않는 것을 확인했다. `npm test` 348/348(이 맥의 Node 22.13에서 `pbkdf2-runtime` 1건은 `registerHooks` 미지원 — 코드와 무관), typecheck 0, lint 오류 0.
- **남은 위험.** 실기기 아이패드 미니가 아니라 CDP 기기 흉내(768×1024, DPR 2)로 봤다. 사파리는 `mask-image` 접두사(`-webkit-`)를 함께 두어 대비했지만 실기기 확인은 남아 있다.

## 2026-09-21 애플 펜슬 지연 줄이기 (`claude/pencil-latency`)

사용자 보고: 애플 펜슬로 그리면 선이 펜보다 늦게 따라온다.

- **원인 1 — 그리는 중에 도화지 전체를 복사했다.** 반투명 브러시(크레용·수채붓)는 세그먼트 알파가 겹치지 않게 하려고 획 시작에 캔버스 전체를 `getImageData`로 뜨고, `pointermove`마다 `putImageData`로 되돌린 뒤 획 전체를 다시 그렸다. 도형 미리보기도 같은 방식이었다. 2026-09-20에 도화지가 화면 3장 크기가 되면서 이 복사가 **9메가픽셀·34MB**가 됐다.
- **원인 2 — 펜 표본을 버렸다.** 애플 펜슬은 초당 240번 좌표를 보내는데 `pointermove`는 주사율(60Hz)로만 온다. 나머지는 `getCoalescedEvents()`에 묶여 오는데 읽지 않고 있었다.
- **고침.** `.live-canvas`(도화지와 같은 픽셀 크기, `pointer-events:none`, 점선 층 아래)를 두고 진행 중인 획과 도형 미리보기를 거기에 그린다. 층을 비우고 다시 그리므로 알파 겹침은 그대로 막히고, 도화지 픽셀은 손을 뗄 때만 바뀐다. 지우개는 도화지 픽셀을 파내는 도구(`destination-out`)라 예전처럼 도화지에 그린다. `pointermove`는 `movePoints()`로 코얼레스된 표본을 모두 읽는다. 곁들여 획 중에는 `getBoundingClientRect()`를 시작 때 한 번만 부른다.
- **실측(아이패드 미니 세로 768×1024, DPR 2, 래스터 2680×3358 = 9MP).** 같은 하네스로 같은 획을 그어 앞뒤를 비교했다.

  | 도구 | 획 시작 | `pointermove` 중앙값 | `pointermove` 최대 |
  |---|---|---|---|
  | 수채붓 | 13.7ms → **4.6ms** | 1.7ms → **0.1ms** | 6.9ms → **0.5ms** |
  | 크레용 | 7.3ms → **0.7ms** | 1.4ms → **0.1ms** | 4.4ms → **0.3ms** |
  | 연필 | 0.6ms → 0.7ms | 0.1ms → 0.1ms | 0.2ms → 0.3ms |

  그린 결과는 같다: 획 중에는 얇은 층에만 잉크가 있고(수채붓 31·크레용 9 표본), 손을 뗀 뒤 층은 비고 도화지에 같은 획(31 표본)이 남는다. 연필은 예전처럼 도화지에 바로 그린다.
- **검증.** typecheck 0, lint 오류 0, `npm test` 358/358(이 맥의 Node 22.13에서 `pbkdf2-runtime` 1건은 `registerHooks` 미지원 — 코드와 무관). 실제 브라우저에서 세 도구로 획을 긋고 화면 갈무리로 선이 정상인지 확인했다.
- **남은 위험.** ① 측정은 맥의 헤드리스 Chrome이다. 아이패드 사파리는 GPU 읽기가 더 느려 실제 이득은 더 클 것으로 보이지만 실기기 확인은 남아 있다. ② 코얼레스 표본은 합성 이벤트로 만들 수 없어 실측하지 못했다 — 코드와 테스트로만 확인했다. ③ 점 사이 최소 거리 2.5단위는 정규화 단위라 도화지가 3배가 되면서 실제로 3배 거칠어졌다. 아직 손대지 않았다.
## 2026-09-22 링크 미리보기 카드를 지금 대문으로

사용자가 카카오톡에 `https://kkumtle.app`을 붙여 넣었더니 옛 파란 대문 이미지와 옛 소개 문구가 떴다.

- `public/og.png`를 지금 대문(초록)의 실캡처로 바꿨다. OG 표준 크기 1200×630, 292KB(옛 파일은 1488×1057·1.4MB).
- `app/layout.tsx`의 소개 문구를 제품 방향에 맞게 바꿨다 — "아이가 그리고, AI는 묻습니다. 설치 없이 교실에서 쓰는 초등 그림 창작 코칭 웹앱"(옛 문구는 은퇴한 커리큘럼을 설명하고 있었다). 이미지 크기 메타와 alt도 함께 고쳤다.
- 검증: 로컬 빌드 서버에서 `og:image` 1200×630, `og:description` 새 문구, `/og.png` 200(292KB) 응답 확인. typecheck 0, lint 오류 0, `npm test` 351/351.
- 남은 점: 카카오톡·슬랙 등은 미리보기를 캐시한다. 운영에 반영된 뒤 카카오 개발자 도구의 캐시 초기화가 필요하다.

## 2026-09-23 몽그리 카드를 읽기 전용으로 (`claude/mongri-read-only`)

사용자 결정: 답하기 왕복을 없애고 읽고 바로 그리러 가게 한다. 보고: "답하기를 눌러도 어떤 반응도 일어나지 않던데".

- **재현.** 아이패드 미니 세로(768×1024)에서 코칭 응답만 CDP로 채워 넣고 칩을 눌러 봤다. 칩에는 ✓가 들어가고 `이제 그려 볼 일` 묶음이 아래에 나타나기는 한다 — 다만 **몽그리는 아무 말도 하지 않는다**. 답을 고르기 전에는 다음 행동이 아예 보이지 않아, 아이는 "무엇을 하라는 건지"를 답을 고른 뒤에야 안다.
- **카드.** 관찰 한마디(`growthEvent`) → `몽그리가 궁금해요` + 질문 → `이제 그려 볼 일`을 **한 번에** 보여 준다. 답 칩·직접 말하기·`그린 뒤 했어요`와 `recordCoachingAnswer`를 없앴다. `✨ 다른 것도 물어보기`로 다시 부른다. 다시 부르거나 닫을 때 앞 도움 기록은 `closeCoachingEvent`가 `dismiss`로 닫는다.
- **같이 고친 것 — 머리 단추 겹침.** `.grimi-head>button` 정사각 규칙(34/44/48px)이 `그리러 가기`까지 잡아 글자가 단추 밖으로 넘쳤다. 768px에서 `✏️ 그 [×] 가기`로 깨져 보였다(실측). 정사각은 닫기(×)만 받도록 `:not(.grimi-collapse)`로 좁혔다.
- **얼굴.** 답 고르기가 사라져 `listening`(듣는 중) 표정이 설 자리가 없다. 접힌 제안 → 질문 두 상태만 남겼다(`tests/mongri-face.test.mjs` 함께 갱신).
- **검증.** 실제 브라우저 3뷰포트(768×1024·320×568·1440×900): 관찰·질문·다음 행동이 모두 보이고 답 칩 0개, 머리 단추 겹침 없음, `그리러 가기` 글자 잘림 없음, 최소 터치 44px, 카드가 화면 안, 가로 넘침 0. typecheck 0, lint 오류 0, production build 통과, `npm test` 391/392(이 맥의 Node 22.13에서 `pbkdf2-runtime` 1건은 `registerHooks` 미지원 — 코드와 무관).
- **계약 테스트.** `tests/auto-grimi.test.mjs`에 카드가 읽기 전용이라는 계약을 더했다 — 답을 보내는 길(`recordCoachingAnswer`·`action: "answer"`·칩·`그린 뒤 했어요`)이 되살아나면 실패한다.
- **남은 것.** 서버의 `answer` 액션과 `recordCoachingAfter(question_answer)` 경로는 지우지 않았다. 가이드 완료(`finishGuide`)가 같은 분기를 쓰고 저장 계약 테스트가 이 경로를 검증한다. 화면에서 부르는 곳만 사라졌다.
