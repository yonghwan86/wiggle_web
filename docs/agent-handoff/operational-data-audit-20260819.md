# Wiggle Web 운영 데이터 조사 보고서

- 조사 시각: 2026-08-19 08:10:08 +09:00
- Sites 프로젝트: `appgprj_6a5fb40ee5d88191bb0c36dafa54605b` (`wiggle-classroom-web`)
- 조사 범위: 운영 D1 `DB`, 운영 R2 논리 바인딩 `ARTWORKS`
- 조사 방식: Sites의 읽기 전용 데이터베이스 조회, 저장소 구성·과거 인수인계 기록 검토
- 운영 데이터 변경: 없음

## 결론

**로컬 연습·브라우저 점검용만 있는 상태가 아니다. 실사용 데이터가 포함되어 있다.**

운영 D1에는 학생 프로필 27개와 작품 32개가 있다. 저장소에 명시된 좁은 연습·점검 표식만 적용해도 22개 학생 프로필과 29개 작품이 남는다. 또한 `docs/claude-handoff-2026-07-27.md`는 운영 학급 안의 특정 프로필을 실제 학생으로 명시하고, 별도의 테스트 프로필과 구분하여 보존하도록 기록한다. 따라서 이 운영 데이터를 테스트 데이터로 간주해 초기화하거나 일괄 이전에서 제외하면 안 된다.

정확한 실사용/연습 데이터 분할은 이름만으로 확정할 수 없다. 현재 스키마에 데이터 출처나 테스트 여부를 나타내는 필드가 없기 때문이다. 아래 제외 수치는 알려진 명시적 표식만 센 보수적인 참고값이며, 삭제 기준으로 사용할 수 없다.

## D1 실측

| 테이블 | 운영 행 수 |
|---|---:|
| `teachers` | 2 |
| `classrooms` | 9 |
| `student_profiles` | 27 |
| `artworks` | 32 |

추가 관찰:

- 완료 상태 작품: 7개
- `artwork_versions`: 1행
- 작품 및 작품 버전 행이 현재 참조하는 고유 R2 키: 31개
- 저장소에 알려진 연습·브라우저 점검용 별명/제목 표식으로 잡힌 범위: 학생 프로필 5개, 작품 3개
- 위 표식만 제외하고 남는 범위: 학생 프로필 22개, 작품 29개
- 운영 학급명 중 저장소가 생성하는 정확한 이름인 `로컬 연습반`, `브라우저 점검반`, `중복 점검반`과 일치하는 행은 0개

`31개`는 D1이 참조하는 고유 객체 키 수이지 R2 버킷의 실제 객체 수가 아니다. 누락 객체나 DB에서 더는 참조하지 않는 고아 객체가 있을 수 있으므로 두 값을 동일하게 취급하면 안 된다.

## R2 조사 결과

| 항목 | 결과 |
|---|---:|
| D1에서 참조 중인 고유 객체 키 | 31개 |
| R2 `ARTWORKS` 실제 객체 수 | **미확인** |
| R2 `ARTWORKS` 실제 총 용량 | **미확인** |

현재 제공된 Sites 읽기 전용 연결 기능은 D1 스키마와 테이블 행은 조회할 수 있지만 R2 객체 목록, 객체 메타데이터 또는 버킷 사용량 조회 기능을 제공하지 않는다. 로컬 환경에도 운영 R2 S3 조회 자격 증명이 없고, Wrangler는 비대화형 조회에 필요한 Cloudflare API 토큰이 없어 버킷 통계를 읽지 못했다. 운영 사이트의 R2는 비공개이며 애플리케이션 API도 학생·교사 권한이 확인된 개별 작품만 반환하므로 이를 우회하여 전체 버킷을 열람하지 않았다.

따라서 이 조사만으로 실제 R2 객체 수나 총 바이트를 추정값으로 보고하지 않는다. 정확한 두 수치를 채우려면 다음 중 하나가 필요하다.

1. Sites가 프로젝트 R2의 읽기 전용 `list/head` 또는 사용량 통계를 제공
2. 해당 `ARTWORKS` 버킷에만 제한된 임시 S3 읽기 전용 자격 증명 제공

자격 증명을 사용할 경우에도 객체 목록과 크기만 읽고, `put`·`delete`·메타데이터 변경은 수행하지 않아야 한다.

## 판정 근거와 주의사항

- 과거 운영 인수인계 기록이 실제 학생 프로필과 테스트 프로필을 명시적으로 구분한다.
- 완료 작품이 존재하고, 명시적 점검 표식을 제외한 다수의 학생·작품 관계가 남아 있다.
- 학생/작품 이름에는 테스트 여부를 확정할 수 없는 데이터가 있으므로 이름 패턴만으로 삭제하거나 이전 제외하면 안 된다.
- 재플랫폼 또는 DB 교체 시 D1 4개 핵심 테이블뿐 아니라 R2 객체와 이미지 키 관계를 함께 보존해야 한다.
- R2 실측 전에는 `31개 참조 키`를 `31개 실제 객체`로 단정하지 않는다.

## 저장소 상태

조사 시작 시 브랜치는 `claude/vercel-replatform-20260818`, HEAD는 `9bef66289be2a3a8dcba8e71a1b6d58666bbf458`였다. 작업 트리에 이미 존재하던 개발 변경은 수정하거나 되돌리지 않았고, 이번 작업에서는 이 보고서만 추가했다.
