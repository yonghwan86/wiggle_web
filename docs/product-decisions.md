# Wiggle Web 확정 제품 결정

> 마지막 갱신: 2026-09-20
> 이 문서에는 사용자가 명확히 확정한 결정만 기록한다. 제안·추측·검토 중인 내용은 `pending-decisions.md`에 기록한다.

## 프로젝트와 제품 중심

1. 기존 Flutter 저장소를 수정하지 않고 `wiggle_web`을 별도 Git·GitHub 저장소로 운영한다.
2. (2026-08-23 확정) 이후 웹 개발은 신규 팀 저장소 `https://github.com/wwwiggle/new_wiggle`에서 계속한다. 기존 `yonghwan86/wiggle_web`은 Vercel 연결을 실제로 바꾸기 전까지 현재 운영 배포 원본으로 보존한다.
3. (2026-08-23 확정) 신규 팀 저장소는 `main`을 기본 브랜치로 사용하고, 현재 검증 브랜치 `codex/updated-design-storybook`을 Pull Request로 `main`에 병합한다. 이후 기능도 직접 `main`에 쌓지 않고 기능 브랜치와 PR 흐름을 사용한다.
4. 제품 중심은 `AI 그림 학습 도구 + 교실 창작 코칭 플랫폼`이다.
5. 이야기·가족 공유·구독은 그림 학습과 교사 코칭 이후 단계적으로 확장한다.
6. AI는 그림을 대신 완성하거나 평가하지 않는다. 부르는 방식은 2026-09-12에 바뀌었다 — 아이가 부를 때에 더해 **몽그리가 먼저 말을 거는 자동 개입**을 둔다(같은 절 6-1항). AI 도우미의 이름은 `몽그리`다(2026-09-07 사용자 결정, 옛 이름 `그리미`). 예외: 완성한 그림책에 대한 **교사용 루브릭 평가**는 2026-09-09 별도 요청으로 두며, 아이 화면이 아니라 교사 화면에만 나온다.
7. 점수·순위·재능 진단·공개 SNS 경쟁은 만들지 않는다. 완성 그림책의 교사용 루브릭 채점·피드백 PDF만 예외다(2026-09-09 사용자 요청).
8. 핵심 학생 대상은 **초등학교 3~6학년**이다(2026-09-12 사용자 정정. 2026-09-09에 1~5학년으로 잡았던 것을 바꾼다). 글을 읽을 수 있는 나이지만 읽기 부담은 낮춘다 — 핵심 행동은 그림·아이콘·큰 터치 영역으로도 알 수 있어야 한다(브라우저 읽어 주기 버튼은 2026-09-09 사용자 지시로 제거).

## 학생 입장과 기록

1. 수업 코드와 학급 QR은 학급 입장 수단이며 학생 신원이나 작품 접근 권한이 아니다.
2. 작품과 과정 기록은 서버가 발급한 익명 학생 ID에 저장한다.
3. 학생 이메일·학교명 없이 참여한다. 학생 실명은 교사가 만든 학급 명단에만 두고 그 학급 담임 교사에게만 보여 준다(2026-09-07 사용자 결정). 이유: 교사가 15명 남짓한 학급을 직접 열고 명부와 대조하려면 부르는 이름만으로는 부족하다는 현장 의견. 학생 화면·가족 공유·그림책·AI 요청에는 실명을 싣지 않으며, 학급을 지우면 명단도 함께 지운다.
4. (2026-09-09 결정, 2026-09-12 개정) **입장은 두 단계다: 반을 정하고 → 아이 참여 코드 네 자리.** 반은 **학급 QR이 기본**이고, QR을 못 쓸 때 대문에서 4자리 수업 코드를 넣는다. 참여 코드는 여섯 자리에서 **네 자리로 줄였다**(2026-09-12 사용자 결정) — 여섯 자리는 저학년이 누르기 벅찼다. 종전 문장은 다음과 같다. ~~입장은 수업 코드 → 아이 참여 코드 6자리, 두 단계다.~~ 교사가 명단을 만들면 자리(번호·실명)마다 무작위 참여 코드가 붙고, 아이는 대문에서 4자리 수업 코드나 학급 QR로 반을 확인한 뒤 자기 참여 코드 하나만 누르면 바로 자기 도화지로 들어온다. 처음 들어올 때만 동물 하나를 고르고, 별명은 그 동물의 기본 별명이 자동으로 붙는다(아이가 글자를 치지 않음). 동물을 고르면 곧바로 빈 도화지가 열린다(학습 과정 0항). 이유: 번호 + 그림 비밀번호 3개는 1~2학년이 매번 기억해야 해서 너무 어려웠다("지금 너무 어려워서 변경할려고"). 코드는 종이에 적혀 있고 코드가 곧 그 아이의 자리라, 다음 회차에 같은 코드를 넣으면 같은 학생 ID로 돌아오므로 아이가 자기 지난 그림을 찾을 필요가 없다.
4-2. (2026-09-13 사용자 결정) **입장 화면 두 곳에 앱 안 QR 찍기를 둔다.**
   - 첫 화면(수업 코드 입력): `QR로 찍기`. 반 QR을 찍으면 그 반 참여 코드 화면으로, 아이별 쪽지 QR을 찍으면 키패드 없이 바로 들어간다.
   - 참여 코드 화면: `내 쪽지 QR로 찍기`. 아이별 쪽지 QR만 쓸모가 있다(반 QR에는 참여 코드가 없다고 안내한다).
   - **쪽지 QR은 아이마다 다르다.** `<반 주소>/join/<수업 코드>#entry=<참여 코드>`. 선생님 화면·전자칠판의 큰 QR은 반 QR 그대로다 — 한 아이 코드를 반 전체에 띄우면 안 된다.
   - 참여 코드는 쿼리가 아니라 **주소 조각(`#`)**에 담는다. 조각은 서버로 가지 않아 접속 로그·Referer에 남지 않고, 입장 화면이 읽자마자 `history.replaceState`로 주소창·방문 기록에서 지운다.
   - 스캐너는 찍은 주소를 따라가지 않는다. 네 자리 숫자 둘만 꺼내 우리 주소로 다시 만든다. 형식이 조금이라도 다르면 거절한다.
   - 이유와 위험: 저학년이 네 자리를 누르기 어렵다. 쪽지 한 장이 입장 수단이라는 점은 종전과 같다 — 같은 쪽지에 참여 코드가 이미 글자로 인쇄돼 있기 때문이다(5항). 코드를 새로 뽑으면 옛 QR도 함께 죽는다(QR이 같은 코드를 담으므로). 무차별 대입은 기존 학급+IP 제한이 그대로 막는다. 사용자에게 "쪽지 한 장이 그 아이 그림을 여는 열쇠가 된다"는 위험을 알린 뒤 결정했다.
4-3. (2026-09-13 사용자 결정) **첫 입장의 동물 고르기는 "나랑 닮은 친구를 골라요" 화면이다. 고른 친구의 이름이 곧 별명이 된다.**
   - 친구 10종과 이름: 토끼 솔이, 곰 보리, 여우 루루, 호랑이 호두, 판다 모모, 강아지 두부, 고양이 나비, 코알라 코코, 사자 해솔, 개구리 초롱.
   - (2026-09-14 사용자: "캐릭터 20종류까지 늘려줘 옆으로 이동할 수 있도록 표시도해주고") 친구를 20종으로 늘렸다. 더한 10종: 펭귄 도리, 햄스터 콩이, 돼지 봄이, 원숭이 미소, 송아지 누리, 양 구름, 기린 키키, 코끼리 다솜, 부엉이 별이, 수달 하루. 화면은 한 쪽에 10종(5×2, 휴대폰 2열)씩 두 쪽이고, 양옆 화살표·아래 쪽 점으로 넘기며 손가락으로 밀어도 넘어간다. 휴대폰은 화살표를 점 양옆으로 내린다. 버튼은 "솔이와 시작하기"처럼 받침에 맞춰 와/과를 붙인다.
   - 서버 기본 별명(`lib/nickname-ideas.ts` 각 동물의 첫 후보)이 곧 친구 이름이다. 화면(`lib/animal-characters.ts`)은 이름을 따로 적지 않고 그 첫 후보를 읽어, 화면과 들어간 뒤 별명이 어긋나지 않는다. 이미 들어온 학생의 별명은 바꾸지 않는다.
   - 같은 반에서 같은 친구를 고르면 별명이 겹친다. 입장은 참여 코드로 구분되고 선생님은 실명으로 보므로 막지 않는다.
   - 화면은 시안(`docs/design-assets/animal-picker/mockup-2026-09-13.png`) 배치다. 동물 그림은 코덱스(사용자 ChatGPT 구독의 Codex CLI 이미지 생성)로 만든 폭신한 3D 인형 10종이다(2026-09-14, 사용자: "너가 직접 코덱스가 구독으로 되어있는데, 그걸 연결해봐라"). 원본은 `codex-assets/`, 화면 파일은 같은 이름의 `public/entry-green/animals/<slug>.webp`. 옆 장식(손글씨·몽그리·크레용 그림)은 카드를 가리지 않는 큰 화면에서만 보이고, 태블릿에서는 숨긴다(Claude 판단 2026-09-14 — 태블릿은 격자 옆 여백이 없다).
5. 참여 코드는 교사 화면(명단·설정 탭, 코드표 인쇄)에만 보인다. 학생 응답에는 번호·이름·코드 목록을 절대 싣지 않는다. 코드 종이를 잃어버리거나 새어 나가면 교사가 그 아이 코드만 새로 뽑고(그림·별명은 그대로), 옛 코드는 바로 죽는다. 코드는 네 자리(만 개)이고 학급 + IP 버킷(60회/10분)이 무차별 대입을 막는다. 여섯 자리보다 추측이 쉬워진 것은 사실이나, 코드는 그 학급 안에서만 통하고 얻는 것은 아이 그림 접근뿐이라 이 한도로 감당한다고 판단했다(2026-09-12). 교사 화면에 그대로 보여 줘야 하므로 해시가 아니라 평문으로 저장한다(수업 코드와 같은 등급의 값).
6. 같은 기기 재접속과 공유 태블릿 학생 전환을 지원한다. 전환은 나가기 뒤 다른 아이가 자기 코드를 넣는 것이다.
7. 메인 화면의 수업 코드와 학급 QR은 모두 학급을 확인한 뒤 같은 참여 코드 입력 화면으로 이어진다. 수업 코드와 QR은 없애지 않는다(2026-09-09 사용자: "이거는 없애면 안돼지않아?").
8. (폐기 2026-09-09) ~~기존 학생은 해당 학급 안에서 `동물 + 별명 + 그림 비밀번호 3개`를 입력해 기존 익명 학생 ID와 작품을 연다.~~ 재입장도 참여 코드 하나다.
9. 다른 공용 태블릿에서 같은 코드로 들어가는 것은 예외 복구가 아니라 정상적인 재입장이다.
10. 학급 학생 전체 목록이나 입장 순서 번호를 학생 식별 수단으로 사용하지 않는다.
11. (폐기 2026-09-07) ~~학생 프로필이 하나도 없는 새 학급은 신규 프로필 생성 화면을 바로 열고…~~ 명단 없는 학급은 "아직 준비 중이에요"에서 멈춘다.
12. (폐기 2026-09-07) ~~`새로 시작하기`와 `내 그림 이어가기` 두 버튼.~~
13. (폐기 2026-09-09) ~~기본 재입장 수단은 `동물 + 별명 + 그림 비밀번호 3개`…~~ 그림 비밀번호, 개인 QR 복구, 교사 복구 재발급은 코드와 함께 제거했다. `recovery_credentials` 표는 데이터 보존용으로만 남는다.
14. 그림 별명 후보는 동물별로 10개 이상을 제공한다. 후보는 초등학생이 읽기 쉬운 한글 낱말로 만들고 전체 후보가 서로 겹치지 않아야 한다. 2026-09-09부터 아이는 별명을 고르지 않고 동물의 첫 후보가 자동으로 붙는다(`다른 별명` 주사위는 입장 화면에서 사라졌다).
15. 별명의 띄어쓰기는 표시용으로만 유지한다. 별명은 더 이상 식별 수단이 아니므로(코드가 식별) 같은 반에 같은 별명이 있어도 괜찮다. 교사 화면은 여전히 `같은 별명 있음`을 표시한다.
16. 예외(2026-09-09 사용자 결정): 씨앗 선 커리큘럼의 "친구 그림 보기"를 위해, **같은 학급 안에서 교사가 그 회차를 연 동안만** 반 작품을 이름·번호 없이 그림만 볼 수 있다. 학급 입장 정보만으로 다른 학생 작품을 여는 것은 여전히 불가하고, 작품 소유권·저장 경로는 바뀌지 않는다.

6-1. (2026-09-12 사용자 결정) **몽그리가 먼저 말을 건다.** 종전 원칙 「아이가 호출했을 때만 개입한다」를 바꾼다. 이유: 아이가 「몽그리 부르기」를 스스로 찾는 일이 드물어, 부를 때만 여는 구조에서는 이 역할이 사실상 없는 것과 같았다.
   - **뜨는 때**: 아이가 여덟 획 이상 그렸고, 마지막 획에서 75초가 지났고, 지금 펜이 닿아 있지 않을 때.
   - **뜨지 않는 때**: 도화지를 연 지 2분 안, 도화지가 거의 빈 상태(무엇을 그릴지는 선생님 몫이고 몽그리는 새 소재를 가져오지 않는다), 아이가 직접 부른 뒤 30초 안, 완성·소감 창이 열렸을 때, 저장 충돌이나 선생님이 보고 있다는 표시가 있을 때.
   - **빈도**: 그림 한 장에 자동 두 번까지, 자동끼리 최소 5분 간격. 아이가 직접 부르는 것은 종전대로 제한하지 않는다.
   - **형태**: 화면을 덮는 창이 아니라 옆 패널로 열리고, 아이가 다시 그리기 시작하면 스스로 접힌다. 누가 먼저 말을 걸었는지 카드에 표시한다. **그리는 것을 막지 않는 것이 이 결정의 조건이다.**
   - 안전 규칙(평가·점수·재능 진단 금지, 대신 그리지 않기, 한 번에 한 질문)과 서버 상한은 그대로다. 자동과 수동은 같은 경로를 지난다 — 필터를 우회하는 두 번째 길을 만들지 않는다.

## 학습 과정

> **2026-09-12 전면 개정: 커리큘럼을 은퇴시킨다.** 수업은 **빈 도화지**에서 시작하고
> 진행은 선생님이 교실에서 말로 이끈다. 제품은 회차·아크·씨앗 선·오늘 회차 카드를 갖지 않는다.
> 아래 1~4항(교육학 근거)과 5~7항(점선·시범), 그리고 그림책 결정은 그대로 살아 있다.
> 회차 아크를 전제하던 2항 예시와 8~24항은 이 개정으로 폐기했다 — 문장은 이력으로 남긴다.

0. (2026-09-12 사용자 결정, 이 절의 아크 관련 항목을 모두 대체) **수업의 단위는 회차가 아니라 한 번의 수업이고, 아이가 보는 것은 빈 도화지다.**
   - 아이는 참여 코드로 들어오면 곧바로 도화지를 연다. 오늘 회차 카드도, 활동 고르기도, 학생 홈도 없다.
   - 무엇을 그릴지는 **선생님이 교실에서 말로 정한다.** 제품은 주제·순서·차시를 제안하지 않는다.
   - 그리다 만 그림이 있으면 그것을 이어 열고, 없으면 새 도화지를 편다.
   - 이유: 내장 커리큘럼은 교사의 실제 수업 운영과 맞지 않았다. 제품이 진도를 쥐면 선생님이 자기 수업을 못 한다.
   - 함께 은퇴: 이야기 아크 5개(`lib/arc-content.ts`), 씨앗 선 심기, 교사 회차 변경과 수업 안내, 학생 홈의 오늘 회차 카드, 몽그리의 회차 문맥.
   - 남긴 것: 그림책 만들기, 내 그림 보관함, 교사의 오늘 수업 그림 보기(학생별 **최근 그림** 기준), 몽그리 부르기.
   - 기록은 지우지 않는다. `artworks.arc_id`·`episode_id`는 그때 그린 그림의 이력이라 컬럼을 남긴다.


> 2026-08-30 개정: 아래 1~4항은 기존 `4단계 기능 훈련 사다리`를 대체한다.
> 교체 이유와 폐기된 결정은 이 절 끝의 「폐기된 결정」에 보존한다.

1. 제품의 교육학적 근거는 다음 세 가지의 조합이다. **`VTS 기반`이라고 표기하지 않는다.**
   - **See-Think-Wonder** (Harvard Project Zero, Visible Thinking) — 관찰·추론·질문을 끌어내는
     발문 루틴. 변형이 허용되고 훈련·라이선스 부담이 없다.
   - **Studio Thinking**의 Reflect / Critique (Harvard Project Zero, Getty 후원) —
     **아이 자기 작품**에 대한 구조화된 발문의 정통 근거.
   - **Storyline Method** (1965, 스코틀랜드 Harkness·Bell·Rendell) — 에피소드를 이어
     하나의 이야기를 여러 회차에 걸쳐 완성하는 구조. 아래 2항의 회차 아크가 이 방법론이다.

   목표는 미술사 지식이나 그림 실력이 아니라 **아이가 스스로 관찰하고 근거를 대는 것**이다.
   기존 몽그리 원칙(대신 완성 금지·평가 금지·모르면 추측 금지·한 번에 하나만 질문)은
   이 계열의 진행자 규칙과 같은 것이다. VTS에서 가져오는 것은 세 가지로 한정한다 —
   **근거를 묻는 정신, 환언·가리키기, 정답 없음·정보 미제공 원칙.**

   **VTS 표기를 쓰지 않는 이유** (2026-08-30 조사): VTS는 ①타인의 기성 작품 ②또래 집단 토론
   ③인증 훈련받은 진행자 ④문구·순서 고정을 전제한다. 이 제품은 아이 자기 그림에 1:1로
   AI가 개입하므로 네 가지를 모두 벗어난다. 특히 2번 문항 `무엇을 보고 그렇게 생각했나요?`는
   저자가 없는 이미지용 장치여서, 아이가 저자인 그림에서는 `내가 그렇게 그렸으니까`로
   자명해지거나 자기 그림을 변호하라는 추궁이 된다. Gaufberg et al. 2026(The Clinical Teacher)은
   문구 변경·미훈련 진행자에 의한 진행을 충실도 위반으로 명시 비판한다.
2. (폐기 2026-09-12 — 0항) ~~**학습의 결과물 단위는 그림 1장이 아니라 회차가 누적된 동화책 1권이다.**~~ 그림책은 남지만 회차 누적이 아니라 아이가 원할 때 자기 그림으로 엮는다.
   각 수업은 독립 활동이 아니라 하나의 이야기로 이어지는 한 장면이다. 예시 5회차 아크:
   - 1회차: 동그라미에서 출발해 자전거를 그린다
   - 2회차: 그 자전거가 달리는 장면을 그린다
   - 3회차: 친구들과 함께 자전거를 타는 장면을 그린다
   - 4회차: 자전거를 타고 집에 가는 장면을 그린다
   - 5회차: 집에 도착해 부모님과 함께 자전거를 청소하는 장면을 그린다
   - → 다섯 회차가 모여 동화책 한 권이 된다
3. **출발점은 발산(divergence)이다.** 동그라미 하나처럼 열린 자극을 주고
   아이마다 서로 다른 결과가 나오는 것을 정상으로 본다. 정해진 그림을 따라 그려
   같은 결과에 수렴시키는 활동을 학습의 중심에 두지 않는다.
   발산의 폭은 학급을 이해하는 관찰 지표이지 아이를 매기는 점수가 아니다
   (점수·순위·재능 진단 금지 원칙은 그대로 유지된다).
4. AI는 아이의 생각과 상상을 그림으로 표현하고 확장하도록 돕는다.
   이 역할은 대신 완성하거나 평가하지 않는다는 기존 한계 안에서만 수행한다.
   몽그리의 개입 방식은 **회차 안의 단계에 따라 두 가지를 나누어 쓴다.**
   - **그리는 중 — `확장 협업자`**: 아이가 말하거나 끄적이면 몽그리가 그것을 받아
     다음을 제안한다. 아이의 발화·표시가 먼저이고 몽그리는 뒤따른다.
     근거: StoryDrawer(CHI 2022), 만 6–10세 대상 검증.
   - **마무리할 때 — `틀리는 해석자`** (2026-09-26 개정): 몽그리가 먼저 해석을 내놓고 아이가 고치게 한다.
     아이를 추궁하지 않으면서 근거 발화를 끌어내기 위한 설계다. 근거: Weng et al. 2025(BMC Medical Education).
     **짐작은 실제로 본 것에서 나온다. 일부러 빗나가게 고르지 않는다**(2026-09-26 사용자 결정 "가로 가자", 미결정 P-014):
     - 알아볼 수 있으면 **본 대로** 짐작하고 아이에게 확인을 청한다(`내 눈에는 집처럼 보이는데, 맞아?`).
     - 알아보기 어려우면 아무 이름이나 대지 않고 **갈래로** 말한다(`동물처럼 보이는데?`).
     종전 문장은 이력으로 남긴다: ~~틀릴 수 있는 상태로 제시한다. 일부러 덜 구체적으로 짐작하고
     두 번째로 그럴듯한 읽기를 고른다(`어? 난 이게 우산처럼 보이는데?` → `아니야, 자전거 바퀴야`).~~
     개정 이유: 그 설계의 전제는 **그럴듯하게 살짝 빗나간 짐작**인데(우산 ↔ 자전거 바퀴처럼 형태가 닮은 것),
     첫 짐작이 맞아야 성립한다. 그런데 AI에 보내던 그림이 넓은 도화지 전체라 아이 그림이
     57×48px(넓이의 5.6%)로 들어갔고, 그 위에서 다시 두 번째 읽기를 고르니 **완전히 무작위**가 됐다
     (운영 제보: "집을 그리면 다른 걸로 추측해버린다", "너무 뜬금없는 대답을 한다").
     아이가 받는 인상이 "다르게 읽었네"가 아니라 "아무 말이나 하네"였다.
     아이에게 맞는지 묻는 것은 판정이 아니다 — 아이의 답을 맞다 틀리다 가르지 않는다는 한계는 그대로다.
     함께 고친 것: 몽그리에 보내는 이미지를 `documentImage`로 바꿔 **그린 칸만 잘라** 보내고(9배 크게),
     `detail`을 `low`(512px)에서 `high`로 올렸다.
   두 방식 모두 몽그리가 그림을 대신 완성하거나 잘잘못을 평가하지 않는다는 한계를 지킨다.
   몽그리는 아이가 호출했을 때만 개입한다는 원칙도 그대로다.
4-1. (2026-09-09 확정) **몽그리의 `단계 가이드 만들기`와 빠른 주제 칩을 은퇴시킨다.** 아이가 주제를 말하면 AI가 6~15단계로 쪼개 주던 기능이다. 이유: 씨앗 이야기 아크가 회차마다 장면 문장 하나와 힌트 안내 셋을 이미 주므로 역할이 겹치고, 2회차부터는 아이 자신의 지난 그림이 씨앗인데 새 주제 가이드가 그 연결을 끊는다. 안내 문장은 AI가 만들지 않고 확정된 시나리오 문장을 쓴다는 원칙과도 어긋났다. 몽그리는 확장 협업자와 틀리는 해석자 두 역할만 맡는다.
5. 점선 가이드는 아이 그림과 분리된 레이어이며 숨기거나 거절할 수 있어야 한다.
6. 정지된 점선만 보여주지 않고 `짧은 연필 시범 → 점선 연습 → 독립 그리기`로 안내한다.
7. 점선 따라 그리기는 아이가 안내 요소를 그리는 순서에 의존하지 않아야 한다.
   (5~7항의 점선·시범은 아이가 요청했을 때 쓰는 보조 수단으로 유지하며,
   학습 흐름의 기본 골격에서는 내린다.)

(폐기 2026-09-12 — 0항) 8. **회차 아크는 제품에 내장하고 교사는 고르기만 한다.** 교사가 5회차 시나리오를
   직접 짜야 하면 준비 부담 때문에 두 번째 회차부터 쓰이지 않는다. 완성된 아크를
   제품이 제공하고 교사의 수업 준비는 `아크를 하나 고른다`로 끝나야 한다.
   아크 제작은 제품 팀의 일이다. (2026-08-30)
(폐기 2026-09-12 — 0항) 8-1. **다만 아크의 진행은 교사가 자유롭게 정한다.** (2026-08-30)
   - 제품이 제공하는 아크 중 **몇 개를 할지, 어떤 순서로 할지** 제한하지 않는다.
   - **한 아크의 회차 수를 5로 고정하지 않는다.** 아크마다 다를 수 있고,
     교사가 도중에 마무리하거나 더 이어가는 것도 정상 흐름이다.
   - 한 아크가 끝난 뒤 다음 아크로 자동으로 넘어가지 않는다. 넘어갈지는 교사가 정한다.
   - **교사가 아크를 바꾸거나 도중에 마치는 것은 이탈이 아니라 정상 사용이다.**
     이를 실패 신호로 계측하지 않는다.
   - 교사가 아크를 직접 만드는 편집기는 이번 범위에 넣지 않는다(8항 유지).
(폐기 2026-09-12 — 0항) 9. **아이의 아크 진행은 날짜가 아니라 회차 단위이며, 빠진 회차는 빈 쪽으로 남는다.**
   결석한 아이의 동화책은 해당 쪽이 비어 있는 상태로 유지되고 나중에 언제든 채울 수 있다.
   빠진 회차를 건너뛰고 완성 처리하지 않으며, 결석 때문에 아이가 결손감을 느끼게 하지 않는다.
   동화책은 빈 쪽이 있는 상태로도 계속 열람·이어작업이 가능해야 한다. (2026-08-30)
10. 오늘 그릴 장면은 **태블릿이 아이에게 알려준다.** 아이가 매번 고르거나 교사가
   구두로만 지시하는 방식이 아니다. 따라서 제품은 학생별 아크 진행 상태를 보유하며,
   교사의 회차 지정은 기존 `오늘 활동 선택`을 통해 이루어진다. (2026-08-30)
(폐기 2026-09-12 — 0항) 11. 아이는 이전 회차 그림을 **참조용으로 확인**할 수 있다. 지난 그림을 나란히 놓고
   이어 그리는 방식이 아니라 회차마다 새로 그리며, 지난 그림은 볼 수 있으면 된다.
   따라서 `5회차 = 그림 5장 = 동화책 5쪽`이다. (2026-08-30)
12. 교사는 태블릿 전체 확인·태블릿 조언·앞에서 진행·돌아다니며 오프라인 조언을
   상황에 따라 병행한다. 따라서 **교사가 태블릿 앞에 있지 않아도 수업이 굴러가야 하며**,
   교사 진행실은 상시 주시가 아니라 간헐적 확인에 맞춘다. (2026-08-30)
13. **반 진도와 개인 진도가 어긋나면 반 진도가 이긴다.** 결석했던 아이도 오늘은
   반과 같은 회차를 그리고, 빠진 회차는 빈 쪽으로 남는다. 수업이 하나로 굴러가는 것과
   교사 진행의 단순함을 이야기 순서보다 우선한다. (2026-08-30)
14. **몽그리의 역할 전환은 아이가 `다 그렸다`고 할 때 일어난다.** 그리는 중에는
   확장 협업자, 완료를 누른 뒤에는 틀리는 해석자다. 몽그리가 스스로 때를 판단해
   끼어들지 않으므로 `아이가 호출했을 때만 개입한다`는 원칙이 유지된다. (2026-08-30)
15. **동화책 완성의 보상은 `아이가 제품 안에서 책으로 넘겨보는 것`이다.**
   인쇄·PDF 내보내기·가족 비공개 공유·자동 다음 아크 시작은 이번 범위에 넣지 않는다.
   가족 공유와 구독은 기존 결정대로 후속 단계로 유지한다. (2026-08-30)
16. **구현된 활동 30개(`lib/lesson-content.ts`)는 테스트 진행용이므로 제거한다.**
   실제 커리큘럼 자산이 아니다. 제거는 아크 콘텐츠 준비와 같은 변경에서 함께 한다. (2026-08-30)
17. **그림책 편집기의 `main` 반영은 그림책 편집기 구현 작업을 진행할 때 함께 한다.**
   별도 배포 결정으로 분리하지 않는다. (2026-08-30)
18. **빈 쪽은 아이가 집에서 채운다.** 수업 시간은 반이 함께 진행하는 오늘 회차에 쓰고,
   빠진 회차는 집에서 숙제처럼 채운다. 따라서 접속 위치에 따라 보이는 회차가 다르다. (2026-08-30)
   - **교실에서 들어오면** 교사가 지정한 오늘 회차를 그린다(반 진도 우선, 13항).
   - **집에서 들어오면** 자기 동화책의 빈 쪽을 채운다(개인 진도).
   - 집에서 하는 것은 선택이며 **하지 않아도 불이익이 없다.** 미완료를 독촉하거나
     하지 않은 아이를 표시하지 않는다. 점수·순위·평가를 만들지 않는다는 원칙이 그대로 적용된다.
   - 집 접속도 기존 재입장 방식(`학급 + 동물 + 별명 + 그림 비밀번호 3개`)을 그대로 쓴다.
     별도의 가정용 계정이나 부모 계정을 만들지 않는다.
19. **학급을 삭제하면 그 학급 아이는 자기 그림과 동화책을 더 이상 열 수 없다.** (2026-08-30)
   - 학급 삭제는 되돌릴 수 없다. 교사 화면에 학급 복원 수단을 두지 않는다.
   - 아크를 바꿀 때의 동화책 보존(2항 계열)과는 다른 사건이다.
     아크 전환에서는 이전 책이 남고 열리지만, 학급 폐기에서는 접근이 함께 끝난다.
   - 삭제 확인 문구는 되돌릴 수 없다는 것과 아이가 자기 그림·동화책을 다시 열 수 없다는 것을
     숨기지 않는다. `내부 데이터는 복구를 위해 안전하게 보관됩니다` 같은 문구는 쓰지 않는다.
   - 학생 개별 삭제(`archiveStudent`)는 다르다 — 복원 수단이 있으므로 복원 가능하다고 안내한다.
20. **AI가 생성한 문장은 음성으로 내보내지 않는다.** (2026-08-31)
   음성은 제품에 확정된 시나리오 문장 전용이다(7항의 연장). 몽그리 말풍선은 텍스트로만 표시한다.
   비문해 아이의 몽그리 참여는 답 칩의 이모지와 장면 삽화가 담당한다.
21. **회차 콘텐츠에 장면 삽화를 포함한다.** (2026-08-31)
   회차 형상은 `{episodeId, title, sceneText, sceneImage}`다. 글·음성·그림 세 채널로
   비문해 아이에게도 오늘 장면이 전달된다. 삽화는 캐릭터와 같은 AI 생성 파이프라인으로 제작한다.

### 폐기된 결정 (2026-08-30)

다음 두 항목은 사용자의 명시적 결정으로 교체됐다. 이력 보존용으로만 남긴다.

- (폐기) 학습 흐름은 4단계다 — 1단계 선·도형 기초 10개 / 2단계 따라 그리기 10개 /
  3단계 관찰 그리기 10개 / 4단계 자유 창작.
- (폐기) 별도의 `바로 자유롭게 그리기` 메뉴는 두지 않고 자유 창작은 4단계에서 제공한다.

**교체 이유:** 기존 4단계는 기능 훈련 사다리였고, 각 수업이 하나의 이야기로 이어져
동화책이라는 결과물이 된다는 제품의 실제 구상이 어느 문서에도 기록된 적이 없었다.
`따라 그리기 10개`는 발산 출발점과 방향이 반대이고, `자유 창작은 4단계에서만`은
1회차부터 창작하는 이야기 아크와 충돌한다.

**구현된 활동 30개(`lib/lesson-content.ts`)는 테스트 진행용 임시 콘텐츠이므로 제거한다**
(2026-08-30 사용자 확인). 아크 안의 워밍업으로 흡수하지 않는다.
단, 제거 시점은 아크 콘텐츠가 준비된 뒤이거나 같은 변경 안이어야 한다 —
먼저 지우면 운영 제품에 아이가 할 활동이 하나도 남지 않는다.
22. (폐기 2026-09-12 — 0항) ~~커리큘럼은 기존 30개 레슨을 기준으로 삼지 않고 **씨앗 이야기** 방식으로 짠다(2026-09-09 사용자 결정, 같은 날 12회 독립 씨앗 초안을 이 구조로 바꿈). 계획 정본은 `docs/curriculum-seed-plan.md`다.
   - 씨앗 선은 **첫 회차 한 번뿐**이고, 2회차부터는 아이 자신의 지난 그림이 다음 회차의 씨앗이다(동그라미 → 피자 → 피자 먹는 사람 → …).
   - 씨앗별로 5회차 아크를 3~4개 내장한다. 회차 수는 5로 두되 교사가 언제든 멈추거나 더 연다(8-1항).
   - 2회차부터의 안내는 힌트가 있는 문장이다(1~2학년이 덜 막히도록).
   - 아크가 끝나면 동화책 한 권이 된다(2항). 한 회차의 리듬은 씨앗(또는 지난 그림) 받기 → 그리기 → 이야기 한 줄 → 친구 그림 보기로 고정한다.
23. 반 친구 그림을 보고 하는 토론은 **교실에서 말로** 한다. 앱은 그림만 크게 띄운다(2026-09-09 사용자 결정). 앱 안 댓글·스티커·투표는 만들지 않는다.
24. (폐기 2026-09-12 — 0항) ~~씨앗 이야기는 **아크 4개(`circle-story`·`curve-story`·`zigzag-story`·`cross-story`, 각 5회차)로 제품에 내장**한다(2026-09-09). 아크 모델(8항)은 그대로이고 회차에 선택 항목 셋을 더한다: 씨앗 선(`seed`, 1회차에만, 작품 생성 시 지울 수 있는 획으로 심음), 힌트 안내 문장(`prompts`, 아이 화면), 교실 토론 질문(`discussion`, 교사 화면 전용). 교사는 조종석에서 아크를 고르고 회차를 열기만 한다.

25. (2026-09-23 결정 → **2026-09-26 개정**) **몽그리 카드는 왼쪽 위 플로팅 카드이고, 아이가 그 자리에서 답할 수 있다.**
   (인계 `docs/design-assets/mongri-floating-handoff-20260926/`, 시안 `reference/selected-floating-answer.png`)
   - **첫 만남에는 알아맞히지 않고 묻는다**(2026-09-26 사용자 결정): 「이 동그란 건 뭐야?」처럼 무엇을 그리는지 묻고,
     몽그리가 보기에 그럴듯한 것들을 **선택지로** 준다. 아이가 알려 준 답이 다음 차례의 맥락으로 돌아온다.
     몽그리가 스스로 판정하려다 빗나가던 것이 뜬금없는 대답의 뿌리였다(P-014).
   - 카드 차례: 관찰 한마디 → 궁금한 점 → **답 고르기 / 내 말로 쓰기** → 이제 그려 볼 일 → 다른 것도 물어보기.
   - 선택지 하나 또는 아이가 쓴 문장 **둘 중 하나만** 답으로 나간다. 답하지 않고 「그리러 가기」·×로 닫는 길도 그대로다.
   - 넓은 가로 화면은 도화지 **왼쪽 위에 떠 있는 카드**(도화지를 밀지 않는다), 좁은 화면은 기존 바텀시트를 유지한다.

   종전 문장은 이력으로 남긴다: ~~(2026-09-23 사용자 결정: "읽기 전용으로 바꾸는 게 맞는 것 같아") **몽그리 카드는 읽기 전용이다.**
   답 칩·직접 말하기·`그린 뒤 했어요`로 답을 되돌려 보내던 왕복은 없앤다.~~

   **읽기 전용으로 갔던 세 이유는 설계로 막는다**(그래서 되살려도 같은 일이 되풀이되지 않는다):
   ① 답을 골라야 「이제 그려 볼 일」이 나타나 늦게 알았다 → **답하기 전부터 보인다.**
   ② 답을 바꿔 두 번 보내면 409 `이미 처리한 도움 기록이에요`였다 → 새 `action:"reply"`가 **같은 줄을 덮어쓴다.**
     그린 뒤 기록용 `action:"answer"`(document·image·newElements를 요구하고 한 번만 기록)는 건드리지 않고 그대로 둔다.
   ③ 칩을 눌러도 반응이 없었다 → 고른 즉시 **초록 윤곽 + 체크 + `aria-pressed`**로 표가 난다(색만으로 구분하지 않는다).
   - 이유: ① 답을 골라야 `이제 그려 볼 일`이 나타나 아이가 무엇을 할지 늦게 알았다. ② 고른 답을 바꾸면 단추가 다시 열려, 같은 질문에 답을 두 번 보내면 409 `이미 처리한 도움 기록이에요`가 떴다(2026-09-20 사용자 보고). ③ 칩을 눌러도 몽그리가 반응하지 않아 아이 눈에는 아무 일도 일어나지 않았다(2026-09-23 사용자 보고).
   - 관찰 한마디는 이미 AI 응답에 있던 `growth_event`를 쓴다. 판정이 아니라 관찰 문장이라 칭찬 금지 규칙과 부딪히지 않는다. `choices`는 응답 스키마에 남지만 화면에 그리지 않는다.
   - 몽그리가 스스로 먼저 말을 거는 경우(자동 호출)도 같은 카드를 쓴다. 어느 쪽이든 답을 요구하지 않는다.

   **(2026-09-26 사용자 결정) 답을 들으면 「이제 그려 볼 일」 한 줄을 아이 말에 맞춰 다시 쓴다.**
   - 문제: 화면에 떠 있던 제안은 아이가 답하기 **전에** 만들어진 말이다. 아이가 "농구공이야"라고
     알려 줘도 몽그리가 사과인 줄 알고 쓴 줄이 그대로 남았다. 답은 저장만 되고 다음에 몽그리를
     다시 불러야 쓰였다 — 아이 눈에는 **답해도 아무 일이 없는 것**이었다(읽기 전용으로 갔던 ③번과 같은 병).
   - 하는 일: `action:"reply"`가 답을 저장한 뒤 `next_action` **한 줄만** 새로 받아 응답으로 돌려주고,
     화면이 그 자리에서 바꾼다. 확인 문구(「…라고 알려 줬어」)는 고정 문구로 둔다 — 그것까지 받아 오면
     아이가 더 기다린다.
   - **새 질문을 이어 붙이지 않는다.** 카드가 도화지를 가리고 있고 "한 번에 하나만 묻는다"가 원칙이다.
   - **깊이(그 대상을 더 그려 넣기)와 옆(어울리는 것을 하나 더하기) 중 어느 쪽이든 상관없다**
     (사용자: "옆으로 깊이로 둘중 어느것을 줘도 상관없을 것 같아"). 종전의
     ~~「새 소재나 새 주제를 네가 가져오지 않는다」~~ 지침은 뺐다. 남는 계약은 **출발점이 아이**라는 것뿐이다.
   - 예산: **한 도움 기록당 첫 답 한 번만** 부른다(답을 고쳐 보내면 글자만 덮어쓴다). 그래서 이 호출은
     `ai-create` 예산을 쓰지 않아도 ask 횟수를 넘을 수 없다. 코칭 예산은 10분 **8회 → 20회**
     (2026-09-26 사용자 결정).
   - 실패하면 화면의 종전 줄을 그대로 둔다. 답은 이미 저장됐고, 몽그리가 쉬어도 아이는 그대로 그린다.

## 캔버스와 입력

1. 패드 사용을 중심으로 펜 입력 모드를 기본값으로 한다.
2. 손가락 그리기는 사용자가 명시적으로 선택할 때만 활성화한다.
3. 펜 모드에서 손바닥과 일반 터치는 그림을 방해하지 않아야 한다.
4. 두 손가락 제스처는 확대·축소와 화면 맞춤에 사용한다.
5. 모두 지우기는 확인 후 한 번에 지우며 한 번의 되돌리기로 복원할 수 있어야 한다.
6. 지우개는 실제 지워지는 범위를 네모 커서로 보여준다.
7. 색상은 갈색·민트색을 포함해 충분히 제공하고 선택한 색의 이름을 표시한다.
8. 선 굵기는 현재 선택된 펜을 다시 누르면 그 펜 왼쪽에 펼쳐지는 드래그 슬라이더로 조절한다(2026-08-30 사용자 변경 결정, 2026-09-07 시안으로 확인). 화면 바깥·다른 도구·Esc로 닫는다. 이유: Freeform식의 좁은 세로 플로팅 도구 레일과 넓은 도화지를 유지하면서 굵기를 바꾸기 위해서다. 이 슬라이더는 떠 있는 레일을 쓰는 넓은 화면(가로 900px·세로 600px 이상)에만 둔다. 휴대전화와 세로가 짧은 화면은 2026-08-18의 5단 점 버튼을 그대로 쓴다 — 좁은 화면에서는 팝오버가 도화지를 가리고 44px 터치 목표를 깨기 때문이다. 점은 현재 선택한 색으로 칠하고 지우개 선택 시에는 잉크색으로 남긴다.
9. 도화지는 화면을 가득 채운다(2026-09-07 사용자 결정). 새 작품은 첫 획을 긋기 전까지 그릴 수 있는 넓이의 비율을 따라가고, 한 획이라도 그으면 그 비율로 굳는다 — 그린 뒤에 비율을 바꾸면 이미 그린 선이 늘어난다. 이유: 그리기 화면 시안의 넓은 도화지를 그대로 쓰기 위해서다. 비율은 문서에 함께 저장하며, 세로 값이 없는 기존 작품은 예전처럼 정사각(1024×1024)으로 남는다 — 좌표가 x·y 모두 0~1로 정규화돼 있어 비율을 바꾸면 이미 저장한 아이 그림이 찌그러지기 때문이다. 점선·시범 안내 좌표는 정사각 기준으로 그려 둔 것이라 도화지 가운데의 정사각 영역 안에 넣어 동그라미가 타원이 되지 않게 한다.

## 학생 UI

0-1. (2026-09-13 사용자 시안) **"아직 준비 중이에요"(명단 없는 반) 화면은 카드 없이 크림 바탕에 고개 내민 몽그리, 제목, 두 줄 안내, 노란 "다시 확인하기", "수업 코드 다시 입력하기"만 둔다.** 위 왼쪽 로고, 오른쪽 반 이름. 교실 장식은 두지 않는다. 시안 원본은 `docs/design-assets/not-ready/mockup-2026-09-13.png`. 몽그리 그림은 시안에서 잘라 쓰며 불투명이라 배경은 시안과 같은 `#fefaea`로 고정한다.
0. (2026-09-13 확정) **입장 화면(수업 확인·참여 코드 입력)은 대문과 같은 방식으로 만든다 — 배경에는 늘어나도 되는 것만 두고, 모양이 있는 것은 독립 요소로 놓는다.**
   - 벽은 평평한 색, 바닥은 가로로만 늘어나는 띠, 문·화분·이젤·액자는 화면 아래 양옆에 놓는 장식 그림, 몽그리는 레이아웃 안의 그림, 문구·버튼은 가운데 카드다.
   - 장식과 바닥 띠는 하나의 배율 변수(`--sceneH`)를 함께 써서 걸레받이 선이 좌우에서 어긋나지 않게 한다. 원본 장면(1672×941)의 상대 위치를 그대로 유지한다.
   - 좁은 화면에서는 장식을 내려놓는다(대문의 잎사귀와 같은 규칙). 벽·바닥·몽그리·카드만 남는다.
   - (2026-09-13 추가) **문구 카드는 모든 크기에서 화면 가운데다. 태블릿을 기준으로 맞춘다.** 장면(문·몽그리·이젤)은 카드를 밀어내지 않고, 가운데 카드 옆에 남는 폭 안에 몽그리가 들어가도록 스스로 줄어든다. 몽그리는 원래 장면에서 서 있던 문 앞 자리에 둔다. 카드를 몽그리 오른쪽으로 비켜 세우는 방식은 카드가 가운데에서 벗어나 채택하지 않았다.
   - 이유: 종전에는 교실을 한 장짜리 장면으로 깔았다. 고정 비율로 두면 화면 비율이 다를 때 빈 띠가 남았고(2560×1080에서 좌우 641px, 1440×900에서 아래 90px 실측), 꽉 채우면(cover) 오리가 잘렸다(390×844에서 몸통만, 820×1180에서 머리가 화면 밖). 한 장짜리 장면으로는 대문처럼 자연스럽게 늘어나게 만들 수 없다.
   - 그림에 그려진 문·나무 간판에 글자를 맞추던 절대 좌표는 쓰지 않는다. 장식이 숨거나 크기가 바뀌면 어긋난다.



1. 학생 홈(2026-08-31 개정): **수업이 열려 있으면 오늘 회차 카드가 홈을 차지**하고
   다른 메뉴는 아래에 작게 둔다. `활동 고르기` 메뉴는 레슨 철거와 함께 소멸한다.
   수업 닫힘 상태의 홈 구성은 P-004 잔여(이어 그리기 + 내 그림 2메뉴 유력).
   (구 결정 `이어 그리기 → 내 그림 → 활동 고르기` 3메뉴는 이 항으로 대체)
2. 수업 마치기는 일반 메뉴에서 빼고 상단의 별도 행동으로 둔다.
3. 학생이 읽지 못해도 그림·아이콘·음성·큰 터치 영역으로 핵심 행동을 찾을 수 있어야 한다.
4. (폐기 2026-08-31) ~~대문과 학생 화면은 그림책형 참고 이미지의 시각적 방향을 따른다~~
   — 시각 정본은 `ux-wiggle-web-2026-08-31/DESIGN.md`로 대체됐다.
5. 긴 설명은 줄이고 공간이 충분한 짧은 문장은 불필요하게 여러 줄로 나누지 않는다.
6. 신규 생성과 기존 학생 로그인은 태블릿 크기에서는 한 화면에 모든 입력을 보여주고, 휴대전화 크기와 세로 공간이 짧은 화면에서는 3단계 안내형으로 보여준다.
7. 음성 안내는 각 페이지 상단에 하나만 두고, 그 페이지에서 해야 할 일을 짧게 처음부터 안내한다. 동물·별명·그림 비밀번호별 개별 음성 버튼은 두지 않는다.
   - 안내 문장은 AI가 새로 만들거나 바꾸지 않고 제품에 확정된 시나리오 문장을 그대로 읽는다.
   - 재생은 한국어 음성을 우선 선택하고, 아이가 알아듣기 어려울 정도로 인위적으로 느리게 만들지 않는다.
8. 메인 화면의 제목·문구·학생 안내·4자리 수업 코드 입력 카드·그림책 색감을 유지한다. 2026-09-06 사용자가 화가 오리 중심의 전시관 시안을 선택하고 사이트용 개별 에셋 제작을 요청하여, 메인의 교실 그림 고정 원칙은 갱신한다. 기준은 `docs/design-assets/gallery/selected-reference.png`이며 오리·전시 그림 3종과 제품 차별점 영역을 포함한다. 이유: 화가 오리를 브랜드 중심으로 보이고 제품의 특징을 설명하기 위해서다. 2026-09-07 사용자 지시로 실제 대문 라우트를 이 시안으로 구현했다. 그림은 개별 에셋으로 배치하고 문구·버튼·수업 코드 입력칸은 HTML로 만들며, 하단에 `모두 다른 답`·`AI는 대신 그리지 않아요`·`비교보다 발견` 소개 영역을 둔다. 은퇴한 것은 대문 그림 하나에 HTML을 얹던 단일 무대 방식이며, 4자리 코드 입장과 교사 수업 열기 동작은 그대로다.
9. `examples/KakaoTalk_20260817_182137151.jpg`, `examples/KakaoTalk_20260817_185428983.jpg`, `examples/KakaoTalk_20260817_194906484.jpg`는 각각 메인 화면, 학생 프로필 생성 화면, 학생 홈의 확정된 넓은 화면 시안이다. 메인 에셋에는 위 8번의 최신 결정을 우선한다. 학생 프로필 생성·학생 홈은 현재 구현이 기존 시안의 구성·비율·위계를 제대로 재현하도록 수정한다.
10. 위 세 화면에 대한 사용성 피드백은 시안을 바꾸라는 새 디자인 요구가 아니라, 현재 코드에서 시안과 다르게 구현된 배치·크기·간격·잘림·음성 위치·반응형 문제의 수정 목록으로 사용한다.
11. 시안은 시각적 구성 기준이며 이후 확정된 기능 결정을 되돌리지 않는다. 수업 코드는 4자리를 유지하고, 학생 프로필이 있는 학급의 `새로 시작하기` / `내 그림 이어가기` 분기와 기존 학생의 `동물 + 별명 + 그림 비밀번호 3개` 재입장 흐름을 적용한다.
12. 설계 설명용 목업의 `우리 반 20명 목록이나 다른 학생의 그림은 보여주지 않아요`와 같은 주석은 실제 아이용 제품 문구로 사용하지 않는다.
13. 학생 홈 상단의 선생님 말씀 버튼은 💌 아이콘을 쓴다. 넓은 화면에서는 `💌 선생님 말씀`처럼 글자 라벨을 함께 보여주고(헤더의 아이콘+글자 버튼 규약), 휴대전화 폭에서는 44px 정사각 아이콘 버튼으로 접는다. 👩‍🏫 이모지는 버튼 아이콘으로 쓰지 않는다 — Windows에서 조합 이모지가 깨져 보이고 버튼의 용도가 읽히지 않는다(2026-08-18 사용자 결정). 말풍선·이력 안의 말하는 주체 표시로는 계속 쓴다.
14. 학생 홈 하단 선반 카드는 시안대로 아이콘·제목·상태 칩을 칸 중앙의 한 덩어리로 배치하고, `활동 고르기` 카드에는 상태 칩을 붙이지 않는다.
15. 앱의 기본 색은 파란색이 아니라 초록색이다(2026-09-07 사용자 결정, 2026-09-09 최신 main에 적용). 화면·버튼·배경 그림의 파란 계열은 초록·크림 팔레트로 맞추고, 브랜드 로고·앱 아이콘·오리 몽그리의 파란 모자·스카프와 그리기 팔레트의 물감 색은 바꾸지 않는다.
16. `/join`의 수업 확인 대기·실패 화면은 `docs/design-assets/entry-green/reference/approved-green-palette-fixed.png` 교실 시안을 따른다. 넓은 화면은 팔레트를 잡은 오리 완성 장면을 배경으로 쓰고 문구·버튼은 HTML로 올리며, 좁은 화면은 크림 배경과 독립 오리로 바꾼다. 도움 버튼 문구는 기존 `선생님 불러요`를 유지한다.

### 폐기된 결정 — 재설계 금지 조항 (2026-08-31)

사용자 결정으로 다음 항목을 폐기한다. **디자인을 처음부터 다시 만든다.**

- (폐기) 8. 메인 화면의 현재 제목·문구·교실 그림·학생 안내·수업 코드 입력 카드·색감은 유지한다. 새 디자인으로 교체하거나 요소를 없애지 않고, 기존 요소의 폭·간격·정렬과 반응형 순서만 조정해 답답함을 줄인다.
- (폐기) 9. `examples/KakaoTalk_20260817_182137151.jpg`, `examples/KakaoTalk_20260817_185428983.jpg`, `examples/KakaoTalk_20260817_194906484.jpg`는 각각 메인 화면, 학생 프로필 생성 화면, 학생 홈의 확정된 넓은 화면 시안이다. 세 화면은 추가로 재설계하지 않고 현재 구현이 시안의 구성·비율·위계를 제대로 재현하도록 수정한다.
- (폐기) 10. 위 세 화면에 대한 사용성 피드백은 시안을 바꾸라는 새 디자인 요구가 아니라, 현재 코드에서 시안과 다르게 구현된 배치·크기·간격·잘림·음성 위치·반응형 문제의 수정 목록으로 사용한다.

**폐기 이유**
- 이 조항들은 구현이 시안에서 반복적으로 벗어나던 것을 막으려는 **방어적 결정**이었다.
- 그런데 근거가 되는 확정 시안(`examples/*.jpg`)은 `.gitignore`의 `/examples/`로 커밋되지 않아
  작업 환경에 따라 **볼 수 없다.** 검증할 수 없는 제약은 실효가 없다.
- 이야기 아크 도입으로 학습 흐름의 중심이 바뀌었고(「학습 과정」 개정 참조),
  기존 화면 구성이 전제하던 활동 선택 모델이 더 이상 정본이 아니다.

**대체**
- 시각 결정의 정본은 `docs/bmad/planning-artifacts/ux-designs/ux-wiggle-web-2026-08-31/DESIGN.md`,
  동작·정보구조의 정본은 같은 폴더의 `EXPERIENCE.md`다.
- 두 문서는 목업·와이어프레임·import보다 우선한다.
## 그림책 만들기

1. 외부 Canva 편집 화면을 붙이지 않고 Wiggle 안에서 직접 동작하는 그림책 편집기를 사용한다(2026-08-20 사용자 결정).
2. 각 쪽의 이야기 글은 하나만 두며 페이지 위쪽 가운데의 고정 영역에서만 편집한다. 글 요소를 추가·복제·이동·회전하는 기능은 제공하지 않는다(2026-08-21 사용자 결정). 이유: 초등학생이 글 배치를 따로 조정하지 않고 이야기 작성에 집중하게 한다.
3. 학생의 완성 그림과 새 그림은 글 아래의 지정된 그림 영역 안에서만 추가·이동·크기 변경·회전할 수 있다. 그림과 선택 박스는 같은 종횡비를 유지하고 지정 영역 밖으로 나갈 수 없다(2026-08-21 사용자 결정). 원본 이미지의 흰색·투명 빈 캔버스는 선택 박스에 포함하지 않고 실제로 보이는 그림의 픽셀 경계에 맞춘다. 원본 자산은 보존하고 요소별 내용 경계를 저장하며, 자동 계산과 `빈 여백 없이 맞추기`를 함께 제공한다(2026-08-23 사용자 결정).
4. 사용자가 올린 배경 이미지는 페이지 전체를 채우는 배경층이 되며, 그 위에 고정 이야기 글과 그림이 올라간다. 배경색은 이미지가 없거나 투명한 부분의 바탕으로 유지한다(2026-08-21 사용자 결정).
5. (2026-09-17 변경) 새 그림책은 Book Print API 고화질 스퀘어북 하드커버 `SQUAREBOOK_HC`(PUR, 재단 243 × 248mm) 한 가지로 고정한다. 생성 전 모양 선택과 편집 중 모양 변경을 제거한다. 이유: 사용자 첨부 상품과 같은 규격으로 제작사에 PDF를 제출하기 위해서다. 기존 저장 책은 배치 보존을 위해 원래 비율로 읽고, 인쇄 시 지정 판형 안에 비율을 유지해 담는다.
6. 그림의 이동·크기 변경·회전·스냅 상호작용은 MIT 라이선스의 `react-moveable`을 사용하고 라이선스 고지를 저장소에 둔다. 그림책 문서와 이미지 소유권·저장은 Wiggle의 학생 세션, Turso, R2 경계를 그대로 따른다.
7. 편집기는 페이지 추가·복제·순서 이동, 그림 앞뒤 순서·잠금·투명도, 실행 취소·다시 실행, 캐릭터 배경 오리기, 미리보기와 저장을 제공한다.
8. 학생 홈의 확정된 3개 큰 메뉴 순서는 유지하며, 그림책 작업실은 그 아래의 별도 그림책형 카드와 완성 작품 상세의 `이 그림으로 그림책 만들기` 행동으로 진입한다.
9. (2026-09-23 변경) 그림책 목록·편집·교사·관리자 등 후속 화면에도 `ux-wiggle-web-2026-08-31/DESIGN.md`를 적용한다. 이전 책 팀의 독립 시각 기준은 종료한다. 첫 화면부터 학생 입장·그리기까지는 현재 디자인을 그대로 유지한다. 이미 기준에 맞는 화면 구성과 기능은 보존하며 색·글자·모서리·그림자·터치 크기 등 불일치만 수정한다.
10. 학생이 그림책에서 `완성하기`를 누르면 그 학급의 교사는 완성 그림책 목록에서 동물·별명, 완성 시각, 쪽 수를 확인하고 책 전체를 넘겨 보거나 브라우저 인쇄의 `PDF로 저장`을 사용할 수 있어야 한다(2026-08-27 사용자 결정).
11. 교사는 완성 그림책을 체크박스로 여러 권 골라 한 번에 피드백을 요청하거나 한 권씩 요청할 수 있다. 루브릭이 확정되기 전에는 AI 피드백을 임의 생성하지 않고 요청만 `루브릭 대기` 상태로 보존한다(2026-08-27 사용자 결정).

## 교사와 메시지

1. 교사는 전체 학급 또는 특정 학생에게 텍스트 도움말을 보낼 수 있다.
2. 학생은 닫은 교사 메시지를 다시 확인할 수 있어야 한다.
3. 교사가 학생 작품을 보고 있을 때 학생 화면에 이를 알린다.
4. 음성은 후속 단계이며 텍스트를 기본 수단으로 유지한다. (2026-09-09 사용자 지시) 학생 화면의 브라우저 읽어 주기(들어 보기) 버튼은 전부 제거했다. 교사 목소리 귓속말은 (2026-09-14 사용자: "음성 귓속말 기능 코드는 아예 제거해줘") API·검증·학생 표시·릴레이 연결까지 코드째 지웠다. 외부 중계 서비스가 필요했고 기본값이 꺼져 있어 쓰인 적이 없다.
5. (2026-09-14 사용자 결정) 선생님이 그리는 중인 아이 도화지에 관여하는 방식은 자유 채팅이 아니다. 선생님 표시(동그라미·화살표·짧은 글)는 점선·시범처럼 아이 원본 그림과 **따로 된 층**에 올리고 저장 그림에 합성하지 않는다. 사용자: "1번은 따로 된 층을 추천해주고". 이유: 3~6학년이 글자를 치면 그리기가 멈추고, 선생님 한 명이 여러 대화를 동시에 잇기 어렵고, 그림 조언은 위치를 가리켜야 전달된다. 제안 순서는 ① 원본 크기 거의 실시간 보기 ② 따로 된 층 표시 ③ 아이는 큰 버튼으로 답하기 ④ 아이가 먼저 손들기. 네 가지 모두 2026-09-14에 만들었다. 운영 규칙: 아이 화면에는 답하지 않은 최신 표시 하나만 뜨고, 완성한 그림에는 보내지 않는다. 손은 표시·그 아이 메시지·선생님 손 내리기·아이 스스로 내리기로 내려가고 10분 지나면 사라진다(Claude 판단 — 필요하면 사용자와 조정). (2026-09-14 사용자: "1번으로 진행해줘") 실시간 보기 지연은 선생님이 보고 있을 때만 줄인다 — 아이 자동 저장을 멈춘 뒤 0.5초·길어도 2초, 선생님 확인을 1초로. 보지 않을 때 저장 간격은 그대로다. 새 선만 따로 보내기·실시간 연결은 교실에서 느리다고 느낄 때 다음 단계로 둔다.
7. (2026-09-14 사용자: "첫번째 시안대로 가자") 선생님 화면을 처음 불러오는 동안은 코덱스 시안 1(`docs/design-assets/teacher-loading/loading.png`)대로 실제 머리 줄(로고·학급 목록)과 학급 이름·버튼·탭·학생 그림 카드 8장의 회색 뼈대, 작은 원형 표시와 "학급을 불러오고 있어요"를 보인다. 시안 2(가운데 "이 학급을 열 수 없어요" 카드)는 쓰지 않았다. 대신 첫 응답이 실패하면 끝없는 로딩이 되지 않도록 같은 안내 줄을 서버 오류 문구와 [학급 목록으로][다시 시도]로 바꾸고 뼈대는 멈춰 흐리게 둔다(Claude 판단 — 시안 1만으로는 403이 끝없는 뼈대가 된다).
8. (2026-09-14 사용자: "1 조용한 버튼 + 완성 강조 … 이게 좋네") 그리기 화면 머리 줄 오른쪽 버튼 묶음은 코덱스 시안 1(`docs/design-assets/studio-header-actions/1-quiet-ghost.webp`)이다. 과정 보기·선생님·선생님 말씀은 테두리 없는 선 아이콘+글자, 몽그리 부르기만 연노랑 바탕에 몽그리 얼굴, 완성만 진초록. 이모지 아이콘을 쓰지 않고 편지함에도 "선생님 말씀" 글자를 붙인다. 손 든 상태는 연초록 "손 내리기", 안 읽은 말씀은 연노랑 바탕 + 빨간 수. 휴대폰 세로는 네 버튼을 둘째 줄에 아이콘 위·글자 아래로 두고 완성은 첫 줄에 남긴다. 가로로 눕힌 좁은 휴대폰은 한 줄에 아이콘만 둔다(Claude 판단 — 높이가 낮아 두 줄이면 종이가 너무 작다).
9. (2026-09-14 사용자: "그리고 도구는 B안으로 진행해줘") 그리기 도구는 코덱스 시안 B(`docs/design-assets/studio-tool-dock/B-crayon-box.webp`) — 모든 화면에서 화면 아래에 떠 있는 크림색 도구 막대 하나. 되돌리기·다시하기, 세워진 연필·크레용·마커·수채붓·지우개·좌우 대칭(나비), 색, ⋯ 더보기. 고른 도구는 진초록 바탕에서 올라오고, 붓 끝·띠는 지금 색으로 칠한다. 고른 도구를 한 번 더 누르면 굵기 5단이 뜬다(예전 슬라이더 대신). 막대에는 자주 쓰는 8색+무지개(12색 창 → 색 더보기 대화상자), 폭 900px 아래·낮은 가로 화면은 지금 색 버튼 하나+무지개. ⋯ 안에 채우기·도형·글씨(도형·글씨는 옵션을 이어 고르도록 창을 열어 둠)·펜/손가락 모드. 전체 지우기는 2026-09-20 사용자 요청으로 ⋯에서 막대의 되돌리기·다시하기 옆으로 꺼냈다(휴지통 단추, 확인 창과 되돌리기는 그대로). 휴대폰 세로는 도구 줄 아래 되돌리기·색·더보기 줄, 낮은 가로 화면은 한 줄. 막대가 도화지를 가리지 않게 그 높이만큼 아래를 비운다. 시안의 "스티커" 표기는 실제 기능(좌우 대칭)으로 바로잡았다. 작은 화면에서 몽그리 판은 예전처럼 막대를 덮는다(Claude 판단 — 막대 위로 올리면 좁은 화면에서 도화지가 사라짐).
10. (2026-09-15 사용자: "굵기 5단계 점 … 이런것보다는 상세 픽셀단위로 드래그할 수 있는게 좋은데") 9항의 굵기 5단을 바꾼다. 고른 도구를 한 번 더 누르면 뜨는 창에서 1~60픽셀(1024 도화지 기준 정수)을 끌어 고르고, 옆에 −·+ 1픽셀 버튼과 지금 굵기 점·숫자를 둔다. 저장 형식도 5개 값 목록 대신 1~60 정수를 받는다 — 예전 값(3·8·16·30·48)이 범위 안이라 옛 작품은 그대로 열린다. −·+와 상한 60은 Claude 판단(손끝으로 1픽셀을 맞추기 어렵고, 예전 최대 48보다 조금 넉넉하게).
11. (2026-09-15 사용자: "일단 과정 보기는 제거해주고") 그리기 화면 머리 줄의 과정 보기 버튼을 뺐다. 8항의 버튼 묶음은 몽그리 부르기·선생님·선생님 말씀·완성이다. 과정 기록 저장과 가족 보기의 과정 재생은 그대로 둔다(Claude 판단 — 지시가 그리기 화면 버튼을 가리킴).
12. (2026-09-15 사용자: "도화지는 항상 어떤기기 든지 화면을 꽉채우게 만들어줘") 도화지는 머리 줄 아래를 여백 없이 끝까지 채우고, 도구 막대는 도화지 위에 뜬다(9항의 "막대 높이만큼 아래를 비운다"를 바꿈). 새 작품의 세로 범위를 320~2240(가로로 눕힌 휴대폰 약 3:1 ~ 세로 휴대폰 약 1:2.2), 1 단위로 넓혀 휴대폰 세로·가로에서도 빈틈이 없다. 한 획이라도 그은 작품은 비율이 굳어 있어 모양이 다른 화면에서 열면 가운데 놓인다 — 늘리면 선이 찌그러지고, 비율을 바꾸려면 이미 그린 그림을 옮기거나 잘라야 해서다(Claude 판단). 막대를 도화지 위에 띄운 것도 Claude 판단이다 — 막대 자리를 비우면 막대를 접어도 도화지가 커지지 않는다.
13. (2026-09-15 사용자: "확대 축소 기능을 추가해주고") 도화지 오른쪽 위에 확대(+)·지금 배율(누르면 화면 맞춤)·축소(−) 단추를 둔다. 1.5배씩, 1배~4배. 두 손가락 벌리기, 트랙패드 벌리기(ctrl+휠, 커서 자리 기준), 확대한 동안 스크롤로 옮기기도 된다. 1배 아래로 줄이지 않는 것은 Claude 판단(도화지가 이미 화면을 채운다).
14. (2026-09-15 사용자: "밑에 그리기 도구는 아코디언으로 접었다 펼첬다") 도구 막대 위 가운데 손잡이 탭으로 접고, 접으면 화면 아래에 "도구" 단추 하나만 남는다. 접거나 펴면 굵기·색·더보기 창은 닫는다. 새로 열 때는 늘 펼친 상태로 시작한다(Claude 판단).
15. (2026-09-15 사용자: "도화지 크기는 화면을 꽉채우지 안 잖아, 그리고 내가 생각한 아코디언은 누르면 위로 올라가고 내리면 아래로 내려가는 느낌이었는데") 12항을 이미 그린 그림까지 넓힌다. 화면이 도화지보다 세로로 길면 그림을 가운데 둔 채 위아래로 종이를 덧대 늘린다(선 모양은 한 픽셀도 바뀌지 않고, 늘린 도화지가 저장된다). 화면이 더 넓으면 도화지를 화면 폭에 맞춰 꽉 채우고 넘친 위아래는 스크롤·두 손가락으로 옮겨 본다 — 그림을 줄이면 글씨·스티커 크기와 채우기 결과가 달라질 수 있어서다(Claude 판단). 줄이는 쪽으로는 늘린 도화지를 되돌리지 않는다. 14항의 막대 접기는 막대가 화면 아래로 미끄러져 내려가고 손잡이 탭만 남는 움직임이다. 탭을 누르면 번갈아, 아래로 끌면 접히고 위로 끌면 펴진다.
16. (2026-09-17 사용자: "복사하기를 누르면 다른곳에서 복사가 완료되었습니다 뜨는 것보다, 복사하기 글자가 v 형태의 체크느낌으로") 복사 성공은 누른 그 단추에서 알린다 — 글자나 아이콘이 초록 체크로 1.8초 바뀌었다가 돌아온다. 화면 다른 곳의 "복사했어요" 줄은 없앴다. 실패는 그대로 글로 알린다(교실 태블릿의 비보안 맥락에서 조용히 실패하던 2026-09-13 신고). 읽어 주는 화면은 체크를 볼 수 없으므로 들리는 안내(sr-only 상태 줄)를 함께 둔다.
17. (2026-09-20 사용자: "엄청 큰 도화지 … 삼성 노트 처음화면에서 축소를 하면 도화지가 확장되자나 그런 느낌", 두 안 중 **2번**) 새 작품의 도화지는 100%에서 화면 한 장이고, 끝까지 축소하면 **화면 3장 너비(가로·세로 3배, 넓이 9배)** 전체가 보인다. 무한 도화지는 저장 형식을 절대 좌표로 바꿔야 해서 고르지 않았다. 저장 형식은 그대로 두고 문서에 `span`(화면 몇 장 너비인가, 1~3)만 더한다 — `span`이 없는 옛 작품은 1로 읽혀 예전과 똑같이 열린다. 화면에서 고르는 굵기(1~60)와 글자 크기는 도화지 단위로 **`span`으로 나눠** 저장해 100%에서 옛 도화지와 같은 굵기로 보이게 한다(넓은 도화지는 100%에서 화면 `span`장 너비로 펼쳐지기 때문). 그래서 굵기는 0.01 단위 소수까지 저장한다 — 정수만 받으면 넓은 도화지에서 가는 선을 그릴 수 없다. 배율은 100%를 기준으로 1/span(전체 보기)~4배다.
17-1. (같은 결정에 딸린 것) 도화지 래스터는 "화면에 보이는 크기 × 기기 픽셀"로 잡는다(최대 3072px, 900만 픽셀). 예전에는 늘 가로 1024로만 그려 큰 화면·확대에서 선이 뭉개졌다("도화지 축소시 픽셀 깨져보이고").
17-2. (같은 결정에 딸린 것) 넓은 도화지의 완성 PNG·썸네일은 **그린 칸만 잘라 내보낸다**. 도화지 전체를 1024로 줄이면 아이 그림이 1/3(약 341px)로 들어가 그림책·인쇄에서 뭉개지기 때문이다(2026-09-20 조사). 자른 이미지는 긴 변 1536(썸네일 256)으로 그린다. 옛 작품(span 1)은 예전처럼 도화지 전체를 내보낸다.
18. (2026-09-20 사용자: "도화지 그리는 중도 다른 화면 처럼 뜨게하고싶은데") 기다리는 화면은 모든 화면이 같은 것을 쓴다 — 크림 바탕에 선 너머로 고개 내민 몽그리와 두 줄(2026-09-09 입장 화면 시안). 그리기 화면은 "잠깐만 기다려 줘! / 도화지를 펴고 있어요"다. 글자만 있던 옛 대기 화면(`.drawing-loading`)과 `/student` 중간 화면의 "도화지를 펴는 중" 카드는 지웠다. 불러오기가 실패하면 둘째 줄에 그 안내를 보여 주고, 우리말이 아닌 오류(예: "Failed to fetch")는 아이가 읽을 수 있는 문장으로 바꾼다.
19. (2026-09-20 사용자: 완성 화면의 "마음에 드는 곳은?"·"왜 마음에 들어?" 화면을 가리키며 "이거는 없애줘") 완성 화면에서 소감 고르기 두 묶음과 "직접 글로 쓰고 싶어요"를 없앤다. 마무리에 남는 것은 몽그리의 틀린 짐작을 아이가 고치는 부분(학습 과정 4항의 `틀리는 해석자`)과 완성 단추다. **소감은 더 이상 완성의 조건이 아니다** — 서버와 오프라인 큐의 "마음에 드는 곳·이유가 있어야 완성" 검사도 함께 풀었다(완성에는 완성 그림만 필요하다). 옛 작품에 남은 소감 기록은 그대로 두고 내 그림 자세히 보기에서 계속 보인다. 성장 기록은 아이 말로 `storyText`를 먼저 쓰므로 그대로 동작한다.
20. (2026-09-20 사용자: "단색 아이콘을 누르면 단색아이콘만 뜨고, 무지개 색 아이콘을 누르면 RGB를 고르는 화면이 뜨게") 색 막대의 두 단추는 서로 다른 것을 연다. **단색 점**은 단색 12색 창만 열고, **무지개**는 색 섞는 화면(밝기·진하기 면 + 색상 슬라이더)을 바로 연다. 전에는 두 단추가 같은 12색 창을 열고 그 안의 `색 더보기`로 한 번 더 들어가야 했다 — 아이 눈에 두 단추가 같은 일을 했다. 단색 창에는 단색만 둔다. 단색 점은 모든 화면 폭에서 보인다(넓은 화면에서도 12색 중 9~12번째 색으로 가는 길이 그 단추뿐이다).
21. (2026-09-21 사용자: "애플 펜으로 그릴때, 애플 펜보다 그림이 늦게 그려진다") **그리는 중인 획과 도형 미리보기는 도화지가 아니라 얇은 층에 그린다.** 도화지 픽셀은 손을 뗄 때만 바뀐다. 반투명 브러시(크레용·수채붓)가 알파 겹침을 막으려고 도화지 전체를 ImageData로 뜨고 되돌리던 방식은 쓰지 않는다 — 도화지가 화면 3장 크기가 되면서 그 복사가 9메가픽셀·34MB가 됐다. **펜 좌표는 pointermove 하나에 묶여 온 표본을 모두 읽는다**(`getCoalescedEvents`). 애플 펜슬은 초당 240번 보내는데 표본을 버리면 선이 펜 뒤로 처지고 모서리가 잘린다. 예측 점(`getPredictedEvents`)은 사파리에 없어 쓰지 않는다. 지우개는 도화지 픽셀을 파내는 도구라 층에 올리지 않는다.
6. 학생을 식별하는 최종 운영 방식은 아직 확정하지 않고, 먼저 교사가 학생 프로필을 확인하는 화면만 만든다.
6. 첫 교사 프로필 확인 화면은 동물, 별명, 생성 시각, 마지막 접속 시각, 전체 작품 수, 진행·완료 작품 수, 현재 진행 상태, 중복 별명 여부를 읽기 전용으로 보여준다.
7-1. (2026-09-12 사용자 요청) 명단은 **엑셀(.xlsx)·CSV 파일로도** 넣을 수 있고, **엑셀 양식을 내려받아** 채운 뒤 올릴 수 있다. 양식은 읽는 코드와 같은 자리에서 만들어 규칙이 어긋나지 않게 한다. 파일은 교사 브라우저 안에서만 읽고 서버로 보내지 않는다 — 실명이 든 파일이기 때문이다. 읽은 결과는 입력칸을 채울 뿐이고, 번호·이름 규칙과 저장은 손으로 적을 때와 똑같은 경로를 지난다. 제목 줄은 건너뛰고, 번호가 없으면 1번부터 붙인 뒤 무엇을 했는지 교사에게 문장으로 알린다. 옛 `.xls`는 읽지 않는다.
7-3. (2026-09-20 사용자 결정: "각자 칸의 번호칸 이름칸 나눠서 적도록") 명단에 학생을 추가하는 화면은 **번호 칸과 이름 칸을 줄마다 따로** 받는다. 한 칸에 `1 김민준`처럼 몰아 적던 텍스트 상자는 없앤다 — 어디까지가 번호인지 선생님이 매번 확인해야 했다. 번호는 기존 명단의 마지막 번호 다음부터 자동으로 채워 두고 고칠 수 있으며, 마지막 줄에 이름을 적으면 다음 줄이 저절로 열린다. 엑셀·CSV 파일 불러오기와 표 붙여넣기는 그대로 두고, 읽은 결과를 칸에 펼친다(번호가 없으면 앞 줄 다음 번호로 채운다). 저장 경로와 서버 검증은 바뀌지 않는다.
7-2. (2026-09-12) 참여 코드표는 **브라우저 인쇄**로 나간다. 팝업 창을 새로 열지 않는다 — 1장은 실명이 있는 선생님 보관용, 2장은 잘라 나눠 줄 쪽지(반 QR·수업 코드·내 참여 코드)다.
7. 교사가 학급을 만들 때 명단(번호 + 실명)을 직접 입력하고, 만든 뒤에도 학생을 추가·제거할 수 있다(2026-09-07 사용자 결정, P-001 확정). 학생은 수업 코드나 학급 QR로 들어와 ~~자기 번호를 입력하고 그림 비밀번호로 본인을 확인한 뒤~~ 자기 참여 코드 6자리를 넣고 자기 창을 연다(2026-09-09 변경, `학생 입장과 기록` 4번). 명단은 학생 화면에 보여 주지 않는다 — 수업 코드를 아는 사람에게 반 전체 실명이 노출되기 때문이다. 명단·설정 탭은 자리마다 참여 코드를 보여 주고 복사·새로 뽑기·코드표 인쇄를 제공한다.
8. (2026-09-07 확정) 자기 등록 입장 흐름 — `새로 시작하기 / 내 그림 이어가기` 갈림길, 별명·동물로 프로필을 찾던 재입장, 중복 프로필 허용(`allowDuplicate`) — 은 **대체가 아니라 제거**다. 명단이 없는 학급에서 아이가 스스로 프로필을 만드는 예비 경로를 남기지 않는다. 이유: 두 경로가 공존하면 같은 아이가 명단 자리와 자기 등록 프로필 둘로 갈라져, 교사 화면의 번호와 실제 작품 주인이 어긋난다. 명단이 아직 없는 학급의 학생 화면은 "아직 준비 중이에요"에서 멈추고 선생님을 부르게 한다(`NO_ROSTER`).
9. 교사 학급 상세는 **오늘 수업 / 작품·그림책 / 명단·설정** 세 탭으로 나눈다(2026-09-09 사용자 선택). 오늘 수업은 현재 회차 학생 그림을 큰 썸네일 격자로 보여 주고 회차 변경·수업 안내·전체 메시지만 상단 행동으로 둔다. 작품 탭은 학생·회차 필터와 완성 그림책 진입을 맡고, 명단 탭은 학생 추가·수정·삭제/복원과 입장 코드·QR·가족 링크·학급 삭제를 맡는다. 이유: 수업 중 확인과 평소 관리 행동이 한 화면에 섞여 있던 복잡도를 줄이기 위해서다.
10. 오늘 수업의 학생 그림은 현재 학급에 열린 회차와 같은 `arcId + episodeId` 작품만 표시한다. 회차가 없을 때만 학생별 최근 작품임을 화면에 명시한다(2026-09-09 사용자 선택을 구현하며 확정). 다른 회차에서 나중에 저장한 그림을 오늘 그림으로 보이면 교사가 현재 수업 상황을 잘못 판단하기 때문이다.

## 배포 원칙

1. 기존 Flutter 저장소에는 변경을 만들지 않는다.
2. (2026-08-18 확정) 배포 체계를 전환한다: 개발은 로컬 폴더 → GitHub push → **Vercel 자동 배포**. ChatGPT Sites와 Codex 독립 검증 게이트는 전환 완료와 함께 은퇴한다. 이유: Sites 프록시 의존(SIWC 헤더 인증)이 이전 불가였고, git push 자동 배포로 반복 속도를 올린다.
3. (2026-08-19 확정) 운영 데이터는 이전하지 않고 **신규 시작**한다. 기존 Sites와 그 데이터는 보존해 가역성을 남기고, 폐기는 사용자 명시 승인이 필요하다. 이유: 실사용 데이터가 소규모(작품 32개)였고 서버가 빈 DB에서 자가 프로비저닝된다.
4. (2026-08-18 확정) 스택은 Next.js + Turso(libSQL) + 기존 R2(S3 API), 교사 인증은 구글 OAuth 자체 구현이다. 호스트 중립 어댑터로 다른 호스팅 이전 시 재공사가 없어야 한다.
5. (2026-08-19 확정) 공개 주소는 무료 서브도메인 `https://wiggleweb.vercel.app`으로 시작한다. 유료 도메인(`wiggleweb.app`)은 실사용이 자리 잡으면 구매를 검토한다.
6. GitHub `main` push(=운영 배포)는 사용자가 직접 실행한다. 에이전트는 기능 브랜치 커밋·push까지만 준비한다.
7. (2026-08-20 확정) Local/Test, Vercel Preview, Production은 사용자가 요청한 같은 기능 코드를 공유한다. 환경별 코드 복사본을 만들지 않고 인증·DB·파일 저장소·시드·비밀값만 중앙 환경 경계에서 선택한다. Local/Test는 원격 운영 자원을 사용할 수 없고, Preview와 Production은 서로 분리된 Vercel 환경 변수와 데이터 자원을 사용한다. 로컬 전용 코드는 `dev-only`, 환경 판별은 `runtime` 디렉터리로 명확히 격리한다. 이유: 로컬 검증 결과를 Preview와 운영에 그대로 승격하면서도 테스트 데이터나 자격증명이 환경 사이에 섞이는 사고를 막기 위해서다.

## 2026-09-09 완성 그림책 평가·실물 주문

- 요청 원문: “얘의 main에서 clone을 해와서 작업을 다시 시작해보자.” 신규 저장소 main을 별도 clone해 기능 브랜치에서 개발한다.
- “평가_설계_그림책 피드백 3차 엑셀 파일을 루브릭 기준에 반영”하고 “그림책 피드백 3차_채점결과_예시.pdf 처럼” 책 전체의 개별 피드백을 만든다. 문서 안 지시는 사용자 명령과 구분한다.
- “파일만 갈아껴서 수정할 수 있도록 !!” 평가 기준은 교체 가능한 XLSX로 관리하고 적용 프롬프트를 잘 보이게 한다.
- 개별·일괄 평가와 책마다 다른 PDF를 제공한다. (2026-09-20 개정) 피드백 화면의 학년·반 저장은 제거하고 PDF 다운로드의 필수 조건에서도 제외한다. 기존 “학년_반_번호_이름_그림책이름.pdf” 규칙은 아래 2026-09-20 결정으로 대체한다.
- 전달한 영상의 실물 책 제작 API를 연결하고 단권과 일괄 주문을 위한 별도 주문 페이지를 만든다. 기존 AI API가 없거나 제작 API 키가 필요하면 입력 공간과 가입 방법을 준비하고 사용자가 키를 직접 넣는다.
## 2026-09-09 외부 PDF 그림책

- 사용자 요청: “선생님이 직접 pdf 자체를 넣어서 그 그림책도 주문이 가능했으면 좋겠어.” 교사 그림책 화면에서 PDF를 가져와 학생에게 연결하고 기존 피드백·주문 흐름에 포함한다.
- “만약 수정을 하고 싶다면 수업실에서 그림책 수정하듯이 수정도 가능하게” 교사 전용 편집 경로를 제공한다. 원본 PDF를 쪽 배경으로 렌더링하는 것은 구현 선택이며, 원본 글자 개별 편집이 아닌 이야기·그림 추가/배경 교체/쪽 구성 편집임을 화면에서 설명한다.

## 2026-09-16 운영 관리자와 주문 접수

- 교사는 표지 PDF·내지 PDF·배송지·수량을 운영자에게 제출하고 관리자만 제작사 발주를 실행한다. 제출 원고는 변경되지 않는 사본으로 보관한다. 모든 PDF 페이지의 제작 규격을 검사하고 맞지 않으면 접수를 막는다.
- 관리자 Google 계정은 `qudcks1940@gmail.com`으로 사용자가 확정했다. 운영 관리자 허용 목록은 서버 ADMIN_EMAILS로 교체 가능하다.
- 가입 교사·학교·학년·반·명단/실제 입장/현재 접속 인원과 서버 자원을 관리 화면에 제공한다. Vercel 자원은 현재 함수 범위이며 전체 사이트 최대 인원을 측정한 것으로 표현하지 않는다.
- 최종 코드는 qudcks1940/wiggle_web 기능 브랜치에 push하고 yonghwan86/wiggle_web main 대상으로 PR을 만든다. main push·병합·배포는 사용자가 실행한다.

## 2026-09-17 그림책 인쇄 판형 고정

- 사용자 요청: `yonghwan86/wiggle_web` 현재 main을 새 clone해 다른 기능은 변경하지 않고 그림책 모양과 PDF 규격만 맞춘다.
- 첨부 상품: 하드커버/PUR, 243 × 248mm, 24~130쪽·2쪽 단위, 아르떼 울트라 130g 내지·스노우 150g 표지·실크필 코팅. 제작사 상품 UID로 지정한다.
- 공식 PDF 규격: 내지 249 × 254mm(사방 3mm 도련), 표지 24~64쪽 544 × 288mm / 66~130쪽 550 × 288mm. 기존 최소 쪽·짝수 빈 쪽 추가와 첫/마지막 쪽 표지 재사용 방식 유지.
- 근거: https://api.sweetbook.com/docs/concepts/pdf-size/ (2026-09-17 확인).

## 2026-09-18 관리자 진입

- 사용자 요청: 교사 대시보드에서 관리자 페이지로 이동할 수 있게 한다. 통상적인 방식인 관리자에게만 버튼 표시를 적용하고, 직접 URL/API 접근도 서버의 관리자 허용 목록으로 검사한다. 일반 교사에게 운영 관리·제작사 발주 권한을 부여하지 않는다.


## 2026-09-18 PDF 가져오기와 인쇄 쪽 수 분리

- 사용자: “최소 24페이지라는거지 왜 1~24 쪽만 가져올 수 있지? 넘어서도 pdf를 가져올 수 있어야지. 그런데 책 주문이 최소 24페이지인거지. pdf는 그냥 전부 다 가져와지게 해야해.”
- PDF 가져오기는 24쪽 상한을 없애고 전 쪽을 순서대로 저장한다. 130쪽 인쇄 상한도 가져오기에 적용하지 않는다. 교사는 가져온 긴 책을 다시 열어 수정하고 쪽을 추가할 수 있다.
- 파일당 30MB·쪽별 이미지 전송 제한은 유지한다. 인쇄 주문의 24~130쪽·2쪽 단위 규격과 부족한 내지의 빈 쪽 보충은 기존대로 유지한다.


## 2026-09-19 피드백 진행 표시·학생별 그림책과 PDF 모음

- 사용자: “선택한 피드백 PDF 받기 버튼이나 피드백 만들기 버튼이 눌러도 응답이 없는 것처럼 보여 동작 중이라는 정확한 표시를 줘야해.” 접수·피드백 작성·PDF 준비를 구분하고, 회전 표시·처리 수·현재 책·경과 시간과 완료/실패 안내를 행동 근처에 표시한다.
- 사용자: “한 명이 여러 책을 만들 수도 있으니까 학생마다의 피드백이 모아져 있어야겠지? 그림책도 학생들마다 구분이 되도록 해 줘.” 학생 ID를 기준으로 책을 묶고 번호 순서로 보여 준다. 동명이인은 합치지 않는다.
- 아래에 학생별 완성 피드백 PDF 목록을 둔다. 책별 PDF 다운로드와 학생의 여러 PDF를 한 ZIP으로 받는 기능을 제공한다. PDF 내용은 기존 책별 평가를 유지한다. 같은 제목의 파일도 덮어쓰지 않는다.

## 2026-09-20 교사의 평가 기준 수정 안내·피드백 선택 관리

- 사용자: “학년 반 저장이 의미가 없어 아예 없애줘.” 그림책 피드백 화면에서 입력과 저장 버튼을 없애고, 학년·반 없이 PDF를 받을 수 있게 한다. 번호·이름은 기존 학급 명단을 사용한다.
- 사용자: “저 엑셀 파일이 어떻게 생겼는지 모르니까 양식?을 보여주든지. 어디를 원하는대로 수정할지를 알려줘야해.” 현재 평가 기준이 들어 있는 수정 양식 다운로드, 실제 셀 미리보기와 수정 위치 안내를 제공한다.
- 사용자: “저 중에 선생님이 골라서 pdf를 다운받을 수 있도록 … 학생 각각의 피드백을 아주 편리하게 자유자재로 관리 … 페이지를 아예 따로 만들어도 좋아.” 학생·책별로 PDF에 포함할 평가 영역을 고르고, 개별 또는 여러 학생의 파일을 한꺼번에 받을 수 있는 관리 화면을 제공한다.
- 사용자: “끝내면 나한테 먼저 보여줘야해.” 운영 반영 전 로컬 미리보기를 먼저 제시한다. main push·병합·배포는 하지 않는다.

## 2026-09-23 그림책 편집·완성·원고 구분

사용자 요청: “글이 써지는 곳에 직접 글을 쓰게”, “보이는 곳까지만”, “최소 24페이지”, “첫 번째 페이지만 제외한 2페이지부터 끝까지를 내지 pdf”, “파일명 제목을 설정하지 않으면 그림책 완성이 되지 않도록”.

- 학생은 책 위에 직접 왼쪽 정렬로 쓴다. 자동 줄바꿈을 제공하고 넘치는 추가 입력을 막으며 기존 긴 글은 숨기거나 버리지 않는다. 우측 본문 입력란은 제거한다.
- 학생 완성 조건은 제목 직접 지정과 표지 포함 최소 24쪽이다. 빈 제목과 과거 기본 제목 ‘나의 새 그림책’·‘나의 그림책’은 완성 시 경고한다. 초안은 제목 없이 저장할 수 있다.
- 첫 쪽만 앞표지로 쓰고 나머지는 순서대로 내지로 쓴다. 과거 첫/마지막 쪽 표지 재사용은 폐기한다. 인쇄 규격의 최소·짝수 내지를 위한 빈 쪽은 뒤에 추가하고 수를 안내한다.
- 피드백 기준·생성·PDF 구성 선택/다운로드를 별도 피드백 관리 화면에 통합한다. 완성책 목록에는 관리 진입을 둔다.
- 기존 비율 크기 조절을 유지하고 비율 유지 선택을 끄면 한 방향만 크기를 바꿀 수 있다. 완성 화면에서 학생 첫 화면으로 이동할 수 있어야 한다.
- 작업 결과는 로컬로 먼저 보여 준다. 운영 배포는 사용자 실행이다.

- 2026-09-23 추가 확정: “피드백 관리 페이지에서 평가 기준, 전체 배점 기준과 AI 프롬프트 확인을 맨 아래로 내려”. 기준·엑셀 설정과 전체 프롬프트는 페이지 마지막에 두고, 생성 및 학생별 PDF 관리를 먼저 보여 준다.

## 2026-09-23 그림책 작업 실시간 보기·조언

- 사용자: “이어서 수정 버튼들이 겹쳐져 보여 … 겹쳐 보이지 않도록”, “아주 똑같은 로직의 방법으로 그림책도 실시간으로 학생이 작업하고 있는 화면을 선생님도 볼 수 있어야해”, “그냥 조언 정도 주는 정도로 작업해줘 … 렉이 걸리지 않는 게 아주 중요”, “끝낸거까지 pr 날려줘”.
- 초안 목록은 제목과 수정 버튼을 분리한 행으로 표시하고 좁은 화면에서는 줄바꿈한다.
- 기존 그림 보기처럼 저장 번호로 변경을 확인하고 교사가 고른 학생 한 명의 현재 쪽만 전송한다. 학생이 펼친 쪽을 따라가거나 교사가 다른 쪽을 읽을 수 있다. 전체 학생의 이미지나 책 본문을 매초 받지 않는다.
- 교사의 실시간 개입은 쪽별 조언과 학생 읽음 확인으로 제공한다. 동시 원문 편집에 따른 저장 충돌과 입력 부담을 피하기 위한 구현 선택이며, 기존 별도 교사 편집기는 유지한다.
- 교사가 보는 동안 학생은 입력 중에도 최대 2초 주기로 자동 저장한다. 폴링은 겹치지 않게 처리하고 숨겨진 탭은 중단하며 이미지 캐시를 재사용한다. 운영 지연은 실제 교실 네트워크에서 추가 확인한다.

## 2026-09-23 이야기 영역 직접 클릭·터치 입력

- 사용자: “이야기 쓰기 버튼을 눌러야 글씨가 써지는 게 아니라 그냥 이야기가 써지는 곳을 클릭해서 그 곳에서 글을 쓰게 만들어. 이야기 쓰기 버튼같은건 없애.”
- 도구 막대의 ‘이야기 쓰기’ 버튼을 제거한다. 책 위 이야기 입력란에서 클릭·터치로 직접 커서를 놓고 쓴다. 글 입력에 초점이 가면 그림 선택 손잡이를 해제하며 자동 줄바꿈·한글 조합·저장·미리보기는 유지한다.

## 2026-09-23 후속 화면 디자인 시스템 통일

- 사용자: “그 디자인 md 파일대로 모든 사이트에 적용해야해 … 그림책 만드는 곳, 관리자 페이지 등등 … 모두 다 디자인 md 대로 적용이 되어있는지 체크하고 반영 … 첫 화면부터 그림 그리는 곳까지는 그대로 둬야해.”
- 학생 그림책·보관함·상세와 교사 수업실·그림책·피드백·주문·실시간 보기, 관리자·가족 화면을 점검한다. 학생은 초록·노랑과 둥근 버튼, 어른 화면은 진초록·흰 종이와 작은 모서리·그림자 없음으로 맞춘다.
- 대문·입장·그리기에는 스타일 변경이 전파되지 않도록 후속 경로에만 디자인 경계를 둔다. 실제 그림·책 배경색·본문 서체·저장된 배치와 피드백 점수 등 이미 확정된 기능은 변경하지 않는다.
