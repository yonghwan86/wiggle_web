# Wiggle Web 미결정 항목

> 마지막 갱신: 2026-09-26
> 이 문서는 아직 사용자의 최종 선택이 필요한 항목을 보존한다. 다음 작업자는 아래 내용을 임의로 확정하거나 구현하지 않는다.

## P-001 학생을 실제로 구분하는 운영 방식 — 2026-09-07 확정, `product-decisions.md`로 이동

교사가 학급을 만들 때 명단(번호 + 실명)을 직접 입력하고, 학생은 수업 코드나 QR로 들어와
자기 참여 코드 6자리로 자기 창을 연다(2026-09-09: 번호 + 그림 비밀번호를 참여 코드로 대체). 명단은 학생 화면에 보여 주지 않는다.
확정 내용과 이유는 `product-decisions.md`의 `학생 입장과 기록` 7번에 있다.

### 법적 검토 메모 (2026-08-31, 사용자 질문에 대한 분석 — 법률 자문 아님)

사용자 질문 원문: "학급 반 번호를 입력하게 해서 아이들을 판단하려고 하는데 법적으로 문제가 없을려나"

- 반 번호는 단독으로는 식별 불가하지만, 개인정보보호법의 개인정보 정의는
  **"다른 정보와 쉽게 결합하여 알아볼 수 있는 정보"**를 포함한다.
  이 기능의 목적 자체가 교사가 명부와 결합해 아이를 특정하는 것이므로
  "서버에 학교명이 없으니 익명"이라는 방어가 약하다.
- 초등 1~3학년은 전원 **만 14세 미만**이다. 개인정보 처리로 판정되면
  법정대리인 동의 + 동의 확인 절차가 필요해질 가능성이 높고,
  "설치·가입·동의서 없이 수업 코드로 시작"이라는 제품 포지션이 무너진다.
  개인정보처리방침의 "학생은 익명으로 참여" 문장도 거짓이 된다.
- 개인정보보호위원회는 아동 데이터에 엄격한 해석을 적용하는 추세다.
- **결론: 현장 검증(P-005) 전에 결정하지 않는다.** 검증 항목에
  "교사가 실제로 학생 구분에 곤란을 겪는가"가 이미 있다(PRD §9.4).
  검증 후에도 필요하면 도입 전 개인정보 전문 검토를 받는다.
- 중간 경로: 교사가 종이 명부에 `여우화가 = 12번`을 적는 방식은
  서버가 아무것도 수집하지 않아 법적 변화가 없다. 1차 검증에서는 이걸 안내한다.

### 추가 질문 (2026-09-07 — 이름까지 수집하는 안)

사용자 질문 원문: "반 번호 이름으로 수집해서 진행하기로 했어, 법적으로 문제가 없는건가?
선생님한테 물어보니까 학기초에 개인정보제공동의서를 받는다고하는데, 흠 괜찮을려나?"

분석 (법률 자문 아님):

- 이름+반 번호는 결합 논쟁 없이 개인정보다. 만 14세 미만이므로 법정대리인 동의가 필요하고,
  아이 본인·교사 동의로 대체할 수 없다(개인정보보호법 22조의2).
- 학기초 동의서가 유효하려면 문면에 ①수집 항목(이름·반·번호) ②목적(외부 교육 서비스
  이용/위탁) ③제공받는 자(운영 주체 업체명) ④보유 기간이 실제로 있어야 한다.
  학교 자체 목적으로 받은 동의의 전용은 목적 외 이용이다. **동의서 실물 확인 전에는
  "괜찮다"고 판단하지 않는다.** 검증처 3곳(학교 2·지역아동센터)마다 각각 확인해야 한다.
- 동의가 유효해도 서버에 이름이 저장되는 순간 서비스가 개인정보처리자(또는 수탁자)가 되어
  처리방침 개정·안전조치·유출 통지·위탁계약 의무가 생기고, 랜딩 문구
  "학생 이메일·실명 없이 안전하게"와 제품 불변 원칙(실명 금지)을 뒤집게 된다.
- 유지 권고: 위의 중간 경로(교사 오프라인 명부 매핑 `토끼화가 = 3반 12번`)로 1차 검증을
  진행하고, 그래도 부족하면 동의서 실물 + 개인정보 전문 검토를 거쳐 도입을 결정한다.

### 부분 결정 (2026-09-07): "그럼 일단은 반 번호로만 진행하자"

> ⚠️ 2026-09-09 병합에서 대체됨: 아래의 "선택 입력 반 번호"는 같은 날 다른 세션에서 확정된
> **교사 명단(번호+실명) 모델**로 대체돼 구현이 제거됐다(`product-decisions.md` 학생 입장 7·8항).
> 두 경로가 공존하면 같은 아이가 명단 자리와 자기 등록 프로필로 갈라지기 때문이다.

- 이름 없이 **반 번호(출석번호)만 선택 입력**으로 수집한다 — 확정 결정 「학생 입장과 기록」 16항.
- 이름 수집 여부는 계속 미결이다. 열려면 검증처별 학기초 동의서 실물 확인 + 전문 검토가 선행 조건.

## P-003 교사 확인 화면 이후의 운영 범위

2026-08-18 읽기 전용 1차 화면에 다음 정보가 구현됐다.

- 동물과 별명
- 프로필 생성 시각과 마지막 활동 시각
- 진행 중·완료 작품 수
- 현재 진행 상태와 최근 작품
- 같은 학급 안의 중복 별명 표시

번호·실명 명단은 2026-09-07에 확정돼 P-001에서 옮겨 갔다(`product-decisions.md`). 아직 결정하지 않은 것은 교사 메모와 외부 명부(NEIS 등) 연결이며, 그 입력란은 운영 방식이 정해지기 전에는 추가하지 않는다.

## P-004 그림책 피드백 실행 정책 — 루브릭·PDF 요구 2026-09-09 확정

사용자가 XLSX 10개 평가 영역과 서술형 PDF 예시, 교체 가능한 파일·프롬프트, 개별/일괄 실행을 제공했다. 기존 루브릭 미제공 대기는 해소됐다. 상세 확정 원문은 product-decisions.md 참조.

현재 구현은 한 권씩 처리·실패 재시도, 교사 전용 AI 초안/PDF다. 다음 질문은 아직 사용자 결정이 없다: “교사가 AI 초안을 검토·수정·승인한 뒤 학생에게 보여 줄지, 교사용으로만 둘지”. 학생 자동 공개와 앱 안 초안 편집·승인 기능은 이번에 추가하지 않았다.

실물 운영은 키 발급 후 Sandbox 검증, 사용자 배포 후 운영 실측, 대표 1부 교정이 남아 있다. 2026-09-23 사용자 확정: 첫 쪽만 앞표지, 2쪽~끝은 내지로 사용한다. 기존 표지 재사용 결정은 폐기했고, 제작 규격의 최소·짝수 내지를 위한 끝부분 빈 쪽 추가는 화면과 운영 안내에 표시한다.

## P-005 신규 GitHub 저장소와 Vercel 연결 전환

### 현재 합의

- 2026-09-16 최신 지시: 개인 `qudcks1940/wiggle_web` 기능 브랜치 push 후 `yonghwan86/wiggle_web`으로 PR을 제출한다.
- 신규 저장소는 `main`을 기본 브랜치로 사용하고 현재 기능 브랜치를 PR로 병합한다.
- 기존 공개 운영 `https://wiggleweb.vercel.app`은 현재 `yonghwan86/wiggle_web`의 `main`과 연결돼 있다.

### 아직 결정하지 않은 것

- Vercel 프로젝트의 Git 저장소 연결을 언제 신규 저장소로 바꿀지
- 기존 저장소를 전환 후 읽기 전용으로 보존할지

## 갱신 규칙

1. 사용자가 항목을 확정하면 결정 내용과 이유를 `product-decisions.md`로 옮긴다.
2. 구현만 끝났다고 제품 결정이 확정된 것으로 보지 않는다.
3. 질문 번호만 기록하지 말고 질문 문장과 선택 결과를 함께 남긴다.
4. 최근 대화와 문서가 충돌하면 사용자의 가장 최근 명시적 지시를 우선하고 문서를 즉시 수정한다.

## P-004 이야기 아크 채택에 따른 학생 홈 구조 (2026-08-30 신설)

### 배경

2026-08-30 학습 과정이 `회차 누적 이야기 아크 → 동화책 1권`으로 교체됐다
(`product-decisions.md`「학습 과정」). 그런데 기존 확정 결정은 그림책을 곁다리로 배치한다.

- PD/학생UI-1: 학생 홈 큰 메뉴 순서는 `이어 그리기 → 내 그림 → 활동 고르기`
- PD/그림책-8: 위 3개 메뉴 순서는 유지하고, 그림책 작업실은 **그 아래 별도 그림책형 카드**로 진입

동화책이 커리큘럼의 종착점이 된 이상, 그림책을 3개 메뉴 아래의 별도 카드로 두는 배치가
여전히 맞는지 확인이 필요하다.

### 아직 결정하지 않은 것

- 학생 홈의 1순위 행동을 `이어 그리기`(그림 단위)로 둘지, `우리 이야기 이어가기`(회차 단위)로 바꿀지
- `활동 고르기`가 회차 아크 안에서 무엇을 의미하는지 (교사가 회차를 정하면 아이의 선택지는 무엇인가)
- 회차 아크 밖의 낱장 그리기를 계속 허용할지, 허용한다면 어디에 두는지
- 동화책 1권이 완성된 뒤 다음 아크로 넘어가는 흐름

### 결정 전까지 하지 않을 것

- 확정 시안 3화면(`examples/*.jpg`)의 재설계 (PD/학생UI-9 유지)
- 학생 홈 메뉴 순서 변경 구현

## P-005 커리큘럼 현장 검증 설계 (2026-08-30 신설)

### 배경

테스트할 학교와 지역아동센터 2곳이 확정됐다. 사용자가 밝힌 최대 우려는
"실제 클라이언트가 쓰지 않는 기능이 많다"와 "교구 안의 커리큘럼이 실제 수업에서 쓰일지 의문"이다.
`docs/ux-market-audit-2026-07.md`에 1~2학년 5명 무개입 사용성 테스트 프로토콜
(과제 6개, 성공 기준: 주요 과제 80% 이상을 중립적 힌트 1회 이하로 완료)이
설계만 되고 실행된 적이 없다.

### 아직 결정하지 않은 것

- 5회차 아크를 두 기관에서 실제로 돌릴지, 아니면 1~2회차만 먼저 검증할지
- 커리큘럼이 "실제로 쓰였다"고 판정하는 기준 (교사가 몇 회차를 스스로 진행했는가 등)
- 검증 대상 학년 (확정 결정은 `초등 3학년 이상`인데 설계 기준은 비문해 저학년 — 아래 P-006 참조)
- 관찰 기록을 제품 안에 남길지 별도 수기로 남길지 (제품 안에 남기면 데이터 수집 원칙 검토 필요)

### 선행 조건 (블로커)

- **구글 OAuth 앱이 미게시라 두 기관 교사가 로그인 자체를 할 수 없다.** 현장 검증 전에 해소해야 한다.

## P-006 핵심 사용자 학년 (2026-08-30 신설) — 2026-09-09 확정: 초등 1~5학년, `product-decisions.md` 프로젝트와 제품 중심 8번

### 현재 문제

같은 확정 문서 안에서 대상 학년이 갈린다.

- `product-decisions.md`「프로젝트와 제품 중심」6: 핵심 학생 대상은 **초등학교 3학년 이상**
- `CLAUDE.md` 현재 우선순위: **비문해 저학년 독립 사용성**
- 실제 구현 기준(그림 비밀번호 3개, 페이지당 음성 안내 1개, 큰 터치 목표): 글을 못 읽는 아이 기준

3학년 기준이면 현재 온보딩 설계의 상당 부분이 과잉이고,
1~2학년 기준이면 `초등 3학년 이상`이라는 확정 결정을 고쳐야 한다.

### 아직 결정하지 않은 것

- 핵심 사용자 학년을 어디로 확정할지
- 지역아동센터는 학년이 섞이는데 이를 별도 대상으로 볼지
- 두 학년대를 모두 지원한다면 무엇을 학년별로 분기할지

## P-007 씨앗 이야기 커리큘럼 세부 — 2026-09-12 종결 (질문이 사라짐)

커리큘럼 자체가 은퇴해(`product-decisions.md` 학습 과정 0항) 아크 개수·벽 전시 회차 질문은 없어졌다.
다만 **반 작품 크게 보기**는 커리큘럼과 무관하게 남는 질문이다 — 선생님이 수업 중 반 그림을 한 화면에
크게 띄우고 싶을 때, 교사 화면에만 둘지 아이 화면에서도 보이게 할지. 지금은 교사 「오늘 수업」 탭의
격자가 그 자리를 대신한다. **미구현.**

## P-008 참여 코드 네 자리의 추측 위험 (2026-09-12 신설)

코드를 네 자리로 줄여 한 학급의 유효 코드가 만 개 중 수십 개가 됐다. 지금 방벽은 학급+IP 60회/10분 하나다.
교실 밖에서 오래 시도하는 상황을 어디까지 감수할지 사용자 확인이 필요하다.
선택지: 지금대로 둔다 / 한도를 더 조인다 / 수업이 끝나면 코드를 자동으로 새로 뽑는다.

## P-009 운영 Vercel 프로젝트를 새 저장소에 연결하기 (2026-09-12 신설, 실측으로 발견)

Vercel 프로젝트 `wiggle-web`이 옛 저장소 `yonghwan86/wiggle_web`에 연결돼 있어, 정본인
`wwwiggle/new_wiggle`에 올린 것은 운영에 반영되지 않는다. 운영은 2026-08-23 빌드에 멈춰 있다
(근거는 `current-state.md` 2026-09-12 항목). Claude는 대시보드에 접근할 수 없다.

선택지:
- 지금 Vercel 프로젝트의 Git 연결을 `wwwiggle/new_wiggle`로 바꾼다(환경 변수 9종은 프로젝트에 그대로 남는다).
- 새 Vercel 프로젝트를 만들어 붙이고 도메인을 옮긴다(환경 변수 9종을 다시 넣어야 한다).

연결을 바꾼 뒤에는 저장 경로가 바뀐 커밋이 여럿 들어가므로 운영 실측을 한 번 돌린다:
`node scripts/check-deployed.mjs https://wiggleweb.vercel.app <수업코드>`. 검증용 학생이 하나 생긴다.

## P-010 선생님 학급 화면의 불러오기·열 수 없음 상태 디자인 (2026-09-14 신설) — 2026-09-14 종결: 사용자 "첫번째 시안대로 가자", `product-decisions.md` 교사와 메시지 7항

사용자 원문: "이 화면의 수업실을 준비하는 중 너무 디자인 이상해 코덱스 세션에 연결해서 디자인 시안을 요청해봐".

현재: 첫 응답 전까지 빈 페이지 위 점선 상자에 "수업실을 준비하는 중…"만 보인다(`app/components/TeacherApp.tsx`의 `authorized === null`). 첫 응답이 401이 아닌 오류(예: 다른 선생님 계정의 학급 403)면 `authorized`가 계속 null이라 이 상자가 **안내 없이 끝나지 않는다** — 2026-09-14 로컬에서 실측(브라우저가 다른 로컬 선생님으로 자동 로그인돼 시연 반 주소가 6초마다 403).

코덱스 시안(`docs/design-assets/teacher-loading/`):
- `loading.png`: 실제 머리 줄(로고·학급 목록) + 학급 이름·버튼·탭·학생 그림 카드 8장을 회색 뼈대로, 위에 작은 원형 표시와 "학급을 불러오고 있어요".
- `error.png`: 가운데 카드에 잠긴 문 아이콘, "이 학급을 열 수 없어요", "다른 선생님 계정의 학급이거나 삭제된 학급이에요. 로그인한 계정을 확인해 주세요.", [학급 목록으로] [다시 시도], "로그인: 계정 · 다른 계정으로 로그인".

정할 것: 시안대로 구현할지(수정 요청 포함). 구현하면 403·404·네트워크 오류를 오류 카드로 나누고, 로그인 계정 표시에 필요한 교사 이메일은 이미 로그인한 본인 것만 보여 준다.

## P-011 아이 그리기 화면 머리 줄 오른쪽 버튼 묶음 디자인 (2026-09-14 신설) — 2026-09-14 종결: 사용자 "1 조용한 버튼 + 완성 강조 … 이게 좋네", `product-decisions.md` 교사와 메시지 8항

사용자 원문: "이부분의 디자인 시안이 별로야, 코덱스한테 한번 다른 그리기 서비스들은 어떤지 보고 시안을 여러개 뽑아달라고해봐" → 이어서 "내가 말한거는 이쪽에 대한 부분 이었어" (과정 보기·몽그리 부르기·선생님·💌·완성 버튼 묶음 캡처).

처음에 Claude가 그리기 화면 전체로 넓게 받아들여 화면 전체 시안 4종(`docs/design-assets/studio-redesign/`, 하단 도크·왼쪽 레일·플로팅·미술 책상)을 먼저 받았다. 사용자 뜻은 머리 줄 오른쪽 버튼 묶음이었다 — 전체 시안은 참고 자료로만 둔다.

버튼 묶음 조사와 시안: `docs/design-assets/studio-header-actions/` (코덱스, 웹 검색 사용 — Procreate·Tayasui·Fresco·Canva·Seesaw·Book Creator 등, 출처 링크는 Claude가 하나하나 열어 확인하지 않음). 다섯 방향 모두 이모지 대신 한 가지 선 아이콘, 받은편지함에 "선생님 말씀" 글자, 완성만 강한 버튼, 상태(손 내리기·안 읽은 2·몽그리 기다리는 중)와 휴대폰 390 배치를 담았다.
- `1-quiet-ghost`: 과정 보기·선생님·선생님 말씀은 조용한 글자+아이콘, 몽그리는 연노랑 도움 칩, 완성만 진초록.
- `2-grouped`: 과정 보기는 아이콘만(말풍선 이름), [선생님 | 선생님 말씀]을 한 묶음으로.
- `3-icon-over-label`: 같은 너비 칸에 아이콘 위·글자 아래, 완성 진초록.
- `4-overflow-menu`: 몽그리·선생님·완성만 밖에, 과정 보기·선생님 말씀은 "더보기"(안 읽은 수는 더보기에 표시).
- `5-friendly-characters`: 동그란 아이콘 버튼 아래 이름, 완성은 넓은 노란 버튼.

정할 것: 버튼 묶음 방향 하나(또는 조합).

## P-012 그리기 도구를 애플 메모 도구 막대처럼 바꾸기 (2026-09-14 신설) — 2026-09-14 종결: 사용자 "그리고 도구는 B안으로 진행해줘", `product-decisions.md` 교사와 메시지 9항

사용자 원문: "그리기 도구도 이렇게 변경할 수 는 없나.." (애플 메모·마크업 도구 막대 캡처 — 화면 아래 옅은 둥근 막대에 되돌리기·다시하기, 세워진 필기구들, 색 점과 무지개, ⋯).

지금은 오른쪽 세로 떠 있는 판(누운 도구 사진)과 따로 된 16색 판이다. 스타일 규칙이 `app/globals.css`에 넓게 퍼져 있고 `mobile-css`·`responsive-drawing-ui` 등 테스트 20여 곳이 판 구조를 확인하므로 구현은 큰 작업이다. 캡처 원본 파일이 없어 코덱스에는 모양을 글로 설명해 시안을 받았다(`docs/design-assets/studio-tool-dock/`, 설명·위험·필요 자산은 `notes.md`).
- `A-faithful`: 애플 도구 막대에 가장 가깝게. 옅은 회색 막대, 세워진 연필·크레용·마커·수채붓·지우개·나비, 색 점 6개+무지개, 고른 도구는 올라옴. 고른 도구를 한 번 더 누르면 굵기 5단 말풍선.
- `B-crayon-box`: 같은 배치에 크림색 막대와 Wiggle 종이 띠 도구, 고른 도구는 진초록 바탕.
- `C-labels-for-kids`: A에 도구 이름(연필·크레용…)을 붙이고 노란 선택 표시, 펜 모드/손가락 모드를 막대 오른쪽에.
- 공통: 채우기·도형·글씨·펜/손가락 모드는 ⋯ 더보기에(C는 모드를 밖에), 16색은 색 더보기. 휴대폰은 도구 줄 + 되돌리기·색·⋯ 줄로.
- 위험(코덱스 정리): 820px 높이 태블릿에서 막대가 120~160px를 차지해 가림 없는 종이 높이가 약 580~620px로 준다. 세워진 도구 그림 6종과 끝 색을 바꿀 마스크 4종이 새로 필요하다. 연필·크레용·마커 끝 모양을 아이가 헷갈릴 수 있다(C의 이름 표시가 이를 줄인다).
- 참고: 도구 막대 시안의 머리 줄은 코덱스가 글자만 있는 버튼으로 임의 단순화했다 — 머리 줄은 P-011에서 따로 정한다.

정할 것: 도구 막대 방향(A·B·C 또는 조합)과 구현 여부.

## P-013 도구 막대 접기 손잡이가 도화지를 덮는다 (2026-09-23 신설, 실측으로 발견) — 사용자 보류

사용자 원문: "그거는 고치지 말고 일단 남겨둬"

`.dock-toggle`은 76×44px에 배경이 투명한데, 그중 **43px이 막대 위(도화지 쪽)에 떠 있다**.
눈에 보이는 탭은 `::before`의 아래 30px뿐이라 14px은 아무 표시가 없다. 실측(390×844·320×568):

- `elementFromPoint`로 그 구간을 찍으면 `draw-canvas`가 아니라 `dock-toggle`이 잡힌다.
- 펜·마우스로 아래로 42px 끌면 `tool-dock` → `tool-dock is-collapsed`. **획은 하나도 안 그려진다.**
- 손가락은 이동 슬롭 때문에 click이 취소돼 막대는 그대로지만, 획이 안 그려지는 것은 같다.

원인은 두 겹이다: 44px 터치 규격을 맞추느라 커진 손잡이의 초과분이 도화지를 덮고,
그 영역이 세로 이동 16px을 접기 스와이프로 해석한다(`moved > 16 ? false : moved < -16 ? true : !open`).

선택지:
- 손잡이를 탭 전용으로 바꾼다(`moved` 판정 삭제). 가장 단순하지만 2026-09-15 "내리면 내려가는 느낌"이 사라진다.
- 히트 영역을 보이는 탭 30px 안으로 가둔다. 터치 목표 44px 규칙에 걸려 폭으로 보완해야 한다.
- 손잡이를 막대 안쪽으로 옮긴다. 도화지를 안 덮지만 시안 모양이 바뀐다.

## P-014 몽그리가 뜬금없는 짐작을 한다 (2026-09-26 신설, 운영 제보) — 2026-09-26 종결: 사용자 "가로 가자", `product-decisions.md` 학습 과정 4항

사용자 제보 원문: "너무 뜬금없는대답을 한다고 하기도하고, 처음에 추측을 할때, 집을 그리면, 다른걸로 추측해버린다"

### 원인은 둘이고, 서로 곱해진다

**① 몽그리가 그림을 제대로 못 본다 (기술)**

- AI에 보내는 이미지는 `imageData(canvasRef.current, 1024)`다 — **그린 칸을 자르지 않고** 도화지 전체를 보낸다.
  넓은 도화지는 span 3(화면 세 장)이라 대부분이 흰 여백이다.
- 게다가 `lib/openai-coaching.ts:313`이 `detail: "low"`라 OpenAI가 긴 변 **512px**로 다시 줄인다.
- 실측(2026-09-26, 헤드리스 크롬): span 3 도화지에 340×290 크기로 집을 그리면
  모델이 받는 그림은 **512×96**이고 그 안에서 집은 **57×48px, 넓이의 5.6%**다.
  그린 칸만 잘라 보내면 **512×437px — 9배**가 된다.
- 같은 문제를 썸네일·완성본에서는 이미 풀어 두었다. `documentImage()`가 span>1이면 `contentBounds`로
  그린 칸만 잘라 낸다("흰 여백이 대부분이라 … 아이 그림이 1/3 크기로 들어간다"). **AI 경로만 그 방식을 안 쓴다.**

**② 일부러 빗나가게 시킨다 (설계)**

`STORY_INTERPRETATION_INSTRUCTIONS`(같은 파일 33~34행):

> "일부러 덜 구체적으로 짐작한다. 정확한 사물 이름 대신 갈래로 말하거나 **두 번째로 그럴듯한 읽기를 고른다.**"

이것은 `학습 과정 4항 틀리는 해석자`(Weng et al. 2025)를 그대로 구현한 것이다. 버그가 아니다.
다만 그 설계의 전제는 **"그럴듯하게 살짝 빗나간 짐작"**이다(우산 ↔ 자전거 바퀴처럼 형태가 닮은 것).
①로 첫 짐작이 이미 틀리면, 거기서 다시 두 번째 읽기를 고르므로 **완전히 무작위**가 된다.
아이가 받는 인상은 "몽그리가 내 그림을 보고 다르게 읽었네"가 아니라 "몽그리가 아무 말이나 하네"다.

### 결정이 필요한 것

①은 명백한 결함이라 고치는 데 결정이 필요 없다(비용·지연은 확인 필요). ②는 제품 결정이다.

- **(가) 확신도로 갈린다 — 2026-09-26 사용자 채택.** — 확실히 알아보면 맞게 짐작하고 "맞아?"라고 확인한다.
  애매할 때만 갈래로 말한다("동물처럼 보이는데?"). 근거 발화를 끌어내는 목적은 유지하면서
  아이가 "몽그리가 못 알아본다"고 느끼지 않는다. 코칭 쪽에는 이미 `uncertain` 필드가 있어 개념이 있다.
- **(나) 빗나가되 가까이** — 형태가 닮은 것으로만 빗나간다. 원 설계(우산↔자전거 바퀴)에 가장 충실하지만
  "닮은 것"을 모델이 고르게 하기가 어렵고, ①을 고쳐도 여전히 아이가 틀린 말을 먼저 듣는다.
- **(다) 짐작을 버린다** — "무엇을 그렸는지 말해 줄래?"로 바꾼다. 가장 안전하지만
  학습 과정 4항의 교육 설계를 포기하는 것이므로 그 근거를 뒤집는 결정이 된다.

### 남은 위험

- `detail`을 올리면 토큰·비용·지연이 는다. 그린 칸만 잘라 보내면 이미지가 작아져 상쇄되는 부분이 있다 — 실측 필요.
- 로컬에 `OPENAI_API_KEY`가 없어 실제 응답 품질은 아직 비교하지 못했다. 고친 뒤 실제 호출로 확인해야 한다.

## 몽그리 (2026-09-09 추가)

- 틀리는 해석자의 짐작 문장을 글을 못 읽는 1학년에게 어떻게 전달할까? AI 문장은 음성으로 내보내지 않기로 했고(제품 결정 20항) 지금은 답 칩 이모지가 유일한 비문해 채널이다. 선택지: 위 학년에만 열기 / 짐작을 고정 시나리오 문장 틀에 끼워 음성 허용 / 이모지만으로 짐작 표현.
- (해결 2026-09-09) 단계 가이드와 빠른 주제 칩은 은퇴시켰다. `product-decisions.md` 학습 과정 4-1항.
